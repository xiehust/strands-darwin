/.agents/
/.claude/
/.codegraph/
/.codex/
/.kiro/
# Trellis workflow layer was removed on 2026-09-04; darwin's own skills keep the
# iteration record (docs/iteration-log.md, docs/research/, docs/reflections/).
/.trellis/
node_modules/
dist/
.darwin/sessions/
.darwin/last-session.json
# Often carries API tokens in headers/env. Use ${VAR} interpolation and delete
# this line if your team wants the file in the repo.
.darwin/mcp.json
# /.claude/ and /.codex/ above are per-platform local layers; only .darwin/config.json
# and the repo's own skills are tracked.
