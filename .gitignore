/.agents/
/.claude/
/.codegraph/
/.codex/
/.kiro/
node_modules/
dist/
.darwin/sessions/
.darwin/last-session.json
# Often carries API tokens in headers/env. Use ${VAR} interpolation and delete
# this line if your team wants the file in the repo.
.darwin/mcp.json
# Regenerated per platform by `trellis update`, same as /.claude/ and /.codex/
# above. Only .darwin/config.json and the commit-message sample skill are this
# repo's own.
.darwin/agents/
.darwin/commands/
.darwin/hooks/
.darwin/skills/trellis-*/
# Personal copy of Anthropic's bundled skill, not project-authored.
.darwin/skills/skill-creator/
