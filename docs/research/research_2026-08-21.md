# Darwin self-evolution research — 2026-08-21

## Run — 2026-08-21T09:55:51Z

### Backlog-first decision

- Backlog inspected first: `docs/research/backlog_index.md`
- Unfinished directions found: none
- Decision: fresh research

### Research path

```text
research-path: tui
focus: TUI interaction and visual polish
share: 20% (weight 2 of 10)
draw: 2 of 20 half-units
path-source: roll
rolled-at: 2026-08-21T09:55:51.058Z
weights: tui=2 observability=0.5 sdk=1 open=1.5 peer=5
```

### Source availability and method

This is a self-review `tui` path, directed by the binding roll while the user supplied the product topic: project-scoped long-term memory without vector retrieval. Repository source, specs, tests, README and recent git history were available locally and inspected at `3c165ec` on 2026-08-21 UTC. No peer product was consulted and no product claim is made from model memory. The earlier broad automatic-memory proposals in the 2026-08-17 and 2026-08-19 reports were also read so this run would not silently re-rate or duplicate them.

### Reference sources

No peer-product reference table applies to this self-review path. Evidence is the repository paths and symbols cited below; no product URL was opened or characterized.

### Peer highlights and innovations

Not applicable on this repository self-review path. The user requirement, rather than a peer claim, narrows the target to existing textual retrieval and explicitly excludes vector search.

### Current Darwin baseline

| Capability / architecture | Repository evidence | Current behavior |
|---|---|---|
| Durable project-scoped history | `.trellis/spec/backend/session-trajectory.md:9-39,71-113`; `src/agent/session.ts:58-87`; `src/trajectory/record.ts:474-499` (`searchableText`) | Recorded sessions already preserve bounded user input, assistant blocks, tool calls/results, failures and user `!` commands under one project key. Reasoning is excluded and every truncation/dropped class is stated. No second memory store is needed. |
| Existing text retrieval | `src/trajectory/search.ts:1-176` (`searchTrajectories`, `firstMatch`, `excerpt`); `spike/verify-trajectory.ts:1633-1686` (`searchContracts`) | Case-insensitive, non-regex substring search already scans this project's trajectories, supports type/session/limit filters, returns 160-code-point excerpts with session/turn/record provenance, tolerates recorded damage, distinguishes missing records, and is covered without a model call or network. It is deliberately not vector retrieval. |
| Current access surface | `src/cli-trajectory.ts:239-285` (`search`); `README.md:556-575`; `src/tui/App.tsx:765-783` | Search is available only as `darwin trajectory search` in another shell. In the TUI, `/trajectory` reports the current recorder's counters and explicitly sends users to the CLI for search/replay/fork; neither the running model nor the current composer has a retrieval surface. |
| Model-context boundary | `.trellis/spec/backend/session-trajectory.md:16-19`; `src/agent/runtime.ts:687-705` (`send`) | The trajectory is currently observer-only and never contributes model context. A memory feature must change this contract explicitly, through an ordinary visible tool result, rather than silently preloading old sessions into every prompt. |
| TUI transparency for reads | `src/tui/App.tsx:403-450,629-875`; `.trellis/spec/frontend/live-frame.md:10-55,134-205`; `src/tui/ToolCallPanel.tsx` via the shared tool lifecycle | Tool calls already have bounded live rows and finished `<Static>` history, while local reports are transcript notices rather than permanent header rows. A memory read can use the existing tool presentation and add no frame surface, timer, modal, or hidden injection. |
| Existing bounded historical reader precedent | `.trellis/spec/frontend/prompt-recall.md:10-84`; `src/trajectory/prompt-history.ts`; `spike/verify-prompt-recall.ts` | Prompt recall proves project history can be read asynchronously, mutation-free, with bounded sessions/bytes/entries and explicit degradation. It is human-editor history only and exposes no assistant/tool context to the model. |
| Prompt and command discoverability | `src/commands/custom-commands.ts:10-65`; `src/tui/help-format.ts`; `src/tui/App.tsx:291-340,1192-1220`; `.trellis/spec/frontend/tui-testing.md:96-109` | Canonical built-ins drive completion and `/help`; adding a local command costs a completion slot and real-pty key/budget verification. A model tool is discoverable to the model from its description and visible to the human in the ordinary tool panel, without adding another slash command. |

### Comparison and gaps

| Requirement / repository evidence | Darwin evidence | Parity / gap / deliberate difference | Implication |
|---|---|---|---|
| Long-term recall should reuse text search, not embeddings or a generated memory database | `searchTrajectories` already searches every recorded project session; `session-trajectory.md` defines the existing durable bytes | **Storage and retrieval primitive already exist** | Wrap the existing reader; do not add an index, vector dependency, embedding call, summary file, retention scheme or second writer. |
| The running agent needs to recover relevant prior work | Only the offline CLI calls `searchTrajectories`; runtime tools do not expose it | **Gap** | Add one project-scoped read-only model tool that returns bounded textual matches with source provenance. Retrieval remains explicit: the model asks for keywords and the call is visible. |
| History must not become invisible ambient context | `session-trajectory.md:16-19` currently forbids model use; the TUI already renders ordinary tool calls/results | **Deliberate boundary must be narrowed, not erased** | Permit only explicit tool-result disclosure. Do not auto-inject startup memories, mine turns after completion, or mutate the system prompt/cache. The current session id should be excluded by default so the just-durable query cannot match itself and ordinary conversation context remains authoritative. |
| Historical data may be absent, damaged or bounded | Search outcomes already carry `withoutRecord`, per-session damage and `limited`; record fields/files are capped | **Honesty data exists but the CLI formatter owns its present wording** | Build one bounded memory projection over `SearchOutcome` shared by the tool/tests. It must state no matches, missing records, tolerated damage and limit cuts, and cap query/result size and sessions read. |
| TUI frame must stay stable | Tool panel and `<Static>` finished rows already own model-tool presentation; `live-frame.md` forbids uncounted rows | **No new visual surface required** | Present `search_memory` exactly like any other safe read tool. Add no header row, completion row, local command, focus mode or bespoke panel. Expanded/compact tool bounds remain the only rendering bounds. |

### Ranked iteration directions

`Score = 2 × Importance + Architecture fit + Evidence confidence − Implementation difficulty − Implementation risk`

| Rank | Proposed ID | Direction | Importance | Architecture fit | Evidence confidence | Implementation difficulty | Implementation risk | Score | Evidence and rationale |
|---:|---|---|---:|---:|---:|---:|---:|---:|---|
| 1 | SER-031 | Add explicit project long-term recall as a bounded read-only `search_memory` model tool over existing trajectory substring search: no vectors, embeddings, generated summaries or second store; exclude the active session by default; return source-labelled session/turn/type excerpts and honest damage/omission/limit state through the ordinary visible tool lifecycle | 4 | 4 | 5 | 3 | 3 | **11** | `src/trajectory/search.ts` and `spike/verify-trajectory.ts` prove the retrieval primitive; `runtime.ts` has one tool assembly boundary; the shared tool panel gives human visibility without new TUI furniture. Fit is 4, not 5, because this intentionally narrows the existing observer-only rule and therefore requires an explicit spec/API boundary plus privacy-conscious bounds. Risk is contained by project scoping, active-session exclusion, read-only permission classification, no startup injection, and no new persistence. This is a new narrowly retrieved capability, not a re-rating of the earlier rejected automatic generated-memory direction. |

### Gated-out directions

| Direction | Score | Gate | Why it fell below | Kept anyway? |
|---|---:|---:|---|---|
| Automatically mine every completed turn into generated durable “memories” and inject them on future runs | 2 (I 3, F 2, E 4, D 5, R 5) | 6 | Unchanged from `research_2026-08-19.md`: generated facts add stale-memory correction, secret redaction, retention/deletion and hidden-context policy. The user asked for existing text retrieval, not automatic synthesis. | no |
| Add a vector/embedding index over trajectories | 1 (I 2, F 1, E 2, D 4, R 4) | 6 | Explicitly excluded by the user; it also creates a second derived store, dependency/provider work, rebuild consistency and privacy semantics when exact text search already exists. | no |
| Add a new hand-edited `memory.md` store with remember/forget commands | 3 (I 3, F 2, E 3, D 4, R 4) | 6 | A second persistence source can diverge from trajectories and AGENTS.md, and needs write authorization, precedence, deletion and concurrent-edit contracts. It does not reuse the proven search primitive. | no |
| Add a bespoke `/memory` search panel in the composer | 5 (I 2, F 4, E 5, D 3, R 5) | 6 | The offline CLI already serves human-only search; a new focus/key/frame surface duplicates it and increases TUI ownership risk. The ordinary visible tool lifecycle gives the running agent recall without a new panel. | no |

No dimension rating was restated to cross the gate, and no below-gate exception is used. The prior broad automatic-memory proposal remains rejected at its recorded score; SER-031 is materially narrower because it writes nothing, generates nothing, injects nothing automatically and uses only explicit bounded substring retrieval.

### Recommendation

- Batch to implement: `SER-031`
- Start with: `SER-031`
- Why this order: one accepted direction, no dependency and no score override. The existing search reader is the implementation seam; the tool is only a bounded adapter and visible projection.
- Independently observable acceptance: seed multiple real project trajectory files (including the active session, an older matching session, a missing-record session, Unicode/long fields and a damaged tail); invoke the real tool with no model/network and prove case-insensitive substring matches carry bounded session/turn/type provenance, the active session is excluded, no-match/damage/missing/limit state is explicit, and all source records plus the resume pointer are byte-identical. Prove no embeddings/vector dependency/new state file/system-prompt injection; classify the tool read-safe while plan mode remains read-only; prove it is available to parent and allowed child agents through normal tool assembly and its calls/results use existing tool rendering with no new live-frame row. Re-run `pnpm typecheck`, `pnpm test`, focused trajectory/permission/subagent/TUI rendering checks, `pnpm build`, Trellis validation, and commit/diff/clean-tree checks.
- Backlog updates: add `SER-031` as `not-started`; change it to `in-progress` only immediately before the developer handoff.

### Batch iteration outcome

| Direction | Child session and managed tasks | Host acceptance (exact independent checks and outcomes) | Commit | Final status | Iteration-log entry |
|---|---|---|---|---|---|
| SER-031 | Child session `session-20260821-100118501`; managed task `bg-fbe66514-bfc6-4573-865e-a60157d01502` (succeeded, exit 0, no correction turn). | Host inspected the 24-file implementation/task/spec diff and independently re-ran `pnpm typecheck`; `pnpm test` (all fast suites green, exit 0); `spike/verify-memory-tool.ts` (23/23); `verify-trajectory.ts` (267/267); `verify-subagents.ts` (69/69); `verify-permission-modes.ts` (108/108); `pnpm build`; archived-task validation (passed with only the recorded pre-existing >32 KiB injection warnings); `git diff --check`; `git show --check` for all three child commits; AGENTS.md size (21,746 bytes); and clean-tree verification. The offline real SDK-tool fixture proved active-session exclusion, case-insensitive Unicode-safe text retrieval, provenance, explicit damage/missing/limit states, byte-identical trajectories/snapshot/pointer, read-safe plan behavior, ordinary tool lifecycle, no system-prompt/startup/TUI surface, and no vector/index/writer/model/network path. | `0e16f23` (+ task archive `5f2a835`, journal `8fc2b84`) | done | [`Batch 45`](../iteration-log.md#batch-45--explicit-textual-long-term-recall-2026-08-21) |

- Halt condition that ended the loop: batch exhausted
- Aggregate token spend across every child delegation: `input=298 output=36,476 cacheRead=15,849,954 cacheWrite=159,701`
- Remaining backlog after this run: none unfinished; fresh research is appropriate for a future batch
