# Darwin self-evolution research — 2026-08-20

## Run — 2026-08-20T01:05:09Z

### Backlog-first decision

- Backlog inspected first: `docs/research/backlog_index.md`
- Unfinished directions found: none
- Decision: fresh research

### Research path

```text
research-path: tui
focus: TUI interaction and visual polish
share: 20% (weight 2 of 10)
draw: 2 of 20 half-units
path-source: roll
rolled-at: 2026-08-20T01:03:48.557Z
weights: tui=2 observability=0.5 sdk=1 open=1.5 peer=5
```

### Source availability and method

This was a repository-evidenced `tui` self-review on 2026-08-20 UTC. No peer product was consulted, as permitted for this path. The run read the governing TUI contracts first (`.trellis/spec/frontend/tui-testing.md`, `.trellis/spec/frontend/live-frame.md`, and `.trellis/spec/frontend/prompt-completion.md`), then inspected the current input/completion implementation, focused pty and render tests, README, and recent relevant history. Claims below cite repository paths and symbols rather than product URLs.

### Reference sources

No peer-product reference table applies to this self-review path. Repository evidence is cited in the baseline and gap tables below; no product was opened or characterized from memory.

### Peer highlights and innovations

Not applicable: no peer product was consulted on this repository-only TUI self-review.

### Current Darwin baseline

| Capability / architecture | Repository evidence | Current behavior |
|---|---|---|
| Completion navigation and acceptance | `src/tui/App.tsx` `completions`, `selectedCompletion`, `acceptCompletion`, and the Up/Down handlers (lines 1159–1178, 1463–1475); `src/tui/InputBox.tsx` `visible` (lines 75–86) | Up/Down wraps over the full candidate list and acceptance indexes that full list, but rendering always slices candidates from index zero. Once selection moves beyond the rendered prefix, no row carries the `❯` marker and Tab/Enter can accept a candidate the user cannot see. |
| Completion frame budget | `src/tui/InputBox.tsx` `MAX_COMPLETIONS` and `planPromptBox`; `.trellis/spec/frontend/live-frame.md` lines 163–168; `.trellis/spec/frontend/prompt-completion.md` lines 52–64 | The menu is one counted block, short terminals may grant fewer than the offered rows, and omitted rows are stated. Any fix must window within the existing grant rather than add an uncounted row or unbound the menu. |
| Existing completion verification | `spike/verify-tui.ts` `pathCompletion` (lines 1037–1058) and `completion`; `spike/verify-frame-budget.ts` lines 436–498 | Tests prove the cap, omission marker, normal selection, and rendered-height bound, but do not move selection past the visible prefix and therefore do not observe invisible acceptance. |
| Keyboard behavior and discoverability | `src/tui/App.tsx` `useInput` (lines 1317–1535), `Header` (lines 1739–1769), `src/tui/InputBox.tsx` `completionTitle`; README key table lines 310–319 and known limitation lines 1115–1119 | The editor implements multiline entry (`Ctrl+J` and trailing `\\`), Home/End plus readline-style `Ctrl+A`/`Ctrl+E`, `Ctrl+K`/`Ctrl+U`, `Ctrl+W`, prompt recall, queue take-back, and `Ctrl+B`. The live header advertises only `/`, `@`, Ctrl+C, and exit; the README key table omits most editor keys and incorrectly says `/` lists skills, while the known-limitations section still says input is single-line and pasted multiline text submits at the first newline. |
| Local command seam | `src/commands/custom-commands.ts` `BUILTIN_COMMAND_NAMES`/`BUILTIN_COMMAND_DESCRIPTIONS`; `src/tui/App.tsx` local report branches before the busy guard | Built-ins have canonical menu names/descriptions and local read-only reports can render as transcript notices without a model call, queueing, or a new live-frame surface. There is no in-session help command. |

### Comparison and gaps

| Peer evidence | Darwin evidence | Parity / gap / deliberate difference | Implication |
|---|---|---|---|
| No peer consulted; repository-only interaction invariant | `App.tsx` navigates all `completions`, while `InputBox.tsx` renders `completions.slice(0, plan.completionItems)` | Gap and correctness bug: keyboard state and visible state diverge whenever candidates exceed the current grant (already normal for `@`, and possible for slash commands with custom commands/skills). | Make the rendered completion viewport follow the selected candidate, keeping one visible `❯`, preserving ordering, candidate identity, omission honesty, and the existing row grant. |
| No peer consulted; repository-only discoverability finding | `App.tsx` implements editing chords; Header and README expose only a subset; README lines 1117–1119 contradict the shipped multiline contract in `.trellis/spec/frontend/tui-testing.md` lines 26–76 | Gap: working interactions are hidden, and the primary documentation actively tells users two false input behaviors. | Add a bounded, local `/help` projection for commands, prompt syntax, and keyboard interactions, and bring README statements into agreement. It must reuse canonical command metadata where possible and cost no model call or live-frame row. |

### Ranked iteration directions

`Score = 2 × Importance + Architecture fit + Evidence confidence − Implementation difficulty − Implementation risk`

| Rank | Proposed ID | Direction | Importance | Architecture fit | Evidence confidence | Implementation difficulty | Implementation risk | Score | Evidence and rationale |
|---:|---|---|---:|---:|---:|---:|---:|---:|---|
| 1 | SER-029 | Keep completion selection visible across overflow: window the existing bounded slash/path menu around the selected candidate, with omission above/below stated and Tab/Enter accepting exactly the visibly selected row | 4 | 5 | 5 | 3 | 2 | 13 | Directly demonstrated by `App.tsx` full-list selection/acceptance versus `InputBox.tsx` prefix-only rendering. Fits the pure presentation/planning seams and existing frame budget; risk is concentrated in key precedence, selected-index clamping, and exact row accounting. This must precede SER-030 because adding a built-in increases the slash candidate set. |
| 2 | SER-030 | Add bounded in-session `/help` for Darwin commands, prompt syntax, and keyboard editing, and correct the stale README input/key documentation | 3 | 5 | 5 | 2 | 2 | 12 | `App.tsx` has substantial undiscoverable interaction behavior and README lines 316/1117–1119 are false. The existing local-command/notice seam avoids model calls and frame furniture; canonical built-in metadata should be reused rather than copied. Depends on SER-029 so the enlarged command inventory remains visibly navigable. |

### Gated-out directions

None. Both non-duplicate directions considered by this run scored at or above 6.

### Recommendation

- Batch to implement: `SER-029`, then `SER-030`
- Start with: `SER-029`
- Why this order: SER-029 repairs the completion viewport invariant first; SER-030 then adds one built-in without making overflow navigation less trustworthy. No score override is needed.
- Independently observable acceptance:
  - `SER-029`: focused pure/render checks prove selected indices at the first, middle, last, and wrapped positions always have exactly one visible `❯`; above/below omissions are truthful within the unchanged grant; Tab and Enter through a real pty accept the row that was visibly selected for both slash and `@` lists; `spike/verify-frame-budget.ts`, `spike/verify-tui.ts completion`, `pathCompletion`, cursor/recall precedence checks, `pnpm typecheck`, and `pnpm test` pass.
  - `SER-030`: `/help` appears in built-in completion with a concise description; invoking it idle and during a busy turn produces a bounded local transcript report with no model call, queue mutation, or frame row; the report accurately covers `/`, `@`, `!`, multiline entry, completion, recall/queue take-back, readline editing, detail toggle, cancel, and exit; README no longer claims single-line/first-newline behavior; `MAX_COMPLETIONS` is adjusted per repository policy and free `completion`, `pathCompletion`, `recall`, `queue`, `toolDetails`, `multiline`, and focused help checks plus `pnpm typecheck`/`pnpm test` pass.
- Backlog updates: add `SER-029` and `SER-030` as `not-started`; change only `SER-029` to `in-progress` immediately before its developer handoff.

### Batch iteration outcome

| Direction | Child session and managed tasks | Host acceptance (exact independent checks and outcomes) | Commit | Final status | Iteration-log entry |
|---|---|---|---|---|---|
| SER-029 | Child `session-20260820-011013386`; implementation task `bg-4f672d5b-2702-4c23-b13a-52fafe646a3a` exit 0, no correction. One deterministic pre-launch task `bg-2cf91b41-8050-42fe-a108-3c585b8d5fd5` failed before a session/usage record because the Host passed an extra `--`. | Host inspected the full diff and independently re-ran `pnpm typecheck`, `pnpm test`, `verify-frame-budget.ts` (75), pty `completion` (52), `pathCompletion` (23), `cursor` (5), `recall` (20), `recallEmpty` (4), `queue` (17), and commit/diff/clean-tree checks; all passed. | `91ce096` (+ archive `18ebbcc`) | done | [`Batch 37`](../iteration-log.md#batch-37--visible-completion-overflow-selection-2026-08-20) |

- Halt condition that ended the loop: pending; continuing with SER-030
- Aggregate token spend across every child delegation so far: `input=262 output=36,717 cacheRead=12,984,949 cacheWrite=158,115`; the deterministic pre-launch failure emitted no usage line
- Remaining backlog after this iteration: `SER-030` (`not-started` pending handoff)
