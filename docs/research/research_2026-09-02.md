# Darwin self-evolution research — 2026-09-02

## Run — 2026-09-02T02:29:40Z

### Backlog-first decision

- Backlog router inspected first: `docs/research/backlog_index.md`
- Metadata searched across routed priority pages: `docs/research/backlog/directions-001-020.md`, `docs/research/backlog/directions-021-040.md`, `docs/research/backlog/directions-041-060.md`, `docs/research/backlog/directions-061-080.md`
- Unfinished directions found: none (`grep -n -E '^- Status:' … | grep -v -E 'done|abandoned'` matched nothing; 66 records, all `done` or `abandoned`)
- Decision: fresh research

### Research path

Rolled once, before any repository or product source was opened:

```text
research-path: open
focus: anything else worth improving
share: 15% (weight 1.5 of 10)
draw: 7 of 20 half-units
path-source: roll
rolled-at: 2026-09-02T02:30:02.382Z
weights: tui=2 observability=0.5 sdk=1 open=1.5 peer=5
```

The path came from the roll, not a user override. The script was run exactly once.

### Source availability and method

Self-review path: the evidence is this repository, read on 2026-09-02 (UTC) at HEAD `39b71eb`. Method: the CLI grammar (`src/cli-args.ts`, `src/cli.ts`), config validation (`src/config.ts`), headless driver (`src/headless-runner.ts`), `/compact` handling (`src/tui/App.tsx`, `src/agent/compact.ts`, `src/agent/runtime.ts`), the error-handling degradation table (`.trellis/spec/backend/error-handling.md`), the user guide (`docs/user-guide/reference.md`, `getting-started.md`), the pinned SDK's `SummarizingConversationManager` declaration, and three free local probes: `pnpm tsx src/cli.ts --help|--version|-h` (all exit 2 with `Unknown argument`), `node dist/src/cli.js -h` (same), and `loadConfig` against a private `HOME` whose `config.json` contained the misspelled key `thinkingEfort` (loaded without complaint). One repository-adjacent check: the GitHub issues API for `xiehust/strands-darwin` (`state=open`) returned an empty list, so no user-reported issue steered this run.

**No peer product was consulted.** The reference-source table below is therefore intentionally empty of products; the repository paths and symbols in the baseline table are the sources.

### Reference sources

| Ref | Product | Source type | URL | Accessed (UTC) | Claims supported |
|---|---|---|---|---|---|
| — | none | — | — | 2026-09-02 | Self-review path (`open`): no peer product opened; every claim below cites a repository path/symbol or a local probe |

### Peer highlights and innovations

Not applicable on this path; nothing is listed because nothing was consulted.

### Current Darwin baseline

| Capability / architecture | Repository evidence | Current behavior |
|---|---|---|
| CLI grammar discovery | `src/cli-args.ts:201–202` (`default: throw new CliUsageError(\`Unknown argument …\`)`), `src/cli.ts:5–12` (usage grammar exists only as a source comment), `src/cli.ts:62–68` (usage errors print `error: <message>` and exit 2), `src/version.ts:33` (`DARWIN_VERSION` already resolved for trajectory records) | `darwin --help`, `darwin -h`, `darwin --version` all fail with `error: Unknown argument "--help"` and exit 2 — verified live against `src/cli.ts` and `dist/src/cli.js`. No CLI surface prints the grammar or the version; `docs/user-guide/reference.md:6–24` is the only place it is written down |
| Config key validation | `src/config.ts:283` (`MODEL_KEYS`), `:305` (`SESSION_KEYS`), `:593–608` (`validate`), `:666–681` (misplaced-half detection for *known* keys), `:690` (`enable` on entries) | Known keys in the wrong half raise `ConfigError`; values are type-checked. A key that is in neither list is never examined: a private-HOME probe with `{"thinkingEfort":"high"}` loaded successfully and silently kept the default effort |
| Error-handling principle for config | `.trellis/spec/backend/error-handling.md:28` (invalid value → `ConfigError`), `:30` ("Explicit intent must never be guessed or silently ignored"), `:49` ("a key in the wrong half would silently do nothing") | The recorded rationale for refusing misplaced keys — silent no-op is a failure — applies verbatim to misspelled keys, which currently do exactly that |
| Headless prompt input | `src/headless-runner.ts:34` (`HeadlessOptions = CliOptions & { prompt: string }`), `:194`/`:206` (the `-p` string is the only model input); `grep -n stdin src/cli.ts src/headless.ts src/headless-runner.ts` → no matches | `-p` takes the prompt only from argv; piped stdin is ignored without notice, so `git diff \| darwin -p "review this"` sends the sentence and not the diff |
| Headless verification harness | `spike/verify-headless.ts:388–389` spawns `-p` with `stdio: ['ignore', 'pipe', 'pipe']`; `src/tools/background-bash.ts:351` starts jobs with `stdio: ['ignore', fd, fd]` | Existing headless suites and the developer skill's managed background children present `/dev/null` on stdin — a stdin reader would see immediate EOF there |
| `/compact` | `src/tui/App.tsx:1423–1431` (`/compact takes no arguments`), `src/agent/runtime.ts:603–606` (one process-lifetime `compactionManager` built with `summaryRatio: 0.8` and no `summarizationSystemPrompt`), `src/agent/runtime.ts:1061` (`compact()` is its only consumer), `src/agent/compact.ts:29–60` (reversible reduce loop) | Compaction summarizes with the SDK default prompt only; the user cannot say what the summary must keep |
| SDK summarization prompt hook | `node_modules/@strands-agents/sdk/dist/src/conversation-manager/summarizing-conversation-manager.d.ts:31–34` (`summarizationSystemPrompt?: string`, constructor-only), `…/compression/context-compression.d.ts:3` (`DEFAULT_SUMMARIZATION_PROMPT` declared there, not re-exported from the package root) | The extension point exists but is fixed at construction and the default prompt is only reachable through a deep import |
| Session identity after quit | `src/tui/status-format.ts:100` (session id shown by `/status`), `src/cli.ts:250–256` (exit path prints nothing), `docs/user-guide/reference.md:9–13` (`--resume` bare form reopens the last session) | Nothing is printed at exit; the bare `--resume` already covers the common case |

### Comparison and gaps

| Peer evidence | Darwin evidence | Parity / gap / deliberate difference | Implication |
|---|---|---|---|
| n/a (self-review) | `src/cli-args.ts:201–202`, live probe exit 2 | **Gap** — no `--help`/`--version`; the one place the grammar is authoritative (`src/cli.ts:5–12`) is a comment | A bounded local `--help`/`-h`/`--version`/`-V` routed like `sessions`/`trajectory` — before any runtime, model or Ink import — with usage errors pointing at `--help` |
| n/a | `src/config.ts:593–608` vs `error-handling.md:30,49`; misspelled-key probe | **Gap, contradicting a recorded principle** — misspelled keys are silently ignored while misplaced known keys are refused | Extend `validate`/`validateModelChoices` to refuse unknown root and entry keys with a `ConfigError` naming the key, its location and the nearest known key |
| n/a | `src/headless-runner.ts:194,206`; no `stdin` reference in the headless path | **Gap** — piped stdin is dropped silently in `-p` mode | Read non-TTY stdin to EOF under a stated byte cap and append it to the one-shot prompt as a delimited block; TTY/empty stdin behaves exactly as today |
| n/a | `src/tui/App.tsx:1428`; SDK `summarizationSystemPrompt` constructor-only | **Gap with an architecture wart** — `/compact <focus>` is feasible only by building a per-call manager, and the SDK default prompt is not root-exported | Optional focus text for `/compact`; unfocused `/compact` output must stay byte-identical, and the default prompt must not be copied into darwin where it would drift |
| n/a | `src/cli.ts:250–256`, `reference.md:9` | **Noted, not proposed** — an exit line naming the session id would duplicate what bare `--resume` and `darwin sessions` already provide | No direction this run; revisit only with user evidence |

### Ranked iteration directions

`Score = 2 × Importance + Architecture fit + Evidence confidence − Implementation difficulty − Implementation risk`

| Rank | Proposed ID | Direction | Importance | Architecture fit | Evidence confidence | Implementation difficulty | Implementation risk | Score | Evidence and rationale |
|---:|---|---|---:|---:|---:|---:|---:|---:|---|
| 1 | SER-048 | Add `--help`/`-h` and `--version`/`-V` to the CLI as bounded local output routed before any runtime import, and point every `CliUsageError` at `--help` | 3 | 5 | 5 | 1 | 1 | 14 | `src/cli-args.ts:201–202` rejects them today (live probe: exit 2 on `src` and `dist`); `src/cli.ts:5–12` already holds the canonical grammar and `src/version.ts:33` the version; `sessions`/`trajectory` (`src/cli.ts:48–58`) show the pre-runtime routing pattern. Pure text, no model/network/config read; `spike/verify-cli-args.ts` is the existing free suite |
| 2 | SER-049 | Refuse unknown keys in `~/.darwin/config.json` (root and `models` entries) with a `ConfigError` that names the key, where it was found and the nearest known key | 3 | 5 | 5 | 2 | 2 | 12 | `MODEL_KEYS`/`SESSION_KEYS` (`src/config.ts:283,305`) already enumerate the schema; `:666–681` is the pattern for the message shape; `error-handling.md:30,49` record the principle the current behaviour violates; misspelled-key probe loaded silently. Risk is a start refusal for users carrying stray keys — mitigated by naming the exact key and file, the same trade the spec already made at `:49` |
| 3 | SER-050 | Let `-p` read piped (non-TTY) stdin to EOF under a stated byte cap and append it to the one-shot prompt as one delimited block; TTY or empty stdin leaves behaviour byte-identical | 3 | 3 | 4 | 2 | 3 | 8 | No `stdin` handling anywhere in `src/cli.ts`/`src/headless.ts`/`src/headless-runner.ts`; `verify-headless.ts:388` and `background-bash.ts:351` use `stdio 'ignore'`, so existing harnesses and managed children see EOF immediately. Risk: a parent that holds a pipe open without writing makes `-p` wait (must be stated in `reference.md`); `userInput` record cap `MAX_FIELD_CHARS` (`src/trajectory/record.ts:24`) already bounds what the trajectory keeps |
| 4 | SER-051 | Accept optional focus text on `/compact <focus>` (and `--compact-before`'s existing behaviour unchanged): build the compaction manager per call with the SDK default summarization prompt plus a bounded user focus, never altering unfocused output | 3 | 3 | 4 | 3 | 3 | 7 | `src/tui/App.tsx:1428` refuses arguments; `runtime.ts:603–606` builds one manager whose only consumer is `compact()` (`:1061`), so a per-call manager is a local change; SDK `summarizationSystemPrompt` is constructor-only and `DEFAULT_SUMMARIZATION_PROMPT` lives at `…/compression/context-compression.d.ts:3`, not the package root — the child must reach it without a copied string (deep import through the pinned patch or an SDK re-export), or halt on that premise |

No dependencies among the four; ordering is by score, which here is also cheapest-and-safest first, so each later iteration starts from an accepted tree.

### Gated-out directions

| Direction | Score | Gate | Why it fell below | Kept anyway? |
|---|---:|---:|---|---|
| none | — | 6 | Every scored direction this run met the gate; the exit-time session-id line was not scored because it duplicates bare `--resume` (`reference.md:9`) and is recorded above as noted-not-proposed | — |

### Recommendation

- Batch to implement: `SER-048`, `SER-049`, `SER-050`, `SER-051`
- Start with: `SER-048`
- Why this order: independent directions, ranked by score; the two zero-model-call CLI/config changes ship first, the two behaviour changes (stdin, focused compaction) follow, with the one that carries an SDK-surface premise last so a halt there costs nothing already accepted
- Independently observable acceptance: SER-048 — `pnpm tsx src/cli.ts --help`/`-h`/`--version`/`-V` exit 0, print the grammar or `darwin <version>` matching `package.json`, and import no runtime/Ink module (assert in `spike/verify-cli-args.ts` the way `verify-trajectory.ts` asserts import graphs); every `CliUsageError` path still exits 2 and now names `--help`; `reference.md` documents the flags; `pnpm typecheck` + full `pnpm test` green. SER-049 — `spike/verify-config.ts` cases: misspelled root key, unknown entry key, `$schema`-style stray key each raise `ConfigError` naming key + location + nearest known key; every valid documented key still loads; `error-handling.md` table gains the row; `configuration.md` states the rule; `pnpm typecheck` + full `pnpm test` green. SER-050 — `spike/verify-headless.ts`: piped stdin appears in the model-facing prompt once and in the `userInput` record under the field cap; `stdio 'ignore'`/TTY runs are byte-identical to today; over-cap stdin is refused or truncated with the omission stated; `reference.md` documents the pipe semantics and the open-pipe caveat; `pnpm typecheck` + full `pnpm test` green. SER-051 — `/compact` with no argument produces the same summary message shape as before (`spike/verify-compact.ts` extended); `/compact keep the file list` reaches the summarizer prompt as one bounded block; `spike/verify-tui.ts compacting` green; `reference.md`/`help-format.ts` updated; `pnpm typecheck` + full `pnpm test` green
- Backlog updates: appended `SER-048` (Priority 67), `SER-049` (68), `SER-050` (69), `SER-051` (70) as `not-started` to `docs/research/backlog/directions-061-080.md`, then `SER-052` (71) from the Host-found direction below; no rollover (range 061–080 now holds 11 records); `spike/verify-skills.ts` record count raised 66 → 70 → 71; no direction abandoned by the gate; the selected record moves to `in-progress` only immediately before each `developer` handoff

### Host-found direction during the batch

While accepting SER-051, the Host found that the live `spike/verify-tui.ts compacting` scenario has been red since `f4e3271` (2026-08-31) for a reason unrelated to the focus feature, and proved it deterministically: `compactConversation` (`src/agent/compact.ts`) loops on `reduce()` until the message count drops to `preserveRecentMessages + 1`, but the SDK's `_summarizeOldest` (`summarizing-conversation-manager.js:73–100`) always summarizes `max(1, floor(len × 0.8))` messages and splices one summary back, so a 2-message conversation with `preserveRecentMessages: 0` never shrinks — a fake-model probe made 26 paid summarizer calls before a Host bail-out — and because darwin calls `reduce()` without `error`, the SDK's proactive path swallowed the failure (`:49–64`) and darwin still reported `compacted: true`. Compaction is also uncancellable (`App.tsx`: `compaction cannot be cancelled safely`). This is the run's fifth direction, appended with the same scoring and gate:

| Rank | Proposed ID | Direction | Importance | Architecture fit | Evidence confidence | Implementation difficulty | Implementation risk | Score | Evidence and rationale |
|---:|---|---|---:|---:|---:|---:|---:|---:|---|
| 5 | SER-052 | Make `/compact` terminate and report honestly: stop when a pass does not shrink, surface swallowed summarization failures as failure, repair the `compacting` scenario | 4 | 5 | 5 | 2 | 2 | 14 | `src/agent/compact.ts` loop; SDK `_summarizeOldest` arithmetic and `reduce()` swallow semantics; Host probe (26 calls, `compacted: true` with `after=2`); scenario history (green in Batch SER-041, red since `f4e3271`). Unbounded paid model calls the user cannot cancel, reported as success — correctness and cost, one guard plus honest error routing |

### Batch iteration outcome

| Direction | Child session and managed tasks | Host acceptance (exact independent checks and outcomes) | Commit | Final status | Iteration-log entry |
|---|---|---|---|---|---|
| SER-048 | child session `session-20260902-024725444`; managed task `bg-42f5b885`; Trellis task `.trellis/tasks/09-02-ser-048-cli-help-version` (archived `7c9e5bd`) | `verify-cli-args.ts` 43/43, `pnpm typecheck` clean, full `pnpm test` exit 0 with zero FAIL lines, `pnpm build` clean, `dist` probes for `--help`/`-V`/`--bogus` correct, whole diff read, tree clean | `ca7aa7c` | `done` | `docs/iteration-log.md` Batch 71 |
| SER-049 | child session `session-20260902-030515467`; managed task `bg-f45d1e13`; Trellis task `.trellis/tasks/09-02-ser-049-unknown-config-keys` (archived `68b4027`) | `verify-config.ts` 300/300, `pnpm typecheck` clean, full `pnpm test` exit 0 with zero FAIL lines (confirming run; first run hit the pre-existing heartbeat flake), `pnpm build` clean, private-HOME `dist` probe names both unknown keys with suggestions and exits 1, whole diff read, tree clean | `8481bab` | `done` | `docs/iteration-log.md` Batch 72 |
| SER-050 | child session `session-20260902-034134274`; managed task `bg-8164618f`; Trellis task `.trellis/tasks/09-02-ser-050-piped-stdin` (archived `1b5059c`) | `verify-headless.ts` 182/182, `verify-cli-args.ts` 43/43, `pnpm typecheck` clean, full `pnpm test` exit 0 with zero FAIL lines, `pnpm build` clean, live `dist` probes (piped block once in replay and one `userInput`; `/dev/null` no block; 300 000-byte pipe refused exit 2), whole diff read, tree clean | `5ca7772` | `done` | `docs/iteration-log.md` Batch 73 |
| SER-051 | child session `session-20260902-040942462`; managed task `bg-2aeba1f8`; Trellis task `.trellis/tasks/09-02-ser-051-compact-focus` (archived `4db9405`) | `verify-compact.ts` 52/52, `verify-help-command.ts` 34/34, `tui completion` 68/68, `pnpm typecheck` clean after fresh patch apply, full `pnpm test` exit 0 with zero FAIL lines, `pnpm build` clean, re-export present in installed SDK, whole diff read, tree clean; live `tui compacting` red but proven pre-existing by a Host deterministic probe (26 summarizer calls, no shrink, swallowed failure reported as success) → SER-052 | `bd050d2` | `done` | `docs/iteration-log.md` Batch 74 |

- Halt condition that ended the loop: `<pending>`
- Aggregate token spend across every child delegation: `<pending>`
- Remaining backlog after this run: `<pending>`
