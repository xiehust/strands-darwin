# Darwin self-evolution research — 2026-09-02

## Run — 2026-09-02T02:29:40Z

### Backlog-first decision

- Backlog router inspected first: `docs/research/backlog_index.md`
- Metadata searched across routed priority pages: `docs/research/backlog/directions-001-020.md`, `docs/research/backlog/directions-021-040.md`, `docs/research/backlog/directions-041-060.md`, `docs/research/backlog/directions-061-080.md`
- Unfinished directions found: none (`grep -n -E '^- Status:' … | grep -v -E 'done|abandoned'` matched nothing; 66 records, all `done` or `abandoned`)
- Decision: fresh research

### Research path

Rolled once, before any repository or product source was opened:

```text
research-path: open
focus: anything else worth improving
share: 15% (weight 1.5 of 10)
draw: 7 of 20 half-units
path-source: roll
rolled-at: 2026-09-02T02:30:02.382Z
weights: tui=2 observability=0.5 sdk=1 open=1.5 peer=5
```

The path came from the roll, not a user override. The script was run exactly once.

### Source availability and method

Self-review path: the evidence is this repository, read on 2026-09-02 (UTC) at HEAD `39b71eb`. Method: the CLI grammar (`src/cli-args.ts`, `src/cli.ts`), config validation (`src/config.ts`), headless driver (`src/headless-runner.ts`), `/compact` handling (`src/tui/App.tsx`, `src/agent/compact.ts`, `src/agent/runtime.ts`), the error-handling degradation table (`.trellis/spec/backend/error-handling.md`), the user guide (`docs/user-guide/reference.md`, `getting-started.md`), the pinned SDK's `SummarizingConversationManager` declaration, and three free local probes: `pnpm tsx src/cli.ts --help|--version|-h` (all exit 2 with `Unknown argument`), `node dist/src/cli.js -h` (same), and `loadConfig` against a private `HOME` whose `config.json` contained the misspelled key `thinkingEfort` (loaded without complaint). One repository-adjacent check: the GitHub issues API for `xiehust/strands-darwin` (`state=open`) returned an empty list, so no user-reported issue steered this run.

**No peer product was consulted.** The reference-source table below is therefore intentionally empty of products; the repository paths and symbols in the baseline table are the sources.

### Reference sources

| Ref | Product | Source type | URL | Accessed (UTC) | Claims supported |
|---|---|---|---|---|---|
| — | none | — | — | 2026-09-02 | Self-review path (`open`): no peer product opened; every claim below cites a repository path/symbol or a local probe |

### Peer highlights and innovations

Not applicable on this path; nothing is listed because nothing was consulted.

### Current Darwin baseline

| Capability / architecture | Repository evidence | Current behavior |
|---|---|---|
| CLI grammar discovery | `src/cli-args.ts:201–202` (`default: throw new CliUsageError(\`Unknown argument …\`)`), `src/cli.ts:5–12` (usage grammar exists only as a source comment), `src/cli.ts:62–68` (usage errors print `error: <message>` and exit 2), `src/version.ts:33` (`DARWIN_VERSION` already resolved for trajectory records) | `darwin --help`, `darwin -h`, `darwin --version` all fail with `error: Unknown argument "--help"` and exit 2 — verified live against `src/cli.ts` and `dist/src/cli.js`. No CLI surface prints the grammar or the version; `docs/user-guide/reference.md:6–24` is the only place it is written down |
| Config key validation | `src/config.ts:283` (`MODEL_KEYS`), `:305` (`SESSION_KEYS`), `:593–608` (`validate`), `:666–681` (misplaced-half detection for *known* keys), `:690` (`enable` on entries) | Known keys in the wrong half raise `ConfigError`; values are type-checked. A key that is in neither list is never examined: a private-HOME probe with `{"thinkingEfort":"high"}` loaded successfully and silently kept the default effort |
| Error-handling principle for config | `.trellis/spec/backend/error-handling.md:28` (invalid value → `ConfigError`), `:30` ("Explicit intent must never be guessed or silently ignored"), `:49` ("a key in the wrong half would silently do nothing") | The recorded rationale for refusing misplaced keys — silent no-op is a failure — applies verbatim to misspelled keys, which currently do exactly that |
| Headless prompt input | `src/headless-runner.ts:34` (`HeadlessOptions = CliOptions & { prompt: string }`), `:194`/`:206` (the `-p` string is the only model input); `grep -n stdin src/cli.ts src/headless.ts src/headless-runner.ts` → no matches | `-p` takes the prompt only from argv; piped stdin is ignored without notice, so `git diff \| darwin -p "review this"` sends the sentence and not the diff |
| Headless verification harness | `spike/verify-headless.ts:388–389` spawns `-p` with `stdio: ['ignore', 'pipe', 'pipe']`; `src/tools/background-bash.ts:351` starts jobs with `stdio: ['ignore', fd, fd]` | Existing headless suites and the developer skill's managed background children present `/dev/null` on stdin — a stdin reader would see immediate EOF there |
| `/compact` | `src/tui/App.tsx:1423–1431` (`/compact takes no arguments`), `src/agent/runtime.ts:603–606` (one process-lifetime `compactionManager` built with `summaryRatio: 0.8` and no `summarizationSystemPrompt`), `src/agent/runtime.ts:1061` (`compact()` is its only consumer), `src/agent/compact.ts:29–60` (reversible reduce loop) | Compaction summarizes with the SDK default prompt only; the user cannot say what the summary must keep |
| SDK summarization prompt hook | `node_modules/@strands-agents/sdk/dist/src/conversation-manager/summarizing-conversation-manager.d.ts:31–34` (`summarizationSystemPrompt?: string`, constructor-only), `…/compression/context-compression.d.ts:3` (`DEFAULT_SUMMARIZATION_PROMPT` declared there, not re-exported from the package root) | The extension point exists but is fixed at construction and the default prompt is only reachable through a deep import |
| Session identity after quit | `src/tui/status-format.ts:100` (session id shown by `/status`), `src/cli.ts:250–256` (exit path prints nothing), `docs/user-guide/reference.md:9–13` (`--resume` bare form reopens the last session) | Nothing is printed at exit; the bare `--resume` already covers the common case |

### Comparison and gaps

| Peer evidence | Darwin evidence | Parity / gap / deliberate difference | Implication |
|---|---|---|---|
| n/a (self-review) | `src/cli-args.ts:201–202`, live probe exit 2 | **Gap** — no `--help`/`--version`; the one place the grammar is authoritative (`src/cli.ts:5–12`) is a comment | A bounded local `--help`/`-h`/`--version`/`-V` routed like `sessions`/`trajectory` — before any runtime, model or Ink import — with usage errors pointing at `--help` |
| n/a | `src/config.ts:593–608` vs `error-handling.md:30,49`; misspelled-key probe | **Gap, contradicting a recorded principle** — misspelled keys are silently ignored while misplaced known keys are refused | Extend `validate`/`validateModelChoices` to refuse unknown root and entry keys with a `ConfigError` naming the key, its location and the nearest known key |
| n/a | `src/headless-runner.ts:194,206`; no `stdin` reference in the headless path | **Gap** — piped stdin is dropped silently in `-p` mode | Read non-TTY stdin to EOF under a stated byte cap and append it to the one-shot prompt as a delimited block; TTY/empty stdin behaves exactly as today |
| n/a | `src/tui/App.tsx:1428`; SDK `summarizationSystemPrompt` constructor-only | **Gap with an architecture wart** — `/compact <focus>` is feasible only by building a per-call manager, and the SDK default prompt is not root-exported | Optional focus text for `/compact`; unfocused `/compact` output must stay byte-identical, and the default prompt must not be copied into darwin where it would drift |
| n/a | `src/cli.ts:250–256`, `reference.md:9` | **Noted, not proposed** — an exit line naming the session id would duplicate what bare `--resume` and `darwin sessions` already provide | No direction this run; revisit only with user evidence |

### Ranked iteration directions

`Score = 2 × Importance + Architecture fit + Evidence confidence − Implementation difficulty − Implementation risk`

| Rank | Proposed ID | Direction | Importance | Architecture fit | Evidence confidence | Implementation difficulty | Implementation risk | Score | Evidence and rationale |
|---:|---|---|---:|---:|---:|---:|---:|---:|---|
| 1 | SER-048 | Add `--help`/`-h` and `--version`/`-V` to the CLI as bounded local output routed before any runtime import, and point every `CliUsageError` at `--help` | 3 | 5 | 5 | 1 | 1 | 14 | `src/cli-args.ts:201–202` rejects them today (live probe: exit 2 on `src` and `dist`); `src/cli.ts:5–12` already holds the canonical grammar and `src/version.ts:33` the version; `sessions`/`trajectory` (`src/cli.ts:48–58`) show the pre-runtime routing pattern. Pure text, no model/network/config read; `spike/verify-cli-args.ts` is the existing free suite |
| 2 | SER-049 | Refuse unknown keys in `~/.darwin/config.json` (root and `models` entries) with a `ConfigError` that names the key, where it was found and the nearest known key | 3 | 5 | 5 | 2 | 2 | 12 | `MODEL_KEYS`/`SESSION_KEYS` (`src/config.ts:283,305`) already enumerate the schema; `:666–681` is the pattern for the message shape; `error-handling.md:30,49` record the principle the current behaviour violates; misspelled-key probe loaded silently. Risk is a start refusal for users carrying stray keys — mitigated by naming the exact key and file, the same trade the spec already made at `:49` |
| 3 | SER-050 | Let `-p` read piped (non-TTY) stdin to EOF under a stated byte cap and append it to the one-shot prompt as one delimited block; TTY or empty stdin leaves behaviour byte-identical | 3 | 3 | 4 | 2 | 3 | 8 | No `stdin` handling anywhere in `src/cli.ts`/`src/headless.ts`/`src/headless-runner.ts`; `verify-headless.ts:388` and `background-bash.ts:351` use `stdio 'ignore'`, so existing harnesses and managed children see EOF immediately. Risk: a parent that holds a pipe open without writing makes `-p` wait (must be stated in `reference.md`); `userInput` record cap `MAX_FIELD_CHARS` (`src/trajectory/record.ts:24`) already bounds what the trajectory keeps |
| 4 | SER-051 | Accept optional focus text on `/compact <focus>` (and `--compact-before`'s existing behaviour unchanged): build the compaction manager per call with the SDK default summarization prompt plus a bounded user focus, never altering unfocused output | 3 | 3 | 4 | 3 | 3 | 7 | `src/tui/App.tsx:1428` refuses arguments; `runtime.ts:603–606` builds one manager whose only consumer is `compact()` (`:1061`), so a per-call manager is a local change; SDK `summarizationSystemPrompt` is constructor-only and `DEFAULT_SUMMARIZATION_PROMPT` lives at `…/compression/context-compression.d.ts:3`, not the package root — the child must reach it without a copied string (deep import through the pinned patch or an SDK re-export), or halt on that premise |

No dependencies among the four; ordering is by score, which here is also cheapest-and-safest first, so each later iteration starts from an accepted tree.

### Gated-out directions

| Direction | Score | Gate | Why it fell below | Kept anyway? |
|---|---:|---:|---|---|
| none | — | 6 | Every scored direction this run met the gate; the exit-time session-id line was not scored because it duplicates bare `--resume` (`reference.md:9`) and is recorded above as noted-not-proposed | — |

### Recommendation

- Batch to implement: `SER-048`, `SER-049`, `SER-050`, `SER-051`
- Start with: `SER-048`
- Why this order: independent directions, ranked by score; the two zero-model-call CLI/config changes ship first, the two behaviour changes (stdin, focused compaction) follow, with the one that carries an SDK-surface premise last so a halt there costs nothing already accepted
- Independently observable acceptance: SER-048 — `pnpm tsx src/cli.ts --help`/`-h`/`--version`/`-V` exit 0, print the grammar or `darwin <version>` matching `package.json`, and import no runtime/Ink module (assert in `spike/verify-cli-args.ts` the way `verify-trajectory.ts` asserts import graphs); every `CliUsageError` path still exits 2 and now names `--help`; `reference.md` documents the flags; `pnpm typecheck` + full `pnpm test` green. SER-049 — `spike/verify-config.ts` cases: misspelled root key, unknown entry key, `$schema`-style stray key each raise `ConfigError` naming key + location + nearest known key; every valid documented key still loads; `error-handling.md` table gains the row; `configuration.md` states the rule; `pnpm typecheck` + full `pnpm test` green. SER-050 — `spike/verify-headless.ts`: piped stdin appears in the model-facing prompt once and in the `userInput` record under the field cap; `stdio 'ignore'`/TTY runs are byte-identical to today; over-cap stdin is refused or truncated with the omission stated; `reference.md` documents the pipe semantics and the open-pipe caveat; `pnpm typecheck` + full `pnpm test` green. SER-051 — `/compact` with no argument produces the same summary message shape as before (`spike/verify-compact.ts` extended); `/compact keep the file list` reaches the summarizer prompt as one bounded block; `spike/verify-tui.ts compacting` green; `reference.md`/`help-format.ts` updated; `pnpm typecheck` + full `pnpm test` green
- Backlog updates: appended `SER-048` (Priority 67), `SER-049` (68), `SER-050` (69), `SER-051` (70) as `not-started` to `docs/research/backlog/directions-061-080.md`, then `SER-052` (71) from the Host-found direction below; no rollover (range 061–080 now holds 11 records); `spike/verify-skills.ts` record count raised 66 → 70 → 71; no direction abandoned by the gate; the selected record moves to `in-progress` only immediately before each `developer` handoff

### Host-found direction during the batch

While accepting SER-051, the Host found that the live `spike/verify-tui.ts compacting` scenario has been red since `f4e3271` (2026-08-31) for a reason unrelated to the focus feature, and proved it deterministically: `compactConversation` (`src/agent/compact.ts`) loops on `reduce()` until the message count drops to `preserveRecentMessages + 1`, but the SDK's `_summarizeOldest` (`summarizing-conversation-manager.js:73–100`) always summarizes `max(1, floor(len × 0.8))` messages and splices one summary back, so a 2-message conversation with `preserveRecentMessages: 0` never shrinks — a fake-model probe made 26 paid summarizer calls before a Host bail-out — and because darwin calls `reduce()` without `error`, the SDK's proactive path swallowed the failure (`:49–64`) and darwin still reported `compacted: true`. Compaction is also uncancellable (`App.tsx`: `compaction cannot be cancelled safely`). This is the run's fifth direction, appended with the same scoring and gate:

| Rank | Proposed ID | Direction | Importance | Architecture fit | Evidence confidence | Implementation difficulty | Implementation risk | Score | Evidence and rationale |
|---:|---|---|---:|---:|---:|---:|---:|---:|---|
| 5 | SER-052 | Make `/compact` terminate and report honestly: stop when a pass does not shrink, surface swallowed summarization failures as failure, repair the `compacting` scenario | 4 | 5 | 5 | 2 | 2 | 14 | `src/agent/compact.ts` loop; SDK `_summarizeOldest` arithmetic and `reduce()` swallow semantics; Host probe (26 calls, `compacted: true` with `after=2`); scenario history (green in Batch SER-041, red since `f4e3271`). Unbounded paid model calls the user cannot cancel, reported as success — correctness and cost, one guard plus honest error routing |

### Batch iteration outcome

| Direction | Child session and managed tasks | Host acceptance (exact independent checks and outcomes) | Commit | Final status | Iteration-log entry |
|---|---|---|---|---|---|
| SER-048 | child session `session-20260902-024725444`; managed task `bg-42f5b885`; Trellis task `.trellis/tasks/09-02-ser-048-cli-help-version` (archived `7c9e5bd`) | `verify-cli-args.ts` 43/43, `pnpm typecheck` clean, full `pnpm test` exit 0 with zero FAIL lines, `pnpm build` clean, `dist` probes for `--help`/`-V`/`--bogus` correct, whole diff read, tree clean | `ca7aa7c` | `done` | `docs/iteration-log.md` Batch 71 |
| SER-049 | child session `session-20260902-030515467`; managed task `bg-f45d1e13`; Trellis task `.trellis/tasks/09-02-ser-049-unknown-config-keys` (archived `68b4027`) | `verify-config.ts` 300/300, `pnpm typecheck` clean, full `pnpm test` exit 0 with zero FAIL lines (confirming run; first run hit the pre-existing heartbeat flake), `pnpm build` clean, private-HOME `dist` probe names both unknown keys with suggestions and exits 1, whole diff read, tree clean | `8481bab` | `done` | `docs/iteration-log.md` Batch 72 |
| SER-050 | child session `session-20260902-034134274`; managed task `bg-8164618f`; Trellis task `.trellis/tasks/09-02-ser-050-piped-stdin` (archived `1b5059c`) | `verify-headless.ts` 182/182, `verify-cli-args.ts` 43/43, `pnpm typecheck` clean, full `pnpm test` exit 0 with zero FAIL lines, `pnpm build` clean, live `dist` probes (piped block once in replay and one `userInput`; `/dev/null` no block; 300 000-byte pipe refused exit 2), whole diff read, tree clean | `5ca7772` | `done` | `docs/iteration-log.md` Batch 73 |
| SER-051 | child session `session-20260902-040942462`; managed task `bg-2aeba1f8`; Trellis task `.trellis/tasks/09-02-ser-051-compact-focus` (archived `4db9405`) | `verify-compact.ts` 52/52, `verify-help-command.ts` 34/34, `tui completion` 68/68, `pnpm typecheck` clean after fresh patch apply, full `pnpm test` exit 0 with zero FAIL lines, `pnpm build` clean, re-export present in installed SDK, whole diff read, tree clean; live `tui compacting` red but proven pre-existing by a Host deterministic probe (26 summarizer calls, no shrink, swallowed failure reported as success) → SER-052 | `bd050d2` | `done` | `docs/iteration-log.md` Batch 74 |
| SER-052 | child session `session-20260902-044757342`; managed task `bg-37875105`; Trellis task `.trellis/tasks/09-02-ser-052-compact-loop-guard` (archived `87ef668`) | `verify-compact.ts` 70/70, `verify-headless.ts` 182/182, `pnpm typecheck` clean, full `pnpm test` exit 0 with zero FAIL lines, `pnpm build` clean, live `tui compacting` 5/5 (15 s), Host fake-model probe (2/0 one call no-op; 16/0 three calls → 2; failures reject + restore), whole diff read, tree clean | `618f771` | `done` | `docs/iteration-log.md` Batch 75 |

- Halt condition that ended the loop: **batch exhausted** — all five directions (`SER-048`–`SER-052`) closed `done` after independent acceptance; no record in the batch remains unfinished
- Aggregate token spend across every child delegation: `input=692 output=261,911 cacheRead=39,518,620 cacheWrite=664,410` (five children — SER-048 `76/31,530/3,398,594/87,958`, SER-049 `198/51,400/11,707,500/137,896`, SER-050 `140/55,198/8,246,748/140,811`, SER-051 `166/48,647/10,007,632/151,162`, SER-052 `112/75,136/6,158,146/146,583` — every child reported all four buckets; excludes the Host's own two one-call live probes for SER-050 and two live `compacting` runs)
- Remaining backlog after this run: nothing unfinished — all 71 routed records are `done` or `abandoned`, so a fresh research run is the recommended next step. Candidate for that run, not queued here: the `verify-subagent-heartbeats.ts` "increasing elapsed heartbeats" wall-clock assertion flaked twice under full-suite load this run (never alone), a test-stability item

## Run — 2026-09-02T09:37:20Z

### Backlog-first decision

- Backlog router inspected first: `docs/research/backlog_index.md`
- Metadata searched across routed priority pages: `docs/research/backlog/directions-001-020.md`, `docs/research/backlog/directions-021-040.md`, `docs/research/backlog/directions-041-060.md`, `docs/research/backlog/directions-061-080.md`
- Unfinished directions found: none (`rg -c 'Status.*(not-started|in-progress)'` over every page matched no status line; the two `in-progress`/`not-started` tokens in `directions-061-080.md:19` are inside SER-043's repair notes, not status lines; 73 records, all `done` or `abandoned`, last closure `578cce5`)
- Decision: fresh research

### Research path

The user directed this run at Claude Code's tools reference (`https://code.claude.com/docs/en/tools-reference`), so the script was run once with `--path peer` before any source was opened. This is an override, not a roll:

```text
research-path: peer
focus: comparable coding-agent product analysis
share: 50% (weight 5 of 10)
draw: none — path was not rolled
path-source: override (user-directed)
rolled-at: 2026-09-02T09:37:20.166Z
weights: tui=2 observability=0.5 sdk=1 open=1.5 peer=5
```

The script was run exactly once.

### Source availability and method

Accessed 2026-09-02 (UTC) at HEAD `578cce5`. Tools: `curl` for primary documentation and repository raw files, the `web-search` MCP server for discovery only, `pnpm tsx` for one offline classifier probe against `src/agent/permission.ts` (`classify` + `assessRisk`, no model call). The user's page was fetched twice: once as HTML through the runtime's `http_request` (656 KB, offloaded by the context offloader — itself evidence for SER-056 below) and once as `tools-reference.md` (106 KB markdown, the version read). Scope narrowing is deliberate and user-directed: Claude Code's built-in tool set is the subject; the other required peers were consulted only for their tool sets. PenguinHarness publishes no per-tool documentation in its README, so only its stated design stance is cited; no claim is made about its individual tools.

### Reference sources

| Ref | Product | Source type | URL | Accessed (UTC) | Claims supported |
|---|---|---|---|---|---|
| S1 | Claude Code | Primary docs (tools reference, markdown edition) | <https://code.claude.com/docs/en/tools-reference> | 2026-09-02 | Full built-in tool table with permission column; Bash: separate process per command, `cd` carry-over with reset notice, default 2 min / ceiling 10 min timeouts, output >30k chars saved to a file with preview, exit-1 benign list, auto-background on timeout with explicit result text, never for `sleep`/`git`/unparseable compounds; Edit: exact match, uniqueness, `replace_all: true`, read-before-edit, "file changed on disk" note; Glob 100-file mtime-sorted cap with truncation flag; Grep on ripgrep with three output modes, rg diagnostics passed through; LSP after-edit diagnostics; Monitor background event feed (not on Bedrock); Read PARTIAL view / empty-file / past-end notices, image resize, PDF pages; WebFetch HTML→Markdown, `Accept` prefers Markdown, HTTPS upgrade, cross-host redirect reported not followed, 15 min cache, lossy-by-design; WebSearch 200/session cap; Task tools removed on newer models; ToolSearch deferred loading; AskUserQuestion with auto-continue timeout |
| S2 | Codex | Source repository (tool handler directory listing and specs) | <https://github.com/openai/codex/tree/main/codex-rs/core/src/tools/handlers> ; `get_context_remaining_spec.rs`, `request_user_input_spec.rs`, `tool_search_spec.rs` | 2026-09-02 | Handlers exist for `apply_patch`, `unified_exec`/`shell`, `view_image`, `plan`, `request_user_input`, `request_permissions`, `tool_search`, `mcp_resource`, `get_context_remaining` ("Get the remaining tokens in the current context window"), `new_context_window`, `multi_agents`, `sleep`, `wait_for_environment`, `current_time`. Inference (not sourced): the set is capability-parity with Claude Code on user questions, tool search and MCP resources |
| S3 | DeepSeek harness | Source repository docs (`docs/architecture.md`, `docs/cookbook/extension-cookbook.md`) | <https://github.com/deepseek-ai/deepseek-harness> | 2026-09-02 | Shipped tool families `dsh-tool-*` are bash, fs, web, subagent, todo; "ToolSearch / progressive disclosure" is a scoped `ctx.tools.restrict()` registration; "Permission system / AskUserQuestion" is `ask` from `tools/pre-execute` plus a separate model-facing ask tool; scheduled tasks are plugin tools that `followup()` when idle / `inject()` when busy |
| S4 | PenguinHarness | Source repository README | <https://github.com/Prism-Shadow/penguin-harness> | 2026-09-02 | States "a deliberately minimal toolset over clean low-level interfaces: fewer tool calls, fewer tokens"; per-tool-call `approve` callback in its SDK sample. No per-tool documentation found in the README; no claim about individual tools |
| S5 | Strands Agents SDK (pinned `@strands-agents/sdk@1.16.0`) | Installed package (`node_modules/@strands-agents/sdk/dist/src/vended-tools/`, `interrupt.d.ts`) | local | 2026-09-02 | Vended tools shipped: `bash`, `file-editor`, `http-request`, `notebook`, `shell`, `sleep`; `interrupt.d.ts` documents tool/hook interrupt-and-resume; `http-request.js:54–55` does `fetch` + `response.text()` with no size cap or content negotiation |

### Peer highlights and innovations

| Product | Sourced feature / innovation | Reference | Why it matters |
|---|---|---|---|
| Claude Code | Bash runs "a built-in set of read-only commands without prompting" and treats `git` specially in its auto-background rules | S1 | The whitelist idea darwin also uses; the peer keeps the set curated and documents its edges |
| Claude Code | A timed-out foreground command is moved to the background with an explicit result: `Command did not complete within its 120s timeout and was moved to the background`, task id and output-file path; `cd` inside it never carries over and the result says so | S1 | Work done before the timeout is never lost, and the model is told exactly what state it is in |
| Claude Code | `Edit` accepts `replace_all: true` when `old_string` appears more than once; otherwise uniqueness is required | S1 | Bulk renames are one gated edit instead of N, without falling back to `sed -i` |
| Claude Code | `WebFetch` converts HTML to Markdown, sends an `Accept` header preferring Markdown, upgrades HTTP→HTTPS, reports cross-host redirects instead of following them, truncates large pages to a fixed limit, and documents itself as lossy | S1 | A web page reaches the model as readable text at a bounded size instead of raw HTML |
| Claude Code | `Glob`/`Grep` are dedicated read-only tools (100-file cap with truncation flag; rg output modes; rg diagnostics passed through) | S1 | Permission-free structured search; in darwin `rg`/`find` via bash are already permission-free (`SAFE_BASH_COMMANDS`) |
| Claude Code | `AskUserQuestion` multiple-choice questions with an `askUserQuestionTimeout` auto-continue | S1 | Mid-turn clarification without ending the turn |
| Claude Code, Codex, DSH | Tool search / deferred tool loading (`ToolSearch`, `tool_search`, scoped `restrict()`) | S1, S2, S3 | Keeps large MCP catalogues out of every request |
| Codex | `get_context_remaining` tool: remaining tokens or null | S2 | The model, not only the user, can see context pressure |
| Claude Code | Task tools (`TaskCreate` …) are left out on newer models because "the tools' definitions and reminders take up context" | S1 | Confirms darwin's single bounded `update_plan` as a reasonable middle |
| Claude Code | `Monitor`, `Cron*`, `ScheduleWakeup`, `PushNotification`, `RemoteTrigger`, `Artifact`, `SendUserFile`, `SendFeedback`, `EndConversation`, `ShareOnboardingGuide`, `ReportFindings` | S1 | Cloud-, plan- or idle-injection-dependent; `Monitor` is documented as unavailable on Amazon Bedrock |

### Current Darwin baseline

| Capability / architecture | Repository evidence | Current behavior |
|---|---|---|
| Static read-only bash whitelist | `src/agent/permission.ts:794–808` (`SAFE_BASH_COMMANDS`, `SAFE_GIT_SUBCOMMANDS`), `:840–863` (`assessBashRisk`: metacharacter check, then first word per segment; `git` checks only the subcommand token) | Offline probe (`classify`+`assessRisk`, project root): `find . -name "*.tmp" -delete` → **safe**; `find . -name "*.log" -exec rm {} \;` → **safe**; `git branch -D main` → **safe**; `git branch -m main old` → **safe**; `git diff --output=/tmp/x.patch` → **safe**; `git log --output=/tmp/log.txt` → **safe**. In `default`/`auto` these run silently (`beforeToolCall`, `:473`); `plan` is unaffected because bash is `execute` kind (`planGuard`, `:400–409`). `docs/user-guide/permissions.md:24` documents the allowlist as read-only |
| Foreground bash timeout | pinned SDK `vended-tools/bash/bash.js:254–258` (`isTimedOut = true; cleanup(); this.stop(); reject(new BashTimeoutError(\`Command timed out after ${effectiveTimeout} seconds\`))`); by contrast the patch's `BashSessionError` at `:224–232` carries `output`/`error`/`cwd`/`exitCode` | Captured stdout/stderr is discarded, the persistent shell is killed (next call restarts at the initial cwd), and the model receives only the one-line message |
| `str_replace` | pinned `file-editor.js:258–271` (`Multiple occurrences of old_str … Please ensure it is unique`); schema commands `view`/`create`/`str_replace`/`insert`; system-prompt rule 8 routes mutations through `fileEditor`, never `sed -i` | Multi-occurrence replacement is N gated calls or a forbidden shell edit |
| HTTP fetch | `runtime.ts:644` registers the SDK `httpRequest` singleton; `http-request.js:54–55` returns `response.text()` unbounded; spec `strands-sdk-contracts.md:258–305` forbids wrapping it; `ContextOffloader` offloads oversized results | This run: the user's page arrived as 656 KB of HTML, was offloaded with a preview, and needed a second round (`curl … .md`) to read |
| Search | `SAFE_BASH_COMMANDS` includes `grep`, `rg`, `find`, `head`, `tail`; probe: `rg foo \| head -5` safe, `rg foo \| sort \| uniq -c` prompts | Permission-free search already exists; only pipelines through non-listed filters prompt |
| Image read | `src/tools/image-viewer.ts:11–20` (`MAX_IMAGE_BYTES`, `MAX_IMAGE_DIMENSION`, resize attempts via sharp) | Parity with S1's resize/recompress |
| Progress checklist | `src/tools/update-plan.ts` (parent-only whole-list replacement) | Parity-in-spirit with S1's reduced Task tooling |
| Mid-turn user questions | `src/agent/permission.ts` `PermissionGate.beforeToolCall` is the only mid-turn modal; `interrupt.d.ts` unused; 2026-08-23 run gated `ask_user` at Score 3 | Questions are asked in the final answer, between turns |
| Notebook tool | SDK ships `vended-tools/notebook`; not registered | No notebook editing |

### Comparison and gaps

| Peer evidence | Darwin evidence | Parity / gap / deliberate difference | Implication |
|---|---|---|---|
| Curated read-only Bash set (S1) | `assessBashRisk` accepts destructive `find`/`git branch`/`--output` forms as "read-only" | **Safety gap** — the whitelist's stated invariant ("provably read-only") is false for at least six forms | SER-053 |
| Timeout moves the command to background with explicit state (S1) | Timeout discards output and kills the shell | **Gap** — darwin's persistent shell cannot detach the command, but it can return what it captured and say what happened | SER-054 |
| `Edit(replace_all)` (S1) | `str_replace` refuses multiple occurrences | **Gap** — bounded, opt-in | SER-055 |
| `WebFetch` readable bounded projection (S1) | Raw `http_request` body, offloader as the only bound | **Gap** — a separate tool; `http_request` stays untouched per spec | SER-056 |
| `Glob`/`Grep` tools (S1) | `rg`/`find` permission-free via bash | **Parity by other means** | not proposed (gated) |
| `AskUserQuestion` / `request_user_input` / DSH ask tool (S1–S3) | Between-turn questions only | **Deliberate difference (2026-08-23 gate)** — the unresolved semantics recorded then (string-only `send`, headless, queue ownership, modal keys, trajectory, resume) are unchanged | gated again; a user-directed run may revisit |
| ToolSearch / deferred tools (S1–S3) | Full MCP catalogue every request, cached by prompt cache | **Gap with high cost** — tool-list changes invalidate the cache point and the SDK registry is static per invocation | gated |
| `get_context_remaining` (S2) | `contextEstimate()` advisory to the user only | **Difference** — darwin advises the user post-turn, never auto-compacts | noted, not proposed |
| Monitor/Cron idle injection (S1, S3) | Prompt queue drains only at idle through `submit()`; no timer channel | **Deliberate difference** (queue is next-turn-only) | gated |
| Task tools removed on newer models (S1) | one bounded `update_plan` | **Parity** | none |
| Notebook tool shipped by SDK (S5) | not registered | Gap without demand evidence | gated |

### Ranked iteration directions

`Score = 2 × Importance + Architecture fit + Evidence confidence − Implementation difficulty − Implementation risk`

| Rank | Proposed ID | Direction | Importance | Architecture fit | Evidence confidence | Implementation difficulty | Implementation risk | Score | Evidence and rationale |
|---:|---|---|---:|---:|---:|---:|---:|---:|---|
| 1 | SER-053 | Close the mutating escapes in the static read-only bash classifier: a whitelisted first word is safe only when none of its arguments is a known mutating option — `find` `-delete`/`-exec`/`-execdir`/`-ok`/`-okdir`/`-fprint*`/`-fls`, `git branch` with `-d/-D/-m/-M/-c/-C/-u/--set-upstream-to/--unset-upstream/--edit-description/--delete/--move/--copy`, `git log/diff/show` `--output`; keep the whitelist principle, the metacharacter and per-segment rules, and the user-guide table honest | 5 | 5 | 5 | 2 | 2 | 16 | S1 documents the peer's curated set; offline probe above shows six destructive forms classified `safe` and therefore run silently in `default`/`auto` (`permission.ts:473`). Fix is local to `assessBashRisk` and its tests (`spike/verify-classifier.ts`, `verify-permission-modes.ts`); wildcard rules (`permission-rules.ts:217`) are user-authored and stay as they are. Safety first in the batch |
| 2 | SER-054 | Make a foreground bash timeout truthful and non-destructive to evidence: the `BashTimeoutError` result carries the bounded tail of captured stdout/stderr (the same field cap the background `wait` projection uses), the effective timeout, the fact that the persistent shell was restarted and where its cwd now is, and one pointer to `start`/`wait` for long work; exit/kill semantics unchanged | 4 | 5 | 5 | 2 | 2 | 14 | S1's timeout result names the timeout, task id and output path; darwin's `bash.js:254–258` rejects with a message only, while the patch's own `BashSessionError` (`:224–232`) already carries `output/error/cwd` — the precedent and the seam are in the pinned patch. `verify-background-bash.ts` is the home for the check |
| 3 | SER-056 | Add a parent-only `web_fetch` tool: GET one `http(s)` URL with an `Accept` header preferring `text/markdown`/`text/plain` over HTML, upgrade `http`→`https`, follow same-host redirects and report a cross-host redirect as a result instead of following it, convert HTML to bounded readable text (dependency-free: drop `script`/`style`/`nav`, keep headings, paragraphs, lists, links as `text (url)`, code/pre, decode entities), cap the body at a stated code-point budget with an explicit truncation notice, and state that the projection is lossy; classified by the existing unknown→`execute` fail-closed path (never `safe`); `http_request` stays byte-identical | 4 | 3 | 5 | 3 | 3 | 10 | S1 `WebFetch` behavior; this run's own 656 KB HTML fetch and offload; `http-request.js:54–55` unbounded `text()`; spec forbids wrapping `http_request`, so this is a sibling tool with the same catalogue rule (parent-only, `runtime.ts:644`); no second output channel |
| 4 | SER-055 | Add optional `replace_all: boolean` (default false) to `str_replace` in the pinned file-editor patch: when true, every non-overlapping occurrence is replaced and the result names the count and line numbers; when false or absent, behavior is byte-identical (miss advisory, uniqueness error); the permission box and finished diff row show one old→new pair plus an explicit "every occurrence" marker derived from the input, never from disk; the same-path serial wrapper passes the field through | 3 | 4 | 5 | 3 | 3 | 9 | S1 `Edit` `replace_all`; `file-editor.js:267–269` refuses multiples; rule 8 forbids shell edits; `edit-diff.ts` invariant "diff of the tool input, never read from disk" constrains the rendering; `verify-file-editor.ts`, `verify-edit-diff.ts`, `verify-file-editor-serial.ts` are the homes |

No hard dependencies among the four; the order is safety first, then the two bash/fetch evidence-preserving changes, then the opt-in editor extension. SER-056 is ranked above SER-055 because its score is higher and its failure mode (a lossy fetch) is stated to the model, whereas an editor schema change carries the diff-rendering invariant.

### Gated-out directions

| Direction | Score | Gate | Why it fell below | Kept anyway? |
|---|---:|---:|---|---|
| Dedicated `glob`/`grep` read-only tools (S1) | 5 (I 1, F 4, E 4, D 3, R 2) | 6 | `rg`/`find`/`grep`/`head`/`tail` already run without a prompt (`SAFE_BASH_COMMANDS`) and respect `.gitignore`; the offloader bounds output; only pipelines through `sort`/`uniq`/`cut` prompt, which SER-053's argument-aware classifier could later extend | no |
| `ask_user` multiple-choice question tool (S1, S2, S3) | 3 (I 3, F 3, E 4, D 5, R 5 — unchanged from 2026-08-23) | 6 | Peer evidence grew (three products) but no darwin-side semantics changed: `send()` is still string-only, headless has no answer channel, the prompt queue is next-turn-only, modal key ownership is shared by permission box/menu/search, trajectory/resume have no question outcome. Re-rating would need those seams designed first | no |
| ToolSearch / deferred MCP tool loading (S1–S3) | 2 (I 3, F 2, E 3, D 5, R 4) | 6 | Tool definitions sit before the prompt-cache point; changing them per call invalidates the cache and the SDK registry is fixed per invocation | no |
| Monitor / Cron / ScheduleWakeup idle injection (S1, S3) | 2 (I 3, F 2, E 4, D 5, R 5) | 6 | Requires a timer channel that injects turns; the prompt queue decision (2026-08-19) is next-turn-only via `submit()`; `Monitor` is unavailable on Bedrock even for the peer | no |
| MCP resources list/read (S1, S2) | 2 (I 2, F 2, E 3, D 4, R 3) | 6 | SDK `McpClient` exposes no resource API (`mcp/client.d.ts`); would need raw protocol access | no |
| Tool-enforced read-before-edit with changed-on-disk note (S1) | 5 (I 2, F 3, E 5, D 3, R 4) | 6 | `create` already refuses existing files and `str_replace` is exact-match, so the harm is bounded; per-process read sets would refuse after `--resume` and miss reads made via `rg`/`sed -n` | no |
| Register the SDK `notebook` vended tool (S5) | 5 (I 1, F 4, E 3, D 2, R 2) | 6 | No evidence any darwin user edits notebooks; would still need a `write` classification for plan-mode denial and diff rendering | no |
| LSP after-edit diagnostics (S1) | 3 (I 3, F 2, E 4, D 5, R 4) | 6 | No language-server plumbing; `pnpm typecheck` via bash is the project's own gate | no |
| `get_context_remaining` model-facing tool (S2) | 5 (I 2, F 3, E 4, D 2, R 2) | 6 | darwin's context-pressure decision is "advise the user, never auto-compact"; giving the model the number invites model-initiated compaction the design rejects | no |

### Recommendation

- Batch to implement: `SER-053`, `SER-054`, `SER-056`, `SER-055`
- Start with: `SER-053`
- Why this order: SER-053 closes a real silent-execution safety hole and is the cheapest; SER-054 and SER-056 both stop evidence from being lost (timeout output, page content) without touching schemas the TUI renders; SER-055 changes an editor schema whose input diff has a rendering invariant, so it goes last on an accepted tree
- Independently observable acceptance: SER-053 — the six probe commands above classify `dangerous` with a reason naming the option, every previously-safe read-only form (`ls -la`, `cat`, `rg foo | head -5`, `git log --oneline -5`, `git branch --show-current`, `find . -name '*.ts'`) still classifies `safe`, `spike/verify-classifier.ts`/`verify-permission-modes.ts` cover both sets, `docs/user-guide/permissions.md` and the spec name the argument rule; SER-054 — a fake or real timed-out `execute` returns an error result containing the bounded captured stdout/stderr tail, the timeout in seconds, the restart/cwd statement and the `start`/`wait` pointer, checked in `verify-background-bash.ts` without a model call, `SHELL_RESTART_NOTICE` semantics unchanged; SER-056 — `verify-web-fetch.ts` (in `pnpm test`) with a local HTTP fixture proves markdown negotiation, HTML→text projection, truncation notice, cross-host redirect report, `http`→`https` upgrade, default-mode denial classification, absence from child catalogues and zero network in plan mode; `http_request` registration byte-identical (`verify-http-request-tool.ts` unchanged); SER-055 — `verify-file-editor.ts` proves `replace_all: true` replaces every occurrence with a count/lines result, absent/false paths byte-identical to today's outputs, `verify-edit-diff.ts` proves the marker and that no disk read occurs, `verify-file-editor-serial.ts` passes the field through; `pnpm typecheck`, full `pnpm test`, `pnpm build` for each
- Backlog updates: appended `SER-053` (Priority 74), `SER-054` (75), `SER-056` (76), `SER-055` (77) as `not-started` to `docs/research/backlog/directions-061-080.md` (range 061–080 now holds 17 records; no rollover); `spike/verify-skills.ts` record count raised 73 → 77; nine directions gated and recorded above only; `SER-053` moves to `in-progress` only immediately before the developer handoff

### Batch iteration outcome

| Direction | Child session and managed tasks | Host acceptance (exact independent checks and outcomes) | Commit | Final status | Iteration-log entry |
|---|---|---|---|---|---|
| SER-053 | child session `session-20260902-095357955`; managed task `bg-7edbe478` (exit 0, `usage: input=48 output=19567 cacheRead=1592920 cacheWrite=78013`); Trellis task `09-02-ser-053-bash-classifier-mutating-args` (archived `c5dcd39`) | Host read the whole `permission.ts` diff; 25-command `classify`+`assessRisk` probe (six escapes + combined flags/`=` spellings/bare/piped/chained all `dangerous` naming the option; nine read-only forms still `safe`); `verify-permission-modes.ts` 154/154; `pnpm typecheck` exit 0; full `pnpm test` exit 0, zero FAIL; `pnpm build` exit 0; tree clean | `14378bc` | `done` | `docs/iteration-log.md` Batch 78 |
| SER-054 | child session `session-20260902-101002155`; managed task `bg-ca0ec822` (exit 0, `usage: input=114 output=43099 cacheRead=6091600 cacheWrite=132193`); Trellis task `09-02-ser-054-bash-timeout-evidence` (archived `772187e`, journal `fdab9c2`) | Host read the whole patch diff (`TIMEOUT_TAIL_LIMIT`, `boundedTail`, `describeTail`, ordered `BashTimeoutError` message + fields, `stop()` cwd reset; `src/` untouched); throwaway `createForegroundBashTool` probe (`timeout: 1` over `echo before-out; echo before-err 1>&2; sleep 5`) returned the fields and ordered message and the next command ran at the project root; `verify-background-bash.ts` 157/157; `pnpm typecheck` exit 0; full `pnpm test` exit 0, zero FAIL; `pnpm build` exit 0; tree clean | `0255368` | `done` | `docs/iteration-log.md` Batch 79 |
| SER-056 | child session `session-20260902-103415340`; managed task `bg-e4bf96cd` (exit 0, `usage: input=124 output=75459 cacheRead=7686325 cacheWrite=165771`); Trellis task `09-02-ser-056-web-fetch-tool` (archived `58965f8`, journal `980fc6c`) | Host read the diff (new `web-fetch.ts`, `PARENT_ONLY_TOOL_NAMES` in `runtime.ts`, 73-assertion fixture suite; `permission.ts`/`verify-http-request-tool.ts`/lockfile untouched); real-network probes: the tools-reference page negotiated as `text/markdown` with upgrade + `[truncated: 3000 of 105823 code points]`, `example.com` HTML projected, GitHub cross-host 302 reported not followed; `verify-web-fetch.ts` 73/73, `verify-http-request-tool.ts` 7/7; `pnpm typecheck` exit 0; full `pnpm test` exit 0, zero FAIL; `pnpm build` exit 0; tree clean. Accepted repair: children no longer receive `http_request` (the documented invariant was not enforced before) | `d14b27c` | `done` | `docs/iteration-log.md` Batch 80 |
| SER-055 | child session `session-20260902-110209882`; managed task `bg-8d0298ff` (exit 0, `usage: input=150 output=58126 cacheRead=11238714 cacheWrite=232664`); Trellis task `09-02-ser-055-str-replace-all` (archived `23a56ce`, journal `8af72c6`) | Host read the diff (patched `file-editor.js` `replace_all` schema/`buildStrReplaceResult`; `edit-diff.ts` `REPLACE_ALL_ROW` header row; `permission.ts` detail; lockfile hash only); throwaway `SerializedFileEditorTool.stream()` probe on a three-occurrence temp file: no flag → today's exact multiple-occurrence error, flag → 3 replaced at `[1,3,4]` in one write, flag + miss → today's exact advisory, `classify`/projection carry the row; `verify-file-editor.ts` 89/89, `verify-file-editor-serial.ts` 50/50, `verify-edit-diff.ts` 112/112, `verify-visual-language.tsx` 74/74, `verify-export-command.ts` 32/32; `pnpm typecheck` exit 0; full `pnpm test` exit 0, zero FAIL; `pnpm build` exit 0; tree clean | `4c40426` | `done` | `docs/iteration-log.md` Batch 81 |

- Halt condition that ended the loop: **batch exhausted** — all four directions (`SER-053`, `SER-054`, `SER-056`, `SER-055`) closed `done` after independent acceptance; no correction turn was needed for any of them
- Aggregate token spend across every child delegation: `input=436 output=196,251 cacheRead=26,609,559 cacheWrite=608,641` (four children, one task each — SER-053 `48/19,567/1,592,920/78,013`, SER-054 `114/43,099/6,091,600/132,193`, SER-056 `124/75,459/7,686,325/165,771`, SER-055 `150/58,126/11,238,714/232,664`; every child reported all four buckets; excludes the Host's own zero-model-call probes and the Host's two real-network `fetchWebPage` probes)
- Remaining backlog after this run: nothing unfinished — all 77 routed records are `done` or `abandoned`, so a fresh research run is the recommended next step. Candidates recorded by the children for that run, not queued here: (a) bare `git branch <name>` (positional create) and `git branch -t <name>` still classify `safe` (SER-053 scope limited the rule to options); (b) `validatePortableToolInput` in `src/hooks/tool-hooks.ts` does not list `replace_all`, so a Codex `PreToolUse updatedInput` carrying it is refused fail-closed; (c) AGENTS.md is 32,667 B — 101 B under `MAX_INSTRUCTIONS_BYTES` — so the next architecture row must live in `load-bearing-decisions.md` alone or an existing row must be shortened; (d) the `ask_user` direction stays gated at Score 3 until the mid-turn-input seams (headless answer channel, queue ownership, modal keys, trajectory/resume outcome) are designed


## Run — 2026-09-02T14:43:25Z

### Backlog-first decision

- Backlog router inspected first: `docs/research/backlog_index.md`
- Metadata searched across routed priority pages: `docs/research/backlog/directions-001-020.md`, `docs/research/backlog/directions-021-040.md`, `docs/research/backlog/directions-041-060.md`, `docs/research/backlog/directions-061-080.md`
- Unfinished directions found: none (`grep -h '^- Status:'` over every page: 75 × `done`, 2 × `abandoned`, no `not-started`/`in-progress`; last closure `ff496d5`, HEAD `d7f8e0f`)
- Decision: fresh research

### Research path

Rolled once, before any repository or product source was opened (the only prior reads were the backlog index, the routed pages' metadata lines and `git status`):

```text
research-path: peer
focus: comparable coding-agent product analysis
share: 50% (weight 5 of 10)
draw: 10 of 20 half-units
path-source: roll
rolled-at: 2026-09-02T14:43:25.317Z
weights: tui=2 observability=0.5 sdk=1 open=1.5 peer=5
```

The script was run exactly once.

### Source availability and method

Accessed 2026-09-02 (UTC) at HEAD `d7f8e0f`. Tools: the runtime's `web_fetch` for first contact and `curl` (`.md` editions and raw repository files, saved under `/tmp` and grepped) for the pages whose HTML projection was too large to keep in context; no `web-search`. All five required scopes were reachable. Prior peer runs had already used the Claude Code tools/hooks/memory/checkpointing/interactive-mode pages, the Codex config/customization/multi-agent/features pages and the DeepSeek `architecture.md`, so this run opened pages none of them cited: the Claude Code **CLI reference**, the Codex **developer commands** reference (the `developers.openai.com/codex/cli/reference` URL now 308-redirects to `learn.chatgpt.com/docs/developer-commands?surface=cli`, recorded as fetched there), the DeepSeek `dsh` **CLI README** and Web-UI guide, the PenguinHarness README at `main`, and — as the fifth product, never opened by an earlier run — the **Gemini CLI** commands reference (the `docs/cli/commands.md` path 404s on `main`; the file now lives at `docs/reference/commands.md`). Two Codex pages embed `<ConfigTable>` components whose option tables are not in the markdown edition, so flag-level Codex claims below are limited to what the prose states. Darwin evidence is read from source at HEAD; one offline check (`wc -c AGENTS.md` = 32,667 B against `MAX_INSTRUCTIONS_BYTES` = 32,768) was run because it constrains every direction's handoff.

### Reference sources

| Ref | Product | Source type | URL | Accessed (UTC) | Claims supported |
|---|---|---|---|---|---|
| S1 | Claude Code | Primary docs (CLI reference, markdown edition) | <https://code.claude.com/docs/en/cli-reference> | 2026-09-02 | `claude doctor` "Print read-only installation and settings diagnostics from the terminal without starting a session, including install health, settings-file validation errors"; `claude logs <id>` prints recent output of a background session; `--name`/`-n` display name resumable by name; `--fork-session`; `--max-turns` and `--max-budget-usd` (print mode only); `--append-system-prompt`/`--append-system-prompt-file`; `--add-dir`; `--fallback-model`; `--no-session-persistence`; `--bare`; `--init-only`; `--exclude-dynamic-system-prompt-sections`; `--tools`/`--allowedTools`/`--disallowedTools` |
| S2 | Codex | Primary docs (developer commands reference, markdown edition; reached by redirect from `developers.openai.com/codex/cli/reference`) | <https://learn.chatgpt.com/docs/developer-commands?surface=cli> | 2026-09-02 | `codex doctor` command exists; `/copy` copies "the latest completed Codex output" (uses the latest *completed* output while a turn runs; unavailable before the first output and right after a rollback) with `Ctrl+O` as its chord; `/ps` shows "each background terminal's command plus up to three recent, non-empty output lines"; `/stop` stops all background terminals; `/diff` shows staged, unstaged and untracked changes; `/raw` raw scrollback for terminal selection; `/goal` (≤4,000 chars, pause/resume/clear); `/rename`, `/fork`, `codex fork --last`; "Press Esc twice with an empty composer to edit the previous user message and fork the chat from that point"; "Press Enter while Codex is working to inject new instructions into the current turn"; `codex exec --json` + `--output-last-message` |
| S3 | DeepSeek harness | Source repository (`apps/cli/README.md`, `docs/user/guide/index.md` at `master`) | <https://github.com/deepseek-ai/deepseek-harness/blob/master/apps/cli/README.md> | 2026-09-02 | `dsh` is the sole launcher; profiles (`web`, `headless`, `sdk`, `sdk-minimal`, `acp`) are ordered plugin-bundle patch layers; `dsh --profile headless "job"` runs one fresh persisted session and prints the final answer; `--dump-default-config`/`--dump-config` inspect the composed configuration tree "without booting it"; "Invalid commands, options from another mode, configuration errors, and boot failures exit nonzero". No claim about TUI slash commands (the terminal app is an optional profile) |
| S4 | PenguinHarness | Source repository README at `main` | <https://github.com/Prism-Shadow/penguin-harness/blob/main/README.md> | 2026-09-02 | Product surfaces are a desktop app, `penguin web`, `penguin run -m "<task>"` one-shot, `penguin chat` REPL (`/compact`, `/clear`, `/exit`, Ctrl-C), `penguin server`, and a TS SDK with a per-tool-call `approve` callback; "snapshot before every round and every request observable in the Trace view". No per-command or diagnostics documentation found; no further claim |
| S5 | Gemini CLI | Source repository docs (`docs/reference/commands.md` at `main`) | <https://github.com/google-gemini/gemini-cli/blob/main/docs/reference/commands.md> | 2026-09-02 | `/copy` copies the last output ("Local sessions use system clipboard tools (pbcopy/xclip/clip). Remote sessions (SSH/WSL) use OSC 52 and require terminal support"); `/rewind` "Press Esc twice as a shortcut" with history-only / files-only / both choices; `/shells` background-shell view; `/stats session|model|tools`; `/tools [desc]`; `/chat save|resume|share <tag>` project-scoped checkpoints; `/directory add`; composer undo/redo chords; `/about` |

### Peer highlights and innovations

| Product | Sourced feature / innovation | Reference | Why it matters |
|---|---|---|---|
| Claude Code, Codex | An offline `doctor` subcommand: read-only installation and settings diagnostics, "without starting a session" | S1, S2 | A broken config, a mistyped MCP file or a skipped skill can be seen without paying for a model call or wading through a live header |
| Codex, Gemini CLI | `/copy` puts the last completed answer on the clipboard; Gemini states the transport honestly — platform tools locally, OSC 52 over SSH/WSL | S2, S5 | Selecting text out of a wrapped, re-rendered TUI transcript is the hard case; both products give the user a one-word answer to it |
| Codex, Gemini CLI | Esc twice on an empty composer re-opens the previous prompt / the rewind picker | S2, S5 | A two-keystroke path to the branch-from-earlier-prompt feature both products already have as a command |
| Codex, Claude Code | `/ps` lists each background terminal with "up to three recent, non-empty output lines"; `claude logs <id>` prints a background session's recent output | S2, S1 | A glance at a background job's progress without asking the model to run `wait`/`output` |
| Claude Code | `--append-system-prompt[-file]` beside `--system-prompt[-file]`; `--max-budget-usd`; `--fallback-model`; `--name` + resume by name; `--fork-session` | S1 | Scripted-run controls; darwin already has `--max-model-calls`, a config-level system-prompt override file and hand-named `--session <id>` |
| Codex | `Enter` while working injects instructions into the *current* turn; `Tab` queues for the next | S2 | Darwin's queue decision (SER-027) is explicitly next-turn-only — a deliberate difference, not a gap |
| Codex | `/diff` shows staged, unstaged and untracked changes in the CLI | S2 | Darwin's `!git diff` already does this, but its report is fed into the next prompt by design (SER-024) |
| Gemini CLI | `/stats tools` per-tool call statistics; `/tools desc` lists the model-facing tool descriptions | S5 | Inspection surfaces darwin lacks; value is modest because darwin's `/status`, `/mcp` and `/usage` cover the parts that vary |
| DeepSeek harness | `--dump-config` prints the composed configuration tree without booting; configuration errors exit nonzero | S3 | The same "inspect before you run" idea as `doctor`, from a third product |
| PenguinHarness | Trace view with a snapshot before every self-evolution round; per-tool-call `approve` callback in the SDK | S4 | Parity with darwin's trajectory record and permission gate; nothing new to adopt from the README alone |

### Current Darwin baseline

| Capability / architecture | Repository evidence | Current behavior |
|---|---|---|
| Offline CLI verbs | `src/cli-usage.ts` `CLI_USAGE`; `src/cli-sessions.ts` (`SESSIONS_COMMAND`, read-only projection of the snapshot store); `src/cli-trajectory.ts` (`list|search|replay|fork`) | `darwin sessions` and `darwin trajectory …` are the only no-model subcommands; every configuration/skills/MCP/hooks problem is surfaced only by starting a session (`ConfigError` refuses to start; the rest skips-and-surfaces per `.trellis/spec/backend/error-handling.md`) or by `/status`/`/mcp` inside one |
| Configuration and extension loaders | `src/config.ts` (`loadConfig`, `loadProjectPolicy`, `configPath`, `permissionRulesPath`, `globalHooksPath`, `projectHooksPath`, `ConfigError`); `src/mcp/registry.ts` (`mcpConfigCandidates`, `MCP_CONFIG_FILENAME`, `ROOT_MCP_CONFIG_FILENAME`); `src/skills/loader.ts` (`scanSkills`, `REQUIRED_BUILTIN_SKILLS`, 200-entry preflight); `src/hooks/codex-hooks.ts` (`decodeCodexHooks`); `src/agent/system-prompt.ts` (`loadSystemPrompt`, `SYSTEM_PROMPT_FILENAME`) | Every loader already exists and returns structured problems/skips; nothing composes them into one report without a runtime |
| Built-in slash commands | `src/commands/custom-commands.ts` `BUILTIN_COMMAND_NAMES` (19 names: agents, clear, compact, context, effort, exit, export, help, mcp, memory, mode, model, permissions, rewind, status, tasks, trajectory, usage, workflow); `src/tui/InputBox.tsx` `MAX_COMPLETIONS = 20` | No `/copy`; `/export <path>` writes a file (never clipboard, per its description comment) |
| Clipboard | `src/tui/clipboard-image.ts` (`clipboardImageCommand`: `wl-paste`/`xclip`/`pngpaste`, read side only); `Ctrl+O` attaches an image (`src/tui/help-format.ts:47`) | Darwin reads the clipboard for images and never writes it; `Ctrl+O` is taken, so Codex's chord cannot be mirrored |
| Committed answer text | `src/tui/turn-state.ts` (`turnReducer`, `AnswerPart` label decisions at push time); `src/tui/markdown.ts` projection whose ANSI-stripped output *is* the committed text; `/export` = `formatReplay(replayRead(...))` | The exact text of the last completed answer exists in TUI state and in the trajectory record, byte-identical to what `/export` writes |
| Escape key | `src/tui/App.tsx` ~2018: Escape closes a completion menu, else `endRecall()`; `src/tui/help-format.ts:50` states this | On an empty, idle composer Escape does nothing visible; `/rewind` is reached only by typing the command (`App.tsx` ~1313, `openRewindSearch`) |
| Background jobs view | `src/tui/task-format.ts` `formatTasksReport` (id, state, duration, summarized command); `src/tools/background-bash.ts` `BackgroundTaskStatus` (`outputPath`, `outputBytes`, no output text), `readOutput` advances the shared `cursor` | `/tasks` shows no output at all; the only readers of a job's output are the model-facing `output`/`wait` modes, which move the shared cursor |
| Headless controls | `src/cli-args.ts` (`--max-model-calls`, `--context-offload`, `--compact-before`, `--continue`, `--session <id>` hand-named ids); `src/agent/system-prompt.ts` config-level override file | Parity with `--max-turns` and `--system-prompt-file`; no append-only prompt flag, no spend cap, no fallback model |
| Instruction budget | `AGENTS.md` = 32,667 B; `src/agent/instructions.ts` `MAX_INSTRUCTIONS_BYTES = 32 * 1024` | 101 B of headroom: a new load-bearing row cannot be added to AGENTS.md without shortening an existing one; rationale goes to `docs/architecture/load-bearing-decisions.md` |

### Comparison and gaps

| Peer evidence | Darwin evidence | Parity / gap / deliberate difference | Implication |
|---|---|---|---|
| `claude doctor`, `codex doctor`, `dsh --dump-config` (S1, S2, S3) | `darwin sessions`/`trajectory` only; loaders in `src/config.ts`, `src/mcp/registry.ts`, `src/skills/loader.ts`, `src/hooks/` | **Gap** | A `darwin doctor` subcommand composing the existing loaders into one read-only report — no session, model, MCP connection or write |
| `/copy` with OSC 52 over SSH (S2, S5) | No clipboard write path; `clipboard-image.ts` read helpers; committed answer text in `turn-state.ts` | **Gap** | `/copy` over the last completed answer's committed text, OSC 52 first (darwin is typically driven over SSH), platform tool when a display is present, bounded and stated |
| Esc-Esc on empty composer → previous prompt / rewind (S2, S5) | Escape handler at `App.tsx` ~2018; `/rewind` + `openRewindSearch` | **Gap (small)** | A second Escape on an empty idle composer opens the existing rewind picker; `/help` states it |
| `/ps` recent output lines, `claude logs` (S2, S1) | `formatTasksReport` prints no output; `readOutput` moves the shared cursor | **Gap** | `/tasks` gains a bounded tail per job read from `outputPath` without touching the cursor |
| `--append-system-prompt` (S1) | Fixed composition order (AGENTS.md "System prompt composition"); config-level override file | Gap, but touches a documented fixed order and the cache point | Gated below |
| `/diff` (S2) | `!git diff` (SER-024) whose report is fed to the next prompt by design | Deliberate difference plus a second shell channel | Gated below |
| `Enter` injects into the current turn (S2) | SER-027: queue drains next-turn only, never mid-stream | **Deliberate difference** | Not proposed |
| `--name`/`/rename`, resume by name (S1, S2) | `--session <id>` already accepts a hand-named id (`cli-sessions.ts:13`) | Near-parity | Not proposed |
| `/stats tools`, `/tools desc` (S5) | `/status`, `/mcp`, `/usage` | Minor gap | Gated below |
| `--max-budget-usd`, `--fallback-model` (S1) | `--max-model-calls`; stream-resumption is the one driver continuation | Gap needing pricing data / conflicting with a load-bearing row | Gated below |

### Ranked iteration directions

`Score = 2 × Importance + Architecture fit + Evidence confidence − Implementation difficulty − Implementation risk`

| Rank | Proposed ID | Direction | Importance | Architecture fit | Evidence confidence | Implementation difficulty | Implementation risk | Score | Evidence and rationale |
|---:|---|---|---:|---:|---:|---:|---:|---:|---|
| 1 | SER-057 | `/copy`: put the last *completed* answer's committed text on the clipboard — OSC 52 written to the terminal first, a platform tool (`wl-copy`/`xclip`/`pbcopy`) only when a display is present; bounded payload with the truncation stated; one transcript notice; never model/trajectory/`/export` mutation | 3 | 4 | 5 | 2 | 2 | 11 | S2 (Codex `/copy`, latest completed output while a turn runs), S5 (Gemini: OSC 52 for SSH). Darwin: `turn-state.ts` owns the committed answer text; `clipboard-image.ts` is the dependency-free platform-helper pattern to mirror on the write side; `Ctrl+O` is taken (image attach), so command only. Independent: a pty test can assert the exact `ESC ] 52 ; c ; <base64> BEL` bytes. Needs `MAX_COMPLETIONS` 20→21 and a `/help` row |
| 2 | SER-058 | `darwin doctor`: an offline read-only diagnostics subcommand composing the existing loaders — config validity (`ConfigError` reported, never thrown), model choice, MCP config source + per-server command presence on `PATH`, skills catalogue with skipped entries, hooks files and dialect, project policy, sessions directory, versions — no session, model call, MCP connection or write; exit 1 when a problem is found | 3 | 4 | 5 | 3 | 2 | 10 | S1 (`claude doctor` "read-only … without starting a session"), S2 (`codex doctor`), S3 (`--dump-config`). Darwin: every loader already returns structured skips (`error-handling.md` table); `cli-sessions.ts` is the subcommand pattern; `cli-usage.ts` pins the grammar with `docs/user-guide/reference.md` |
| 3 | SER-059 | Esc Esc on an empty idle composer opens the existing `/rewind` picker (second Escape within a short window; first Escape keeps its current owners: menu, history search, recall); busy/queue/permission states ignore it; `/help` and README state the chord | 2 | 4 | 5 | 2 | 2 | 9 | S2 (Codex), S5 (Gemini `/rewind` "Press Esc twice"). Darwin: `App.tsx` Escape branch (~2018) and `openRewindSearch` already exist; SER-037/SER-046 set the pattern for key ownership and its documentation |
| 4 | SER-060 | `/tasks` shows, under each background job row, up to three recent non-empty output lines read as a bounded tail of `outputPath` — never through `readOutput`, so the model's shared cursor and `wait` semantics are untouched; rows stay one `<Text>` each and counted | 2 | 4 | 4 | 2 | 1 | 9 | S2 (`/ps` "up to three recent, non-empty output lines"), S1 (`claude logs`). Darwin: `formatTasksReport` is the one formatter; `BackgroundTaskStatus.outputPath` already exposes the file; the invariant is "read the file, not the cursor" |

Dependency order: none of the four depends on another; the batch runs in score order (ties by ID). Priorities 78–80 fit `directions-061-080.md`; SER-060 takes Priority 81 and opens `directions-081-100.md` with one new route in `backlog_index.md`.

### Gated-out directions

| Direction | Score | Gate | Why it fell below | Kept anyway? |
|---|---:|---:|---|---|
| `--append-system-prompt[-file]` for `-p` runs (S1) | 5 (I 2, F 2, E 4, D 2, R 3) | 6 | Value is limited to scripted operators; fit 2 because it changes the documented fixed composition order and sits before the cache point; the config-level override file and the prompt itself already carry operator instructions | no |
| `/diff` in-session (S2) | 5 (I 2, F 2, E 3, D 2, R 2) | 6 | `!git diff --stat && git status --short` already works; a display-only `/diff` would be a second shell-output channel beside the one bounded `!` projection (SER-024) — fit 2 | no |
| `/tools [desc]` list of registered tools (S5) | 5 (I 1, F 3, E 3, D 2, R 1) | 6 | Built-in tool set is fixed and documented; the varying parts (MCP tools, servers) are already in `/mcp` and `/status` | no |
| `/stats tools` per-tool call counts (S5) | 4 (I 2, F 3, E 3, D 3, R 3) | 6 | Would need a new counter channel or a trajectory reader in the live loop; `/usage` and `trajectory replay` cover the need | no |
| `--max-budget-usd` spend cap (S1) | 4 (I 2, F 3, E 3, D 3, R 3) | 6 | Requires per-model pricing data darwin does not have; `--max-model-calls` is the existing ceiling | no |
| `--fallback-model` (S1) | 3 (I 3, F 1, E 4, D 4, R 5) | 6 | Conflicts with "Stream interruption — one driver-owned continuation" and `/model`'s explicit user switch; fit 1 | no |
| `/rename` / resume by name (S1, S2) | 5 (I 2, F 3, E 5, D 3, R 2) | 6 | `--session <id>` already accepts hand-named ids; a post-hoc rename would need a new write into the snapshot store, which `sessions` treats as read-only | no |

### Recommendation

- Batch to implement: `SER-057`, `SER-058`, `SER-059`, `SER-060`
- Start with: `SER-057`
- Why this order: score order, no inter-dependencies; the two command-surface changes (`/copy`, `doctor`) carry the most user value, the two smaller ones (Esc Esc, `/tasks` tail) are cheap and independently verifiable
- Independently observable acceptance: SER-057 — free pty scenario asserting the OSC 52 sequence's base64 decodes to the exact last committed answer, a "nothing to copy" notice before the first answer, and `/export` bytes unchanged; `MAX_COMPLETIONS` 21 and `completion` scenario green. SER-058 — `pnpm tsx src/cli.ts doctor` against a throwaway HOME/project: valid config → exit 0 report; invalid key/JSON → named problem, exit 1, no file created anywhere, no MCP process spawned (`strace`/`ps`-free proof: a fake server command that would write a marker file never runs); suite pinning the grammar in `cli-usage.ts` and `reference.md`. SER-059 — pty scenario: Esc Esc on an empty idle composer shows the rewind picker; single Esc / Esc with a draft / Esc while busy do not; help row present. SER-060 — `verify-background-bash.ts`/tasks-format suite: tail lines shown, cursor offsets unchanged after `/tasks`, bounded lines/bytes, rows counted
- Backlog updates: `directions-061-080.md` + `SER-057` (78), `SER-058` (79), `SER-059` (80) appended `not-started`; new `directions-081-100.md` + `SER-060` (81) with one route added to `backlog_index.md`; nothing set `abandoned`; `SER-057` flipped to `in-progress` immediately before its developer handoff. Handoff constraint for every direction: AGENTS.md has 101 B of headroom, so rationale goes to `load-bearing-decisions.md`/specs, not a new AGENTS.md row

### Batch iteration outcome

| Direction | Child session and managed tasks | Host acceptance (exact independent checks and outcomes) | Commit | Final status | Iteration-log entry |
|---|---|---|---|---|---|
