# Darwin self-evolution research — 2026-08-15

## Run — 2026-08-15T15:19:24Z

### Backlog-first decision

- Backlog inspected first: [`docs/research/backlog_index.md`](./backlog_index.md)
- Unfinished directions found: none
- Decision: fresh research

### Source availability and method

Web search and direct HTTP access were available on 2026-08-15 UTC. I inspected Darwin's current source, fast-suite inventory, README, architecture specs, iteration history, and recent relevant git history before comparing products. Product facts below come only from primary documentation or the publisher's repository; qualitative implications are explicitly Darwin-focused inference. Claude Code's current documentation redirected from `docs.anthropic.com` to `code.claude.com`; Codex documentation redirected from `developers.openai.com` to the official ChatGPT Learn host. PenguinHarness's GitHub page renderer failed in search, so its raw first-party README was fetched directly. DeepSeek Harness is explicitly a developer preview, and PenguinHarness says its public benchmark suite is still roadmap work; neither source is treated as independent validation of publisher performance claims.

### Reference sources

| Ref | Product | Source type | URL | Accessed (UTC) | Claims supported |
|---|---|---|---|---|---|
| S1 | Claude Code | Primary product docs | <https://code.claude.com/docs/en/common-workflows> | 2026-08-15 | Documents plan-before-editing as a way to review changes before they touch disk, subagent research to keep the main context clean, resumable conversations, and parallel worktree sessions. |
| S2 | Codex | Primary product docs | <https://developers.openai.com/codex/agent-approvals-security/> and <https://developers.openai.com/codex/multi-agent> | 2026-08-15 | Local clients combine OS-enforced sandboxing with approvals, default to no network and workspace-limited writes, offer read-only mode, and expose parallel specialized subagent threads that can be inspected and switched. |
| S3 | DeepSeek Harness | Primary product page | <https://deepseek.com/harness/en/> | 2026-08-15 | Developer preview makes models, tools, skills, sessions, sandboxes, storage, loops, scheduling, and UI composable plugins; records model-visible activity in an append-only event stream used for trajectory inspection, resume, fork, search, and replay. |
| S4 | PenguinHarness | Publisher repository README | <https://github.com/Prism-Shadow/penguin-harness> | 2026-08-15 | Publisher describes benchmark-driven self-optimization with per-round snapshots and trace observability, built-in creation/evaluation/optimization skills, CLI/SDK use, and per-tool approval. Its public benchmark suite is marked as future work, so performance numbers are unverified publisher claims. |
| S5 | OpenHands | Primary product docs | <https://docs.all-hands.dev/usage/architecture/runtime> and <https://docs.all-hands.dev/overview/faqs> | 2026-08-15 | Docker-backed execution provides host separation; docs distinguish stronger isolated backends from a local process backend and warn that broad mounts, host networking, privileges, or Docker-socket access weaken isolation. |

### Peer highlights and innovations

| Product | Sourced feature / innovation | Reference | Why it matters |
|---|---|---|---|
| Claude Code | A distinct plan-before-editing workflow lets users review intended changes before disk mutation; research can be delegated away from the main context. | S1 | Separates deliberation from execution and limits context pollution. |
| Codex | Read-only operation is an explicit permission setting, while OS sandbox policy separately constrains actual filesystem/network capability. | S2 | Defense in depth: behavioral approval and technical containment solve different problems. |
| Codex | Specialized subagents can run in parallel, with visible threads and source-labelled approval requests. | S2 | Parallelism is useful only when users can inspect ownership, progress, and permission provenance. |
| DeepSeek Harness | The complete runtime is plugin-composable and its append-only event stream powers trajectory inspection, resume, fork, search, and replay. | S3 | Makes runtime variants and execution history first-class rather than ad hoc logs. |
| PenguinHarness | The publisher's self-evolution loop evaluates an agent, snapshots each round, diagnoses losses, and produces the next version with trace visibility. | S4 | Suggests turning product evolution from qualitative feature accumulation into regression-measured iteration; benchmark claims remain unverified. |
| OpenHands | Agent execution can occur in a container separated from the host, with explicit documentation of isolation-weakening configuration. | S5 | Permission prompts alone do not contain an approved but harmful command. |

### Current Darwin baseline

| Capability / architecture | Repository evidence | Current behavior |
|---|---|---|
| SDK extension boundary | [`AgentRuntime.create`](../../src/agent/runtime.ts) and [SDK contracts](../../.trellis/spec/backend/strands-sdk-contracts.md#agent-assembly) | One SDK `Agent` assembly; customization uses interventions, plugins, and conversation managers instead of forking the loop. |
| Permissions | [`PermissionGate`, `ApprovalMode`, and `classify`](../../src/agent/permission.ts) | `default`, classifier-backed `auto`, and unrestricted `yolo`; reads proceed, writes/executes prompt or follow rules. There is no enforced read-only/plan mode. Unknown MCP tools fail closed as execute. |
| Sessions/context | [`createSessionManager`](../../src/agent/session.ts), [`AgentRuntime.create`](../../src/agent/runtime.ts), [`run-tests.ts`](../../spike/run-tests.ts) | Persistent snapshots, explicit resume/session selection, summarization, manual compact, optional durable offload; no user-facing fork/search/replay trajectory. |
| Skills/commands | [`SkillsPlugin`](../../src/skills/plugin.ts), [`loadCustomCommands`](../../src/commands/custom-commands.ts) | Progressive-disclosure skills plus project/user slash-command loading. |
| MCP/hooks | [`loadMcpClients`](../../src/mcp/registry.ts), [`ToolHookGate`](../../src/hooks/tool-hooks.ts) | MCP tool sources and deterministic pre/post tool hooks share the permission intervention boundary. |
| Subagents | [`SubagentTool`](../../src/agents/subagent-tool.ts), [isolated-subagent contract](../../.trellis/spec/backend/strands-sdk-contracts.md#scenario-isolated-subagents-as-a-tool) | Fresh child agents return bounded results and inherit intervention policy, but delegation is a synchronous tool result without inspectable child threads or an explicit parallel orchestration UI. |
| TUI/verification | [`App`](../../src/tui/App.tsx), [pty testing contract](../../.trellis/spec/frontend/tui-testing.md), [`acceptance-e2e.ts`](../../spike/acceptance-e2e.ts) | Ink TUI, structured permission prompts, real-pty scenarios, deterministic fast suites, and opt-in live model acceptance. No product benchmark/evaluation scorecard. |
| Self-development evidence | [`docs/iteration-log.md`](../iteration-log.md) | `/developer` supervises persistent child work with Host re-runs and commit evidence, but selection quality is not measured against a stable task corpus. |

### Comparison and gaps

| Peer evidence | Darwin evidence | Parity / gap / deliberate difference | Implication |
|---|---|---|---|
| Claude plan-before-editing and Codex read-only mode (S1, S2) | `ApprovalMode` has only `default`, `auto`, `yolo`; `classify` already distinguishes read/write/execute. | High-value gap at an existing enforcement seam. | Add a mode that technically denies mutation while preserving safe exploration; do not rely on prompt wording alone. |
| Codex OS sandbox and OpenHands isolated runtimes (S2, S5) | Darwin runs the SDK bash persistent shell and file editor on the host behind `PermissionGate`. | Deliberate local-first architecture, but no containment after approval. | Explore an optional sandbox backend separately; it is not a substitute for permissions and has substantial platform/SDK risk. |
| Codex inspectable parallel subagent threads (S2) | `SubagentTool` creates isolated children but returns only a terminal tool result. | Partial parity on specialization/isolation; gap in concurrency and observability. | Start with read-heavy parallel tasks and source-labelled state; avoid concurrent writes until conflict isolation exists. |
| DeepSeek append-only trajectories (S3) | SDK snapshots support resume and Darwin exposes rendered history/usage, not event-stream search/fork/replay. | Gap in inspectable and derivable execution history. | A session export/trajectory layer should wrap SDK events without replacing the SDK agent loop. |
| Penguin benchmark-driven evolution (S4) | Real acceptance scripts and iteration logs exist, but there is no stable scored corpus. | Strong verification foundation, evaluation gap; peer performance claims are not independently verifiable. | Introduce a small local benchmark contract before automating optimization; snapshots and regression gates matter more than headline scores. |
| DeepSeek everything-as-plugin (S3) | Runtime is intentionally a thin SDK assembly, but sessions, permission mode, TUI, and orchestration are Darwin modules rather than interchangeable plugins. | Deliberate difference: broad pluginization would conflict with the current thin-assembly constraint. | Prefer narrow SDK extension points; do not copy wholesale runtime plugin architecture. |

### Ranked iteration directions

`Score = 2 × Importance + Architecture fit + Evidence confidence − Implementation difficulty − Implementation risk`

| Rank | Proposed ID | Direction | Importance | Architecture fit | Evidence confidence | Implementation difficulty | Implementation risk | Score | Evidence and rationale |
|---:|---|---|---:|---:|---:|---:|---:|---:|---|
| 1 | SER-001 | Add an enforced read-only planning permission mode. | 5 | 5 | 5 | 2 | 2 | 16 | S1/S2; `ApprovalMode` and `PermissionGate` are already the single parent/child enforcement seam. Small enough to verify without changing the SDK loop, and safer than a prompt-only plan convention. |
| 2 | SER-002 | Make subagent work parallel and inspectable with source-labelled status and approvals, initially for read-heavy delegation. | 4 | 3 | 5 | 4 | 4 | 8 | S2; Darwin already has isolated child agents and a serialized permission queue, but needs lifecycle/thread state and conflict policy. Read-heavy-first is a safety dependency, so this ranks below its raw product appeal. |
| 3 | SER-003 | Add append-only session trajectory export plus search/fork/replay primitives over SDK events. | 4 | 3 | 5 | 5 | 3 | 8 | S3; persistent sessions exist, but a durable event schema and replay correctness are substantial. It must remain an observer/adapter, not a forked agent loop. |
| 4 | SER-004 | Add an optional isolated execution backend for shell/file mutation. | 5 | 2 | 5 | 5 | 5 | 7 | S2/S5; high security value but platform portability, mounts, credentials, persistent shell behavior, and local UX make this the riskiest change. Requires a design spike before implementation. |
| 5 | SER-005 | Establish a stable local coding-agent evaluation corpus and regression scorecard for self-evolution. | 4 | 3 | 4 | 5 | 4 | 6 | S4 plus Darwin's spike suite and iteration log. Build measurement before any optimizer; Penguin performance claims are publisher-only and its benchmark suite is not public. |

### Recommendation

- Selected direction: `SER-001`
- Why now: It is the highest-scoring direction, closes a safety and workflow gap demonstrated independently by two mature peers, and maps directly onto Darwin's existing intervention contract without changing its SDK agent loop. It is also a prerequisite for safer planning before larger sandbox or orchestration work.
- Independently observable acceptance: a configured/CLI-selected read-only mode is accepted by config and arguments; read-classified tools still proceed; write and execute calls are denied without prompting, classifier calls, or allow-rule bypass; the same gate protects child agents; the TUI/header and headless diagnostics identify the effective mode; focused tests, `pnpm typecheck`, `pnpm test`, and `git diff --check` pass.
- Backlog updates: `SER-001` through `SER-005` added; `SER-001` changed to `进行中` immediately before developer handoff, all others remain `未开始`.

### Developer outcome

- Direction: `SER-001`
- Child session and managed tasks: child session `session-20260815-152031521`; `bg-14a78197-3457-4dec-ab4d-3655ea301b79` failed before model/session/usage because the local `darwin` bin was not executable; planning succeeded in `bg-6bc36836-0776-42d9-9e55-bbfbe6b940d6`; implementation and commit succeeded in `bg-51a73455-5dc3-48a0-ad8f-3dd25e93b536`.
- Host acceptance: inspected commit `e2e1463` and its complete file list/diff; independently re-ran `pnpm typecheck`, `pnpm test` (exit 0 with the expected MCP `continueOnError` diagnostic), `HOME=<temporary> pnpm tsx spike/verify-tui.ts plan` (4 passed), Trellis task validation, `git diff --check`, and clean-tree verification successfully.
- Final status: `完成`
- Iteration-log entry: [`Batch 5 — enforced read-only planning mode`](../iteration-log.md#batch-5--enforced-read-only-planning-mode-2026-08-15)
