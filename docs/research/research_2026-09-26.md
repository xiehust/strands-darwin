# Darwin self-evolution research — 2026-09-26

## Run — 2026-09-26T03:44:50Z

### Backlog-first decision

- Router first: `docs/research/backlog_index.md`.
- Metadata-only search: all eight routed pages, `directions-001-020.md` through `directions-141-160.md`; last page has no direction yet. No `in-progress` or `not-started` records.
- Decision: fresh research; roll precedes source reading.

### Research path

```text
research-path: peer
focus: comparable coding-agent product analysis
share: 50% (weight 5 of 10)
draw: 10 of 20 half-units
path-source: roll
rolled-at: 2026-09-26T03:44:50.435Z
weights: tui=2 observability=0.5 sdk=1 open=1.5 peer=5
```

### Source availability and method

Primary product docs and public source repositories were accessible with `web-search` and `web_fetch` on 2026-09-26 UTC. Initial Codex developer-doc and Cursor docs redirects crossed hosts; the final first-party targets were fetched directly. No named product was left unverified. Marketing benchmark numbers in PenguinHarness README are not independent performance evidence and are not compared. Peer facts below are cited; Darwin assessments are inferences grounded in local code/contracts.

### Reference sources

| Ref | Product | Source type | URL | Accessed (UTC) | Claims supported |
|---|---|---|---|---|---|
| S1 | Claude Code | Primary documentation | https://code.claude.com/docs/en/checkpointing | 2026-09-26 | 100 checkpoint snapshots; restore code/conversation independently; bash and most subagent edits not tracked. |
| S2 | Codex | Primary documentation | https://learn.chatgpt.com/docs/code-review.md | 2026-09-26 | CLI `/review` presets include uncommitted, base-branch, commit and custom review; dedicated reviewer reports actionable findings without edits. |
| S3 | DeepSeek Harness | Primary repository architecture | https://github.com/deepseek-ai/deepseek-harness/blob/master/docs/architecture.md | 2026-09-26 | Cordis plugin composition; `web`, `headless`, `sdk`, `sdk-minimal`, `acp` profile templates. |
| S4 | OpenCode | Primary documentation | https://opencode.ai/docs/agents/ | 2026-09-26 | Build/Plan primary agents; General/Explore/Scout subagents with configured permissions; child-session navigation. |
| S5 | kiro-cli | Primary documentation | https://kiro.dev/docs/cli/chat/queue-steering/ | 2026-09-26 | Mid-turn steer vs next-turn queue, Ctrl+S switch, safe-point injection and sequential queued prompts. |
| S6 | PenguinHarness | Primary repository README | https://raw.githubusercontent.com/Prism-Shadow/penguin-harness/refs/heads/main/README.md | 2026-09-26 | Claims local-first agent-app generation/evaluation/optimization/deployment, snapshot-per-round self-evolution and Trace; no benchmark advantage independently established. |
| S7 | Cursor (additional product) | Primary documentation | https://cursor.com/docs/cli/using | 2026-09-26 | CLI supports plan/ask modes, ACP, Ctrl+R review UI, local worktrees and session resume. |
| S8 | Codex | Primary documentation | https://learn.chatgpt.com/docs/codex/cli.md | 2026-09-26 | Interactive/exec CLI, review and delegation workflow. |
| S9 | DeepSeek Harness | Primary repository guide | https://github.com/deepseek-ai/deepseek-harness/blob/master/docs/user/guide/index.md | 2026-09-26 | Web UI agents edit, run commands, delegate, plan, request policy-based approval. |
| S10 | kiro-cli | Primary documentation | https://kiro.dev/docs/cli/chat/goal/ | 2026-09-26 | Bounded autonomous goal loop implements, checks criteria and retries failing verification. |
| S11 | Claude Code | Primary documentation | https://code.claude.com/docs/en/sub-agents | 2026-09-26 | Isolated subagent context and restricted tool sets. |

### Peer highlights and innovations

| Product | Sourced feature / innovation | Reference | Why it matters |
|---|---|---|---|
| Claude Code | Snapshots restore selected code and/or conversation; bash/subagent limits explicit. | S1 | Safer experimentation, with narrow rollback semantics. |
| Codex | Choose the exact review target: working tree, commit, or base-branch diff. | S2 | A post-commit audit need not pretend the working tree still contains the change. |
| DeepSeek Harness | Configurable Cordis plugin tree across web/headless/sdk/acp profiles; Web UI keeps policy-based approval. | S3, S9 | Different extension strategy, not automatically a Darwin feature gap. |
| OpenCode | Primary plan/build plus read-only specialist subagents, navigable child sessions. | S4 | Separates research from mutation and exposes child provenance. |
| kiro-cli | Safe-point steering vs sequential next-turn queue; `/goal` loops with verification. | S5, S10 | Contrasts immediate course corrections and autonomous testing. |
| PenguinHarness | README claims snapshot-backed evaluate/optimize workflow and Trace. | S6 | Evidence is self-description only; not a measured quality or cost comparison. |
| Cursor | CLI modes, ACP server, review viewer and optional per-task worktrees. | S7 | Changes editor interoperability and checkout isolation, at integration cost. |

### Current Darwin baseline

| Capability / architecture | Repository evidence | Current behavior |
|---|---|---|
| SDK architecture | `AGENTS.md` SDK reuse row; `docs/architecture/load-bearing-decisions.md` § SDK reuse; `src/agent/runtime.ts:AgentRuntime.create` | SDK `Agent` assembled once, SDK loop not forked; plugins, hooks and interventions used. |
| Permissions/workspace trust | `AGENTS.md` Permissions and Extension layers rows; `src/agent/permission.ts:PermissionGate`; decisions doc §§ Permissions, Workspace trust | Safe/plan/execute gate; project executable config held for consent. No OS sandbox guarantee. |
| Review | `src/commands/review-command.ts:parseReviewCommand`, `src/agent/runtime.ts:expandSlashCommand`, `spike/verify-review-command.ts`, `spike/verify-review-drivers.ts`; existing `SER-087` | Pure prompt expansion currently asks for current staged/unstaged changes and untracked files; optional focus is only literal text. It does not select a commit or merge-base scope. Tool calls use ordinary gate, not enforced read-only. |
| Sessions / rewind | `src/agent/session.ts`; `src/agent/rewind.ts`; decisions doc §§ `/rewind`, Resumed-session full transcript | Per-project resume and SDK conversation-only rewind; deliberately no workspace rollback. |
| Skills/MCP/subagents | `src/agents/loader.ts:DEFAULT_AGENT`, `src/agents/child-recipe.ts:buildRecipeChild`, `src/mcp/registry.ts`; decisions doc §§ Skills, Subagents, `/mcp`, Workflow DAG | Layered skills/agent definitions, MCP projection, bounded dispatches; child tool gate shared, transcripts intentionally private. |
| TUI/verification | `src/tui/frame-budget.ts`; `src/tui/prompt-queue.ts`; `spike/verify-tui.ts`; `AGENTS.md` Commands | Frame budget enforced; queued prompts become next turns; free pty checks plus model-calling suites. |

### Comparison and gaps

| Peer evidence | Darwin evidence | Parity / gap / deliberate difference | Implication |
|---|---|---|---|
| Codex scope presets S2 | `parseReviewCommand`, `SER-087` | Partial parity: `/review` exists but is current-worktree only; arbitrary `Focus:` does not define trustworthy commit/branch scope. | Add narrow, explicit review target grammar without a second executor; preserve no-auto-edit and gate. |
| Claude checkpoint S1 | decisions doc § `/rewind`; `src/agent/rewind.ts` | Deliberate difference: SDK conversation branch avoids filesystem rollback and non-tool edits. | No direction; changing promise is unsafe without a separate design. |
| DeepSeek plugins S3 | decisions doc § SDK reuse; `src/agent/runtime.ts` | Deliberate SDK extension-point architecture, not an invitation to replace the loop. | No direction. |
| OpenCode subagents S4 / Claude subagents S11 | `src/agents/loader.ts`, `docs/architecture/sub-agents.md` § Deliberate boundaries | Delegation and tool restrictions exist; child transcript navigation/recursive dispatch intentionally omitted. | No direction absent a safety/UX design. |
| Kiro steer S5 / goal S10 | decisions doc § The prompt queue; `src/tui/prompt-queue.ts`; `AGENTS.md` Direct driver streaming | Intentional next-turn queue and no driver-owned success classifier; mid-turn injection/goal loop would change contracts. | No direction in this run. |
| Penguin snapshots/Trace S6 | `src/trajectory/record.ts`; decisions doc §§ Session trajectory, Session diagnostics | Trajectory and research/developer verification already exist; no independent benefit measurement for cloning claimed benchmark process. | No direction. |
| Cursor ACP/worktree/review S7 | `src/cli.ts`; `src/tui/App.tsx`; `src/commands/review-command.ts`; `SER-004` | ACP absent, worktree switch absent and sandbox direction abandoned by explicit user; prompt review exists. | ACP/worktree need separate product authorization/design; not queued. |

### Ranked iteration directions

`Score = 2 × Importance + Architecture fit + Evidence confidence − Difficulty − Risk`; threshold 6. Next unused SER ID `SER-102`, next priority `141` (current routed page `directions-141-160.md`).

| Rank | Proposed ID | Direction | Importance | Architecture fit | Evidence confidence | Difficulty | Risk | Score | Evidence and rationale |
|---:|---|---|---:|---:|---:|---:|---:|---:|---|
| 1 | SER-102 | Extend `/review` with an explicit full-SHA commit scope while preserving bare and literal-focus forms | 4 | 5 | 5 | 2 | 2 | 14 | S2 commit review; current `parseReviewCommand`/SER-087 only target uncommitted files. Commit scope catches post-commit regressions without a new execution channel; requires explicit target and evidence in the prompt. |

### Gated-out directions

| Direction | Score | Gate | Why it fell below | Kept anyway? |
|---|---:|---:|---|---|
| Add base-branch review scope now (importance 2, fit 3, confidence 4, difficulty 4, risk 4) | 3 | 6 | S2 supports it, but remote/tracking ref ambiguity and merge-base behavior add command/safety cases; lower incremental value than full-SHA review. | no |
| Add built-in mid-turn steering (importance 3, fit 1, confidence 5, difficulty 5, risk 5) | 2 | 6 | S5 demonstrates UX, but conflicts with next-turn-only queue and driver streaming invariants. | no |
| Ship Cursor-style worktree switch in Darwin (importance 2, fit 2, confidence 4, difficulty 5, risk 5) | 0 | 6 | S7 documents it, but checkout/session/process scope and the SER-004 user decision are unresolved; no authorization to change isolation. | no |

### Recommendation

- Batch: `SER-102` only. Start with SER-102. No dependency override.
- Acceptance: `/review --commit <40 hexadecimal characters>` expands to a fixed review-only prompt naming the exact SHA and its parent diff; legacy `/review` and `/review <focus>` byte-for-byte unchanged; invalid SHA-shaped explicit flag refuses locally with usage (never treated as arbitrary focus), no shell execution/interpolation, no model request on invalid input; test TUI/headless/REPL expansion and gate semantics; `pnpm typecheck`, `pnpm test`, free `spike/verify-tui.ts completion`, `pnpm build` and docs EN/zh-CN. Review a root commit through empty-tree comparison instead of assuming a parent; instructions to model must state git resolution errors honestly. No mandated live-provider quality claim.
- Backlog update: append SER-102 `not-started` to `docs/research/backlog/directions-141-160.md`; set it `in-progress` only immediately before developer handoff. No rollover route. Gated-out suggestions remain report-only.

### Batch iteration outcome

| Direction | Child session and managed tasks | Host acceptance (exact independent checks and outcomes) | Commit | Final status | Iteration-log entry |
|---|---|---|---|---|---|
| SER-102 | `session-20260926-040422866`: `bg-83849d9e-fd03-408c-a757-166ff5dcfda7` invalid launch (exit 1, no session); `bg-dc7bacb3-3df5-4c90-bbc9-b866033abeee` failed before model (exit 1, same session); `bg-657cc00a-c095-4bb1-a273-c57bfc9ada2e` passed (exit 0, fully drained). | Host inspected 14-file implementation and docs diff, ran `pnpm typecheck && pnpm test && AWS_EC2_METADATA_DISABLED=true pnpm tsx spike/verify-tui.ts completion && pnpm build && git diff --check && git status --short` (`bg-31a8c4b6-76bb-46d4-9002-dfba02317254`, exit 0; full suite 9,756 PASS lines, completion 75/0, clean tree); docs EN/zh-CN and architecture synced. | `3ec0da4f139793dd3477d3100a03ee52b3c11dee` | `done` | `docs/iteration-log.md` Batch 152 |

- Halt condition: batch exhausted; one record accepted and no unfinished batch peers.
- Aggregate child token spend: input **unknown** (`-` on stopped attempt; 92 on successful task), output **18,221**, cacheRead **unknown** (`-`; successful task 3,691,767), cacheWrite **unknown** (`-`; successful task 120,506). Failed CLI launch has no usage or cost record. Stopped attempt reports `cost: total=-`; successful task reports $1.2220. Aggregate USD **unknown**. Host acceptance made no model calls.
- Remaining backlog: none unfinished. New research is appropriate only on a subsequent run.

## Run — 2026-09-26T05:11:10Z

### Backlog-first decision

- Read `backlog_index.md` before product research. Metadata-only scan of all eight routed pages (001–020 through 141–160): 141 records, 139 `done`, two `abandoned`, none unfinished. Completed evidence/notes were not loaded for selection.
- Fresh research, user-directed: investigate Claude Code independent-process communication and a Darwin `/list-agents` command. Next stable IDs SER-103/SER-104; priorities 142/143. Heading scan found no duplicate; SER-091 leases and SER-002 subagents are related foundations, not this feature.

### Research path

```text
research-path: peer
focus: comparable coding-agent product analysis
share: 50% (weight 5 of 10)
draw: none — path was not rolled
path-source: override (user-directed)
rolled-at: 2026-09-26T05:11:10.597Z
weights: tui=2 observability=0.5 sdk=1 open=1.5 peer=5
```

### Source availability and method

Primary documentation was accessed on 2026-09-26 using web_fetch, HTTP and search. This is directed peer research, not a random draw. A guessed Codex URL returned 404; search located the canonical first-party page, whose markdown was then read. Kiro's redirect was followed. All seven required scopes were consulted; absence of a feature in these particular pages is not proof a product lacks it. PenguinHarness marketing claims are not independent performance evidence. The Claude API skill was loaded for the named-product trigger before the path script; it supplied no product evidence or model choice for this provider-neutral Darwin change.

### Reference sources

| Ref | Product | Source type | URL | Accessed (UTC) | Claims supported |
|---|---|---|---|---|---|
| S1 | Claude Code | Primary docs | https://code.claude.com/docs/en/cross-session-messaging | 2026-09-26 | Independent sessions discover with `ListAgents` and `/list-agents` (`/peers` alias), send text with `SendMessage`; disk discovery plus Unix socket/named pipe locally; receiving-session permission and inbound controls. |
| S2 | Claude Code | Primary docs | https://code.claude.com/docs/en/agent-teams | 2026-09-26 | Experimental opt-in teams have lead, independently contextualized teammates, shared tasks and peer messages; explicitly distinguishes independent-session messaging. |
| S3 | Codex | Primary docs | https://learn.chatgpt.com/docs/agent-configuration/subagents.md | 2026-09-26 | Parallel specialized agents return summaries; CLI `/agent` inspects/switches threads; cautions about concurrent writes and token cost. |
| S4 | DeepSeek Harness | Primary repository architecture | https://raw.githubusercontent.com/deepseek-ai/deepseek-harness/refs/heads/master/docs/architecture.md | 2026-09-26 | Cordis plugin tree and replaceable loop; core agent registry, SDK JSON-RPC server and authenticated Desktop Host IPC. These are not proof of arbitrary independent-peer messaging. |
| S5 | OpenCode | Primary docs | https://opencode.ai/docs/agents/ | 2026-09-26 | Primary agents vs subagents; navigable child sessions; General plus read-only Explore/Scout. |
| S6 | kiro-cli | Primary docs | https://kiro.dev/docs/custom-agents/subagents/ | 2026-09-26 | Isolated subagent contexts, shared workspace/permission config, DAG dependencies, bounded review loops and Ctrl+G monitoring. |
| S7 | PenguinHarness | Primary repository README | https://raw.githubusercontent.com/Prism-Shadow/penguin-harness/refs/heads/main/README.md | 2026-09-26 | Describes local-first multi-agent application platform, snapshot-per-round self-evolution and Trace. No independent benchmark or cross-process-message claim inferred. |
| S8 | Cursor (additional product) | Primary docs | https://cursor.com/docs/cli/using | 2026-09-26 | CLI ACP server over stdio, cloud handoff, optional worktrees and resumable threads. These are distinct from peer IPC. |

### Peer highlights and innovations

| Product | Sourced feature / innovation | Reference | Why it matters |
|---|---|---|---|
| Claude Code | Independent-process messaging, not only parent/child delegation. Inbound text cannot approve permissions, execute slash commands or import sender history/files. Active-turn delivery occurs between tools; idle delivery starts a new turn. | S1 | Lets separately operated sessions exchange findings; also introduces cost and authorization risks. |
| Claude Code | Agent Teams is a separate, disabled-by-default experimental coordination system. | S2 | Do not implement an autonomous team scheduler merely to add discovery/messaging. |
| Codex | `/agent` navigation and summary-returning subagents. | S3 | Visibility into one delegation tree is not an inventory of independent local processes. |
| DeepSeek Harness | Registry/services are Cordis plugins; Desktop IPC carries startup/readiness/shutdown. | S4 | Useful architectural comparison, but Darwin should retain its Strands loop. |
| OpenCode | Child session navigation and scoped read-only specialists. | S5 | Context isolation and observation are separate concerns. |
| kiro-cli | DAG subagents and capped review loops. | S6 | A larger orchestration surface than simple messaging; not proposed here. |
| PenguinHarness | Trace and snapshot-driven evolution (README self-description). | S7 | Observability matters; no claim that this solves local-peer coordination. |
| Cursor | ACP and cloud handoff plus worktrees. | S8 | Client/server transport, remote execution and isolated checkouts are different products from local peer messages. |

S1 specifically documents same-machine messaging on all providers from v2.1.248, local socket/pipe transport without Anthropic relay, and `crossSessionInbound` accept/hold/refuse. The page distinguishes cross-machine delivery through Anthropic/Remote Control; that cloud scope is excluded here. These are documentation claims, not a live Claude Code interoperability test.

### Current Darwin baseline

Baseline revision: `ce031a94ae7ac1219fdcc3146d646442f6324e91`.

| Capability / architecture | Repository evidence | Current behavior |
|---|---|---|
| Independent session ownership | `src/agent/session.ts:resolveSession`, `inspectLease`, `classifyLease`; decisions doc § Session lease; `spike/verify-session-lease.ts`; history `55469cb` | Each selected session owns `lease.json` with PID/hostname/start time, even before its first snapshot. Live same-host PID blocks reopening; normal retirement/shutdown releases it. This is ownership metadata, not an authenticated IPC endpoint. |
| Session paths/listing | `src/paths.ts:projectKey`, `userProjectSessionsDir`; `src/cli-sessions.ts`; decisions doc § darwin sessions | User-global HOME state partitioned by canonical project key; listing requires a restorable snapshot and stays within the current project. Thus it cannot inventory every running local session. Project keys cannot be decoded into exact working directories. |
| `/agents` | `src/agents/dispatch-registry.ts` maps `records`/`byAgentId`; `src/tui/App.tsx` agents branch; `docs/architecture/sub-agents.md` §§ Dispatch observability / Deliberate boundaries | In-memory dispatches within this runtime, user cancellation and bounded status. No persistent peer registry, running-child steering or autonomous swarm. |
| Command/TUI surface | `src/commands/custom-commands.ts:BUILTIN_COMMAND_NAMES`; `src/tui/prompt-completion.ts:computeCompletions`; App submit routing; `spike/verify-tui.ts` | No built-in `list-agents`; local commands can precede busy queueing and emit bounded Static notices. All built-ins must fit completion discovery. |
| Permission and delivery | `AGENTS.md`; decisions doc §§ Permissions / Direct driver streaming / The prompt queue / Background-task wake | Per-runtime ordinary tool gate, user-only permission decisions; input queues next turns, no arbitrary mid-stream injection. A peer message must not acquire typed-user authority. |
| SDK, extensions and evidence | decisions doc §§ SDK reuse / Skills / Subagents / Session trajectory; `docs/architecture/sub-agents.md`; README | Strands extension points, layered skills/MCP, isolated child histories, observer trajectory, offline real-files/process/pty suites. No loop fork or new unobserved execution channel. |

Read the relevant documented boundaries before proposing changes. Recent history includes session leases (`55469cb`), resume listing (`33d5bb0`), and latest review-only change (`3ec0da4`); none supplies local peer communication.

### Comparison and gaps

| Peer evidence | Darwin evidence | Parity / gap / deliberate difference | Implication |
|---|---|---|---|
| S1 `/list-agents` independent sessions | Existing leases vs in-memory `/agents` and project/snapshot-only `sessions` | Concrete discovery gap. | Reuse leases for a read-only human inventory across this HOME, with explicit limits rather than inventing a persistent registry. |
| S1 local messaging and inbound controls | No IPC; ordinary permissions/next-turn queue/observer trajectory | Useful gap, but receipt can spend tokens and trigger actions under another session's authority. | Separate direction depends on explicit recipient policy; never copy active-turn injection against Darwin's contract. |
| S2 team scheduler / S6 review loops | SDK Graph workflows and documented no-swarm boundary | Deliberate difference, not a missing checkbox. | No team/swarm implementation in this batch. |
| S3/S5 child navigation | Darwin private child transcripts and summary-only parent reports | Deliberate context-isolation difference. | Preserve `/agents`; new inventory must not read child transcripts. |
| S4 plugin/server IPC / S8 ACP | Darwin SDK assembly and TUI/headless drivers | Different transport/extension goals. | Neither requires replacing the agent loop or adding a generic server. |
| S7 Trace | Darwin trajectory/diagnostics/research supervision | Related observability already present. | Peer messaging would need explicit record semantics; do not treat plain IPC logs as durable trajectory evidence. |

### Ranked iteration directions

`Score = 2 × Importance + Architecture fit + Evidence confidence − Difficulty − Risk`; gate 6. Ratings below are fixed for selection.

| Rank | ID | Direction | Importance | Architecture fit | Evidence confidence | Difficulty | Risk | Score | Evidence and rationale |
|---:|---|---|---:|---:|---:|---:|---:|---:|---|
| 1 | SER-103 | Read-only `/list-agents` and `darwin list-agents` over live local session leases | 5 | 5 | 5 | 3 | 3 | 14 | Explicit user need plus S1; existing lease/paths foundation avoids new registry/daemon. Human discovery can ship independently of any messaging authority. |
| 2 | SER-104 | Explicitly authorized bounded local cross-session text messaging | 5 | 4 | 5 | 5 | 5 | 9 | S1 demonstrates value; next-turn/gate extensions fit, but identity, recipient consent, headless delivery, queue bounds and feedback loops need a resolved policy. Depends on SER-103; halt here for user decision, not because of difficulty. |

### Gated-out directions

| Direction | Score | Gate | Why it fell below | Kept anyway? |
|---|---:|---:|---|---|
| Automatic team spawning/shared task scheduler (I=2, fit=1, evidence=4, difficulty=5, risk=5) | -1 | 6 | S2/S6 are much broader than the requested local communication; conflicts with documented no-swarm and shared-write constraints. | no |
| Cloud/cross-machine relay (I=2, fit=2, evidence=5, difficulty=5, risk=5) | 1 | 6 | S1 supports it, but no user authorization for remote data transfer/identity infrastructure. | no |

Neither is appended to the backlog; no existing record was abandoned by the gate.

### Recommendation

- Batch: SER-103 then SER-104, priorities 142/143 in `backlog/directions-141-160.md`; no rollover or index-state duplication. Both proposed as complete `not-started` records and pass the selection gate unchanged.
- SER-103 acceptance: immutable-state offline real-process fixture with two projects and snapshotless sessions; exclude foreign/dead/malformed/oversized/symlink entries; bounded read/scan/output and omission notices, terminal sanitization; PID/session/project-key/start/current marker; no model/network/config/permissions/transcript reads or mutating signal. Real TUI idle/busy handling and completion, CLI grammar, existing leases/sessions unchanged, typecheck/test/build. Scope is this HOME's live local lease holders, not authenticated OS-wide discovery.
- SER-104 decision needed after SER-103: same-project explicit receive opt-in and per-message human acceptance (recommended) versus unattended automatic peer collaboration; cross-project scope must be chosen explicitly. No recipient model work before that choice. Future acceptance must cover real two-process delivery and hostile sender, permission, queue, lifecycle, provenance and loop limits. Keep it unfinished when asking; do not silently substitute a different feature.
- Implementation only through the loaded developer skill, one fresh child per direction. Host owns research/backlog/log and independent acceptance; child may modify targeted source/tests/user docs, run verification and commit but not push, delegate supervision, add dependencies or rewrite history.

### Batch iteration outcome

| Direction | Child session and managed tasks | Host acceptance (exact independent checks and outcomes) | Commit | Final status | Iteration-log entry |
|---|---|---|---|---|---|
| SER-103 | `session-20260926-052906558`; initial `bg-f226cfa3-943e-4bd9-ae93-2a1b2c40d2dd`, correction `bg-6eb5c61a-af1f-421b-9c08-eef63dff038b`; both exit 0 and output drained to `hasMore: false` | First Host typecheck/test/completion/build passed, but actual-HOME smoke omitted this live project's session beyond the 128-project prefix. Focused correction independently reviewed; second acceptance ran `pnpm tsx spike/verify-list-agents.ts`, `pnpm tsx spike/verify-list-agents-pty.ts`, `pnpm typecheck`, `pnpm test`, `pnpm build`, `node dist/spike/verify-list-agents.js`, built-CLI assertion finding this Host session in saturated HOME, diff/clean-tree checks; all passed (`bg-97073123-5491-47c2-b7bb-8d5637af332e`). Free completion passed 75/0 in first Host gate. | Feature `947513b5f4f4e94fe6a5fc56df511842f1bfab3b` plus correction `8d588e242d188d8cb1b3b6ded5e3ea9f05679017`, accepted together | `done` | `docs/iteration-log.md` Batch 153 |
| SER-104 | Not launched; next-priority selection checked after SER-103 acceptance | Score 9 still passes; recipient authorization and cross-project scope remain user decisions, not implementation details | none | `not-started` | Batch 153 halt note |

- Halt condition: **only the user can decide**. Ask for explicit receive policy: recommended same-project session opt-in with per-message approval before any model turn and no automatic replies, versus unattended automatic collaboration. Cross-project authorization must also be chosen. No fresh research until SER-104 is resolved/closed.
- Child spend: initial input 84, output 33,883, cacheRead 4,225,394, cacheWrite 155,181, USD $7.8601; correction input 58, output 24,598, cacheRead 5,470,697, cacheWrite 54,968, USD $7.3883. Aggregate input **142**, output **58,481**, cacheRead **9,696,091**, cacheWrite **210,149**, approximate USD **$15.2484**. Both tasks report `global.openai.gpt-6-astra`; no missing metrics. Host verification used no provider calls.
- Remaining backlog: only SER-104 `not-started`; no gate-abandoned records. Automatic swarm and cloud relay were rejected in research only, not queued. Source and docs are built at accepted HEAD; the separate globally installed npm package and already-running TUI were not replaced. To use immediately: `node dist/src/cli.js list-agents` or launch the repository TUI with `pnpm start`.
- Push the current branch once after the closure commit and verify `git log @{u}..` is empty; report the actual push result to the user, without force or history rewriting.
