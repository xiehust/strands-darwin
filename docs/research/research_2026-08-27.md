# Darwin self-evolution research — 2026-08-27

## Run — 2026-08-27T12:45:27Z

### Backlog-first decision

- Backlog router inspected first: `docs/research/backlog_index.md`
- Metadata searched across routed priority pages: `docs/research/backlog/directions-001-020.md`, `directions-021-040.md`, and `directions-041-060.md`
- Unfinished directions found: none
- Decision: fresh research

### Research path

```text
research-path: peer
focus: comparable coding-agent product analysis
share: 50% (weight 5 of 10)
draw: 16 of 20 half-units
path-source: roll
rolled-at: 2026-08-27T12:43:20.585Z
weights: tui=2 observability=0.5 sdk=1 open=1.5 peer=5
```

### Source availability and method

Web search, direct HTTPS access to primary documentation/source repositories, the Strands documentation catalogue, and the local Darwin repository were available on 2026-08-27 UTC. Claude Code, Codex, DeepSeek Harness, PenguinHarness, and OpenCode were all verified from primary sources. Product-publisher claims in PenguinHarness's README are reported as claims, not independently validated benchmarks. The comparison began from Darwin's source, specs, tests, and recent history; peer facts were not inferred from model memory.

### Reference sources

| Ref | Product | Source type | URL | Accessed (UTC) | Claims supported |
|---|---|---|---|---|---|
| S1 | Claude Code | Primary product documentation | https://docs.anthropic.com/en/docs/claude-code/interactive-mode | 2026-08-27 | The interactive terminal accepts a clipboard image with `Ctrl+V`/platform alternatives, inserts an `[Image #N]` chip, and also exposes background task and checklist controls. |
| S2 | Codex | Primary product documentation | https://developers.openai.com/codex/cli/features | 2026-08-27 | The CLI composer accepts prompts, code snippets, and screenshots, and `/copy` copies the latest completed output. |
| S3 | Codex | Primary product documentation | https://developers.openai.com/codex/image-inputs | 2026-08-27 | An image can be dragged into the composer as context; Codex can also inspect an image path or use a screenshot tool. |
| S4 | DeepSeek Harness | Primary source repository | https://github.com/deepseek-ai/deepseek-harness/blob/master/README.md | 2026-08-27 | DeepSeek Harness is an open-source developer-preview harness whose architecture makes every subsystem a plugin and whose current primary interface is a local Web UI. |
| S5 | DeepSeek Harness | Primary architecture/user documentation | https://github.com/deepseek-ai/deepseek-harness/blob/master/docs/user/guide/index.md | 2026-08-27 | Its agent can read/edit files, run commands, delegate, and maintain a plan; approval-requiring operations are surfaced in the Web UI. |
| S6 | PenguinHarness | Primary source repository | https://github.com/Prism-Shadow/penguin-harness/blob/main/README.md | 2026-08-27 | PenguinHarness describes itself as a local-first multi-agent app-development platform; it claims benchmark-driven self-evolution with a snapshot before each round and requests observable in a Trace view. |
| S7 | OpenCode | Primary product documentation | https://opencode.ai/docs/agents/ | 2026-08-27 | OpenCode provides switchable Build and restricted Plan primary agents plus configurable specialized agents and permissions. |
| S8 | OpenCode | Primary product documentation | https://opencode.ai/docs/cli/ | 2026-08-27 | OpenCode supports programmatic runs, named-session continuation, and source-preserving session fork. |
| S9 | Strands Agents | Primary SDK declaration in installed dependency | `node_modules/@strands-agents/sdk/dist/src/types/agent.d.ts` and `types/media.d.ts` | 2026-08-27 | `Agent.stream()` accepts content-block arrays and the SDK exports byte-backed `ImageBlock`; multimodal input does not require a second model call or a forked agent loop. |

### Peer highlights and innovations

| Product | Sourced feature / innovation | Reference | Why it matters |
|---|---|---|---|
| Claude Code | Clipboard images become positional chips in the prompt composer. | S1 | A screenshot can be supplied at the moment a bug is described instead of first being written into the repository. |
| Codex | Screenshots are first-class composer inputs, while local image inspection remains available too. | S2, S3 | It demonstrates complementary direct-input and path-based visual workflows rather than forcing one transport. |
| DeepSeek Harness | Replaceability is pushed to a plugin architecture that includes the agent loop, tools, model, and session log. | S4 | This is a notable extensibility extreme, but differs deliberately from Darwin's SDK-loop reuse invariant. |
| PenguinHarness | The publisher couples benchmark rounds, snapshots, and request-level trace visibility in its self-evolution workflow. | S6 | It treats evaluation evidence and rollback points as one product loop; the benchmark claims themselves were not independently verified here. |
| OpenCode | Build/Plan agent switching and named session fork are built into the product surface. | S7, S8 | These are useful peer checks, but Darwin already ships live permission modes and source-preserving trajectory/session branch mechanisms. |

### Current Darwin baseline

| Capability / architecture | Repository evidence | Current behavior |
|---|---|---|
| Local visual inspection | `src/tools/image-viewer.ts:createImageViewerTool`, `loadLocalImage`; `.trellis/spec/backend/error-handling.md` imageViewer row | The model can safely request a PNG/JPEG/GIF/WebP already present on disk; bounded decoding/normalization returns an SDK `ImageBlock`. |
| Prompt transport | `src/agent/runtime.ts:AgentRuntime.send`; `src/tui/App.tsx` send path | Every user turn is currently reduced to a string before `Agent.stream(input)`, so the user cannot attach image bytes directly to a prompt. |
| Prompt editing/paste | `.trellis/spec/frontend/tui-testing.md` multiline-input contract; `src/tui/InputBox.tsx` | Bracketed text paste is normalized and inserted without submission; no image-chip state or clipboard-image gesture exists. |
| SDK extension boundary | `src/agent/runtime.ts` and `node_modules/@strands-agents/sdk/dist/src/types/agent.d.ts` | Runtime is the sole thin `Agent` assembly, while the installed SDK already accepts `ContentBlock[]` and supplies `ImageBlock`. |
| Existing peer parity | `src/agent/permission.ts`, `src/agent/rewind.ts`, `src/trajectory/fork.ts`, `src/tools/update-plan.ts` | Plan mode, source-preserving branches, and a visible progress checklist already cover several peer highlights without reopening their settled architecture. |

### Comparison and gaps

| Peer evidence | Darwin evidence | Parity / gap / deliberate difference | Implication |
|---|---|---|---|
| Claude Code and Codex accept screenshots directly in the composer (S1–S3). | `AgentRuntime.send(input: string)` plus the path-only `imageViewer` tool. | Gap: Darwin supports visual reasoning only after an image exists at a path and the model elects to call a tool. | Add a bounded direct attachment path that reuses the existing decoder and SDK content blocks while preserving the literal user text in trajectory/recall. |
| DeepSeek makes the loop itself replaceable (S4). | AGENTS.md “SDK reuse — the agent loop is never forked”; `src/agent/runtime.ts`. | Deliberate difference, not a gap: Darwin treats the SDK loop as load-bearing. | Do not copy DeepSeek's architecture; use the SDK's multimodal invocation contract at the existing runtime seam. |
| PenguinHarness couples self-evolution with benchmark snapshots/traces (S6). | `docs/research/backlog/` plus `docs/iteration-log.md`, trajectory, `/rewind`, and the developer supervision boundary. | Broad functional parity through a more conservative human-accepted workflow; automated benchmark optimization remains a product/risk choice, not evidence for this batch. | No direction proposed from the publisher's unverified benchmark claims. |
| OpenCode offers Plan and session fork (S7–S8). | Permission mode and trajectory/session branching are existing load-bearing decisions and completed backlog work. | Already covered. | Do not duplicate shipped work. |

### Ranked iteration directions

`Score = 2 × Importance + Architecture fit + Evidence confidence − Implementation difficulty − Implementation risk`

| Rank | Proposed ID | Direction | Importance | Architecture fit | Evidence confidence | Implementation difficulty | Implementation risk | Score | Evidence and rationale |
|---:|---|---|---:|---:|---:|---:|---:|---:|---|
| 1 | SER-041 | Attach a clipboard image to the next interactive prompt as a bounded visible chip, using the existing image decoder and SDK `ImageBlock` input while keeping trajectory/recall text-only and truthful. | 5 | 5 | 5 | 4 | 2 | 14 | Claude Code and Codex establish the terminal screenshot workflow (S1–S3); the installed SDK and Darwin's existing safe decoder make this an extension at `AgentRuntime.send`, not a loop fork (S9, `src/tools/image-viewer.ts`). It is independent of the path-based model tool and must preserve terminal paste, permission ownership, frame bounds, and provider-capability errors. |

### Gated-out directions

None. Other peer highlights were already shipped, deliberate architecture differences, or lacked enough independently verified evidence to become a new direction; no below-gate candidate was silently queued.

### Recommendation

- Batch to implement: `SER-041`
- Start with: `SER-041`
- Why this order: the batch has one self-contained direction; it reuses the existing bounded image ingestion before adding any broader attachment abstraction.
- Independently observable acceptance: free unit/pty coverage must prove image bytes and prompt text reach one SDK user message, a visible bounded attachment chip survives editor changes and clears only on actual send/cancel action, clipboard failure/unsupported terminal/provider paths are explicit and non-destructive, ordinary text/bracketed paste/queue/permission/compaction ownership is unchanged, trajectory and recall contain only the user's literal prompt plus a bounded attachment fact rather than bytes or synthetic path text, and `pnpm typecheck`, `pnpm test`, relevant free TUI scenarios, and `pnpm build` pass.
- Backlog updates: append `SER-041` as Priority 59 to `docs/research/backlog/directions-041-060.md`; change only that section to `in-progress` immediately before developer handoff.

### Batch iteration outcome

| Direction | Child session and managed tasks | Host acceptance (exact independent checks and outcomes) | Commit | Final status | Iteration-log entry |
|---|---|---|---|---|---|
| SER-041 | `session-20260827-125133864`; implementation `bg-28198ab5-9ffc-4dc1-8932-c30a5287b5d0`, Host correction `bg-90e4820b-b04f-496e-896a-8813be8cd12d` (both succeeded, exit 0) | Initial result rejected because provider failure consumed the image. After same-session correction, Host passed runtime 5/5, clipboard 8/8, decoder 34/34, queue 31/31, pty `clipboardImage` 14/14, `queue` 17/17, `permissionEscape` 3/3, `compacting` 5/5, `completion` 67/67, `recall` 22/22, `historySearch` 14/14, stream/help suites, typecheck, full test after backlog closure, build, and repository integrity checks. | `2b04e59` + `f4645d1` (archive `152aa7d`, journal `20b39e3`) | `done` | `docs/iteration-log.md` Batch 62 |

- Halt condition that ended the loop: batch exhausted
- Aggregate token spend across every child delegation: `input=590 output=66,794 cacheRead=45,090,854 cacheWrite=246,738`
- Remaining backlog after this run: none unfinished; fresh research is appropriate on the next self-evolution run
