# Darwin self-evolution research — 2026-08-17

## Run — 2026-08-17T01:10:12Z

### Backlog-first decision

- Backlog inspected first: [`backlog_index.md`](./backlog_index.md)
- Unfinished directions found: **none** — `SER-001`–`SER-003` and `SER-006`–`SER-010` are `done`; `SER-004`/`SER-005` were explicitly `abandoned`
- Decision: **fresh research**, because no `in-progress` or `not-started` row existed

### Research path

Rolled exactly once before opening any repository or product source. Verbatim script output:

```text
research-path: peer
focus: comparable coding-agent product analysis
share: 50% (weight 5 of 10)
draw: 18 of 20 half-units
path-source: roll
rolled-at: 2026-08-17T01:10:12.272Z
weights: tui=2 observability=0.5 sdk=1 open=1.5 peer=5
```

This was a chance roll, not a user override, and it was not re-rolled.

### Source availability and method

Web search and direct HTTP access to primary product documentation and repositories were available on 2026-08-17 UTC. All five required scopes were verified: Claude Code, Codex, DeepSeek Harness, PenguinHarness, and the additional product Cursor. Product facts below are limited to those sources. PenguinHarness benchmark and cost statements are identified as publisher claims rather than independent evidence. Repository claims come from the current Darwin tree at `814f1a8`, including source, tests, specs, README, backlog history, and recent commits. The comparison treats previously shipped or explicitly abandoned work as such rather than silently re-proposing it.

### Reference sources

| Ref | Product | Source type | URL | Accessed (UTC) | Claims supported |
|---|---|---|---|---|---|
| S1 | Claude Code | Primary product docs | <https://code.claude.com/docs/en/headless> | 2026-08-17 | Headless mode supports plain text, one structured JSON result carrying session metadata, JSON-Schema-constrained final output, and real-time newline-delimited `stream-json` events. |
| S2 | Codex | Primary product docs | <https://developers.openai.com/codex/noninteractive> and <https://developers.openai.com/codex/app-server/> | 2026-08-17 | `codex exec` normally separates progress on stderr from the final reply on stdout; `--json` changes stdout to a JSONL event stream containing thread/turn/item/error events. App Server exposes the same harness over bidirectional JSON-RPC for richer clients. |
| S3 | DeepSeek Harness | Primary product page | <https://deepseek.com/harness/en/> | 2026-08-17 | The developer preview records model-visible activity in an append-only session log used for source inspection, resume, fork, search and replay; models, tools, skills, sessions, sandboxes, storage, loops, scheduling and UI are plugin-provided. |
| S4 | PenguinHarness | Publisher repository README | <https://github.com/Prism-Shadow/penguin-harness> | 2026-08-17 | The publisher exposes CLI, SDK and headless-service entry points over one engine, plus trace/evaluation views and skills for agent creation, benchmark design, evaluation and optimization. Accuracy and cost figures remain publisher claims. |
| S5 | Cursor | Primary product docs | <https://docs.cursor.com/en/cli/headless> and <https://docs.cursor.com/en/cli/reference/output-format> | 2026-08-17 | Headless CLI use is designed for scripts and CI; `--output-format` offers text, aggregate JSON and streaming JSON. Cursor also documents full-diff review and automatic code checkpoints. |

### Peer highlights and innovations

| Product | Sourced feature / innovation | Reference | Why it matters |
|---|---|---|---|
| Claude Code | One headless command supports human text, a final metadata envelope, schema-constrained result data, or live JSONL events. | S1 | Automation does not have to scrape a human progress protocol or give up streaming to obtain stable fields. |
| Codex | `exec --json` emits typed lifecycle, item and error events; the normal mode retains the same final-stdout/progress-stderr split Darwin uses. | S2 | This demonstrates a backward-compatible route from a good human shell protocol to a machine protocol. |
| Codex | App Server exposes the harness through a bidirectional JSON-RPC boundary. | S2 | A stable protocol can support more clients than the TUI, but a daemon/API is a much larger product and security commitment than structured one-shot output. |
| DeepSeek Harness | One append-only event stream underlies trajectory inspection, resume, fork, search and replay. | S3 | Darwin now has close parity through its trajectory observer; this is evidence to reuse that event vocabulary rather than build an unrelated automation log. |
| PenguinHarness | One engine serves CLI, SDK and a headless service, while trace and evaluation are first-class views. | S4 | Programmatic control and evaluation are central to self-evolution, but the benchmark claim is not independent evidence and Darwin's evaluation direction was explicitly closed by the user. |
| Cursor | Headless text/JSON/streaming-JSON modes converge with Claude Code and Codex; checkpoints and diff review protect interactive code changes. | S5 | Structured automation has evidence from three independent peers. Checkpointing is valuable but has a different and much riskier ownership problem in a shared working tree. |

### Current Darwin baseline

| Capability / architecture | Repository evidence | Current behavior |
|---|---|---|
| Human headless protocol | [`runHeadless`](../../src/cli.ts) and [`runHeadlessTurn`](../../src/headless.ts) | `-p/--print` streams bounded progress records to stderr and writes only the complete final assistant reply plus one newline to stdout after turn, snapshot, pointer and cleanup success. Failure and usage remain human-readable stderr records. |
| Headless CLI surface | [`CliOptions` / `parseCliArgs`](../../src/cli-args.ts), [`verify-headless.ts`](../../spike/verify-headless.ts) | The only output mode is the implicit text protocol; no JSON or JSONL option exists. Session selection and permission override are machine-usable, but their results must be parsed from text. |
| Single stream seam | [`AgentRuntime.send`](../../src/agent/runtime.ts) | TUI, headless and REPL all consume the same SDK stream through the trajectory observer, so structured headless projection can observe events without constructing or forking another `Agent`. |
| Safe event serialization knowledge | [`recordStream`](../../src/trajectory/stream.ts), [`TrajectoryRecorder`](../../src/trajectory/writer.ts), [SDK serialization contracts](../../.trellis/spec/backend/strands-sdk-contracts.md#observing-the-stream-what-darwin-measured-to-record-it) | Darwin already proved SDK `toJSON()` excludes live agent state, rehydrates wire content for replay, strips reasoning text, caps fields/records, and preserves event identity/order. That policy is persistence-specific but prevents a new output format from guessing at raw SDK objects. |
| Offline session inspection | [`cli-trajectory.ts`](../../src/cli-trajectory.ts), [session trajectory spec](../../.trellis/spec/backend/session-trajectory.md) | `trajectory list/search/replay/fork` provide append-only, no-model inspection and already cover the DeepSeek evidence. They are post-run file commands, not a live stdout protocol for a supervising process. |
| Parallel child visibility | [`SubagentTool`](../../src/agents/subagent-tool.ts), [`DispatchRegistry`](../../src/agents/dispatch-registry.ts), `/agents` contract in [`tui-testing.md`](../../.trellis/spec/frontend/tui-testing.md#subagent-dispatch-monitoring-and-source-labelled-approvals) | Read-heavy parallel dispatch, source-labelled approvals and bounded status are shipped (`SER-002`); only the final child report enters parent context. Peer subagent findings are not a new gap. |
| Self-evolution measurement | [`SER-005`](./backlog_index.md), [`docs/iteration-log.md`](../iteration-log.md) | Real acceptance and spend records exist, but a stable evaluation corpus was explicitly abandoned by user decision. PenguinHarness does not justify silently reopening it. |
| Filesystem rollback | `src/agent/runtime.ts`, `src/agent/session.ts`, `src/trajectory/` (no checkpoint manager); git is used only by repository workflows/tests | Conversation snapshots and trajectory forks do not snapshot the working tree. A general rewind would have to distinguish agent changes from concurrent user/tool changes, including mutations made through arbitrary shell commands. |

### Comparison and gaps

| Peer evidence | Darwin evidence | Parity / gap / deliberate difference | Implication |
|---|---|---|---|
| Claude, Codex and Cursor all expose structured headless output (S1, S2, S5) | `parseCliArgs` has no output option; `runHeadless` emits only the documented text split | **High-confidence gap** at an existing driver seam | Add opt-in final JSON and live JSONL while preserving the default byte-for-byte. Supervisors should parse versioned objects, not `session:`/`usage:` prose. |
| Codex's JSONL includes lifecycle, item and failure events (S2) | `runtime.send` already yields typed SDK events; trajectory already projects failure/spend and knows how not to serialize reasoning | Partial internal capability, no public live protocol | Reuse tested event semantics and sanitization rules, but keep persistence records and public stdout envelopes distinct and versioned. |
| DeepSeek trajectory operations (S3) | `trajectory list/search/replay/fork` plus append-only record | **Parity**, delivered by `SER-003`, `SER-006` and `SER-007` | No new direction. A live protocol complements rather than replaces the durable record. |
| Codex App Server and Penguin headless service/SDK (S2, S4) | `AgentRuntime` is process-local and only `cli.ts` constructs a user-facing driver | Real gap, but deliberate current scope | A daemon introduces authentication, concurrent-client ownership, permission transport, lifecycle and compatibility commitments. Score it separately; do not smuggle it into structured one-shot output. |
| Cursor/Claude checkpoints (S1, S5) | session persistence excludes filesystem state; bash can mutate outside fileEditor and users share the same tree | Valuable but unsafe to infer | Do not implement rewind until ownership and conflict semantics are explicit; restoring a stale copy over user work is worse than having no checkpoint. |
| Penguin benchmark-driven optimization (S4) | `SER-005` was abandoned by explicit user decision | Known, deliberately closed gap | Record the peer update, but do not re-propose a duplicate or reinterpret the user's decision. |

### Ranked iteration directions

`Score = 2 × Importance + Architecture fit + Evidence confidence − Implementation difficulty − Implementation risk`.

| Rank | Proposed ID | Direction | Importance | Architecture fit | Evidence confidence | Implementation difficulty | Implementation risk | Score | Evidence and rationale |
|---:|---|---|---:|---:|---:|---:|---:|---:|---|
| 1 | SER-011 | Add opt-in structured headless output: one final JSON envelope or a real-time JSONL event stream, while preserving the existing text protocol byte-for-byte | 5 | 5 | 5 | 3 | 3 | **14** | S1/S2/S5 independently converge on text + JSON + streaming JSON. Darwin already has one headless driver, typed stream events, session/usage/outcome data, and a measured safe serialization seam. The public schema must be explicitly versioned, omit reasoning text, represent failures and cancellation structurally, and never change default stdout/stderr or cleanup/resume semantics. This is one coherent protocol direction rather than a daemon. |

### Gated-out directions

| Direction | Score | Gate | Why it fell below | Kept anyway? |
|---|---:|---:|---|---|
| Add a long-lived local app server / SDK control plane like Codex, PenguinHarness or OpenCode (Importance 3, fit 2, evidence 5, difficulty 5, risk 4) | 4 | 6 | Authentication, concurrent clients, remote permission decisions, process ownership and stable API support multiply the scope; structured one-shot output captures the demonstrated automation need first | `no` |
| Add automatic filesystem checkpoints and rewind (Importance 4, fit 2, evidence 5, difficulty 5, risk 5) | 5 | 6 | Darwin cannot attribute arbitrary bash mutations or concurrent user edits. Without an explicit ownership/conflict design, rewind can destroy work; the safety feature would create a correctness hazard | `no` |

No rating was restated and no below-gate exception is used. The PenguinHarness evaluation loop was checked as a backlog duplicate closed by explicit decision, not assigned a new ID.

### Recommendation

- Batch to implement: `SER-011`
- Start with: `SER-011`
- Why this order: it is the only accepted direction. Three peers independently support the need, and it extends the existing headless/event seams without committing Darwin to a server or a second agent loop.
- Independently observable acceptance: the default `-p` success, failure, interrupt, stderr progress and final stdout remain byte-for-byte compatible in existing tests; invalid output modes and use without `-p` fail before runtime construction; final JSON is exactly one parseable versioned document with session, outcome, result and usage fields; streaming JSON is one object per line, emits at least one live event before turn completion, includes typed tool/lifecycle/terminal failure or success records, and never emits model reasoning text; cancellation stays nonzero and cleanup/pointer durability still gates success; focused scripted-model tests, `pnpm typecheck`, `pnpm test`, `git diff --check`, Trellis validation, and one real provider smoke test pass.
- Backlog updates: add `SER-011` as `not-started` at priority 11; no existing row changes status and no row is abandoned by the gate. Change only `SER-011` to `in-progress` immediately before developer handoff.

### Batch iteration outcome

| Direction | Child session and managed tasks | Host acceptance (exact independent checks and outcomes) | Commit | Final status | Iteration-log entry |
|---|---|---|---|---|---|
| `SER-011` | child session `session-20260817-011454108`; first planning launch `bg-5927f1f9-1bdf-47b7-8bc0-11ca426a87a5` failed before session/model/usage because the Host passed an extra `--`; planning `bg-1469d76f-3003-4935-8e70-2b4dbbe7c790` succeeded; implementation `bg-47edad5c-778c-41b0-b263-efa8c5f3e12d` succeeded; focused correction `bg-62b735bb-87de-4986-b287-67322e70c6f8` succeeded. | Host inspected both commits and full implementation/correction diffs. Independent checks passed: `pnpm typecheck`; all 30 fast suites; focused headless 68/0, structured headless 8/0, trajectory 257/0, max-token recovery 20/0; `pnpm build`; Trellis validation (only known large-spec injection warnings); `git diff --check`; both commit checks; explicit `process.cwd()` location check; Host-owned-path and clean-tree checks. The child ran exactly two authorized low-token real Bedrock smokes with Haiku 4.5, low effort, 128 max tokens and caching off: JSON produced one sequence-1 success (`OK`, empty stderr); stream-json produced five parseable monotonic records ending in one success (`STREAM-OK`, empty stderr). Host confirmed the smoke evidence in the child trajectory. | `58a20b5` + `ebd4796` | `done` | [`Batch 13 — structured headless output`](../iteration-log.md#batch-13--structured-headless-output-2026-08-17) |

- Halt condition that ended the loop: **batch exhausted** — `SER-011` is `done` after independent acceptance, and it was the only accepted direction in this batch.
- Aggregate token spend across every child delegation: `input=450 output=145,216 cacheRead=83,351,198 cacheWrite=856,876`. The failed first launch emitted no usage line and spent no recorded model tokens. Per task: planning `input=66 output=86,605 cacheRead=5,691,673 cacheWrite=335,535`; implementation `input=338 output=53,844 cacheRead=66,285,504 cacheWrite=476,107`; correction `input=46 output=4,767 cacheRead=11,374,021 cacheWrite=45,234`.
- Remaining backlog after this run: nothing unfinished — `SER-001`–`SER-003` and `SER-006`–`SER-011` are `done`; `SER-004`/`SER-005` remain explicitly `abandoned`. Fresh research is appropriate on the next invocation.
