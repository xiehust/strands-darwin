# Darwin self-evolution research — 2026-09-03

## Run — 2026-09-03T14:57:47Z

### Backlog-first decision

- Backlog router inspected first: `docs/research/backlog_index.md`
- Metadata searched across routed priority pages: `docs/research/backlog/directions-001-020.md`, `docs/research/backlog/directions-021-040.md`, `docs/research/backlog/directions-041-060.md`, `docs/research/backlog/directions-061-080.md`, `docs/research/backlog/directions-081-100.md`
- Unfinished directions found: none (`grep -hoE 'Status: \`[a-z-]+\`' docs/research/backlog/directions-*.md | sort | uniq -c` → 79 `done`, 2 `abandoned`; no `in-progress`, no `not-started`). Highest existing IDs: `SER-060` (Priority 81, the only record on the current page), `SRF-021`.
- Decision: fresh research

### Research path

The user directed this run's topic ("研究下 claude code agent teams 的功能" — research Claude Code's agent-teams feature), which is peer-product research, so the script was run once with `--path peer` before any repository or product source was opened. Output verbatim:

```text
research-path: peer
focus: comparable coding-agent product analysis
share: 50% (weight 5 of 10)
draw: none — path was not rolled
path-source: override (user-directed)
rolled-at: 2026-09-03T14:50:09.488Z
weights: tui=2 observability=0.5 sdk=1 open=1.5 peer=5
```

This path came from a user override, not from chance. The script was run exactly once.

### Source availability and method

Accessed 2026-09-03 (UTC) at HEAD `eafb240`. Tools: the runtime's `web_fetch` for first contact with the Claude Code agent-teams page, then `curl` (Markdown editions and raw repository files saved under `/tmp/peer` and read with `sed`/`grep`) for every page whose projection was too large to keep in context; the `strands-agents` docs MCP for one SDK lookup (no page on background tasks was found there, so the SDK claims below cite the installed `node_modules/@strands-agents/sdk@1.16.0` `.d.ts`/`.js` files directly). No `web-search`. All five required scopes were reachable. Repository evidence was read first (`src/agents/*.ts`, `src/agent/runtime.ts`, `docs/architecture/load-bearing-decisions.md` §§ Subagents / Workflow DAG tool, `spike/verify-subagents.ts`, `spike/verify-workflow-tool.ts`), then peer sources.

**Working-tree caveat.** The repository was not clean at invocation: `git status --short | wc -l` = 833 — `.trellis/`, `.darwin/`, `.agents/`, `.claude/`, `.codex/` are renamed to `*_bak/` (untracked), the `<!-- TRELLIS:START -->` block is removed from `AGENTS.md`/`CLAUDE.md`, and `.darwin/skills/commit-message` is moved to `src/skills/builtin/commit-message/`. None of it is this run's work. Research and backlog persistence do not depend on the tree; the developer loop does (section 5 step 1), so see the batch outcome below.

### Reference sources

| Ref | Product | Source type | URL | Accessed (UTC) | Claims supported |
|---|---|---|---|---|---|
| S1 | Claude Code | Primary docs (`agent-teams.md`, "as of v2.1.178") | <https://code.claude.com/docs/en/agent-teams> | 2026-09-03 | Experimental, off by default (`CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`); one lead + teammates that are "separate Claude Code instances", each with its own context window; a shared task list (pending / in progress / completed, dependencies block claiming, "file locking to prevent race conditions", lead-assigns or self-claim); a per-agent JSON mailbox under `~/.claude/teams/{team}/inboxes/`; teammates message each other by name, idle notifications carry the final answer, a failed teammate reports the API error; display modes in-process (agent panel, arrows/Enter to view, `x` to stop, Ctrl+T task list) or tmux/iTerm2 split panes; teammates inherit the lead's permission settings and prompts "appear in the lead session"; a message from another agent is framed as "from another Claude session, not from you" and "can't approve a permission prompt or supply consent on your behalf"; `TeammateIdle`/`TaskCreated`/`TaskCompleted` hooks exit 2 to block and feed back; teammate spawned in plan mode plans first, plan auto-approved; "significantly more tokens"; limitations: no `/resume`/`/rewind` of in-process teammates, task status can lag, one team per session, no nested teams, lead fixed, permissions set at spawn; best practice "Two teammates editing the same file leads to overwrites. Break the work so each teammate owns a different set of files" and "Start with 3-5 teammates" |
| S1b | Claude Code | Primary docs (`sub-agents.md`) | <https://code.claude.com/docs/en/sub-agents> | 2026-09-03 | Background subagents run "concurrently while you continue working"; permission prompts "surface … in your main session and name the subagent that is asking"; results "reach Claude as a completion notification in a later turn"; Ctrl+B backgrounds a running task; "Concurrent subagent limit": default 20 running, spawning another "fails with `Concurrent subagent limit reached`, and the error tells Claude not to retry", `CLAUDE_CODE_MAX_CONCURRENT_SUBAGENTS`; "API errors in subagents" (v2.1.199): a foreground subagent cut off after producing text returns "that partial output with a note that the subagent was cut off"; "Subagent output scanning" (v2.1.210): backslash inserted into text imitating `<system-reminder>` or lines starting `Human:`/`Assistant:`, plus a `[harness: subagent output matched instruction-shaped pattern(s):` marker line when permission settings such as `bypassPermissions` are mentioned — "never removes or rewords anything"; named subagents are addressable for `SendMessage`/resume |
| S1c | Claude Code | Primary docs (`cross-session-messaging.md`) | <https://code.claude.com/docs/en/cross-session-messaging> | 2026-09-03 | `ListAgents` + `SendMessage` between the user's own sessions; "A message is a piece of text one Claude writes to another, never the sender's conversation history or files" |
| S2 | Codex | Primary docs (`multi-agent.md`) | <https://developers.openai.com/codex/multi-agent> | 2026-09-03 | Subagent workflows spawn agents in parallel and "wait until all requested results are available, then return a consolidated response"; `/agent` in the CLI switches between active threads; users can "ask Codex directly to steer a running subagent, stop it, or close completed agent threads"; approvals from inactive threads surface with "the source thread label", `o` opens the thread; `agents.max_concurrent_threads_per_session` caps open spawned threads; `agents.interrupt_message` records a model-visible message on interruption; custom agents can set `sandbox_mode = "read-only"`; "use parallel agents for read-heavy tasks such as exploration, tests, triage" |
| S3 | DeepSeek harness | Source repository docs (`docs/architecture.md`, `docs/subsystems/agent-team.md`, `docs/subsystems/subagent.md` at `master`) | <https://github.com/deepseek-ai/deepseek-harness/blob/master/docs/subsystems/agent-team.md> | 2026-09-03 | "Experimental Agent Teams is a private opt-in coordination seam on `ctx.agentTeams`, with a durable roster, task board, and mailbox layered over continuable subagents"; `TeamTaskSnapshot` has `revision` (compare-and-set), `blockedBy` (acyclic), and `writeScopes` — "normalized advisory path prefixes rather than locks", with views adding "write-scope overlap warnings"; mailbox is durable ("queued-minus-delivered as the recovery mailbox"), delivery steers a running target "at the nearest step boundary", an idle target starts a turn; `waitForChange` is bounded 10 s–1 h; `interrupt` stops one teammate turn without clearing its inbox; continuable children have "at most one process-local Activation" and `sendMessage` "permits only a direct parent or direct continuable child" |
| S4 | PenguinHarness | Source repository README at `main` | <https://github.com/Prism-Shadow/penguin-harness/blob/main/README.md> | 2026-09-03 | Positions itself as a "local-first multi-agent app development platform"; the README documents no subagent, team, mailbox or task-board feature of its coding agent — no claim is made about one |
| S5 | Gemini CLI | Source repository docs (`docs/core/subagents.md` at `main`) | <https://github.com/google-gemini/gemini-cli/blob/main/docs/core/subagents.md> | 2026-09-03 | Subagents have "Independent history", "Isolated tools", and "Recursion protection: … subagents **cannot** call other subagents" even under the `*` tool wildcard; `/agents` manages definitions interactively; no team, messaging or shared-task feature documented |
| S6 | Strands TypeScript SDK 1.16.0 (installed) | Installed package source (`dist/src/background-tasks/*.d.ts`/`.js`, `dist/src/agent/agent.d.ts`, `dist/src/tools/executors/executor.js`, `dist/src/multiagent/swarm.d.ts`) | `node_modules/@strands-agents/sdk/` | 2026-09-03 | `Agent` accepts `backgroundTasks?: boolean \| BackgroundTasksConfig` (`waitForCompletion` default `true`, `agentic`/`always`/`never` tool lists, `maxConcurrency` default 4, `timeout`); the plugin adds `_background_execution` to eligible tool specs, returns an immediate task-id ack, delivers finished results as a synthetic `strands_background_task_result` tool-use/result pair before the next model call (`_deliverReady`), and registers `strands_manage_background_task` (`list`/`get`/`cancel`); `executor.js` yields `BeforeToolCallEvent` and honours `cancel` **before** `routeToolCall`, so hook-time denial precedes background dispatch; `Swarm` is sequential structured-output handoff ("Agents execute sequentially") — not a concurrent team primitive |

### Peer highlights and innovations

| Product | Sourced feature / innovation | Reference | Why it matters |
|---|---|---|---|
| Claude Code | Agent teams: lead + independent teammate sessions, shared dependency-aware task list with locked claiming, per-agent mailbox, direct user↔teammate talk, quality-gate hooks | S1 | The full "team" model. Anthropic itself scopes it as experimental, token-heavy, and best for "research and review" and "competing hypotheses"; same-file edits are named as the failure mode |
| Claude Code | Messages between agents are framed as not-from-the-user and cannot carry consent; a denied teammate cannot relay to bypass | S1 | Permission provenance is a security boundary, not just a label |
| Claude Code | Background subagents: lead keeps working, result arrives as a later completion notification, prompts still surface in the main session naming the asker | S1b | Parallelism without blocking the lead's own turn |
| Claude Code | Concurrent subagent limit (20) with a "do not retry" error | S1b | A cost/safety ceiling on fan-out that the model is told about |
| Claude Code | Subagent output scanning — backslash-escape imitation of harness framing, marker line for permission vocabulary, never rewrites | S1b | Prompt-injection hardening at the one seam where untrusted child output enters the lead's context |
| Claude Code | Partial output returned with a cut-off note when a subagent dies on an API error | S1b | Failed delegation keeps its evidence instead of only an error string |
| Codex | Steer/stop a running subagent; `/agent` thread switching; approval overlay names the source thread; `max_concurrent_threads_per_session`; `interrupt_message` | S2 | Same three needs — visibility, provenance, a fan-out cap — met with lighter machinery |
| DeepSeek harness | Task board with compare-and-set `revision`, acyclic `blockedBy`, and advisory `writeScopes` with overlap warnings; durable recoverable mailbox; bounded `waitForChange` | S3 | The most precise published design for coordinating concurrent writers: declared path prefixes, warned not locked |
| Gemini CLI | Recursion protection: subagents cannot call subagents even under `*` | S5 | Parity with darwin's `PARENT_ONLY_TOOL_NAMES` decision |
| PenguinHarness | (inference: none) | S4 | README documents no team/subagent feature; no claim |

### Current Darwin baseline

| Capability / architecture | Repository evidence | Current behavior |
|---|---|---|
| Delegation | `src/agents/subagent-tool.ts` (`SubagentTool`), `src/agents/child-recipe.ts` (`buildRecipeChild`) | One transient child per `subagent` call; `child.invoke()` consumed privately, "only its final report" returned; the child never gets a `SessionManager`, parent messages or a delegation tool; bash session reaped in `finally` (`stopBashSession`) |
| Parallelism | class doc in `subagent-tool.ts`; `docs/architecture/load-bearing-decisions.md` § Subagents | SDK `ConcurrentToolExecutor` races same-message calls (measured in `spike/verify-subagents.ts`); scoped to **reads** — "nothing here makes concurrent write delegation safe" |
| Fan-out bound | `SubagentTool.dispatch`/`run`; `src/agents/workflow-tool.ts` (`MAX_WORKFLOW_NODES = 8`) | `workflow` caps nodes at 8; `subagent` has **no** concurrent-dispatch cap — N calls in one assistant message build N models and N children; `SubagentDispatchRegistry` already knows every `running` record |
| DAG orchestration | `src/agents/workflow-tool.ts`, load-bearing § Workflow DAG tool | Data-only DAG executed by SDK `Graph`; "parallel branches are for READS ONLY; serialize writes by edges" is stated in the tool description — not checked |
| Visibility / control | `src/agents/dispatch-registry.ts`, `src/tui/subagent-format.ts`, `App.tsx` `/agents` handler | `/agents` lists dispatches (name, phase, state, usage); `/agents cancel <id>` cancels one child; ≤30 s heartbeats; no messaging, steering or resume of a child |
| Permission provenance | `SubagentDispatchSource`, `src/agent/permission.ts` | Every prompt carries `[parent]` or `[<agent>#<dispatch>]`; children share the parent gate; a child cannot approve anything |
| Nesting | `src/agent/runtime.ts` `PARENT_ONLY_TOOL_NAMES` (`subagent`, `workflow`, `update_plan`, `http_request`, memory tools) | Children never orchestrate — parity with S5 and S1 "no nested teams" |
| Child report on failure | `SubagentTool.run` catch → `dispatch.finish('failed')` → rethrow; `withRetainedMaxTokensText` covers only the max-tokens path | A child whose stream throws returns only the error; assistant text it already produced is lost |
| Child report integrity | `SubagentTool.run` returns `withRetainedMaxTokensText(result.toString(), …)` verbatim; `WorkflowTool` returns terminus content | No scan of child output for instruction-shaped framing before it enters the parent's context |
| Background execution | `runtime.ts` constructs `Agent` without `backgroundTasks`; `grep backgroundTasks src` matches only the unrelated `BackgroundBashManager` | Every tool call, including `subagent`/`workflow`, is foreground: the parent cannot issue its next tool call until the whole message's results return |
| Lifecycle hooks | load-bearing § Native lifecycle command hooks (`TurnComplete` + `PermissionRequest`, "non-blocking and output-free; never model/permission/tool/trajectory/SDK-loop input") | A blocking quality-gate hook in the Claude Code `TeammateIdle`/`TaskCompleted` sense is a recorded non-goal |

### Comparison and gaps

| Peer evidence | Darwin evidence | Parity / gap / deliberate difference | Implication |
|---|---|---|---|
| Teams: persistent teammates, mailbox, shared task board (S1, S3) | Transient children, no mailbox, `update_plan` is parent-only, `workflow` is a static DAG run by SDK `Graph`; SDK `Swarm` is sequential (S6) | **Gap, but a deliberate architecture difference**: a roster/mailbox/task board would be a darwin-owned scheduler, which the Workflow decision explicitly avoided ("never a darwin scheduler"), and the SDK has no team primitive to reuse | Not queued — see gated table. Revisit if the SDK ships a team/mailbox primitive |
| Concurrent subagent limit with "do not retry" error (S1b), `max_concurrent_threads_per_session` (S2), SDK `maxConcurrency` default 4 (S6) | No cap on `subagent` dispatches | **Gap** | SER-061 |
| Output scanning of child reports (S1b) | Reports returned verbatim | **Gap** | SER-062 |
| Partial output with cut-off note on API error (S1b); failed teammate reports error text (S1) | Error only, partial text lost | **Gap** | SER-063 |
| Background subagents; lead keeps working; prompts still surface naming the asker (S1b); Codex background panel (S2) | All foreground; the SDK already ships `backgroundTasks` with hook-before-route ordering (S6) | **Gap with an SDK-native fix** | SER-064 |
| "Each teammate owns a different set of files" (S1); `writeScopes` advisory prefixes with overlap warnings (S3) | Reads-parallel/writes-serialized stated in prose only; same-file races measured (load-bearing § Same-path fileEditor ordering) | **Gap** | SER-065 |
| Permission prompts in the lead session name the asker; agent messages cannot carry consent (S1, S2) | `[<agent>#<dispatch>]` on every prompt; children cannot approve | **Parity** | none |
| No nested teams / recursion protection (S1, S5) | `PARENT_ONLY_TOOL_NAMES` | **Parity** | none |
| Steer/stop a running subagent (S2), interrupt one teammate (S3) | `/agents cancel <id>` only | Partial parity (stop yes, steer no) | Steering needs continuable children — deferred with the teams row |
| `TeammateIdle`/`TaskCompleted` blocking hooks (S1) | Native hooks are non-blocking and never model input | **Deliberate difference** — recorded contract | none |

### Ranked iteration directions

`Score = 2 × Importance + Architecture fit + Evidence confidence − Implementation difficulty − Implementation risk`

| Rank | Proposed ID | Direction | Importance | Architecture fit | Evidence confidence | Implementation difficulty | Implementation risk | Score | Evidence and rationale |
|---:|---|---|---:|---:|---:|---:|---:|---:|---|
| 1 | SER-061 | Bound concurrent delegation: a configurable `maxConcurrentSubagents` cap on running dispatches; an over-cap `subagent` call or a `workflow` whose nodes cannot fit is one bounded tool error before any model/child exists, telling the model not to retry | 3 | 5 | 5 | 2 | 1 | 13 | S1b (limit 20, "tells Claude not to retry"), S2 (`max_concurrent_threads_per_session`), S6 (`maxConcurrency` 4). Darwin: `SubagentDispatchRegistry` already tracks `running`; `AppConfig` has a validated schema (`spike/verify-config.ts`). Prerequisite for SER-064: background fan-out without a ceiling is a cost hazard |
| 2 | SER-062 | Harden child reports: one pure projection over every `subagent`/`workflow` report before it becomes the parent tool result — backslash-escape lines imitating darwin's own framing (`<project-instructions>`, `<working-context>`, `<available_skills>`, `<system-reminder>`, `Human:`/`Assistant:` line starts) and prepend one bounded marker line when a pattern or permission-bypass vocabulary matched; never remove or reword | 3 | 4 | 4 | 2 | 2 | 10 | S1b output scanning. Darwin: `SubagentTool.run` and `WorkflowTool` terminus return text verbatim; children read web/MCP/file content the user never saw (`web_fetch`, `http_request` are parent-only, but bash/`web-search` MCP are not). Precedent: offload/memory marker-spoof resistance |
| 3 | SER-063 | Keep a failed child's evidence: when a child's `invoke()` throws after producing assistant text, the tool **error** content carries the bounded last assistant text plus a fixed cut-off note; dispatch still settles `failed`, retry guard still sees a failure, trajectory keeps the error | 3 | 4 | 4 | 2 | 2 | 10 | S1b "API errors in subagents", S1 failed-teammate notification. Darwin: `SubagentTool.run` rethrows with nothing retained; `withRetainedMaxTokensText`/`RETAINED_PARTIALS` shows the retained-partial pattern already exists for max-tokens |
| 4 | SER-064 | Background delegation through the SDK: construct the parent `Agent` with `backgroundTasks: { agentic: ['subagent','workflow'], never: [everything else], waitForCompletion: true }` so the model may mark a delegation `_background_execution` and keep working; results arrive via the SDK's `strands_background_task_result` pair; gate/retry-guard ordering unchanged (hooks fire before `routeToolCall`); TUI/trajectory render the ack and the delivered result as ordinary tool rows; children never get the option | 4 | 5 | 4 | 4 | 3 | 10 | S1b background subagents, S2 background panel, S6 SDK plugin verified in `executor.js:79-84` and `background-tasks.js`. Darwin: only `runtime.ts` constructs `Agent` (load-bearing § SDK reuse) — this is exactly the "customize via SDK extension points" case. Difficulty: `turn-state.ts` reducer, `formatReplay`, `/rewind` `assertCanLoadSnapshot`, cancel semantics all need proving. Depends on SER-061 |
| 5 | SER-065 | Declared write scopes for `workflow` nodes: optional `writeScopes` (normalized relative path prefixes) per node; a DAG where two nodes with overlapping scopes are not ordered by an edge path is refused before any child exists; a scoped node's `fileEditor` `create`/`str_replace`/`insert` outside its scopes is denied by the existing gate via dispatch `source` (deny, never a new gate); unscoped nodes unchanged; bash is stated as out of scope | 3 | 4 | 4 | 3 | 2 | 9 | S3 `writeScopes` "advisory path prefixes rather than locks" + overlap warnings, S1 "each teammate owns a different set of files". Darwin: `workflow` already refuses invalid DAGs before dispatch; the gate already resolves `source` per child; the same-path race is measured in load-bearing § Same-path fileEditor ordering |

### Gated-out directions

| Direction | Score | Gate | Why it fell below | Kept anyway? |
|---|---:|---:|---|---|
| Agent teams proper: continuable teammates with a mailbox, roster and shared claimable task board (S1, S3) | 6 (I 4, F 2, E 5, D 5, R 4) | 6 | Not below the gate — excluded by a **documented architecture concern**: it would be a darwin-owned scheduler over children the recipe makes transient by design, and SDK 1.16.0 offers no team primitive (`Swarm` is sequential handoff, `Graph` a static DAG). Claude Code itself lists it experimental with no resume, lagging task status and one team per session (S1) | `no` — re-rate only when the SDK ships a team/mailbox primitive or the Workflow decision is revisited by the user |
| Continuable children: resume/message a finished or running dispatch by id (S1b resume subagents, S2 steer, S3 continuable Activation) | 9 (I 4, F 3, E 5, D 4, R 3) | 6 | Above the gate; **not queued this run** because the batch is capped at five and its natural shape depends on SER-064 (a background child is the one worth talking to). Retaining child Agents/bash sessions past settlement also inverts `stopBashSession`-in-`finally` | `no` — candidate for the next run after SER-064 lands |
| Blocking quality-gate hooks (`TeammateIdle`/`TaskCompleted`, S1) | 3 (I 2, F 1, E 4, D 3, R 3) | 6 | Contradicts the recorded native-hook contract (non-blocking, output-free, never model input) | `no` |
| Split-pane teammate display via tmux (S1) | 2 (I 1, F 1, E 4, D 4, R 3) | 6 | Requires a second terminal multiplexer surface outside the Ink frame budget; Anthropic's own default moved back to in-process | `no` |

### Recommendation

- Batch to implement: `SER-061`, `SER-062`, `SER-063`, `SER-064`, `SER-065`
- Start with: `SER-061`
- Why this order: SER-061 is the cheapest and is the safety prerequisite for SER-064 (background fan-out must have a ceiling first). SER-062 and SER-063 are small, independent projections at the child-report seam and should land before SER-064 so background-delivered reports inherit both. SER-064 is the largest change and the only one touching `Agent` construction. SER-065 is independent and last.
- Independently observable acceptance:
  - SER-061 — a `pnpm test` suite (extend `spike/verify-subagents.ts` offline cases or a sibling) dispatches cap+1 concurrent `subagent` calls against a fake model and proves the (cap+1)th returns the bounded refusal with **no** model construction and no dispatch record, a `workflow` over the remaining slots is refused before any child exists, and settlement frees the slot; `verify-config.ts` proves the new field's validation (`maxConcurrentSubagents` positive integer, default stated) and a bad value refuses to start; `/status` shows the cap only if it is a pre-existing accessor shape; `pnpm typecheck`, `pnpm test`, `pnpm build`.
  - SER-062 — a `pnpm test` suite feeds reports containing `<project-instructions>`, `Human:` lines and `alwaysAllow`/`--dangerously` vocabulary through the projection and asserts backslash insertion, one marker line, byte-identity for clean reports, and that ANSI-free replay shows the projected text; `verify-workflow-tool.ts` proves the terminus path is covered.
  - SER-063 — a `pnpm test` case where a fake child model emits text then throws proves the tool error content contains the bounded text plus the fixed note, the dispatch is `failed`, and `verify-retry-guard.ts` still counts it; a child that produced nothing yields the unchanged error.
  - SER-064 — offline: a fake-model parent marks a `subagent` call `_background_execution: true`, receives the ack, issues another tool call before the child settles, and receives the `strands_background_task_result` before its next model call; the permission gate is shown to deny a background-marked call before dispatch (plan mode); `verify-trajectory.ts` replay renders ack and result rows; `/rewind` on a session with settled background tasks works; `spike/verify-tui.ts updatePlan`/`queue` green; children's catalogues carry neither `strands_manage_background_task` nor the flag. Live: `spike/verify-subagents.ts` against Bedrock shows a parent that keeps working while a child runs.
  - SER-065 — `verify-workflow-tool.ts` proves overlapping unordered scopes are refused before dispatch, ordered overlap is accepted, and an in-scope/out-of-scope `fileEditor` write from a scoped node is allowed/denied by the existing gate with the dispatch `source`; `verify-permissions-command.ts` unchanged.
- Backlog updates: `docs/research/backlog/directions-081-100.md` gains `SER-061`…`SER-065` as `not-started` (Priorities 82–86; the page has room through 100, so no rollover). No record set `abandoned`. The first section flips to `in-progress` only immediately before developer handoff.

### Batch iteration outcome

| Direction | Child session and managed tasks | Host acceptance (exact independent checks and outcomes) | Commit | Final status | Iteration-log entry |
|---|---|---|---|---|---|
| — (first attempt, 14:57Z) | none delegated | Section 5 step 1 failed before any handoff: `git status --short \| wc -l` = 833 at invocation (see caveat above). Reported to the user, who then committed the migration as `2da5a27` ("remove trellis prompt") and directed: proceed, and do not use Trellis skills | — | — | — |
| (Host repair) | none | At `2da5a27` `pnpm test` was red: `2da5a27` moved `commit-message` into `src/skills/builtin/` but left the byte-identical `.darwin/skills/commit-message` copy (loader: `reserved by built-in`) and `verify-skills.ts` still pinned the old location and three built-ins; the paged-backlog count also needed 81→86 for this run's records (my omission in `b51c459`). Fixed in `43caa8c` (test + shadowed copy removed + AGENTS.md/CLAUDE.md sample-skill path); gate at `43caa8c`: typecheck exit 0, full `pnpm test` 0 failed | `43caa8c` | — | — |
| SER-061 | `session-20260903-152606204`; `bg-87431762-d703-49e8-96ca-ede02bf99b13` (exit 0, no correction); `usage: input=182 output=39359 cacheRead=9486200 cacheWrite=155673` | Diff read (15 files; `runtime.ts` untouched; check before `begin()`/`createModel`); `pnpm typecheck` exit 0; full `pnpm test` (pipefail) exit 0, 88 suites, 4421 passed, 0 failed; `spike/verify-subagent-limit.ts` 27/0; `pnpm build` exit 0; `git show --check` clean; AGENTS.md 31,782 B | `1ae3556` | `done` | [`Batch 86`](../iteration-log.md) |

- Halt condition that ended the loop: (loop continuing — rows appended as directions close)
- Aggregate token spend across every child delegation: `input=182 output=39,359 cacheRead=9,486,200 cacheWrite=155,673` (one child so far).
- Remaining backlog after SER-061: `SER-062`, `SER-063`, `SER-064`, `SER-065` — `not-started`, sharing this run as origin report.
