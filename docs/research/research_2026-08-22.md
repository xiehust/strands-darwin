# Darwin self-evolution research — 2026-08-22

## Run — 2026-08-22T03:02:03Z

### Backlog-first decision

- Backlog inspected first: `docs/research/backlog_index.md`
- Unfinished directions found: none
- Decision: fresh research after explicit user rejection and revert of the prior `search_memory` design (`076d9dd`)

### Research path

```text
research-path: peer
focus: comparable coding-agent product analysis
share: 50% (weight 5 of 10)
draw: none — path was not rolled
path-source: override (user-directed)
rolled-at: 2026-08-22T03:02:03.862Z
weights: tui=2 observability=0.5 sdk=1 open=1.5 peer=5
```

### Source availability and method

Web search was available on 2026-08-22 UTC. Primary product documentation, changelogs and source repositories were preferred. Claude Code, Codex, DeepSeek Harness, Cursor, Windsurf and GitHub Copilot had verifiable primary sources. PenguinHarness had only its public GitHub repository discoverable; search returned no primary documentation or source excerpt establishing a long-term-memory design, so no memory claim is made for it. The prior rejected Darwin implementation was inspected before the peer survey and reverted in commit `076d9dd`; its research artifact was intentionally removed with that product decision rather than reused as evidence.

### Reference sources

| Ref | Product | Source type | URL | Accessed (UTC) | Claims supported |
|---|---|---|---|---|---|
| S1 | Claude Code | Primary docs | <https://docs.anthropic.com/en/docs/claude-code/claude-md> | 2026-08-22 | Two complementary systems: user-authored `CLAUDE.md` instructions and agent-authored auto memory; auto memory learns from corrections and can be maintained by subagents. |
| S2 | Claude Code | Primary localized docs | <https://code.claude.com/docs/es/memory> | 2026-08-22 | Both instruction and auto-memory context are loaded at conversation start and treated as context, not enforced configuration. |
| S3 | Codex | Primary docs | <https://developers.openai.com/codex/memories> | 2026-08-22 | Opt-in memories distill useful context from eligible prior chats into local memory files; active/short-lived sessions are skipped, generated fields are secret-redacted, and background updates are delayed rather than synchronous at every chat end. |
| S4 | Codex | Primary concepts docs | <https://developers.openai.com/codex/concepts/customization/> | 2026-08-22 | Codex separates `AGENTS.md` behavioral instructions, memories carrying local context, skills, and MCP integrations. |
| S5 | DeepSeek Harness | Primary product page | <https://deepseek.com/harness/en/> | 2026-08-22 | Everything model-visible is recorded in one append-only event stream; trajectory inspection, resume, fork, search and replay share that stream. This is session continuity/observability, not verified distilled long-term memory. |
| S6 | DeepSeek Harness | Primary source architecture | <https://github.com/deepseek-ai/deepseek-harness/blob/master/docs/architecture.md> | 2026-08-22 | Model adapter, tool registry, session log and agent loop are replaceable Cordis plugins; a memory layer would belong at a plugin/service boundary rather than by forking the loop. |
| S7 | PenguinHarness | Primary repository, unavailable detail | <https://github.com/Prism-Shadow/penguin-harness> | 2026-08-22 | Repository existence only. Search could not verify a documented memory mechanism; no memory claim. |
| S8 | Cursor | Primary changelog | <https://cursor.com/changelog/1-0> | 2026-08-22 | Memories retain facts from conversations, are scoped per project and individual, and are manageable from Settings. |
| S9 | Windsurf | Primary docs | <https://docs.windsurf.com/windsurf/cascade/memories> | 2026-08-22 | Cascade automatically generates workspace-scoped memories, retrieves them when relevant, separates them from manually authored Rules, and exposes memories for user viewing/editing. |
| S10 | GitHub Copilot | Primary concepts docs | <https://docs.github.com/en/copilot/concepts/agents/copilot-memory> | 2026-08-22 | Memories are tightly scoped learned facts about a repository and can be reused across Copilot agent surfaces. |
| S11 | GitHub Copilot | Primary changelog | <https://github.blog/changelog/2026-03-04-copilot-memory-now-on-by-default-for-pro-and-pro-users-in-public-preview/> | 2026-08-22 | Repository-scoped memories are validated against current code before use and expire after 28 days to limit stale context. |

### Peer highlights and innovations

| Product | Sourced feature / innovation | Reference | Why it matters |
|---|---|---|---|
| Claude Code | Agent-authored notebook distinct from user-authored instructions; startup loading makes recall ambient rather than dependent on the model guessing a search query | S1, S2 | The rejected Darwin tool exposed raw search and required explicit query formulation. A durable curated notebook changes the abstraction from “search old chats” to “carry forward learned project knowledge.” |
| Codex | Two-stage, asynchronous distillation from eligible completed chats into local files, with active/short sessions skipped and secrets redacted | S3, S4 | Extraction should be off the critical turn path, operate only on closed durable records, and produce inspectable text rather than raw transcript hits. |
| DeepSeek Harness | One append-only source stream plus plugin extension points | S5, S6 | Darwin's trajectory is suitable source evidence, but memory must remain a derived layer with provenance—not reinterpret the trajectory itself as memory or fork the SDK loop. |
| Cursor | Automatic conversation-derived facts, project/user scope, user management UI | S8 | Memory needs a human-readable management surface and clear scope. Invisible generated prompt text is not acceptable. |
| Windsurf | Automatic relevant recall separated from manually authored rules; memories are viewable/editable | S9 | “What Darwin learned” and “what the user commands” must remain separate. Users need correction/deletion, not only an opaque retriever. |
| GitHub Copilot | Small repository facts reused across agents, validated against current code and expired | S10, S11 | Freshness is part of correctness. Memories should carry evidence and be rechecked or invalidated instead of becoming permanent hidden instructions. |
| PenguinHarness | No verifiable memory source found | S7 | No inference is made; its absence cannot be used to justify a Darwin design. |

### Current Darwin baseline

| Capability / architecture | Repository evidence | Current behavior |
|---|---|---|
| Durable source record | `.trellis/spec/backend/session-trajectory.md`; `src/trajectory/{writer,reader,record}.ts` | Every completed project session already leaves bounded append-only evidence, but the trajectory is an observer and not a curated knowledge base. |
| Text retrieval | `src/trajectory/search.ts`; `src/cli-trajectory.ts:239-285`; `README.md` “Session trajectory” | Human CLI substring search remains available. Commit `076d9dd` removed the rejected model-facing `search_memory`; source and specs are back to the pre-feature behavior. |
| Prompt composition | `src/agent/system-prompt.ts`, `src/agent/working-context.ts`, `src/skills/prompt.ts`; AGENTS load-bearing “System prompt composition” | Startup context order and cache point are tightly controlled. Any ambient memory must be one explicit bounded block before the cache point, refreshed on resume and never confused with project instructions. |
| Session lifecycle | `src/agent/runtime.ts` (`create`, `send`, `startNewSession`, `retire`); `.trellis/spec/backend/strands-sdk-contracts.md` | Runtime construction and post-turn orchestration already define safe boundaries; the SDK agent loop must not be forked. `/clear` rebuilds session-scoped state through the same factory. |
| Human management precedent | `/permissions`, `/mcp`, `/status`, `/help`; canonical command registry and bounded Static notices | Darwin has read-only/list/revoke command patterns, but no memory inventory, correction, deletion, provenance or freshness surface. |
| Existing persistent instructions | `AGENTS.md`, skills, commands; extension-layer contract | User-authored instructions already exist and must stay authoritative. Generated memories must be descriptive evidence, never silently become higher-priority policy. |
| Rejected design evidence | commit range `72c13df..73006fd`, reverted by `076d9dd` | Raw prior-session substring hits as a model tool were explicitly rejected by the user. This run treats that premise as falsified and does not repackage it. |

### Comparison and gaps

| Peer evidence | Darwin evidence | Parity / gap / deliberate difference | Implication |
|---|---|---|---|
| Claude/Codex/Windsurf automatically distill learnings into persistent text | Darwin stores raw trajectory only | **Core gap** | Add a derived, inspectable Markdown memory layer produced from closed sessions, not a transcript-search tool. |
| Claude loads a compact notebook at startup; Windsurf retrieves memories when relevant | Darwin startup has no learned-context block | **Gap** | MVP should load a small project memory index automatically; topic detail remains on-demand through existing file/text reads. No vector retrieval is needed. |
| Cursor/Windsurf expose memories for management | Darwin has no memory UX | **Gap** | Add `/memory` list/show/forget (and explicit user “remember” path only if scoped safely); every learned item needs provenance and edit/delete affordance. |
| Codex skips active/short sessions and updates asynchronously | Darwin records active input before invocation | **Important deliberate constraint** | Mine only closed successful turns after the user-visible turn is complete, outside the stream, and never block or fail the turn. Batch/delay extraction rather than run on every token or active session. |
| Codex redacts secrets | Trajectories may contain prompts/tool input/results | **Safety gap** | Generated memory fields need secret filtering and must never retain reasoning, credentials, `.env` values, tokens or arbitrary large tool output. Ambiguous sensitive candidates are dropped. |
| Copilot validates current code and expires stale facts | Darwin facts could outlive code changes indefinitely | **Correctness gap** | Store source references and freshness metadata; code-derived facts require revalidation before startup inclusion, with stale/invalid facts omitted and visible to `/memory`. User preferences may have different retention. |
| DeepSeek keeps a canonical event stream and uses plugins | Darwin has trajectory + thin SDK extension seams | **Strong architecture fit** | Memory is a derived session-scoped service/plugin fed by the trajectory after closure; trajectory bytes remain unchanged and authoritative only as evidence. |
| All mature designs separate instructions/rules from memories | Darwin has AGENTS.md/skills | **Must preserve** | Memories are bounded context labelled as learned and fallible. They cannot override AGENTS/project instructions, permissions, hooks or user text. |

### Ranked iteration directions

`Score = 2 × Importance + Architecture fit + Evidence confidence − Implementation difficulty − Implementation risk`

Only directions scoring at or above `MINIMUM_IMPLEMENTATION_SCORE = 6` enter the backlog. Dependencies come first.

| Rank | Proposed ID | Direction | Importance | Architecture fit | Evidence confidence | Implementation difficulty | Implementation risk | Score | Evidence and rationale |
|---:|---|---|---:|---:|---:|---:|---:|---:|---|
| 1 | SER-031 | Add opt-in, project-scoped distilled Markdown memory with a bounded always-loaded index and topic files: derive only from closed successful trajectory turns in a delayed post-turn job, preserve source provenance, redact/drop secrets, never block the turn, and never treat learned memory as instructions | 5 | 4 | 5 | 5 | 4 | **10** | S1–S6 establish curated files, startup recall, delayed eligible-session extraction and a plugin/derived-event boundary. This supersedes—not re-rates—the reverted raw-search implementation: the requirement changed after explicit user rejection. Architecture fit is 4 because post-turn derivation can sit outside the SDK loop, but prompt composition and extraction lifecycle are load-bearing. |
| 2 | SER-032 | Add bounded `/memory` management for the generated store: list index/topic entries with scope, provenance, freshness and sensitivity state; show one; forget one/all; allow a user-authored project memory note without giving the model an ungated persistence path | 4 | 5 | 5 | 3 | 3 | **12** | Cursor/Windsurf/Copilot make management and opt-out first-class (S8–S11). This depends on SER-031's file schema. User writes are explicit local commands; model-generated writes remain only through the guarded extractor. Deletion must synchronously narrow future context. |
| 3 | SER-033 | Revalidate and age generated memories: verify code-derived source anchors against the current worktree before startup inclusion, mark invalid/stale entries, and expire unconfirmed generated facts on a configurable conservative horizon while preserving explicit user-authored notes | 4 | 4 | 5 | 4 | 3 | **10** | Copilot's codebase validation and 28-day expiration (S11) directly address stale-memory correctness. Depends on SER-031 provenance/schema and SER-032 visibility so invalidation never silently loses knowledge. A deterministic text/source check—not embeddings—fits the user's constraint. |

### Gated-out directions

| Direction | Score | Gate | Why it fell below | Kept anyway? |
|---|---:|---:|---|---|
| Restore a model-facing raw trajectory search tool | 0 (I 1, F 1, E 1, D 3, R 2) | 6 | Explicitly rejected by the user and reverted in `076d9dd`; peer evidence points to distilled facts/notebooks, not requiring the model to formulate literal transcript queries | no |
| Vector/embedding retrieval over memories | 1 (I 2, F 1, E 2, D 4, R 4) | 6 | Explicitly excluded by the user; local structured Markdown plus bounded index/topic reads covers the peer pattern without a derived opaque index | no |
| Inject all generated topic files at startup | 4 (I 3, F 3, E 4, D 3, R 3) | 6 | Violates context/cache bounds and makes stale or low-value notes ambient; peers separate a compact index from detail and/or relevant retrieval | no |
| Let the model freely edit memory files through `fileEditor` | 3 (I 3, F 2, E 3, D 2, R 6→rated 5 maximum, formula 4) | 6 | It bypasses extraction, secret, provenance and user-management policy; persistence widening must not be hidden inside a generic write tool | no |

The rejected raw-search direction is not moved across the score gate by changing its ratings: it was removed by an explicit product decision. The new SER-031 reuses the stable ID only because the revert deleted the prior backlog row and its origin report; this report records the supersession and changed requirement explicitly so no history is silently reinterpreted.

### Recommendation

- Batch to implement: `SER-031`, then `SER-032`, then `SER-033`
- Start with: `SER-031`
- Why this order: storage/extraction/prompt contract first; management must operate on a settled schema; freshness then builds on provenance plus a visible invalidation surface. Scores do not override dependency order.
- Independently observable acceptance:
  - SER-031: opt-in default off; seed closed and active trajectories, failed/cancelled/short turns, credentials and code-derived facts; prove only eligible closed evidence yields bounded Markdown index/topic files; raw secrets/reasoning/tool dumps absent; source trajectory byte-identical; extraction failure/timeout cannot fail or delay the completed turn; a fresh/resumed runtime receives one labelled bounded memory index at the documented prompt position; no vector/index dependency and no SDK-loop fork.
  - SER-032: list/show/forget/user-note commands are local, bounded and available without model/network; hash unrelated session/config bytes; forgetting immediately removes startup context; generated provenance/freshness is visible; no model tool can call the user-write path; completion/help/frame checks stay within existing contracts.
  - SER-033: mutate/delete cited code after memory creation and advance a deterministic clock; prove invalid/stale generated facts are omitted before a model request and reported in management, valid facts remain, user notes do not auto-expire, unknown validation is omitted rather than trusted, and no source/worktree bytes are modified.
- Backlog updates: add all three as `not-started`; mark only SER-031 `in-progress` immediately before developer handoff.

### Batch iteration outcome

| Direction | Child session and managed tasks | Host acceptance (exact independent checks and outcomes) | Commit | Final status | Iteration-log entry |
|---|---|---|---|---|---|
| SER-031 | `session-20260822-032546109`; `bg-370b6138-44ac-405e-8501-0a581ed85a7e` (exit 0; no correction turn) | Host inspected the implementation and re-ran `spike/verify-memory.ts` (34), `spike/verify-clear-session.ts` (40), `pnpm typecheck`, full `pnpm test`, `pnpm build`, Trellis archive validation, structural no-search/vector/dependency/extra-Agent checks, AGENTS.md size, and commit/diff checks; all passed. | `73bc11b` (+ `f955c54`, `a90489d`) | done | [`Batch 45`](../iteration-log.md) |
| SER-032 | `session-20260822-040134487`; `bg-e1396eab-d9a8-479a-bef3-695c487cb43c` (exit 0; no correction turn) | Host inspected the implementation and re-ran `verify-memory-command.ts` (21), `verify-memory.ts` (34), `verify-clear-session.ts` (42), free pty `completion` (62), `pnpm typecheck`, full `pnpm test`, `pnpm build`, Trellis archive validation and structural/commit/diff/AGENTS-size checks; all passed. | `06873a5` (+ `b2c565f`, `ae00a9f`) | done | [`Batch 46`](../iteration-log.md) |
| SER-033 | `session-20260822-042916698`; `bg-574c2443-8b57-4252-9dbc-b7d18c263fb3` (exit 0; no correction turn) | Host inspected the implementation and re-ran `verify-memory-validation.ts` (15), `verify-memory-command.ts` (21), `verify-config.ts` (216), `verify-clear-session.ts` (42), `pnpm typecheck`, full `pnpm test`, `pnpm build`, Trellis archive validation and structural/commit/diff/AGENTS-size checks; all passed. | `64989d6` (+ `0b53fb4`, `dc9244f`) | done | [`Batch 47`](../iteration-log.md) |

- Halt condition that ended the loop: batch exhausted — every direction is `done`
- Aggregate token spend across all child delegations: `input=1,062 output=146,844 cacheRead=66,386,328 cacheWrite=551,704`
- Remaining backlog after this run: none unfinished


## Run — 2026-08-22T06:37:01Z

### Backlog-first decision

- Backlog inspected first: `docs/research/backlog_index.md`
- Unfinished directions found: none
- Decision: fresh research on the user-directed TUI path

### Research path

```text
research-path: tui
focus: TUI interaction and visual polish
share: 20% (weight 2 of 10)
draw: none — path was not rolled
path-source: override (user-directed)
rolled-at: 2026-08-22T06:35:58.849Z
weights: tui=2 observability=0.5 sdk=1 open=1.5 peer=5
```

### Source availability and method

This was a user-directed `tui` self-review. Repository source, specifications, tests, recent git history, and the screenshot supplied with the request were inspected locally on 2026-08-22 UTC. No peer product was consulted: the TUI path requires repository evidence, and the two requested gaps are directly observable in the supplied screenshot and current source. Claims below therefore cite repository paths, symbols, commits, and the screenshot rather than external URLs.

The worktree was already dirty at invocation start: nine `.trellis/` files contained an unrelated 0.6.14→0.6.15 update, `image.png` was untracked, and `main` was three commits ahead of `origin/main`. Those bytes were not changed or hidden by this run. This matters to the section 7 starting-point gate recorded below.

### Reference sources

No peer-product table applies to this self-review path. The repository evidence used in place of URLs is:

| Ref | Repository evidence | Claim supported |
|---|---|---|
| R1 | [`image.png`](../../image.png) | `/memory` and `/mcp` results are rendered in a low-contrast dark gray that is substantially harder to read than the cyan user labels and yellow mode warning. |
| R2 | `src/tui/MessageList.tsx:104-110` (`HistoryEntry`) and `src/tui/visual-language.ts:8-17,34-42` (`visualColor`, `noticeColor`) | Every `info` notice renders with both gray foreground and Ink `dimColor`; local command reports use that same notice path. |
| R3 | `src/tui/App.tsx:716-900` (`submit` local-command branches), `src/memory/command.ts:28-197`, `src/tui/mcp-format.ts:31-90`, `src/tui/status-format.ts:81-106`, `src/tui/help-format.ts:18-45` | `/memory`, `/mcp`, `/status`, and `/help` produce bounded plain-text reports and dispatch them as ordinary informational transcript notices; their content is not itself choosing the dark presentation. |
| R4 | `src/cli.ts:120-191` (`runInteractive`) | Interactive startup awaits `AgentRuntime.create()` and resume recap loading before the first Ink `render()`, so initialization has no immediate visual feedback and the first rendered surface is the ordinary `App`. |
| R5 | `src/tui/App.tsx:238,454-462,1636-1695,1726-1865`; `.trellis/spec/frontend/live-frame.md` §§ “one shared row budget” and “one semantic visual language” | Darwin has an existing timer/animation precedent once work is active, but the ready TUI has a strict measured frame budget; a startup animation must exist before the settled `App`, not become permanent frame furniture. |
| R6 | `spike/verify-visual-language.tsx:50-91`; `.trellis/spec/frontend/tui-testing.md` §§ “visual hierarchy” and “header competes for frame height” | Visual semantics are verified both with ANSI-stripped stable markers and deterministic Ink rendering; cosmetic color must not become the only state signal, and ready-frame row count cannot grow casually. |
| R7 | commit `ab71a8c` (`feat(tui): unify the visual language`) | The current palette and compact status-first header are deliberate shared infrastructure; these changes should extend that vocabulary rather than introduce component-local styling. |

### Peer highlights and innovations

Not applicable: no peer product was opened on this user-directed self-review path.

### Current Darwin baseline

| Capability / architecture | Repository evidence | Current behavior |
|---|---|---|
| Informational command output | R1–R3 | Command reports are bounded and structurally clear, but the shared `info` notice renderer applies gray **and** dim intensity to the marker and every report line. The screenshot shows the resulting readability failure. |
| Severity hierarchy | R2, R6, R7 | Stable text markers (`info ·`, `warn !`, `error !`) survive ANSI stripping; warn/error have yellow/red semantic colors. Color is enhancement, not the sole carrier of state. |
| Startup lifecycle | R4 | Runtime/config/MCP/session initialization completes before Ink owns the terminal, leaving no startup state, progress motion, or Darwin identity until the ready screen appears. |
| Animation and timing | R5 | A bounded 100 ms spinner tick already animates active work. There is no startup component or lifecycle, and no reason to add a second timer after `App` is ready. |
| Frame safety | R5, R6 | The settled header, transcript, tools, queues, permission box, and prompt share one strict budget. A splash that remains mounted would compete with action-critical rows and risk Ink whole-screen clears. |

### Comparison and gaps

| User / repository evidence | Darwin evidence | Assessment | Implication |
|---|---|---|---|
| R1 asks for command text that is easy to read | R2 applies gray plus terminal dim to all informational report text | **Direct accessibility/readability gap** | Keep `info ·` as the durable role marker, but render informational content at normal intensity with a higher-contrast semantic marker/body treatment; warnings and errors remain distinct. |
| User asks for a cool dynamic launch screen | R4 renders nothing until initialization is over | **Direct experience gap** | Render an immediately visible, bounded Darwin-themed startup surface while the existing runtime factory works, then replace it atomically with the unchanged ready `App`. |
| The ready frame is already budget-sensitive | R5–R6 | **Deliberate constraint, not a missing decoration** | Startup motion must stop and unmount before the prompt is interactive; it cannot add a permanent row, delay readiness for a fixed theatrical duration, clear scrollback repeatedly, or steal input. |
| Visual language is centralized | R2, R7 | **Strong extension point** | Contrast and splash colors/markers belong in shared semantic vocabulary and deterministic render fixtures, not hard-coded ANSI output. |

### Ranked iteration directions

`Score = 2 × Importance + Architecture fit + Evidence confidence − Implementation difficulty − Implementation risk`

Only directions scoring at or above `MINIMUM_IMPLEMENTATION_SCORE = 6` enter the backlog. The direct readability fix comes first; the lifecycle-changing startup experience follows on the newest accepted revision.

| Rank | Proposed ID | Direction | Importance | Architecture fit | Evidence confidence | Implementation difficulty | Implementation risk | Score | Evidence and rationale |
|---:|---|---|---:|---:|---:|---:|---:|---:|---|
| 1 | SER-034 | Raise informational transcript contrast: render `info ·` with a clear semantic accent and command/report body text at normal terminal intensity, preserving exact report text, stable ANSI-stripped markers, warning/error hierarchy, `<Static>` ownership, and monochrome readability | 5 | 5 | 5 | 1 | 2 | **17** | R1 reproduces the user-visible defect; R2 identifies one shared renderer; R3 proves report producers need no content rewrite. The change is deliberately presentation-only and should cover `/memory`, `/mcp`, `/status`, `/help`, and other informational notices consistently rather than adding command-specific color hacks. |
| 2 | SER-035 | Add a responsive animated Darwin startup screen: render immediately while `AgentRuntime.create()` and resume recap loading run, use a compact Darwin/evolution motif with motion and honest initialization state, then hand the same Ink terminal cleanly to the ordinary `App` with no fixed delay, lingering timer, input theft, transcript artifact, or settled-frame row growth | 4 | 4 | 5 | 4 | 4 | **9** | R4 is the exact lifecycle gap. R5 supplies the timer precedent and R5–R6 the non-negotiable frame/terminal constraints. A pre-`App` component fits better than decorating the permanent header, but startup/error/resume handoff is lifecycle-sensitive and therefore carries higher difficulty/risk. |

### Gated-out directions

| Direction | Score | Gate | Why it fell below | Kept anyway? |
|---|---:|---:|---|---|
| Keep decorative animation running in the ready header | 3 (I 2, F 2, E 4, D 3, R 4) | 6 | It spends a permanent timer and measured live-frame rows after startup, competes with permission/tool content, and adds motion while the user is trying to read or type; R5 documents the concrete overflow cost | no |
| Force a fixed multi-second splash before showing the ready prompt | 3 (I 2, F 2, E 4, D 3, R 4) | 6 | It makes fast startup objectively slower and mistakes animation duration for initialization truth; the splash should last only as long as real setup needs and transition immediately when ready | no |

### Recommendation

- Batch to implement: `SER-034`, then `SER-035`
- Start with: `SER-034`
- Why this order: SER-034 is the direct readability defect with one shared presentation seam and the highest score. SER-035 then builds the launch experience on the accepted visual vocabulary while changing CLI/Ink lifecycle separately. There is no code dependency, so priority follows value and risk rather than score manipulation.
- Independently observable acceptance:
  - SER-034: deterministic forced-color render proves `info ·` remains a distinct accented marker, informational body text has no dim SGR, warn/error colors and markers remain distinct, and ANSI-stripped `/memory`/`/mcp`/`/status`/`/help` bytes are unchanged; run `spike/verify-visual-language.tsx`, focused command suites, free pty `completion`/`mcp`, `pnpm typecheck`, and `pnpm test`.
  - SER-035: a deterministic component fixture proves multiple bounded motion frames, narrow/short fallback, no raw terminal writes and timer cleanup; a delayed-runtime real-pty fixture proves startup appears before readiness, changes while setup is pending, disappears completely at handoff, leaves exactly one ready header/prompt with usable input, and error/resume paths exit cleanly without model/network work or transcript mutation; run `spike/verify-frame-budget.ts`, `verify-visual-language.tsx`, free pty startup/`completion`/`clear`/`resume`, `pnpm typecheck`, and `pnpm test`.
- Backlog updates: add `SER-034` and `SER-035` as `not-started`. Neither is changed to `in-progress` because the mandatory clean-tree starting point failed before developer handoff.

### Batch iteration outcome

| Direction | Child session and managed tasks | Host acceptance (exact independent checks and outcomes) | Commit | Final status | Iteration-log entry |
|---|---|---|---|---|---|
| SER-034 | Not launched — the starting worktree was already dirty with unrelated `.trellis/` changes and the supplied untracked screenshot | Not run; section 7 forbids delegating onto an unverified dirty tree | — | `not-started` | — |
| SER-035 | Not launched | Not run | — | `not-started` | — |

- Halt condition that ended the loop: **the starting point cannot be restored** — making the tree clean would require stashing, committing, or discarding pre-existing changes whose ownership/scope this run cannot invent. The developer workflow was loaded once, but no child was started and no implementation was attempted.
- Aggregate token spend across every child delegation: no child delegation; `input=0 output=0 cacheRead=0 cacheWrite=0`
- Remaining backlog after this run: `SER-034` (`not-started`), `SER-035` (`not-started`). Do not recommend fresh research while either row remains unfinished.

### Batch continuation — 2026-08-22T10:38:24Z

The next invocation inspected the backlog before any research source and resumed this existing batch; no new research path was rolled and no fresh product research was performed.

| Direction | Child session and managed tasks | Host acceptance (exact independent checks and outcomes) | Commit | Final status | Iteration-log entry |
|---|---|---|---|---|---|
| SER-034 | `session-20260822-104129517`; planning `bg-d54a0aa6-48fb-4ce9-afa1-4d1f8c3c9710` and implementation `bg-57f117eb-932d-409f-8d44-88d4cd88575b` (both exit 0; no acceptance correction) | Host inspected the implementation and re-ran `verify-visual-language.tsx` (53), free pty `completion` (62) and `mcp` (13), `pnpm typecheck`, full `pnpm test`, `pnpm build`, Trellis archive validation, commit/diff/clean-tree and AGENTS-size checks; all passed. | `af84a03` (+ `7c49cd9`, `4c135e5`) | done | [`Batch 48`](../iteration-log.md) |

- Token spend for SER-034: `input=104 output=18,891 cacheRead=3,032,635 cacheWrite=131,126` across both managed tasks.
- Remaining batch direction: `SER-035` (`not-started`).

