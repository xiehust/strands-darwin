# Darwin self-evolution research — 2026-09-07

## Run — 2026-09-07T09:36:39Z

### Backlog-first decision

- Backlog router inspected first: `docs/research/backlog_index.md`
- Metadata searched across routed priority pages: `docs/research/backlog/directions-001-020.md`, `directions-021-040.md`, `directions-041-060.md`, `directions-061-080.md`, `directions-081-100.md` (95 records: 93 `done`, 2 `abandoned`; newest origin report `research_2026-09-06.md`, both of its records `done`)
- Unfinished directions found: none
- Decision: fresh research

### Research path

```text
research-path: peer
focus: comparable coding-agent product analysis
share: 50% (weight 5 of 10)
draw: 10 of 20 half-units
path-source: roll
rolled-at: 2026-09-07T09:36:39.436Z
weights: tui=2 observability=0.5 sdk=1 open=1.5 peer=5
```

Rolled once, before any source was opened. `peer` path: the reference-source table below is filled from primary product sources; repository evidence is cited by path and symbol beside it.

### Source availability and method

Tools available: `web_fetch` (HTML→text, 40 000-code-point cap), `curl` in the shell for pages past that cap (grepped locally, never summarized from memory), GitHub's contents API for directory listings, and the repository itself (`rg`/`sed` over `src/`, `spike/`, `docs/`). Access date for every source: **2026-09-07 (UTC)**.

Scope coverage:

- **Claude Code** — verified from the `main` CHANGELOG and the `prompt-caching` doc page (markdown edition).
- **Codex** — verified from the developer-commands reference (CLI surface) and the `rust-v0.152.0`/`rust-v0.153.0` release pages. The `learn.chatgpt.com/docs/changelog` page renders only navigation to `web_fetch`; claims come from the release pages and the command reference instead.
- **DeepSeek harness** — verified from `apps/cli/README.md` and the `docs/subsystems/` listing plus four subsystem pages at `master`. The repository ships a web app and headless/SDK/ACP profiles; a terminal profile is mentioned only as an example ("assuming the tui profile is installed"), so no TUI-behaviour claim is made about it.
- **PenguinHarness** — verified from the `main` README. It documents a desktop app, `penguin web`, `penguin run`, a `penguin chat` REPL (`/compact`, `/clear`, `/exit`, Ctrl-C) and a TS SDK with a per-tool-call `approve` callback; it documents no hooks browser, terminal title, side chat, permission-path rule or cache diagnostics. No claim is made beyond that.
- **Gemini CLI** (additional product) — verified from `docs/reference/commands.md` at `main`.

Previous peer runs (2026-09-02 tools, 2026-09-03 multi-agent, 2026-09-06 background tasks) were read for their gated and noted-not-proposed tables so this run does not re-propose them; nothing below duplicates a backlog heading (`rg '^## S' docs/research/backlog/*.md` collected all 95 headings first).

### Reference sources

| Ref | Product | Source type | URL | Accessed (UTC) | Claims supported |
|---|---|---|---|---|---|
| S1 | Claude Code | Primary release notes (`CHANGELOG.md` at `main`, 2.1.257–2.1.263) | <https://raw.githubusercontent.com/anthropics/claude-code/main/CHANGELOG.md> | 2026-09-07 | 2.1.257: "Added a one-time prompt in auto mode before the first file read outside the working directories, with the option to block such reads (`permissions.blockReadsOutsideWorkingDirectories`)"; 2.1.260: "Added a likely cause for prompt-cache misses (e.g. tool definitions or system prompt changed, idle past the TTL) to `/cost` and the status line's `prompt_cache` field"; 2.1.261: `/skill-doctor` "show which loaded skills go unused and what they cost in context", `bashOutputMaxChars`/`taskOutputMaxChars` up to 128K; 2.1.259: `--permission-prompts none` for unattended headless hosts, "nested background subagent results … saved in the parent subagent's transcript, so resumed subagents keep them"; 2.1.261: "Fixed sustained high CPU usage when a background agent could not be resumed"; "Improved the dangerous-`rm` safety prompt"; "Fixed the terminal progress indicator (iTerm2, Ghostty, ConEmu) showing the session as finished while a background workflow or agent was still running" |
| S1b | Claude Code | Primary docs (`prompt-caching.md`) | <https://code.claude.com/docs/en/prompt-caching.md> | 2026-09-07 | "Actions that invalidate the cache": switching models, changing effort level, fast mode, connecting/disconnecting an MCP server, plugins, denying an entire tool, compacting, many images, upgrading; "Actions that keep the cache": editing files, editing CLAUDE.md mid-session, permission mode, invoking skills, `/rewind`; TTL 5 minutes or 1 hour, each cache hit resets the timer; `/usage` "Prompt cache (main)" line shows hit ratio, miss count, warm/cold and "names the likely cause of the last miss when Claude Code can identify one, for example `likely cause: tool definitions changed`" (v2.1.260+); `/model` "asks you to confirm the switch only while the cache is still warm" |
| S2 | Codex | Primary docs (developer commands reference, CLI surface, markdown edition; fetched with `curl`, 49 270 B) | <https://learn.chatgpt.com/docs/developer-commands.md?surface=cli> | 2026-09-07 | Slash table: `/title` "Configure terminal window or tab title fields interactively … Available title items include app name, project, spinner, status, thread, git branch, model, and task progress", persisted to `tui.terminal_title`; `/hooks` "View and manage lifecycle hooks. Inspect configured hooks, trust new or changed hooks, or disable non-managed hooks before they run"; `/side`, `/btw` "Start an ephemeral side chat … Ask a focused follow-up without disrupting the main chat's transcript", "the TUI continues to show the parent chat's status"; `/agent`, `/subagents` "Switch the active agent thread. Inspect or continue work in a spawned subagent thread"; `/statusline`, `/keymap`, `/ps`, `/fork`, `/rename`, `/diff`; shortcuts: `Esc` twice on an empty composer edits the previous message and forks, `Ctrl+O`/`/copy`, `Ctrl+R` history search, `!` shell prefix, `Tab` queues a follow-up, `Enter` injects into the current turn |
| S2b | Codex | Primary docs (CLI overview) | <https://learn.chatgpt.com/codex/cli.md> | 2026-09-07 | "Fit Codex to your terminal — `codex completion`: Generate completions for your shell, choose a syntax theme, and open longer prompts in the editor configured by VISUAL or EDITOR" |
| S2c | Codex | Primary release notes (`rust-v0.153.0`, 2026-09-03; `rust-v0.152.0`, 2026-09-01) | <https://github.com/openai/codex/releases/tag/rust-v0.153.0>, <https://github.com/openai/codex/releases/tag/rust-v0.152.0> | 2026-09-07 | 0.153.0: `tui.auto_recap = false` disables automatic recaps while keeping `/recap`; "TUI history shows complete patches, input sent to background terminals, and individual completed commands"; Guardian automatic approval review scoped by mode; "Guardian review history survives compaction, restarts, and user-created forks … isolating subagent history". 0.152.0: "Individual MCP tools support an `output_token_limit` setting, with consistent truncation across session resumes"; "The planning tool is disabled by default; enable it with `tools.update_plan.enabled = true`" |
| S3 | DeepSeek harness | Source repository (`apps/cli/README.md`, `docs/subsystems/` listing, `session-title.md`, `terminal.md`, `spill.md`, `token-meter.md` at `master`) | <https://github.com/deepseek-ai/deepseek-harness/blob/master/apps/cli/README.md>, <https://github.com/deepseek-ai/deepseek-harness/tree/master/docs/subsystems> | 2026-09-07 | `dsh` profiles `web`, `headless`, `sdk`, `sdk-minimal`, `acp`; `--dump-config`; session titles are "durable latest-wins title state" with an "optional asynchronous provider" (LLM) and a `fallback` source; "Persistent PTY Sessions" with `TerminalWaitReason = 'stdin_read' | 'inferred_idle' | 'timeout' | 'session_exit'`; spill storage persists oversized tool text and returns "a model-facing locator plus retrieval guidance"; token meter reprices the current surface per route. No TUI-surface claim (see method) |
| S4 | PenguinHarness | Source repository README at `main` | <https://github.com/Prism-Shadow/penguin-harness/blob/main/README.md> | 2026-09-07 | `penguin chat` REPL "(`/compact`, `/clear`, `/exit`, Ctrl-C to interrupt)", `penguin run -m`, `penguin server`, SDK `approve` callback; roadmap "OpenShell integration (permission-governed shell)". No hooks/title/side-chat/path-rule documentation found — no claim |
| S5 | Gemini CLI | Source repository docs (`docs/reference/commands.md` at `main`) | <https://github.com/google-gemini/gemini-cli/blob/main/docs/reference/commands.md> | 2026-09-07 | `/hooks` with `list` (or `show`, `panel`), `enable`/`disable`/`enable-all`/`disable-all`; `/editor` "Open a dialog for selecting supported editors"; `/stats` `session|model|tools`; `/rewind` "Press Esc twice as a shortcut"; `/copy`; `/directory add` multi-directory workspace |

### Peer highlights and innovations

| Product | Sourced feature / innovation | Reference | Why it matters |
|---|---|---|---|
| Claude Code | One-time prompt in auto mode before the first file read **outside the working directories**, with `permissions.blockReadsOutsideWorkingDirectories` to block such reads | S1 (2.1.257) | Reads were the last silent class of tool call; a prompt-injected model reading `~/.ssh` or `~/.aws` leaks secrets into the request and the transcript even when exfiltration is later denied |
| Claude Code | `/cost` and the status line name the **likely cause of the last prompt-cache miss** (tool definitions / system prompt changed, idle past the TTL); `/model` confirms a switch only while the cache is warm | S1 (2.1.260), S1b | A miss on a long conversation is the most expensive request a session sends; naming the cause turns a surprise bill into an avoidable action |
| Claude Code | `/skill-doctor` shows unused loaded skills and their context cost | S1 (2.1.261) | Context hygiene made inspectable (inference: value scales with catalogue size) |
| Claude Code | `--permission-prompts none`: anything that would prompt is denied automatically on unattended hosts | S1 (2.1.259) | Same contract darwin's headless bridge already enforces (`createHeadlessPermissionBridge`) |
| Claude Code | Resumable background subagents; nested results saved so "resumed subagents keep them" | S1 (2.1.259, 2.1.261) | A finished child is a resource, not a discarded transcript |
| Codex | `/title`: terminal window/tab title composed from app name, project, spinner, status, thread, branch, model, task progress; persisted | S2 | Users run several agents in tabs/panes; the title is the only surface visible when the tab is not focused |
| Codex | `/hooks`: inspect configured lifecycle hooks, trust new or changed hooks, disable non-managed ones before they run | S2 | Hooks execute commands on tool events; the user must be able to see in-session what is armed |
| Codex, Claude Code | `/side` / `/btw`: an ephemeral side chat whose transcript is separate from the parent, parent status still shown | S2 | A focused question without polluting the working context |
| Codex | `/agent`: inspect or **continue** work in a spawned subagent thread | S2 | Children are conversations, not one-shot function calls |
| Codex | Longer prompts open in `$VISUAL`/`$EDITOR`; `codex completion` shell completion | S2b | Composer ergonomics for long prompts |
| Codex | Per-MCP-tool `output_token_limit`; planning tool off by default | S2c | Bounded tool output is a per-tool knob there; darwin's is the offloader threshold (not configurable) |
| DeepSeek harness | LLM-generated session titles with a fallback source; persistent PTY sessions with wait reasons; spill storage for oversized tool text | S3 | Alternative shapes for session naming, interactive shells and output bounding — noted, not adopted |
| Gemini CLI | `/hooks list|enable|disable`; `/editor`; `/stats tools`; `Esc Esc` → `/rewind` | S5 | Third product with an in-session hooks projection; `Esc Esc` rewind is darwin parity (SER-059) |

### Current Darwin baseline

| Capability / architecture | Repository evidence | Current behavior |
|---|---|---|
| Read classification | `src/agent/permission.ts` `assessRisk` (`if (request.kind === 'read') return { risk: 'safe', … }`), `classifyFileEditor` (`command === 'view'` → `kind: 'read'`), `SAFE_BASH_COMMANDS` (`ls cat head tail grep rg find pwd which wc echo`), `assessBashRisk` (argument scan only for `find`/`git` mutating options), `permission-rules.ts` `SHELL_METACHARACTERS = /[><`]|\$\(/`, `paths.ts` `isSensitiveDarwinPath` (writes only), `ENV_FILE` (writes only) | Every read is `safe` in every mode: `fileEditor view /home/u/.aws/credentials`, `cat ~/.ssh/id_rsa`, `cat $HOME/.darwin/config.json` (no `$(`) and `head .env` run without a prompt, in `default`, `auto`, and `plan` (`permission.ts:411` `if (request.kind === 'read') return undefined;`). The bytes then enter the provider request and the trajectory record. Writes to `~/.darwin/config.json` and `.env*` are protected; the same paths are unprotected for reads |
| Prompt-cache reporting | `src/agent/usage.ts` `cacheEffectivenessRows` (hit ratio, served from cache), `usageBuckets`; `src/agent/runtime.ts` `lastTurnUsage`, `recordCompletedCall` (`call-stats.ts`), `changeModel`, `changeThinkingEffort`; `src/agent/prompt-cache.ts` (`PROMPT_CACHE_TTLS = ['5m','1h']`, config `promptCacheTtl`); `/usage` in `App.tsx:1133` | `/usage` and `/status` show cumulative and last-turn cache read/write counts and a hit ratio. Nothing names why a turn missed; `/model` and `/effort` print no cache-cost notice; idle time versus TTL is not compared anywhere |
| Hooks visibility | `RuntimeInfo.hookSources` / `hookShadowNotices` (`runtime.ts:392–395`), `src/config.ts` `loadProjectPolicy`; `src/tui/status-format.ts` `formatStatusReport` rows `model, session, mode, mcp, skills, trajectory, diagnostics, tokens, cost, context`; `src/cli-doctor.ts` `reportHooks` | Active hook files are loaded into `RuntimeInfo.hookSources` but rendered nowhere in-session: `rg hookSources src/tui` finds no consumer; only `App.tsx:2751` prints shadow notices at startup and `darwin doctor` lists layers offline |
| Terminal title | `rg -e '\]0;' -e '\]2;' -e 'setTitle' src spike docs` → no match; `src/tui/copy-command.ts:47` writes OSC 52 through Ink's `useStdout().write`; `src/tui/terminal-bell.ts:35` writes BEL to `process.stdout` | darwin never sets the terminal title; two precedents already write raw terminal sequences outside the frame |
| Skills catalogue | `src/skills/`, `<available_skills>` system-prompt section, `RuntimeInfo.skillNames`, `call-stats.ts` counts tool uses per call (not per tool name) | `/status` lists skill names; no per-skill "loaded this session" or byte cost is tracked |
| Subagent lifecycle | `src/agents/subagent-tool.ts:175–230` (`buildRecipeChild` → `child.invoke` → `finally { stopBashSession(child) … }`), `dispatch-registry.ts` (records hold name/task/phase/state/usage, never transcript), load-bearing § Subagents ("Only the final report is returned"), SER-064/SER-070 background delegation | A child Agent is dropped at settlement; there is no way to ask a finished child a follow-up. The 2026-09-03 run rated "Continuable children" 9 and deferred it until background delegation existed; SER-064 and SER-070 are now `done` |
| Headless permission prompts | `src/headless-runner.ts:168–177` `permissionDenied` / `createHeadlessPermissionBridge` | Parity with Claude Code `--permission-prompts none`: anything that would prompt is denied |
| Composer | `src/tui/InputBox.tsx`, `prompt-editor.ts`; `rg -e 'EDITOR' -e 'VISUAL' src` → no match | No external-editor hand-off; keys are hard-wired (no keymap) |
| Tool-output bound | SDK `ContextOffloader` installed by `runtime.ts`; `src/config.ts` `contextOffload: boolean` only | The offload threshold is the SDK default; there is no size knob like `bashOutputMaxChars` |

### Comparison and gaps

| Peer evidence | Darwin evidence | Parity / gap / deliberate difference | Implication |
|---|---|---|---|
| Prompt/blocks for reads outside working directories (S1) | `assessRisk` read branch, `SAFE_BASH_COMMANDS`, `permission.ts:411` | **Safety gap** — reads of credential paths are silent in every mode, including `plan`; write protection for `~/.darwin/config.json`/`.env*` has no read counterpart | SER-071 |
| Likely cause of the last cache miss; warm-cache confirmation on `/model` (S1, S1b) | `cacheEffectivenessRows`, `lastTurnUsage`, `changeModel`, `changeThinkingEffort`, `promptCacheTtl` | **Gap** — darwin has every input (per-call cache counters, the events it performed, the TTL, the wall clock) and derives nothing | SER-074 |
| `/hooks` in three products (S2, S5, Claude Code) | `RuntimeInfo.hookSources` unused by any renderer | **Gap, small** — the data is already loaded; `/status` is the documented consolidated projection | SER-072 |
| `/title` terminal title (S2) | no title write; OSC 52 and BEL precedents | **Gap** — the peers use the tab title as the away-from-tab status surface | SER-073 |
| Continue a subagent thread (S2 `/agent`); resumable background agents (S1) | `subagent-tool.ts` drops the child at settlement; 2026-09-03 deferral now unblocked | **Gap** — deferred by design until background delegation shipped | SER-075 |
| `/side` / `/btw` ephemeral side chat (S2) | no equivalent; a fresh Agent over a copy of the messages is feasible only in `runtime.ts` (SDK reuse row) | Gap; score 6 (meets the gate) but the batch is capped at five | recorded below, not queued |
| `/skill-doctor` (S1) | `skillNames` only, no per-skill load tracking | Gap; score 8 but low importance for darwin's small catalogue | recorded below, not queued |
| `bashOutputMaxChars` (S1), `output_token_limit` (S2c) | offloader threshold not configurable | Gap; score 8, operator convenience only | recorded below, not queued |
| `--permission-prompts none` (S1) | headless bridge denies | **Parity** | none |
| `Esc Esc` fork/rewind (S2, S5) | SER-059 | **Parity** | none |
| `Enter` injects into the current turn (S2) | SER-027 next-turn-only queue | **Deliberate difference** (recorded 2026-09-02/06) | none |
| `/statusline` footer (S2) | header-only frame budget | **Deliberate difference** (load-bearing § TUI frame budget) | none |
| `$EDITOR` for long prompts (S2b, S5) | none | Gap; gated below (Ink raw-mode hand-off risk) | gated |
| `/keymap` (S2) | hard-wired keys | Gap; gated below | gated |
| LLM session titles (S3) | first-prompt listing in `darwin sessions` | Gap; gated (second model call; rename gated 2026-09-02) | gated |
| Persistent PTY sessions (S3) | persistent shell, background jobs | Gap; gated (new process model) | gated |
| Guardian automatic approval review (S2c) | `auto` mode classifier | **Parity in kind** | none |
| `tools.update_plan.enabled` default off (S2c) | `update_plan` always registered | Difference without evidence of harm | none |

### Ranked iteration directions

`Score = 2 × Importance + Architecture fit + Evidence confidence − Implementation difficulty − Implementation risk`, gate 6.

| Rank | Proposed ID | Direction | Importance | Architecture fit | Evidence confidence | Implementation difficulty | Implementation risk | Score | Evidence and rationale |
|---:|---|---|---:|---:|---:|---:|---:|---:|---|
| 1 | SER-071 | Sensitive-path reads are never silent: `fileEditor view` and whitelisted bash readers (`cat`/`head`/`tail`/`grep`/`rg`/`find`/`ls`/`wc`) whose path arguments resolve into a fixed sensitive set (`~/.ssh`, `~/.aws`, `~/.gnupg`, `~/.netrc`, `~/.kube/config`, `~/.docker/config.json`, `.env*`, darwin's own `~/.darwin/config.json` via `isSensitiveDarwinPath`, `/etc/shadow`) become `dangerous` reads — prompted in `default`/`auto`/`plan`, denied in headless, no allow-rule offered — while every other read stays `safe` and byte-identical | 4 | 4 | 4 | 3 | 3 | **10** | S1 (2.1.257). Darwin: `assessRisk` read branch, `assessBashRisk` argument scan already exists for `find`/`git` (SER-053 shape), `isSensitiveDarwinPath` and `ENV_FILE` already name the write-side set. Safety: the trajectory (`src/trajectory/`) records tool results, so a silent read persists the secret on disk. Not adopted from the peer: "outside the working directory" as the criterion — darwin legitimately reads `/tmp`, `/etc/os-release`, global skills; a fixed path set is bounded and explainable. Plan mode keeps "reads proceed" for every non-sensitive read |
| 2 | SER-072 | `/status` gains one `hooks` row: active hook source files (`RuntimeInfo.hookSources`, `MAX_STATUS_NAMES` + `… N more`), `none` when empty, plus the shadow-notice count — a pure projection over the existing accessor, no new channel, `/help`/docs unchanged except the row list | 2 | 5 | 5 | 1 | 1 | **12** | S2, S5 (`/hooks` in Codex and Gemini CLI). Darwin: `hookSources` loaded (`runtime.ts:392`) and unrendered (`rg hookSources src/tui` empty); `/status` decision row ("a formatter over existing accessors, never a new information channel"). Hooks run commands on tool events, so what is armed must be visible in-session, not only in `darwin doctor` |
| 3 | SER-073 | Terminal title: the TUI sets the window/tab title (OSC 2) to a bounded `darwin · <project basename> · <state>` where state is `idle`, `working`, `waiting for approval` or `N queued`, written only when stdout is a TTY, through the same raw-write seam as BEL/OSC 52, updated on state transitions only (no tick), restored to the project basename on exit/`/clear` handover, disabled by one config key `terminalTitle: false`; headless never writes | 3 | 4 | 4 | 2 | 2 | **10** | S2 (`/title` items include app name, project, spinner, status, task progress). Darwin: no title write today; `terminal-bell.ts`/`copy-command.ts` are the precedents for out-of-frame sequences; `App.tsx` already owns `status` and the permission queue. Value is highest with several darwin tabs — the only surface visible when the tab is not focused. Not adopted: a configurable item picker (Codex) — fixed composition, one boolean |
| 4 | SER-074 | Prompt-cache miss cause: per completed model call, when the previous call had cache reads and this one reads ≈0 while the request was large, derive one bounded `likely cause` from facts darwin already holds — `/model` since last call, `/effort` since last call, `/compact` since last call, idle longer than the configured TTL (`5m`/`1h`), first call after resume — else `unknown`; shown on the `/usage` last-turn block and `/status`; plus one bounded notice when `/model` or `/effort` is run while the cache is warm (last call < TTL ago and had cache reads), naming the uncached re-read that follows; no new row/tick/channel, unknown metrics stay `not reported` | 3 | 4 | 4 | 3 | 2 | **9** | S1 (2.1.260), S1b (invalidation list, warm-cache confirmation). Darwin: `cacheEffectivenessRows`, `lastTurnUsage`, `recordCompletedCall`, `changeModel`, `changeThinkingEffort`, `promptCacheTtl`. SRF-025's evidence (1.9 M prompt tokens for 1.6 K output) shows what an uncached re-read costs here. Claude-only (`prompt-cache.ts` gate: OpenAI reports no explicit cache points) — the derivation is silent when cache counters are unreported |
| 5 | SER-075 | Continuable children: a settled `subagent` dispatch's `Agent` (conversation only — its bash session is stopped as today) is retained in a bounded per-runtime store (last `MAX_RETAINED_CHILDREN = 4` settled dispatches, dropped on `/clear`, `/rewind`, shutdown, or eviction) and the parent-only `subagent` tool accepts `continue: "<dispatch id>"` to send a follow-up task into that conversation as a **new** dispatch record (new id, `continuedFrom` on the record, same concurrency cap, same report projection, same heartbeat rows); unknown/evicted/running ids are one bounded tool error; `workflow` nodes are never retained; headless identical | 4 | 3 | 4 | 4 | 3 | **8** | S2 (`/agent` "continue work in a spawned subagent thread"), S1 (resumable background agents). Darwin: `subagent-tool.ts:175–230` drops the child in `finally`; `dispatch-registry.ts` records; 2026-09-03 run rated this 9 and deferred it "until SER-064" — both SER-064 and SER-070 are `done`. Fit 3: retaining an `Agent` past settlement is new lifecycle state and touches the "records never carry child transcript" contract (the store holds the Agent, the record still never carries the transcript). Ordered last so the four bounded directions land first |

### Above the gate, not queued (five-direction cap)

| Direction | Score | Why not queued this run |
|---|---:|---|
| `/side` / `/btw` ephemeral side question: one fresh Agent in `runtime.ts` over a copy of the current messages, same tools (prefix intact for the cache) with a deny-all intervention, question carried in the user message, answer shown as a distinct transcript block and recorded as a new trajectory record, nothing appended to the main conversation (S2) | 6 (I 3, F 3, E 4, D 4, R 3) | Meets the gate exactly; excluded by the cap. Open questions for a future run: busy-time behaviour (refuse or queue), headless form, replay reducer |
| `/skill-doctor` analogue: per-session `load_skill` names and catalogue byte cost in `/status` (S1) | 8 (I 2, F 3, E 4, D 2, R 1) | Needs a new per-tool-name counter (`call-stats.ts` counts uses per call, not per name); darwin's catalogue is small and cached, so the saving is marginal |
| Configurable context-offload threshold (`contextOffloadThreshold`), the `bashOutputMaxChars`/`output_token_limit` analogue (S1, S2c) | 8 (I 2, F 4, E 4, D 2, R 2) | Operator convenience with no darwin-side evidence of demand; the SDK default has not been reported as too small |

### Gated-out directions

| Direction | Score | Gate | Why it fell below | Kept anyway? |
|---|---:|---:|---|---|
| Open the draft in `$VISUAL`/`$EDITOR` (S2b, S5 `/editor`) | 4 (I 3, F 2, E 4, D 4, R 4) | 6 | Requires suspending Ink raw mode, spawning an editor with inherited stdio and re-entering the frame; permission box/menu/search key ownership and the pty suites all interact with it | no |
| `/keymap` remappable shortcuts (S2) | 3 (I 2, F 2, E 4, D 4, R 3) | 6 | Keys are hard-wired across `InputBox.tsx`/`App.tsx` with a documented precedence order (`/help`); a binding layer is a new subsystem | no |
| LLM-generated session titles (S3; Claude Code `session_name` AI title) | 4 (I 2, F 2, E 4, D 3, R 3) | 6 | A second model call per session against the "no second model call" stance of memory/recall; `/rename` was gated 2026-09-02 for the store's read-only contract | no |
| Persistent interactive PTY sessions with wait reasons (S3) | 3 (I 3, F 2, E 3, D 5, R 4) | 6 | A new process model beside the persistent shell and background jobs; the load-bearing process-exit row would be rewritten | no |
| Automatic `/recap` of what happened while away (S2c) | 4 (I 2, F 3, E 3, D 3, R 3) | 6 | Resume already seeds the full replayed transcript (load-bearing § Resumed-session full transcript); a summary would be a second model call | no |
| Planning tool off by default (S2c) | 2 (I 1, F 2, E 4, D 1, R 4) | 6 | `update_plan` is load-bearing for live progress rows; no darwin evidence it harms | no |

### Recommendation

- Batch to implement: `SER-071, SER-072, SER-073, SER-074, SER-075`
- Start with: `SER-071`
- Why this order: SER-071 is the safety fix and goes first regardless of the formula (SER-072 scores higher only because it is trivial). SER-072 and SER-073 are small bounded projections that prove the loop on the new tree. SER-074 is a derivation over existing counters. SER-075 is the only direction that adds lifecycle state and goes last so a failed acceptance cannot block the bounded four. No direction depends on another.
- Independently observable acceptance, per direction:
  - SER-071: `spike/verify-permission-modes.ts` (in `pnpm test`) gains cases proving `fileEditor view` and `cat`/`head`/`rg` on each sensitive path are `dangerous` with the path named, `~`/`$HOME` forms included, in `default`, `auto` and `plan` (prompt, not deny), that `suggestRules` offers no rule for them, that `cat README.md`, `ls ~/.ssh/../` resolving outside the set and every previously-safe fixture stay `safe` byte-identical; headless run with a sensitive read is `permission denied`; `pnpm typecheck`, `pnpm test`, `pnpm build`; load-bearing § Permissions gains the read-side sentence (AGENTS.md has 27 B of headroom — compress the existing gate row's text rather than adding a row).
  - SER-072: `spike/verify-status-command.ts` asserts the `hooks` row for zero, one, and more-than-`MAX_STATUS_NAMES` sources and the shadow count; the report stays byte-identical elsewhere; `pnpm test`.
  - SER-073: a free pty scenario (or `verify-terminal-title.ts` with a fake writer) proves the exact OSC 2 bytes for each state transition, no write when stdout is not a TTY or `terminalTitle: false`, the restore sequence at exit; `spike/verify-tui.ts completion` unaffected; `pnpm test`, `pnpm build`.
  - SER-074: `spike/verify-usage.ts` (or sibling) proves each cause from a synthetic call sequence and `unknown`/silent branches; `/model`/`/effort` warm-cache notice text under a fake clock; the OpenAI-provider path prints nothing new; `pnpm test`.
  - SER-075: `spike/verify-subagents.ts`/new `verify-continuable-children.ts` proves a continuation reaches the retained conversation (the child answers from prior context without a re-brief), eviction at 5, refusal of unknown/running/evicted ids, `/clear` and `/rewind` drop the store, `workflow` nodes never retained, records never carry transcript; `pnpm test`, `pnpm build`; docs: load-bearing § Subagents and `docs/architecture/sub-agents.md`.
- Backlog updates: `docs/research/backlog/directions-081-100.md` + SER-071…SER-075 appended as `not-started` at priorities 96–100 (fills the page exactly; no rollover, no index change); nothing set `abandoned`; SER-071 changed to `in-progress` only immediately before developer handoff.

### Batch iteration outcome

One row per direction handed to the loaded `developer` skill, appended as each direction closes.

| Direction | Child session and managed tasks | Host acceptance (exact independent checks and outcomes) | Commit | Final status | Iteration-log entry |
|---|---|---|---|---|---|

- Halt condition that ended the loop: _(pending)_
- Aggregate token spend across every child delegation: _(pending)_
- Remaining backlog after this run: _(pending)_
