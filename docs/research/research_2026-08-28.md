# Darwin self-evolution research — 2026-08-28

## Run — 2026-08-28T13:03:31Z

### Backlog-first decision

- Backlog router inspected first: `docs/research/backlog_index.md`
- Metadata searched across routed priority pages: `docs/research/backlog/directions-001-020.md`, `docs/research/backlog/directions-021-040.md`, `docs/research/backlog/directions-041-060.md`
- Unfinished directions found: none — a status grep for `` Status: `not-started` `` / `` Status: `in-progress` `` across all three routed pages returned zero matches; every record is `done` or `abandoned`
- Decision: fresh research

### Research path

```text
research-path: tui
focus: TUI interaction and visual polish
share: 20% (weight 2 of 10)
draw: 3 of 20 half-units
path-source: roll
rolled-at: 2026-08-28T13:03:31.283Z
weights: tui=2 observability=0.5 sdk=1 open=1.5 peer=5
```

The script was run exactly once, before any source was read.

### Source availability and method

Self-review path: the evidence for this run is the Darwin repository itself, inspected on
2026-08-28 (UTC) — `src/tui/`, `src/hooks/`, `src/config.ts`, the scope spec
`.trellis/spec/frontend/tui-testing.md` plus `.trellis/spec/frontend/live-frame.md`, the
`spike/` verification suites, and the installed `node_modules/ink/build/parse-keypress.js`
(feasibility of modifier-key input under the exact Ink version Darwin ships). **No peer
product was consulted in this run**; where a direction resembles a known peer behavior the
resemblance is stated as unsourced background, never as a claim about that product.

### Reference sources

No peer sources — self-review path. Repository citations appear inline in the baseline,
gap, and direction tables below in place of URLs.

| Ref | Product | Source type | URL | Accessed (UTC) | Claims supported |
|---|---|---|---|---|---|
| — | none (tui self-review) | repository evidence | — | 2026-08-28 | see repository paths cited per claim |

### Peer highlights and innovations

Not applicable — no peer product was opened. The table is intentionally not padded.

### Current Darwin baseline

| Capability / architecture | Repository evidence | Current behavior |
|---|---|---|
| Composer horizontal movement | `src/tui/App.tsx:2087–2099` (`moveHorizontal(…, key.leftArrow ? -1 : 1, …)`) | Left/Right move exactly one grapheme; there is no word-jump of any kind |
| Modifier chords beyond the fixed set | `src/tui/App.tsx:2132` (`if (key.ctrl || key.meta || key.escape || typed === '') return;`) | Every meta/alt chord is silently ignored; Ctrl chords other than a/e/k/u/w/b/c/d/o/r fall through to the same ignore |
| Word deletion | `src/tui/prompt-editor.ts:178` (`deleteWordBefore`), wired to Ctrl+W at `src/tui/App.tsx:1982` | Backward word delete exists; there is no forward word delete and no word movement to pair with it |
| Editor primitives | `src/tui/prompt-editor.ts` exports (`layoutEditor`, `insertAtCursor`, `moveHorizontal`, `moveToRowEdge`, `killToRowEdge`, `deleteWordBefore`, `snapCursor`, `cellWidth`) with `Intl.Segmenter` grapheme segmentation at `src/tui/prompt-editor.ts:328` | Pure, tested (`spike/verify-prompt-editor.ts`†) grapheme-aware functions; a word-boundary primitive would live beside `deleteWordBefore` |
| Ink key parsing (installed version) | `node_modules/ink/build/parse-keypress.js:465` (`metaKeyCodeRe` → meta+char), modifier-bitflag parse (`key.ctrl = !!(modifier & 4)`, `key.meta = key.meta || !!(modifier & 10)`) and kitty-protocol alt/meta mapping (line 273) | Alt+Left/Right (`CSI 1;3D/C`), Ctrl+Left/Right (`CSI 1;5D/C`) and Alt+B/F all reach `useInput` with `key.meta`/`key.ctrl` set — the input layer already delivers what the app ignores |
| Kill chords | `src/tui/App.tsx:1972–1980` (`killToRowEdge` on Ctrl+K/U) | Destructive with no recovery: a mistyped Ctrl+U over a long draft is gone; no undo primitive exists anywhere in `src/tui/` (grep for `undo` is empty) |
| Draft-restoring modes | `src/tui/prompt-history-search.ts` (Ctrl+R snapshots draft/cursor, Escape restores), `src/tui/prompt-recall.ts` | Restoration exists only *inside* those modes; ordinary editing has no undo |
| Attention when unattended | grep for `\x07` / `\x1b]` / bell / OSC across `src/` is empty | Darwin never signals the terminal: a permission prompt or a finished turn in a background tab/pane is discovered only by looking |
| Lifecycle observation points | `src/hooks/lifecycle-hooks.ts:8–18` (`TurnComplete` outcome/source, `PermissionRequest`), driver-published per AGENTS "Native lifecycle command hooks" | The exact moments an attention signal would fire are already first-class, driver-owned events (external commands only today) |
| Config surface | `src/config.ts` (`AppConfig`, `DEFAULTS`, `numberField`/boolean fields, e.g. `contextOffload: true` at line 328) | Adding one boolean/enum config key follows an established, tested pattern (`spike/verify-config.ts`†) |
| Paste handling | `src/tui/App.tsx:2146–2150` (`usePaste` → `normalizeDraftText` → insert), non-bracketed fallback at 2134–2141 | Any paste, however large, becomes literal draft text; rendering stays bounded by `draftWindow`/`hiddenDraftNotice` (`src/tui/frame-budget.ts`), so the frame survives but cursor travel across a 300-line paste is grapheme-by-grapheme |
| Recorded scope decisions | `.trellis/spec/frontend/live-frame.md` (markdown is a projection, `_underscore_` deliberately unrecognized; SER-027 queue contract; one-frame budget), `.trellis/spec/frontend/tui-testing.md` multiline input contract | Checked each proposal below against these; none re-proposes a recorded deliberate omission |

### Comparison and gaps

Self-review framing: "peer evidence" is replaced by what a Darwin user experiences today.

| User/operator experience | Darwin evidence | Parity / gap / deliberate difference | Implication |
|---|---|---|---|
| Moving the cursor across a long prompt means holding an arrow key; readline/zsh/emacs muscle memory (Alt+B/F, Ctrl/Alt+Arrow) does nothing | `App.tsx:2087` single-grapheme `moveHorizontal`; `App.tsx:2132` meta ignore; parser evidence that the chords do arrive | Gap — the input layer delivers events the app discards | Add word-boundary movement/deletion primitives in `prompt-editor.ts` and wire the chords |
| A mistyped Ctrl+U/Ctrl+K/Ctrl+W destroys draft text irrecoverably | `App.tsx:1972–1994`; no undo primitive in `src/tui/` | Gap — the only "undo" is retyping | Bounded in-editor undo of destructive edits |
| A permission prompt or finished turn in an unfocused terminal goes unnoticed | empty grep for BEL/OSC in `src/`; `lifecycle-hooks.ts` events exist but only run external commands | Gap — the TUI itself never signals | One config-gated BEL at the existing event points |
| A large paste is editable only grapheme-wise; the draft window shows a slice | `App.tsx:2146`; `frame-budget.ts` `draftWindow` | Partial parity — frame safety is already solved; only editing ergonomics suffer | Word-wise movement (above) largely covers the pain; a collapse-chip was considered and gated out (below) |

### Ranked iteration directions

| Rank | Proposed ID | Direction | Importance | Architecture fit | Evidence confidence | Implementation difficulty | Implementation risk | Score | Evidence and rationale |
|---:|---|---|---:|---:|---:|---:|---:|---:|---|
| 1 | SER-042 | Word-wise composer navigation and deletion: Alt/Ctrl+Arrow and Alt+B/F word jumps plus Alt+Backspace/Alt+D word deletes, as grapheme-aware `prompt-editor.ts` primitives beside `deleteWordBefore`, preserving completion/queue/recall/permission key ownership | 4 | 5 | 5 | 2 | 1 | 15 | `App.tsx:2087/2132`, `prompt-editor.ts:178/328`, Ink parser lines 273/465 prove the events arrive and the primitive slot exists; pure-function change verified by extending `spike/verify-prompt-editor.ts` plus a pty scenario |
| 2 | SER-043 | Config-gated terminal attention bell: one BEL when a permission prompt is published and one when a turn completes, emitted at the existing driver lifecycle points (`TurnComplete`/`PermissionRequest`), never inside the Ink frame, off by default, no OSC/title writes | 3 | 4 | 4 | 2 | 2 | 10 | `lifecycle-hooks.ts:8–18` already models both moments; `config.ts` boolean pattern; BEL is invisible to ANSI-stripped assertions so existing pty suites stay byte-stable |
| 3 | SER-044 | Bounded composer undo: Ctrl+_ (and Ctrl+-) restores the draft/cursor state before the most recent destructive edit(s), a small capped snapshot stack owned by the editor state, cleared on submit/queue/clear, never touching recall/search snapshots | 3 | 4 | 4 | 3 | 2 | 9 | `App.tsx:1972–1994` destructive chords with no recovery; `prompt-history-search.ts` shows the snapshot-restore pattern already trusted in this codebase |

### Gated-out directions

| Direction | Score | Gate | Why it fell below | Kept anyway? |
|---|---:|---:|---|---|
| Collapse a large multi-line paste into a bounded draft chip expanded literally at submit | 5 | 6 | Importance 2 (frame safety and hidden-row honesty are already solved by `draftWindow`/`hiddenDraftNotice`; residual pain is cursor travel, which SER-042 addresses), Difficulty 4 and Risk 3 (chip state must stay truthful across recall, queue take-back, trajectory and replay) | no |
| Open the draft in `$EDITOR` (Ctrl+X Ctrl+E) | 3 | 6 | Suspending/restoring Ink raw-mode and the live frame around an arbitrary external process: Difficulty 4, Risk 4 against Importance 3, Fit 2 | no |
| Syntax highlighting inside fenced code blocks | 3 | 6 | Importance 2, Difficulty 4, Risk 3: needs a highlighter with no new dependency, and must hold the markdown projection's byte-stability contract (`live-frame.md`) across streamed fence pieces | no |

### Recommendation

- Batch to implement: SER-042, SER-043, SER-044
- Start with: SER-042
- Why this order: pure score order; no dependencies between the three (each touches a
  disjoint seam: editor primitives, driver lifecycle emission, editor state).
- Independently observable acceptance:
  - SER-042 — extended `spike/verify-prompt-editor.ts` word-boundary cases (graphemes,
    emoji, CJK, punctuation runs) pass; a free pty scenario proves Alt/Ctrl+Arrow moves
    and Alt+Backspace deletes word-wise while completion menu / queue take-back / recall
    still own their keys; `pnpm typecheck`, `pnpm test`, `tui completion`/`recall` stay green.
  - SER-043 — with the option off (default), pty output is byte-identical around
    permission/turn boundaries (no `\x07` present); with it on, `\x07` appears exactly
    once per permission publication and once per turn completion, never mid-frame row;
    config validation covered in `spike/verify-config.ts`; lifecycle suite stays green.
  - SER-044 — editor unit checks: destructive chord then undo restores text+cursor
    exactly, capped stack drops oldest, submit/queue/`/clear` clear it; pty scenario:
    type, Ctrl+U, Ctrl+_ restores; recall/search Escape behavior unchanged.
- Backlog updates: SER-042 appended as `not-started` at Priority 60 to
  `docs/research/backlog/directions-041-060.md` (closing that page's range); SER-043 and
  SER-044 appended as `not-started` at Priorities 61–62 to the new
  `docs/research/backlog/directions-061-080.md`, with exactly one new route added to
  `backlog_index.md`. Gated directions recorded only here. SER-042 flips to `in-progress`
  immediately before developer handoff.

### Batch iteration outcome

| Direction | Child session and managed tasks | Host acceptance (exact independent checks and outcomes) | Commit | Final status | Iteration-log entry |
|---|---|---|---|---|---|
| SER-042 | `session-20260828-133204010`; task `bg-b260775b-2125-4863-be93-c6e808e24113` (succeeded, exit 0) | Diff inspected (pure editor primitives only; chords after every existing key owner); `verify-prompt-editor.ts` 43/43, pty `wordNav` 11/11, `cursor` 5/5, `multiline` 9/9, `completion` 67/67, `recall` 22/22, `recallEmpty` 4/4, `queue` 17/17, `historySearch` 11/11, `pnpm typecheck`, full `pnpm test` exit 0, `pnpm build`, diff/tree checks, AGENTS.md 29,452 B < 32 KiB | `c32e5f6` (+ `6c3c7b1` archive, `bf71a13` journal) | `done` | `docs/iteration-log.md` § Batch 64 — SER-042 word-wise composer editing |

- Halt condition that ended the loop: _pending_
- Aggregate token spend across every child delegation: _pending_
- Remaining backlog after this run: _pending_
