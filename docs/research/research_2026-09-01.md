# Darwin self-evolution research — 2026-09-01

## Run — 2026-09-01T14:47:59Z

### Backlog-first decision

- Backlog router inspected first: `docs/research/backlog_index.md`
- Metadata searched across routed priority pages: `docs/research/backlog/directions-001-020.md`, `docs/research/backlog/directions-021-040.md`, `docs/research/backlog/directions-041-060.md`, `docs/research/backlog/directions-061-080.md`
- Unfinished directions found: none — `grep -n "^- Status: \`\(not-started\|in-progress\)\`" *.md` across all four routed pages returned zero matches over 64 records
- Decision: fresh research

### Research path

```text
research-path: tui
focus: TUI interaction and visual polish
share: 20% (weight 2 of 10)
draw: 3 of 20 half-units
path-source: roll
rolled-at: 2026-09-01T14:46:01.644Z
weights: tui=2 observability=0.5 sdk=1 open=1.5 peer=5
```

The script was run exactly once, before any source was read.

### Source availability and method

Self-review path. The evidence is the Darwin repository, inspected on 2026-09-01 (UTC):
`src/tui/App.tsx` (the `useInput` chord ownership chain), `src/tui/prompt-editor.ts`,
`src/tui/help-format.ts`, `src/tui/markdown.ts`, `src/tui/MarkdownText.tsx`,
`src/tui/visual-language.ts`, the scope specs `.trellis/spec/frontend/tui-testing.md` and
`.trellis/spec/frontend/live-frame.md` plus `.trellis/spec/frontend/prompt-recall.md`, the
free `spike/` suites (`verify-help-command.ts`, `verify-markdown.tsx`), and `README.md` /
`README.zh-CN.md`. **No peer product was consulted in this run.** Both scope specs were read
before proposing, so that a recorded deliberate decision is not re-proposed as an improvement:
this run explicitly discards two candidates on exactly that ground (see gaps table).

### Reference sources

No peer sources — self-review path. Repository citations appear inline below in place of URLs.

| Ref | Product | Source type | URL | Accessed (UTC) | Claims supported |
|---|---|---|---|---|---|
| — | none (tui self-review) | repository evidence | — | 2026-09-01 | see repository paths cited per claim |

### Peer highlights and innovations

Not applicable — no peer product was opened. The table is intentionally not padded.

### Current Darwin baseline

| Capability / architecture | Repository evidence | Current behavior |
|---|---|---|
| Shipped composer chords | `src/tui/App.tsx:2026` (Ctrl+A/E), `:2040` (Ctrl+_ / 0x1f undo), `:2052` (Ctrl+K/U), `:2062` (Ctrl+W), `:2074` (Alt+B/F), `:2083` (Alt+D), `:2192` (Ctrl/Alt+Arrow word jump), `:2243` (Alt+Backspace) | Full readline-style editing set including SER-042 word chords and SER-044 undo |
| `/help` — the canonical local discoverability surface | `src/tui/help-format.ts` "editing and session:" block lists only `Home/End or Ctrl+A/E`, `Ctrl+K/U`, `Ctrl+W`, `Ctrl+B`, `Ctrl+C`, `Ctrl+D` | Names **none** of the SER-042 word chords (Alt/Ctrl+Arrow, Alt+B/F, Alt+Backspace, Alt+D) and **not** SER-044's Ctrl+_ undo |
| `/help` pinned assertions | `spike/verify-help-command.ts:55–56` assert exactly `Ctrl+K/U deletes to the row end/start` and `Ctrl+W deletes the previous word` | The check pins the stale subset, so the omission is invisible to the suite |
| README input documentation | `grep -n "Ctrl+" README.md README.zh-CN.md` → one hit each, `README.md:104` / `README.zh-CN.md:104`, both about Ctrl+R history search | Neither README mentions the word chords or undo; SER-030 established that keeping this documentation current is in scope |
| Markdown line vocabulary | `src/tui/markdown.ts:34` `MarkdownLineKind = 'text' \| 'heading' \| 'fence' \| 'code' \| 'rule'` | Block structure is recognized for headings, fences, code and thematic rules only |
| Markdown block markers in prose | `markdownLines` falls through to `inlineSpans` for any other line (`src/tui/markdown.ts:88`); `inlineSpans` documents that bullet `* item` lines deliberately stay plain (`:117–121`) | A `- item` / `1. item` bullet marker, a `> ` blockquote prefix and table `|` pipes render as undifferentiated plain prose |
| Markdown projection contract | `src/tui/MarkdownText.tsx` header, `spanProps` / `rowToneProps`; `.trellis/spec/frontend/live-frame.md:428` | Every character is kept and markers are dimmed in place, so ANSI-stripped output is the committed text and `/export` stays byte-identical — the extension point for new marker kinds already exists |
| Colour degradation | `src/tui/visual-language.ts` header ("markers … survive ANSI stripping, monochrome terminals"); Ink renders through `chalk` (`node_modules/ink/package.json:54`) | Every state carries a text marker, and `NO_COLOR` is honoured by chalk's own support detection — no Darwin-side gap |

### Comparison and gaps

| Peer evidence | Darwin evidence | Parity / gap / deliberate difference | Implication |
|---|---|---|---|
| none (self-review) | `src/tui/help-format.ts` vs. the chord owners at `src/tui/App.tsx:2040`, `:2074`, `:2083`, `:2192`, `:2243` | **Gap** — two shipped, spec-documented editing features (SER-042, SER-044) exist only for a user who reads the specs | A user who presses `/help` to learn the editor is told a subset; the undo that recovers a mistyped Ctrl+U is undiscoverable exactly when it is needed |
| none (self-review) | `src/tui/markdown.ts:34`, `:88`, `:117–121` vs. `MarkdownText.tsx` `spanProps` | **Gap** — the projection has a marker vocabulary but no block-level classification for lists, quotes or tables | Long structured answers (the common shape of Darwin's own output) read as a flat wall of prose; the fix is additive inside the existing kind/span model |
| none (self-review) | `src/tui/markdown.ts:20` ("Syntax highlighting of code by language is explicitly out of scope") | **Deliberate difference** — recorded decision | Not proposed |
| none (self-review) | `src/tui/visual-language.ts` header + chalk's own `NO_COLOR` handling | **Parity** — monochrome/no-colour degradation is already contracted and every state has a text marker | Not proposed |
| none (self-review) | `src/tui/App.tsx:2052` kill chords + `:2040` undo; grep for a kill ring in `src/tui/` is empty | **Gap, but low value** — killed text is recoverable by undo, just not re-insertable elsewhere | Gated out below |

### Ranked iteration directions

`Score = 2 × Importance + Architecture fit + Evidence confidence − Implementation difficulty − Implementation risk`

| Rank | Proposed ID | Direction | Importance | Architecture fit | Evidence confidence | Implementation difficulty | Implementation risk | Score | Evidence and rationale |
|---:|---|---|---:|---:|---:|---:|---:|---:|---|
| 1 | SER-046 | State the shipped word chords and composer undo in `/help` and the README input documentation | 3 | 5 | 5 | 1 | 1 | 14 | `src/tui/help-format.ts` omits `src/tui/App.tsx:2040/:2074/:2083/:2192/:2243`; `spike/verify-help-command.ts:55–56` pins the stale subset; SER-030 already made README input docs part of this surface. Pure projection over fixed facts, no new channel, no runtime behavior change |
| 2 | SER-047 | Extend the markdown projection's block vocabulary: list markers, blockquote prefixes and table pipes as dimmed marker spans | 3 | 4 | 4 | 2 | 2 | 10 | `src/tui/markdown.ts:34` has no list/quote/table kind and `:88` sends those lines through prose `inlineSpans`; `MarkdownText.tsx` `spanProps`/`rowToneProps` is the existing extension point. Must keep every character (`live-frame.md:428`), so `/export` and pty substring assertions stay byte-identical |

Both directions are independent; the cheaper zero-risk one goes first so the second iteration starts from an accepted tree.

### Gated-out directions

| Direction | Score | Gate | Why it fell below | Kept anyway? |
|---|---:|---:|---|---|
| Add a readline kill ring with `Ctrl+Y` yank of the last Ctrl+K/U/W kill | 5 | 6 | Importance 2 — SER-044's undo (`src/tui/App.tsx:2040`) already recovers every kill, so yank only adds re-insertion at another position; Evidence confidence 2 — readline convention only, no repository or user evidence of the need (fit 3, difficulty 2, risk 2) | `no` |

### Recommendation

- Batch to implement: `SER-046`, `SER-047`
- Start with: `SER-046`
- Why this order: independent directions, so the cheaper and strictly non-behavioural one ships first; both are pure presentation projections with no config, permission or trajectory surface
- Independently observable acceptance: SER-046 — `spike/verify-help-command.ts` extended to assert the word-chord and undo lines, `spike/verify-tui.ts completion` still green, README/README.zh-CN input sections name the same chords as `help-format.ts`, `pnpm typecheck` + full `pnpm test` green. SER-047 — `spike/verify-markdown.tsx` asserts list/quote/table classification *and* that concatenated spans equal the input line for every case, `spike/verify-visual-language.tsx` green, an `/export` byte-identity check, `pnpm typecheck` + full `pnpm test` green
- Backlog updates: appended `SER-046` (Priority 65) and `SER-047` (Priority 66) as `not-started` to `docs/research/backlog/directions-061-080.md`; no rollover needed (range 061–080 held 4 records); no direction abandoned by the gate this run; the selected record moves to `in-progress` only immediately before each `developer` handoff

### Batch iteration outcome

| Direction | Child session and managed tasks | Host acceptance (exact independent checks and outcomes) | Commit | Final status | Iteration-log entry |
|---|---|---|---|---|---|
| SER-046 | child session `session-20260901-150906982`; managed task `bg-2e619bcc`; Trellis task `.trellis/tasks/09-01-ser-046-help-word-chords` | `verify-help-command.ts` 34/34, `verify-tui.ts completion` 68/68, `pnpm typecheck` clean, full `pnpm test` exit 0 with zero FAIL lines, `pnpm build` clean, whole diff read, tree clean | `1d57aae` | `done` | `docs/iteration-log.md` Batch 69 |
| SER-047 | child session `session-20260901-152613977`; managed task `bg-382e53bf`; Trellis task `.trellis/tasks/09-01-ser-047-markdown-block-vocabulary` | `verify-markdown.tsx` 129/129, `verify-visual-language.tsx` 69/69, `pnpm typecheck` clean, full `pnpm test` exit 0 with zero FAIL lines, `pnpm build` clean, whole diff read, plus a Host 25-case classification/round-trip probe with zero failures | `ff21afd` | `done` | `docs/iteration-log.md` Batch 70 |

- Halt condition that ended the loop: **batch exhausted** — both accepted directions closed `done` after independent acceptance; no record in the batch remains unfinished
- Aggregate token spend across every child delegation: `input=168 output=53,588 cacheRead=6,072,445 cacheWrite=163,157` (two children, both reported all four buckets)
- Remaining backlog after this run: nothing unfinished — all 66 routed records are `done` or `abandoned`, so a fresh research run is the recommended next step
