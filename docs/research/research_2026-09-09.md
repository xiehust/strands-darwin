# Darwin self-evolution research — 2026-09-09

## Run — 2026-09-09T09:48:25Z

### Backlog-first decision

- Read `docs/research/backlog_index.md` before product sources. All six routed pages exist: `directions-001-020.md`, `021-040`, `041-060`, `061-080`, `081-100`, `101-120` under `backlog/`.
- Metadata-only scan of headings and exact Status/Priority/Origin report lines found 115 records: 113 `done`, 2 `abandoned`, no unfinished or invalid status. Completed evidence/notes were not used for selection.
- Fresh research is permitted. Highest Priority is 115; latest stable IDs are SER-083 and SRF-032. The initial tree was clean at `4a6362f` on `main`.
- Ran the bundled roll exactly once, before the repository/product survey. The binding result is below; this is not a user override.

### Research path

```text
research-path: peer
focus: comparable coding-agent product analysis
share: 50% (weight 5 of 10)
draw: 14 of 20 half-units
path-source: roll
rolled-at: 2026-09-09T09:48:25.329Z
weights: tui=2 observability=0.5 sdk=1 open=1.5 peer=5
```

### Source availability and method

Primary documentation and public repository pages were accessed with `web_fetch` on 2026-09-09 UTC; web search located sources but its snippets are not implementation evidence. All seven required scopes were verified at documentation/README level, not by running the products. Codex's old features URL redirects across hosts to ChatGPT Learn; the Markdown page below supplied the actual readable content after the HTML projection was dominated by navigation. Claude's long interactive reference was bounded; only the retrieved controls are claimed. Penguin performance/price marketing was not independently verified and is not adopted as fact. No SDK release survey is required on this peer roll.

### Reference sources

| Ref | Product | Source type | URL | Accessed (UTC) | Claims supported |
|---|---|---|---|---|---|
| S1 | Claude Code | Primary interactive reference | https://code.claude.com/docs/en/interactive-mode | 2026-09-09 | Ctrl+S stashes/restores prompt text, cursor and pasted content; Ctrl+G or Ctrl+X Ctrl+E opens an external editor. |
| S2 | Codex | Primary CLI guide (redirect target) | https://learn.chatgpt.com/docs/codex/cli.md | 2026-09-09 | Local review without tree mutation; resume, images, subagents, MCP, permissions/sandbox controls; external editor via VISUAL/EDITOR. |
| S3 | DeepSeek harness | Primary repository README | https://github.com/deepseek-ai/deepseek-harness/blob/master/README.md | 2026-09-09 | Cordis everything-is-a-plugin architecture; developer preview explicitly warns of breaking changes; local Web UI. |
| S4 | OpenCode | Primary TUI guide | https://opencode.ai/docs/tui/ | 2026-09-09 | `/editor` via EDITOR; file references inject content; Git-backed undo; local session switching and opt-in attention controls. |
| S5 | kiro-cli | Primary CLI chat guide | https://kiro.dev/docs/cli/chat/ | 2026-09-09 | `/editor` and quoted `/reply`; multiline input; user `!` commands; directory-scoped resume and picker. |
| S6 | PenguinHarness | Primary repository README | https://github.com/Prism-Shadow/penguin-harness | 2026-09-09 | Describes skill-driven evaluate/optimize rounds with pre-round snapshots and per-request Trace view; local-first multi-agent app platform. This is a documented feature claim, not an independent benchmark. |
| S7 | Aider (additional peer) | Primary command reference | https://aider.chat/docs/usage/commands.html | 2026-09-09 | Ctrl+Y yanks cut text; Ctrl+X Ctrl+E and `/editor` compose prompts; `/test` includes failing command output; `/diff` and history search. |

### Peer highlights and innovations

| Product | Sourced feature / innovation | Reference | Why it matters (inference) |
|---|---|---|---|
| Claude Code | One-chord draft stash, preserving cursor/pasted content | S1 | A long unfinished prompt need not be destroyed to ask an intervening question. |
| Codex | Dedicated read-only review plus terminal automation/delegation | S2 | Review is an explicit workflow, separate from mutation. |
| DeepSeek harness | Everything, including agent capabilities, assembled through plugins | S3 | Broad extensibility, but preview compatibility risk argues against transplanting its runtime. |
| OpenCode | External editor and Git-backed undo | S4 | Familiar editing/recovery gestures; their side effects must not be confused with Darwin's conversation-only rewind. |
| kiro-cli | External editor with optional quoted previous response | S5 | Longer prompts can use a user's normal editor. |
| PenguinHarness | Evaluation/optimization with snapshots and request traces | S6 | Audit evidence should survive each iteration, not only its final report. |
| Aider | Cut/yank composer and failure-focused test command | S7 | Moving text is distinct from undoing an edit; selective context can save irrelevant output. |

### Current Darwin baseline

The survey starts from `AGENTS.md` and its load-bearing index; the relevant frame, clipboard, prompt recall/queue, SDK reuse, permissions, subagent and trajectory sections of `docs/architecture/load-bearing-decisions.md` were read before proposing changes. Referenced historical `frontend/live-frame.md` is not present in the checkout; the current load-bearing doc and runnable pty suites are the available contracts. No replacement spec hierarchy is invented.

| Capability / architecture | Repository evidence | Current behavior |
|---|---|---|
| SDK extension boundary | `src/agent/runtime.ts` (`create`, Agent assembly); architecture § SDK reuse | SDK owns the loop. Driver-local input work needs no SDK/provider modification. |
| Permission and delegation | `src/agent/permission.ts` (`PermissionGate`); architecture §§ Permissions/Subagents; README feature list | Tool-name/input gate, labelled children, bounded dispatch state; reads parallel, writes serialized. No evidence that peer plugin architecture warrants replacement. |
| Durable state and audit | Architecture §§ Session trajectory/Prompt recall; `src/trajectory/`; README §§ Evolution/Durable work | Observer trajectory, project sessions, on-demand memory, supervised commits and iteration log already provide an audit trail. |
| Composer edits | `src/tui/prompt-editor.ts` (`insertAtCursor`, `killToRowEdge`, `deleteWordBefore/After`, `pushUndo`, `popUndo`); `spike/verify-prompt-editor.ts` | Grapheme-aware editing; 16 undo snapshots restore entire drafts, not the cut text at another cursor. Row kills intentionally exclude newline/soft-wrap boundaries. |
| Keyboard ownership | `src/tui/App.tsx` (`useInput`, `applyDestructive`, editor/image refs); `src/tui/help-format.ts` | Global controls, permission, compaction, search own keys before composer. Ctrl+Y/Ctrl+S have no composer handler. No cut-text register or explicit unsent-draft stash exists. |
| Queue versus unsent work | Architecture §§ Prompt recall/The prompt queue; `App.tsx` (`submit`, queue take-back); `spike/verify-tui.ts` queue/recall/undo | Busy Enter queues actual future work; Up takes it back. Recall cannot replace a typed draft; undo clears on ownership changes. Neither preserves one unsent draft across an intervening submission. |
| Image ownership and frame bounds | Architecture §§ Clipboard image input/TUI frame budget; `App.tsx` (`attachedImageRef`, `clipboardReadGeneration`, frame calculation); `spike/verify-clipboard-image.ts`, pty clipboardImage | One transient image travels with draft/queue; literal-text-only recording. Counted rows and one frame budget; no hidden extra image slot is safe by default. |
| Existing product surface | `src/commands/custom-commands.ts` built-in catalogue; README use section | No `/editor`; `/export` explicitly excludes editor launch. `/tangent` branches conversation, not draft state. |
| Recent relevant history | `git log -- src/tui/prompt-editor.ts`: `a22d72d`, `c32e5f6`, `e7f6b00`; baseline HEAD `4a6362f` | Undo followed word navigation/deletion and readline chords; targeted additions should extend those primitives, not replace the editor. |

### Comparison and gaps

| Peer evidence | Darwin evidence | Parity / gap / deliberate difference | Implication |
|---|---|---|---|
| Aider yank (S7) | `applyDestructive`, `insertAtCursor`, undo stack | Gap: undo restores a whole snapshot; cannot relocate killed text while retaining later edits. | Add a one-entry bounded cut register, not a kill ring or system clipboard. |
| Claude draft stash (S1) | `editorRef`, `attachedImageRef`, queue/recall contracts | Gap: no deliberate parking of an unsent draft. This is not SER-044 undo or SER-083 tangent. | Preserve one exact editor/image snapshot; never silently overwrite it or submit it. |
| Five peers' editor support (S1/S2/S4/S5/S7) | Built-in catalogue; `/export` comment; Ink frame contract | Real gap, but inherited terminal subprocesses add raw-mode, redraw, process-lifecycle and credential boundary work. | Below-gate candidate this run; no speculative terminal handoff. |
| Codex review/delegation/MCP/sessions (S2) | README; permission/subagent/trajectory contracts | Substantial parity through ordinary prompts and tools; dedicated review UX is not independently justified here. | Do not duplicate existing capabilities merely for command-name parity. |
| DeepSeek plugin runtime (S3) | SDK reuse invariant | Deliberate architecture difference. | Do not fork/replace Strands. |
| Penguin snapshots/Trace (S6) | Trajectory, developer workflow and iteration log | Audit-oriented parity, different UI. | No generic evaluation-platform direction: SER-005 already covers that heading. |
| OpenCode inline files/Git undo (S4) | `@` inserts paths only; `/rewind` never undoes workspace | Deliberate safety differences. | Keep existing contracts rather than imitate side effects. |

### Ranked iteration directions

`Score = 2 × Importance + Architecture fit + Evidence confidence − Difficulty − Risk`; gate 6. Ratings reflect this run's evidence, not a target score. Metadata heading search found no existing yank/stash direction. SER-010/014/015/027/039/041/042/044/046/083 are adjacent, not duplicates.

| Rank | Proposed ID | Direction | Importance | Architecture fit | Evidence confidence | Difficulty | Risk | Score | Evidence and rationale |
|---:|---|---|---:|---:|---:|---:|---:|---:|---:|---|
| 1 | SER-084 | Ctrl+Y reinserts the last killed text in the current draft from one bounded register | 3 | 5 | 5 | 2 | 2 | 12 | S7; existing pure editor helpers and `applyDestructive` supply a narrow seam. Useful text movement without restoring unrelated edits. |
| 2 | SER-085 | Ctrl+S parks/restores one unsent draft and cursor, including its transient image | 3 | 5 | 5 | 3 | 3 | 10 | S1; App editor/image refs and queue boundaries. One slot with overwrite refusal avoids data loss; implement after SER-084 so ownership resets cover the new register. |

### Gated-out directions

| Direction | Ratings (Importance/Fit/Confidence/Difficulty/Risk) | Score | Gate | Why it fell below | Kept anyway? |
|---|---|---:|---:|---|---|
| External editor for prompts | 3/3/5/5/5 | 4 | 6 | Strong peer evidence but terminal suspension/redraw, subprocess cancellation, cross-platform quoting and GUI wait semantics create a larger boundary than today's editor-local changes. | no |
| Replace Strands assembly with an everything-is-a-plugin runtime | 1/1/3/5/5 | -4 | 6 | S3 does not demonstrate a Darwin user problem; conflicts with the SDK-loop invariant. | no |

Already-shipped or deliberate differences in the comparison table are excluded, not re-added or abandoned a second time.

### Recommendation

- Batch: **SER-084, then SER-085**, Priorities 116–117 in `backlog/directions-101-120.md`; both meet the selection gate without re-rating. No rollover or mutable index summary.
- Requirements and acceptance are fully recorded in those two sections. SER-085 follows SER-084 because stash ownership must reset the new cut register as well as undo; otherwise they are separate user capabilities.
- Implementation stays in the loaded `developer` boundary: fresh source-launched child per direction, no dependency additions, SDK/Ink patch changes, user configuration mutations, or unrelated refactoring. Host owns report/backlog/log closure and the final single push.
- Each child derives a requirement-to-test checklist and verifies it using pure editor/state cases and real offline pty interaction, then the full project gate. Host independently inspects changes and reruns acceptance plus build before `done`.
- Required neighboring checks: `spike/verify-prompt-editor.ts`, `verify-help-command.ts`, `verify-frame-budget.ts`; pty `undo`, `wordNav`, `queue`, `historySearch`, `completion`; image/stash work also covers clipboard, recall, clear and rewind. New behavior requires its own pty scenarios including keyboard precedence and narrow screens. No new live model test is needed for local editor behavior.
- Exactly one record becomes `in-progress` immediately before its handoff. Research/status/log edits are committed so the next child starts from clean, verified HEAD. AGENTS.md is at its 32,768-byte cap and is out of mutation scope.

### Batch iteration outcome

| Direction | Child session and managed tasks | Host acceptance (exact independent checks and outcomes) | Commit | Final status | Iteration-log entry |
|---|---|---|---|---|---|

- Halt condition: not yet reached; supervision pending.
- Aggregate child token spend: not yet incurred.
- Remaining batch: SER-084 and SER-085. No other unfinished backlog record existed at selection.
