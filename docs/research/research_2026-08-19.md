# Darwin self-evolution research — 2026-08-19

## Run — 2026-08-19T01:20:26Z

### Backlog-first decision

- Backlog inspected first: `docs/research/backlog_index.md`
- Unfinished directions found: none — every row through SER-023 is `done` or `abandoned`
- Decision: fresh research

### Research path

```text
research-path: peer
focus: comparable coding-agent product analysis
share: 50% (weight 5 of 10)
draw: 18 of 20 half-units
path-source: roll
rolled-at: 2026-08-19T01:20:26.957Z
weights: tui=2 observability=0.5 sdk=1 open=1.5 peer=5
```

### Source availability and method

Web search and direct HTTP fetch were available on 2026-08-19 UTC. Repository inspection came
first: source, specs, the backlog, prior research runs (to avoid re-proposing shipped or gated
directions), and the working tree's state. Product facts below come only from primary
documentation or the publisher's own repository, fetched today; qualitative implications are
explicitly Darwin-focused inference. Claude Code's `--resume` session picker is widely described
but was **not** verified from a primary page today, so no claim about it is made — the session
direction rests on Codex and OpenCode plus Claude Code's `-c` per-directory continue, which was
seen only in a search snippet and is likewise **not** relied on.

**Working-tree note (context, not a research claim):** at invocation start the repository had
nine modified files and two untracked screenshots — an uncommitted follow-up to SER-023 (finished
`fileEditor` rows now carry the complete diff instead of a bounded excerpt; README gained a
Features section) produced by a dogfooding darwin session on 2026-08-18 evening, visible in the
untracked `image.png`/`image-1.png`. On that dirty tree `pnpm typecheck` exits 0 and `pnpm test`
exits 0 with 46 suite summaries all `passed, 0 failed`. The work is not this run's to commit;
the implementation loop halts on it (see the batch report), and the directions below were rated
against the tree as it stands.

### Reference sources

| Ref | Product | Source type | URL | Accessed (UTC) | Claims supported |
|---|---|---|---|---|---|
| S1 | Claude Code | Primary product docs (interactive mode) | <https://code.claude.com/docs/en/interactive-mode> | 2026-08-19 | "Shell mode with `!` prefix": "Run shell commands directly without going through Claude by prefixing your input with `!`"; shell mode "Adds the command and its output to the conversation context", "Shows real-time progress and output", "Doesn't require Claude to interpret or approve the command", supports `Ctrl+B` backgrounding and history-based autocomplete over previous `!` commands in the current project; exit with `Escape`/`Backspace`/`Ctrl+U` on an empty prompt. Separately: messages typed while Claude works are **queued** (listed above the input box), sent between tool calls or as the next turn, and `Up` from the first line takes queued entries back into the editor. |
| S2 | Codex | Primary product docs (CLI commands reference) | <https://learn.chatgpt.com/docs/developer-commands?surface=cli> | 2026-08-19 | "Prefix a line with `!` to run a local shell command under the current approval and sandbox settings"; `Tab` while working queues a follow-up prompt/slash command/shell command for the next turn; `Enter` while working injects new instructions into the current turn; `Ctrl+O` or `/copy` copies the latest completed output; `Esc` twice with an empty composer edits the previous message. |
| S3 | Codex | Primary product docs (CLI features) | <https://developers.openai.com/codex/cli/features> | 2026-08-19 | "`codex resume` — Return to a saved chat. Reopen a recent chat from the current repository, or search across local chats"; "`codex --search` — Switch a run to live web search when a task depends on current releases, documentation, or external behavior. Search activity stays visible in the transcript"; `codex cloud` moves work to a configured cloud environment; the composer's starter list names `/init`, `/status` ("show current session configuration"), `/permissions`, `/model`, `/review`. |
| S4 | DeepSeek Harness | Primary product page | <https://deepseek.com/harness/en/> | 2026-08-19 | Developer preview; every capability is a swappable Cordis plugin (models, tools, skills, sessions, sandboxes, storage, loops, scheduling, UI); everything the model sees is recorded in an append-only session log inspected by source in the Trajectory view, with resume/fork/search/replay over the same stream; four runtime modes — notably **Minimal** keeps "only a shell tool and a file editor for benchmarking models in a minimal environment" and **Creator** inspects the runtime and tests plugins in memory. |
| S5 | PenguinHarness | Publisher repository README | <https://github.com/Prism-Shadow/penguin-harness> (raw `README.md` on `main`) | 2026-08-19 | Desktop app and CLI share one `~/.penguin/data` root; four built-in skill groups; the self-evolution loop takes "a snapshot before every round" with every request observable in the Trace view. No claim beyond what the 2026-08-18 runs already recorded; accuracy/cost figures remain publisher claims and support nothing here. |
| S6 | OpenCode | Primary product docs (TUI) | <https://opencode.ai/docs/tui/> | 2026-08-19 | "Bash commands — Start a message with `!` to run a shell command… The output of the command is added to the conversation as a tool result"; `/sessions` (aliases `/resume`, `/continue`) lists and switches between sessions with a keybind. |

### Peer highlights and innovations

| Product | Sourced feature / innovation | Reference | Why it matters |
|---|---|---|---|
| Claude Code | `!` shell mode: user-typed commands run directly, without model interpretation or approval, with live output that joins the conversation context; history-based autocomplete over past `!` commands | S1 | The user can gather ground truth (`git status`, a failing test) without spending a model turn, and the model then sees exactly what the user saw. |
| Codex | `!` prefix runs a local shell command **under the current approval and sandbox settings** | S2 | Same affordance, but Codex routes it through its safety envelope — the two peers disagree on whether user-typed commands bypass policy. |
| OpenCode | `!` runs a shell command whose output enters the conversation **as a tool result** | S6 | A third independent peer; also a third answer to *how* the output enters context (context text vs. tool result). |
| Codex | `codex resume` reopens a recent chat from the current repository or searches across local chats | S3 | Session recovery is a picker/search over a per-project store, not just a "last session" pointer. |
| OpenCode | `/sessions` lists and switches sessions in-session | S6 | Confirms listing/selection (not only most-recent resume) is the peer norm. |
| Codex | `/status` shows current session configuration; `--search` makes live web search visible in the transcript | S3 | Consolidated status is a one-look answer; web search is an explicit, transcript-visible mode rather than an ambient capability. |
| Claude Code | Messages typed while the agent works are queued, delivered between tool calls or as the next turn, and can be taken back | S1 | Documented queueing semantics — relevant only as the recorded counterpoint to Darwin's deliberate no-queue contract (SER-010). |
| DeepSeek Harness | Minimal mode strips the runtime to a shell tool + file editor for benchmarking; Creator mode tests plugins in memory | S4 | Runtime variants stay first-class; no new Darwin-actionable claim beyond what earlier runs recorded. |
| PenguinHarness | Snapshot-per-round self-evolution with Trace observability | S5 | Unchanged since 08-18; nothing new actionable. |

### Current Darwin baseline

| Capability / architecture | Repository evidence | Current behavior |
|---|---|---|
| Prompt submit path | `src/tui/App.tsx:2024` (`if (!input.startsWith('/')) return []` in `computeCompletions`); the submit handler recognizes only `/` commands | Everything not starting with `/` is a model prompt. There is no user-typed shell escape: to run `git status` the user switches terminals, and what they saw never reaches the conversation unless they paste it. |
| Shell machinery already present | persistent shell owned by the runtime (`runtime.shutdown()` reaps it; AGENTS.md "Process exit" row); `src/tools/background-bash.ts`; permission gate classifies **model** tool calls (`src/agent/permission.ts`) | Execution, bounding and cleanup for shell commands exist, but only the model can drive them; the gate's subject is tool calls the model makes, so a user-typed command is a different authorization category that today simply does not exist. |
| Session recovery | `--resume` follows the `last-session.json` pointer only (`src/agent/session.ts:20`, `src/cli-args.ts:160`); `--session <id>` requires already knowing the id; `darwin trajectory list` (`src/trajectory/`) lists only sessions recorded with trajectory on, offline, with no resume affordance | Only the most recent session is recoverable without hand-copying an id from the sessions directory; there is no list of this project's sessions with enough context (when, what about) to pick one. |
| Session status visibility | header owns live model/mode rows (`src/tui/App.tsx:955-967`); `/usage`, `/context`, `/mode`, `/permissions`, `/mcp`, `/effort` each project one slice | The pieces of "what is this session running as" exist but are spread over six commands plus the header; nothing answers in one place, and headless `-p` runs have no equivalent at all. |
| Web access | `src/tools/` contains only `background-bash.ts` and `image-viewer.ts`; no fetch/search tool | The model reaches the web only through gated `bash` (`curl`), which is real but unstructured; there is no darwin-owned web tool. |
| Busy-submit contract | SER-010 (`docs/research/backlog_index.md`), `research_2026-08-18.md:92` | "Busy Enter retains rather than queues" is a shipped, recorded product contract; the 08-18 run already declined to re-propose queueing. |
| Overflow / compaction | `SummarizingConversationManager` (`src/agent/runtime.ts:433,441`), `/compact` (`src/agent/compact.ts`) | Peer auto-compaction behavior is already covered by SDK-native overflow recovery plus explicit `/compact`. |

### Comparison and gaps

| Peer evidence | Darwin evidence | Parity / gap / deliberate difference | Implication |
|---|---|---|---|
| `!` shell passthrough in all three TUI peers (S1, S2, S6) | no non-`/` prefix exists; `App.tsx:2024` | **Gap** — the only tri-peer interactive affordance Darwin still lacks after the 08-18 batches | Add a user-typed shell escape. Peers disagree on policy (Claude Code bypasses approval, Codex applies it) and on context shape (context text vs. tool result), so Darwin must decide both explicitly rather than copy blindly. |
| `codex resume` picker/search (S3); `/sessions` list-and-switch (S6) | `last-session.json` pointer only; `--session <id>` blind | **Gap** — recovery exists, selection does not | List this project's sessions with human context and resume by choice. The snapshot store and trajectory reader already hold everything needed; no model call. |
| `/status` session configuration (S3) | six partial projections plus the header | **Gap**, mild — information exists but has no single seam | A read-only `/status` composed from existing state, on the `/mcp` precedent. |
| `codex --search` live web search (S3) | gated `bash` + `curl` only | Gap, but the workaround is gated and real | Scored below the gate this run — see gated-out table. |
| Queue-while-working (S1, S2) | SER-010's recorded no-queue contract | **Deliberate difference** — recorded twice already | Not re-proposed. Only a user product decision can reopen it. |
| Append-only trajectory, resume/fork/search/replay (S4); snapshot-per-round (S5) | `src/trajectory/` (SER-003/006/007), `.trellis/spec/backend/session-trajectory.md` | Parity | Nothing to do. |

### Ranked iteration directions

`Score = 2 × Importance + Architecture fit + Evidence confidence − Implementation difficulty − Implementation risk`

| Rank | Proposed ID | Direction | Importance | Architecture fit | Evidence confidence | Implementation difficulty | Implementation risk | Score | Evidence and rationale |
|---:|---|---|---:|---:|---:|---:|---:|---:|---|
| 1 | SER-024 | Run a user-typed shell command from the prompt with a `!` prefix: executed directly (user-authorized, not a model tool call), live bounded output in the TUI, the command and its bounded output entering the conversation and the trajectory record honestly — permission gate, frame budget and no-queue contract untouched | 4 | 4 | 5 | 3 | 3 | **11** | S1 + S2 + S6: all three TUI peers ship it; Darwin has none (`App.tsx:2024`). The runtime already owns a persistent shell with reaping (AGENTS.md "Process exit"), the trajectory records `userInput` lines (prompt recall depends on them — a `!` entry's recall/exclusion semantics must be decided, not left emergent), and output must be bounded before it enters context (SER-009 vocabulary). The peers' policy disagreement is the design decision to make explicitly: Darwin's gate governs *model* actions, so a user-typed command is the user's own act — but it must be stated on-screen and in the record, never smuggled. Busy-state behavior follows SER-010: while a turn runs, `!` is retained, not queued. |
| 2 | SER-025 | List this project's sessions and resume one by choice: a `darwin sessions` listing (id, age, first user prompt when recorded) over the existing per-project snapshot store, and `--resume <id>` reopening a named session — no model call, no network, `last-session.json` semantics unchanged for bare `--resume` | 3 | 4 | 5 | 2 | 2 | **11** | S3 (`codex resume` — reopen recent from this repository, or search) + S6 (`/sessions`). Darwin evidence: pointer-only `--resume` (`src/agent/session.ts:20`, `cli-args.ts:160-161`), blind `--session <id>`, and `darwin trajectory list` covering only trajectory-on sessions. The snapshot store already keys sessions per project; first-prompt context can come from the trajectory record where present and degrade to "not recorded" where absent (prompt-recall's absence rule). Sessions from other projects stay invisible, exactly like prompt recall. |
| 3 | SER-026 | One consolidated read-only `/status` report: model/provider, cache and effort, permission mode and live allow-rule count, MCP server states, skills count, session id, trajectory/diagnostics state, process token spend and context estimate — composed from existing state only, on the `/mcp` read-only-projection precedent, with a headless twin in `-p` structured output left explicitly optional | 2 | 5 | 4 | 2 | 1 | **10** | S3 documents `/status` as "show current session configuration". Darwin evidence: the header owns live model/mode rows (`App.tsx:955-967`) but rule counts, MCP states, trajectory/diagnostics state and spend live behind six separate commands. Everything already has an in-process accessor (`runtime.usage`, `gate.allowRules`, the MCP registry's read-only projection, config fields); the command is a formatter, not a new information channel. Importance is honestly low — nothing is *unknowable* today — which is why it ranks last. |

Batch order is the table order: SER-024 → SER-025 → SER-026. No hard dependencies; ordered by score, then by user-visible value.

### Gated-out directions

| Direction | Score | Gate | Why it fell below | Kept anyway? |
|---|---:|---:|---|---|
| Dedicated web search/fetch tool (S3's `codex --search`) | 5 (I 3, F 3, E 4, D 4, R 4) | 6 | The model already reaches the web through gated `bash` + `curl`, so importance is convenience, not capability; a darwin-owned web tool opens an untrusted-content channel into context whose trust boundary (what gets fetched, how results are bounded and attributed) needs its own design, and provider-side search parity on Bedrock is unverified | no |
| Queue messages while the agent works (S1, S2) | not scored | 6 | Conflicts with the shipped, recorded no-queue busy-submit contract (SER-010; reaffirmed in `research_2026-08-18.md:92`). Scoring it would present a settled product decision as an open direction; only an explicit user decision can reopen it | no — recorded as a deliberate difference |
| Vim editor mode / transcript viewer / spell check / emoji shortcodes (S1) | 5 (I 2, F 3, E 5, D 4, R 3) | 6 | Editor-mode features with real key-ownership cost (the exact risk SER-015 avoided by dropping `Ctrl+R`) against niche demand in a TUI that already has multiline editing, completion and recall | no |
| `Ctrl+B`-style migration of a running foreground tool call to background (S1) | 2 (I 2, F 2, E 4, D 4, R 4) | 6 | Darwin's background tasks are model-directed (`src/tools/background-bash.ts`); moving an in-flight SDK tool call would intercept the agent loop, which the first load-bearing decision forbids | no |

## Addendum — 2026-08-19T02:01:06Z, user decision reopening queue-while-working

The `01:20:26Z` run above recorded queue-while-working as **not proposed** because it conflicts
with the shipped no-queue busy-submit contract (SER-010) and "only an explicit user decision can
reopen it". That decision has now been made: the user directed, verbatim, "add
queue-while-working in backlog" (2026-08-19). This addendum records the direction the run
declined to score, using the same run's peer evidence (S1, S2); nothing in the run above is
rewritten, and no rating is being restated to cross the gate — the direction was never rated.

| Rank | Proposed ID | Direction | Importance | Architecture fit | Evidence confidence | Implementation difficulty | Implementation risk | Score | Evidence and rationale |
|---:|---|---|---:|---:|---:|---:|---:|---:|---|
| — | SER-027 | Queue messages typed while a turn runs, deliberately superseding SER-010's no-queue busy-submit contract: queued entries listed visibly, sent when the turn ends, and retrievable back into the editor — permission/compaction key ownership, prompt recall's `Up` semantics and the frame budget all preserved | 4 | 3 | 5 | 4 | 3 | **9** | S1: Claude Code queues messages typed while it works, lists them above the input box, sends them between tool calls or as the next turn, and `Up` from the first line takes them back into the editor. S2: Codex `Tab` queues a follow-up for the next turn. Darwin evidence: busy Enter *retains* the draft (`SER-010`, `b11c281`); the specs and suites that pin that contract (`.trellis/spec/frontend/live-frame.md`, `spike/verify-prompt-editor.ts`) must be updated deliberately, never worked around. Difficulty 4 and fit 3 are the honest costs: queued rows are new counted frame rows above the input, and `Up` is already shared by completion menu, cursor movement and prompt recall (SER-015), so take-back must join that ownership chain without eating either. Delivery shape (mid-turn injection vs. next-turn only) is an implementation decision — peers disagree, and next-turn-only is the smaller safe scope. |

Reopened by explicit user product decision; joins the current batch after SER-026.
