# Darwin self-evolution research — 2026-08-16

## Run — 2026-08-16T02:44:00Z

### Backlog-first decision

- Backlog inspected first: [`docs/research/backlog_index.md`](./backlog_index.md)
- Unfinished directions found: `SER-003` (not-started, priority 3), `SER-004` (not-started, 4), `SER-005` (not-started, 5); no `in-progress` row
- Decision: work the existing batch without fresh research — the three unfinished rows share the origin report [`research_2026-08-15.md`](./research_2026-08-15.md) and are therefore one batch, and the skill forbids consulting product sources while any unfinished row exists

> No product-research section belongs in this run. The peer evidence for `SER-003` is the one recorded in [`research_2026-08-15.md`](./research_2026-08-15.md)'s `2026-08-15T15:19:24Z` run (S3, DeepSeek Harness: an append-only event stream is what powers trajectory inspection, resume, fork, search and replay).

### Score gate at selection

Applied to every row of the batch before spending an iteration: `SER-003` = 8, `SER-004` = 7, `SER-005` = 6, all at or above `MINIMUM_IMPLEMENTATION_SCORE = 6`. No row was gated out, and no rating was restated.

### Starting point verified before handoff

Clean working tree, `468b095` at HEAD containing the previously accepted `404aa1c`, `pnpm typecheck` clean and `pnpm test` exit 0 with 25 suites reporting `0 failed`. Recorded because a failure inherited from the previous iteration must not be attributable to the next child.

### Selected direction

`SER-003` — add an append-only session trajectory over SDK events, plus search, fork and replay.

### Darwin evidence for this direction

| Area | Repository evidence | Current behavior and gap |
|---|---|---|
| The single event seam | [`AgentRuntime.send`](../../src/agent/runtime.ts) — was `yield* this.agent.stream(input)` | Every driver (TUI `App.tsx`, `headless.ts`, `dev-repl.ts`) streams through this one method, so one observer covers all three without touching the SDK loop. |
| What persistence exists | [`createSessionManager`](../../src/agent/session.ts), `saveLatestOn: 'invocation'` | A snapshot is *rewritten* every turn and describes what the conversation **is**; nothing recorded what it **did**, so a tool call later compacted away left no trace. |
| Serialization seam | SDK stream event classes' `toJSON()` | Returns `type` plus payload and deliberately excludes `agent`/`invocationState` — the one projection that cannot capture the live `Agent`. Measured by the child, now recorded in [`strands-sdk-contracts.md`](../../.trellis/spec/backend/strands-sdk-contracts.md). |
| Where new per-session state goes | `BackgroundBashManager`'s `<sessionsDir>/<id>/background`, the offloader's `offload/` | An established sibling convention, so `trajectory.jsonl` needed no new path scheme. |
| Degradation rules | [`error-handling.md`](../../.trellis/spec/backend/error-handling.md) | Per-domain skip-and-surface table already dictates that an observer's failure becomes a notice, never a turn failure. |
| Rendering projection | [`turnReducer`](../../src/tui/turn-state.ts) | Replay reuses the live reducer, so replay and the live frame cannot drift into two readings of one session. |
| Frame-height constraint | header comments in [`App.tsx`](../../src/tui/App.tsx), `spike/verify-tui.ts approve` | Forbids an always-on header line, which is why `/trajectory` is a report and the hint line was extended by comment only. |

### Recommendation

- Batch to implement: `SER-003` → `SER-004` → `SER-005`
- Start with: `SER-003`
- Why this order: persisted selection order, and it is the lowest-risk of the three (an observer that cannot influence a turn) while `SER-004` is the batch's riskiest row.
- Independently observable acceptance: append-only under two real turns; a partial trailing line tolerated; caps enforced with truncation recorded; a write failure degrading to a notice; pass-through of the event sequence; search hit/miss honesty; fork leaving its source byte-identical; replay with zero model calls; plus `pnpm typecheck`, `pnpm test`, `verify-tui.ts completion`, `git diff --check` and Trellis validation.
- Backlog updates: `SER-003` set `in-progress` immediately before handoff and `done` after acceptance; `SER-004`/`SER-005` remain `not-started`; no new directions proposed and none gated out this run.

### Host decisions made during supervision

The child's plan raised eight questions; all were resolved from the requirement or repository precedent rather than escalated. The load-bearing ones: recording defaults **on** (snapshots and background logs are already written unconditionally, and recording changes nothing the model sees — unlike `contextOffload`, which is why that one defaults off); `--session <id>` becomes valid in interactive mode (the old restriction guarded nothing, and a fork whose id exists only on stdout would otherwise be unopenable in the TUI); fork copies `offload/` and fails if that copy fails; reasoning is recorded as presence only, never text; the semantic event allowlist is accepted because the dropped counts are written down.

### Batch iteration outcome

| Direction | Child session and managed tasks | Host acceptance (exact independent checks and outcomes) | Commit | Final status | Iteration-log entry |
|---|---|---|---|---|---|
| `SER-003` | child session `session-20260816-024553753`; planning `bg-a832b48a-b6ac-4882-a2ca-3212d5a60dc0` (succeeded), implementation + commit `bg-7a24a529-fc26-4211-8740-dd7da07a4398` (succeeded). Both turns in that one persisted conversation. | Inspected the commit (36 files; `runtime.ts`, `stream.ts`, `writer.ts`, `App.tsx`, `InputBox.tsx`, `cli-args.ts`, `config.ts`, `custom-commands.ts` diffs read in full), then re-ran `pnpm typecheck`; `pnpm test` (26 suites, 0 `FAIL`, exit 0, including `verify-trajectory.ts` 148 passed); `verify-trajectory.ts` standalone (148 passed); `verify-tui.ts completion` (25 passed, network-free); the model-calling `verify-tui.ts approve` (23 passed) because `MAX_COMPLETIONS` and `App.tsx` changed; `git diff --check` clean; Trellis validation `✓`. Plus an independent live end-to-end in a scratch git project with real Bedrock turns: turn 1 recorded 8 records / 2,501 bytes, turn 2 appended to 3,892 with the first 2,501 bytes **byte-identical** and `seq` 0–12 strictly increasing; `trajectory replay` correct with `AWS_REGION` unset and bogus credentials; `search` printing 4 hits (exit 0) and `no matches` (exit 0); `fork` producing a new id while source trajectory, source snapshot and `last-session.json` stayed byte-identical, the fork replaying both turns, carrying a `forkedFrom` record, and answering a follow-up from the forked conversation's own memory; and `trajectory: false` writing no file, with `trajectory list` reporting `no trajectory recorded (snapshot only)`. | `af791f9` | `done` | [`Batch 7 — append-only session trajectories`](../iteration-log.md#batch-7--append-only-session-trajectories-2026-08-16) |
| `SER-004` | Not handed off — no child session exists. | No acceptance was attempted. The Host reported the halt to the user, who closed the row. | — | `abandoned` — explicit user product decision, 2026-08-16 ("把 SER-004、SER-005 标记成放弃"), recorded on the backlog row with the four unanswered decisions that had blocked it. Not a gate closure: its score (7) was above the gate. | — |
| `SER-005` | Not handed off — no child session exists. | No acceptance was attempted. | — | `abandoned` — explicit user product decision, 2026-08-16, same instruction. Unblocked and next in line at the time; its score (6) sat exactly at the gate, so the gate is not what closed it. | — |

- Findings the child reported that changed the design, verified in the diff: SDK `toJSON()` emits the **wire** shape, so a first-draft reasoning strip matching `type === 'reasoningBlock'` never fired and reasoning text would have reached disk; batch-time timestamps were replaced by observation-time stamps after the live run exposed them; and `AgentResult.toString()` already carries a child's reasoning into parent context, which predates this change and is documented rather than papered over.
- Halt condition that ended the loop: **the batch is exhausted**, but by two different endings rather than one. Iteration stopped at `SER-004` on section 7 "only the user can decide" — four decisions (supported isolation backend, credential exposure inside the sandbox, what isolation is promised once the project must be mounted read-write, and authorization to re-implement the persistent-shell/process-group/exit-fallback paths) had no answer in the requirement or in repository evidence, and `SER-004`'s own note demanded a design spike first, so implementing through it would have meant inventing scope. The user then closed both remaining rows as `abandoned`. Every row of the batch is now `done` or `abandoned`, so nothing is left to iterate.
- Aggregate token spend across every child delegation: `input=452`, `output=186,849`, `cacheRead=61,821,979`, `cacheWrite=574,633` (planning `input=82 output=31,440 cacheRead=4,729,330 cacheWrite=155,669`; implementation `input=370 output=155,409 cacheRead=57,092,649 cacheWrite=418,964`). No task reported a `-` metric. `SER-004` and `SER-005` spent nothing: neither was delegated.
- Remaining backlog after this run: nothing unfinished — `SER-001`–`SER-003` `done`, `SER-004`/`SER-005` `abandoned`. A next invocation therefore performs fresh product research. Two of that run's inputs are already known: an isolated execution backend and an evaluation corpus were closed by decision rather than refuted, so either may be re-proposed under a new ID, `SER-004`'s four open decisions must be answered before it is re-queued, and neither closed row may be silently reopened.

## Run — 2026-08-16T10:52:35Z

### Backlog-first decision

- Backlog inspected first: [`backlog_index.md`](./backlog_index.md)
- Unfinished directions found: **none** — `SER-001`–`SER-003` `done`, `SER-004`/`SER-005` `abandoned` by explicit user decision
- Decision: **fresh research**, because no `in-progress` or `not-started` row exists

### Research path

Rolled once, before any source — repository or product — was opened. Verbatim script output:

```text
research-path: observability
focus: logging and observability
share: 12.5% (weight 1 of 8)
draw: 1 of 8
path-source: roll
rolled-at: 2026-08-16T10:52:35.648Z
weights: tui=1 observability=1 sdk=1 open=1 peer=4
```

The 12.5% self-review path came up rather than the 50% `peer` path. It was not re-rolled and not overridden.

### Source availability and method

This is a **self-review path**, so the evidence is this repository: source files, the spec, and one
real recorded `trajectory.jsonl` on this host. **No peer product was consulted** — no URL was
fetched, no product documentation was read, and no competitive claim is made anywhere in this run.
The scope's own spec, [`error-handling.md`](../../.trellis/spec/backend/error-handling.md), was read
first (all 150 lines) precisely to avoid re-proposing a recorded deliberate decision, and
[`session-trajectory.md`](../../.trellis/spec/backend/session-trajectory.md) is the contract the
findings below sit against. Where an SDK behaviour is claimed it was read out of the installed
package under `node_modules/@strands-agents/sdk/dist/` on this host (2026-08-16), not from memory.

### Reference sources

| Ref | Product | Source type | URL | Accessed (UTC) | Claims supported |
|---|---|---|---|---|---|
| — | none | — | — | — | Self-review path: peer research is not part of this run's method, and no product was opened. Repository evidence is cited inline as paths, symbols and line numbers below. |

### Current Darwin baseline

| Capability / architecture | Repository evidence | Current behavior |
|---|---|---|
| Turn-failure reporting, interactive | `runTurn` in [`App.tsx`](../../src/tui/App.tsx) (`catch` → `notice`, severity `error`) | A failed turn renders `turn failed: <message>` as a transcript notice. It lives in the Ink scrollback and nowhere else. |
| Turn-failure reporting, headless | [`cli.ts`](../../src/cli.ts) `runHeadless` `catch` | One `error: <message>` line on stderr; nothing on disk. A supervisor that did not capture stderr has no record of why the child died. |
| What a failed turn leaves in the record | [`recordStream`](../../src/trajectory/stream.ts) `finally` → `TurnRecording.end()` ([`writer.ts:139-160`](../../src/trajectory/writer.ts)); `stopReason` assigned **only** at `writer.ts:191` from `agentResultEvent.result.stopReason` | A throw never produces `agentResultEvent`, so the turn closes with `stopReason: undefined` and **no error field of any kind** — `TrajectoryRecordType` in [`record.ts:67-72`](../../src/trajectory/record.ts) has no failure record and `TurnEndedRecord` has no failure field. |
| Per-turn spend in the record | Measured on a real file: `.../session-20260816-034500776/trajectory.jsonl` — recorded `agentResultEvent.result` keys are exactly `['type','stopReason','lastMessage']`, `metrics` **undefined** | The one durable artifact carries no token counts and no model latency. `AgentResult` declares `metrics?: AgentMetrics` and `traces?: AgentTrace[]` (`node_modules/@strands-agents/sdk/dist/src/types/agent.d.ts:331-355`), so the data exists in the SDK and simply does not reach the file. |
| Live spend accounting | `get usage()` in [`runtime.ts:677-700`](../../src/agent/runtime.ts) — "Counts this process only. Sessions persist messages, not metrics"; `lastTurnDelta` set in `send`'s `finally` | `/usage` and the headless `usage` stderr line are process-scoped and evaporate on exit; a resumed session starts its meter at zero, and `docs/iteration-log.md` spend figures are hand-copied out of a child's stderr. |
| Session inventory report | `listSessions` in [`cli-trajectory.ts:169-195`](../../src/cli-trajectory.ts) | Prints records / turns / bytes (+ damage). Cannot say a session failed, and cannot say what it cost. |
| SDK log routing | [`sdk-logging.ts`](../../src/agent/sdk-logging.ts) `routeSdkLogs` | `warn`/`error` are routed to the renderer (a real, deliberate design). `debug` and `info` are hard-wired to `() => {}` with **no config to route them anywhere** — and the SDK logs Bedrock throttling at `logger.debug('throttled | error_message=…')` (`dist/src/models/bedrock.js:1181`; 60 debug/info call sites in total). A session slowed by provider throttling therefore leaves zero evidence. |
| Degradation contract | [`error-handling.md`](../../.trellis/spec/backend/error-handling.md) rows 79-87 | The trajectory's own failure modes are specified in detail — latch, one notice, never a turn failure. What the record must contain about a **turn's** failure is not specified anywhere, which is why the gap is a gap rather than a decision. |
| Telemetry surface left unused | SDK ships `dist/src/telemetry/` and OTLP exporter deps (`package.json:210-216`); `grep -rn "traces\|telemetry\|otel" src` returns nothing | Deliberate-looking but unrecorded; assessed and gated out below rather than proposed. |

### Comparison and gaps

| Scope evidence | Darwin evidence | Parity / gap / deliberate difference | Implication |
|---|---|---|---|
| Spec rows 79-87 govern recorder failure; nothing governs turn failure | `stopReason: undefined` with no error text (`writer.ts:149`, `writer.ts:191`) | **Gap, not a decision** — the spec's silence is the tell | `trajectory replay` of a failed overnight run cannot distinguish "provider threw" from "user cancelled" from "clean turn with a missing stop reason", and never shows the message. The one artifact designed to answer "what happened" cannot answer it for the case where the question is asked most. |
| SDK exposes `AgentResult.metrics` (tokens, cycles, tool stats, latency) | measured absent from the recorded `agentResultEvent`; `runtime.usage` is process-scoped by documented design | Gap | Session spend is unauditable after exit. The self-development loop pays for this directly: `docs/iteration-log.md` numbers come from scraping child stderr. |
| SDK logs throttling/retries and cache-point placement at `debug` | `debug: () => {}`, not configurable | Gap, with a privacy reason to keep it **off by default** | The two "why was that turn slow / why did caching not help" questions have no answer channel at all. |
| SDK ships OTEL exporters | unused | Plausibly correct absence | A single-user local TUI has no collector to export to; assessed and gated out, not queued. |

### Ranked iteration directions

| Rank | Proposed ID | Direction | Importance | Architecture fit | Evidence confidence | Implementation difficulty | Implementation risk | Score | Evidence and rationale |
|---:|---|---|---:|---:|---:|---:|---:|---:|---|
| 1 | SER-006 | Record why a turn ended abnormally: a failed turn's error, distinguishable from cancel, in `turnEnded`, and reported by `list`/`replay` | 4 | 5 | 5 | 2 | 2 | **14** | `writer.ts:149`/`191` + `stream.ts`'s `finally`: the failure path already runs, it just records nothing. Fits the existing observer contract exactly — the error is rethrown untouched, recording stays synchronous and non-throwing. Cheapest possible fix to the record's biggest blind spot. |
| 2 | SER-007 | Record per-turn token usage and duration, and report session spend from the record | 4 | 4 | 5 | 3 | 2 | **12** | Measured `metrics: undefined` in a real recorded file; `deltaUsage`/`lastTurnDelta` already exist in `runtime.send`. Depends on SER-006: the failure path must close a turn honestly before it is worth recording that turn's spend, and a failed turn's tokens are billed too. Must preserve `usageBuckets`' unknown-stays-unknown contract rather than defaulting to 0. |
| 3 | SER-008 | Opt-in per-session diagnostics log for SDK `debug`/`info` plus darwin notices | 3 | 4 | 4 | 3 | 3 | **8** | `sdk-logging.ts` discards both levels with no escape hatch; `bedrock.js:1181` proves throttling is only visible there. Off by default and capped, because debug output can contain conversation-derived material — the same reason `contextOffload` defaults off. |

### Gated-out directions

| Direction | Score | Gate | Why it fell below | Kept anyway? |
|---|---:|---:|---|---|
| Dollar-cost estimation from a bundled provider pricing table (I 3, fit 2, ev 3, diff 3, risk 4) | 4 | 6 | No in-repo source of truth for prices, and a stale table turns a report into a confidently wrong number; Bedrock/Mantle/OpenAI pricing would all have to be maintained by hand | `no` |
| OTLP/OpenTelemetry export through the SDK's `telemetry` module (I 2, fit 2, ev 4, diff 4, risk 3) | 3 | 6 | The capability is real, but a single-user local TUI has no collector; it would add heavy dependencies to serve a use case nobody in this repo has | `no` |

### Recommendation

- Batch to implement: `SER-006` → `SER-007` → `SER-008`
- Start with: `SER-006`
- Why this order: dependency and honesty first. SER-006 makes an abnormal turn describable at all; SER-007 then attaches spend to turns including the ones that failed; SER-008 is a separate, opt-in channel and is last because it is the only one with a privacy dimension. Score order and dependency order agree, so no override is needed.
- Independently observable acceptance: **SER-006** — a turn whose stream throws leaves a record naming the error, distinguishable from `stopReason: 'cancelled'` and from a clean turn; the thrown error still reaches the caller unchanged; recording still cannot throw; `list`/`replay` report the failure; earlier bytes byte-identical. **SER-007** — two real turns record their own token deltas summing to the process meter, an unreported provider metric stays unknown rather than 0, a report aggregates per session, and older `v:1` records without the field stay readable. **SER-008** — off by default writes no file; on, it captures a `debug` line the SDK emits and stays inside its cap; a failure to write degrades to one notice and never fails a turn. All three: `pnpm typecheck`, `pnpm test`, `spike/verify-trajectory.ts`, `git diff --check`, Trellis validation.
- Backlog updates: `SER-006`/`SER-007`/`SER-008` added `not-started` at priorities 6/7/8; no row was set `abandoned` by the gate this run; `SER-006` flips to `in-progress` immediately before handoff.

### Batch iteration outcome

| Direction | Child session and managed tasks | Host acceptance (exact independent checks and outcomes) | Commit | Final status | Iteration-log entry |
|---|---|---|---|---|---|
| `SER-006` | child session `session-20260816-105933280`; planning `bg-8be41a6f-b90b-4b4f-8323-7e78dda10448` (succeeded), implementation + commit `bg-351694eb-52ca-4e3c-acd5-4afcd109209b` (succeeded). Both turns in that one persisted conversation. | Read the whole 16-file diff (`stream.ts`, `writer.ts`, `record.ts`, `replay.ts`, `cli-trajectory.ts` in full), then re-ran `pnpm typecheck` (exit 0); `pnpm test` (26 suites, every suite `passed, 0 failed`, exit 0, zero `FAIL` occurrences); `spike/verify-trajectory.ts` standalone (**203 passed**, up from 148 at HEAD); `spike/verify-tui.ts completion` (25 passed, network-free); `git diff --check` clean; `task.py validate 08-16-turn-failure-record` `✓`. Plus an independent live end-to-end in a throwaway HOME (`/tmp/host-accept-fibA`, session `session-20260816-113529031`): turn 1 clean against real Bedrock (`stopReason: "endTurn"`, no `failure`), turn 2 in the same session against a well-formed but invalid `AWS_BEARER_TOKEN_BEDROCK` failed for real (exit 1, `error: Authentication failed: Please make sure your API Key is valid.`) and recorded no `stopReason` plus `failure: {"name":"ModelError","message":"Authentication failed…","cause":"AccessDeniedException"}`; the file's first 1,375 bytes hashed identically before and after that append (`e381f6f999130933…`); `trajectory list`, `replay`, `search AccessDeniedException` (1 match) and a search miss all exited 0 and named the failure. The Host also checked the bound it had demanded, directly against `formatTurnFailure`: a 9,000-code-point class name plus a 9,000-code-point message renders to exactly 120 code points with no newline, and non-`Error` throws render honestly (`non-error string`, `non-error null`). | `1f2c147` | `done` | [`Batch 8 — a failed turn says so in the record`](../iteration-log.md#batch-8--a-failed-turn-says-so-in-the-record-2026-08-16) |

- Host decisions made during supervision: the child asked exactly two questions and both were answered from the requirement rather than escalated — the live check runs in a temp `HOME` (with the class, message, record line and session id reported so the Host could re-read them), and `list` names the class plus a collapsed, bounded message for the first three failed turns. One boundary correction was sent with the approval: the child was told **not** to touch `docs/research/backlog_index.md` or `docs/iteration-log.md`, because a child marking its own direction `done` would make the record claim an acceptance that had not happened. One extra assertion was demanded and delivered: the `list` bound must hold for a message *and* a class name sitting at the field cap.
- Design change driven by measurement, verified in the diff and live: `Model.streamAggregated` rethrows a `ModelError` untouched but wraps anything else in `new ModelError(message, { cause })`, so without `failure.cause` every real provider rejection would have recorded as an indistinguishable `ModelError`. Confirmed live as `ModelError` + `cause: AccessDeniedException`.
- Finding recorded rather than fixed: turn ordinals restart at 1 in each process (`TrajectoryRecorder.turns`), so a two-run session reports `1 turn(s)` and both turns say `turn 1`, which contradicts the spec's "1-based turn ordinal within this file". Pre-existing since `SER-003`; fixing it changes existing record semantics, so it is noted on the `SER-006` row as a candidate for a later direction instead of being absorbed into this one.
- Token spend for `SER-006`: planning `input=76 output=25,189 cacheRead=3,272,233 cacheWrite=112,707`; implementation `input=228 output=72,593 cacheRead=23,134,319 cacheWrite=261,669`. Direction total `input=304 output=97,782 cacheRead=26,406,552 cacheWrite=374,376`. No task reported a `-` metric.
