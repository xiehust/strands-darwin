# Darwin self-evolution research — 2026-09-15

## Run — 2026-09-15T12:43:40Z

### Backlog-first decision

- Backlog router inspected first: `docs/research/backlog_index.md`
- Metadata searched across routed priority pages: `docs/research/backlog/directions-001-020.md` … `directions-121-140.md` (exact `- Status:` lines: 119 `done`, 2 `abandoned`, none `in-progress`/`not-started`; highest ID `SER-089`/`SRF-032`, highest Priority 121)
- Unfinished directions found: none
- Decision: fresh research

### Research path

Rolled once from the skill directory named by the `load_skill` result (`dist/src/skills/builtin/self-evolution-research/scripts/roll-research-path.mjs`), after backlog routing and before any repository or product source was opened. Verbatim output:

```text
research-path: peer
focus: comparable coding-agent product analysis
share: 50% (weight 5 of 10)
draw: 12 of 20 half-units
path-source: roll
rolled-at: 2026-09-15T12:43:40.689Z
weights: tui=2 observability=0.5 sdk=1 open=1.5 peer=5
```

No override, no second draw.

### Source availability and method

Access date 2026-09-15 (UTC). Product pages were read with `web_fetch`/`curl` against primary documentation, release notes and repositories. Five read-only research subagents opened the per-product sources in parallel and returned quoted claims; the Host re-fetched and grepped every claim that a proposed direction rests on (Claude Code `mcp.md`, `permissions.md`, `hooks.md`, `env-vars.md`; Codex `config-reference.md` and the `rust-v0.154.0` release body; kiro `session-management.md` and `v3/new-features.md`). Claims that only a subagent saw are marked *(subagent-read)* in the tables and no direction rests on them alone. All seven required scopes were reachable: Claude Code, Codex, DeepSeek harness (release `dsh-v0.1.6-alpha.1` published today), OpenCode, kiro-cli, PenguinHarness (`v0.2.11`), plus Gemini CLI and Aider as additional products. PenguinHarness has no in-repo `docs/` directory (GitHub API 404); its release notes were used. Codex `0.155.0` exists only as alphas with empty bodies — nothing is claimed from them.

Repository evidence was read in source: `src/config.ts` (`loadProjectPolicy`, `loadHookLayer`), `src/paths.ts` (`sensitiveDarwinPaths`, `userProjectDir`), `src/mcp/registry.ts` (`loadMcpClients`), `src/agent/session.ts` (`resolveSession`, `writePointer`), `src/trajectory/writer.ts`, `src/cli-main.ts` (`runInteractive`), `src/agents/child-recipe.ts` and `src/agents/loader.ts`, `src/tools/shell-env.ts`, `src/tui/App.tsx` (`/exit`), `docs/architecture/sub-agents.md`, and the AGENTS.md load-bearing table. Prior peer runs (2026-09-07/08/09/13) were re-read for their highlight and gated-out tables so nothing below re-proposes a recorded decision.

### Reference sources

| Ref | Product | Source type | URL | Accessed (UTC) | Claims supported |
|---|---|---|---|---|---|
| S1 | Claude Code | Primary docs (permissions) | https://code.claude.com/docs/en/permissions.md | 2026-09-15 | "`permissions.allow` rules … in a project's `.claude/settings.json` grant capability, so Claude Code applies them only after you accept the workspace trust dialog … The dialog lists the rules and directories the folder would grant so you can review them first"; the "What runs before you trust a folder" table (hooks in settings files, `headersHelper` on `.mcp.json` servers held until trust); trust keyed on the git repository root, stored outside the repo; `-p`/SDK sessions never show the dialog |
| S1b | Claude Code | Primary docs (MCP) | https://code.claude.com/docs/en/mcp.md | 2026-09-15 | "`⏸ Pending approval (run `claude` to approve)`: a project-scoped server from `.mcp.json` that you haven't approved yet"; "A cloned repository can't approve its own servers: `enableAllProjectMcpServers` or `enabledMcpjsonServers` committed to the project's `.claude/settings.json` is ignored in an untrusted folder" (v2.1.196) |
| S1c | Claude Code | Primary docs (hooks) | https://code.claude.com/docs/en/hooks.md | 2026-09-15 | "Claude Code checks workspace trust before it runs any hook from a settings file … Interactive session: Claude Code holds back hooks from every settings file … until you accept the workspace trust dialog" |
| S1d | Claude Code | Primary docs (env vars) | https://code.claude.com/docs/en/env-vars.md | 2026-09-15 | `CLAUDECODE` "Set to `1` in subprocesses Claude Code spawns (Bash and PowerShell tools, tmux sessions, hook commands, status line commands, stdio MCP server subprocesses)"; `CLAUDE_CODE_CHILD_SESSION` likewise for Bash/hook/status-line subprocesses |
| S1e | Claude Code | Changelog *(subagent-read)* | https://raw.githubusercontent.com/anthropics/claude-code/main/CHANGELOG.md | 2026-09-15 | 2.1.271: "Added `omitClaudeMd` to agent frontmatter and `--agents` JSON, letting custom and plugin subagents run without user, project and local CLAUDE.md files"; hook-run spinner with elapsed time; per-command `allowed_domains`; 2.1.269 `bashEditDiffEnabled`; 2.1.267 `maxEffortLevel`; head 2.1.272 |
| S1f | Claude Code | Primary docs (sub-agents) *(subagent-read)* | https://code.claude.com/docs/en/sub-agents.md | 2026-09-15 | "Explore and Plan skip your CLAUDE.md files and the parent session's git status to keep research fast and inexpensive" |
| S2 | Codex | Primary docs (config reference) | https://learn.chatgpt.com/docs/config-file/config-reference.md | 2026-09-15 | "You can also add project-scoped overrides in `.codex/config.toml` files. Codex loads project-scoped config files only when you trust the project."; project entries with `trust_level = "untrusted"` in user-level config |
| S2b | Codex | Release note | https://github.com/openai/codex/releases/tag/rust-v0.154.0 | 2026-09-15 | 0.154.0 (2026-09-09): "Resuming a conversation open in another app now shows a read-only transcript with a retry option while preserving your draft"; "Experimental worktree support … `--worktree` or `/worktree`"; "Answer questions inline while Codex continues working"; 0.155.0 alphas carry no notes |
| S2c | Codex | Primary docs (slash commands) *(subagent-read)* | https://learn.chatgpt.com/docs/cli/slash-commands.md | 2026-09-15 | `/ps` background terminals with three recent lines; `/debug-config` layer diagnostics; Tab-to-queue slash commands; `/goal` |
| S3 | DeepSeek harness | Release notes *(subagent-read)* | https://github.com/deepseek-ai/deepseek-harness/releases/tag/dsh-v0.1.6-alpha.1 | 2026-09-15 | Headless `--session-id` + NDJSON `--json`; remote SSH workspace; "Run Node PTC in separate processes … `process.env` is empty"; image-offload session event |
| S3b | DeepSeek harness | Subsystem docs *(subagent-read)* | https://github.com/deepseek-ai/deepseek-harness/blob/master/docs/subsystems/permission-presets.md , …/compaction.md , …/token-meter.md | 2026-09-15 | Named sandbox+approval presets; compaction bracketed by a durable lock (orphan detectable); token meter states `usage` vs `estimated` baseline |
| S4 | OpenCode | Primary docs (permissions, agents, config) *(subagent-read)* | https://opencode.ai/docs/permissions/ , https://opencode.ai/docs/agents/ , https://opencode.ai/docs/config/ | 2026-09-15 | "`doom_loop` — triggered when the same tool call repeats 3 times with identical input" (default `ask`); "`external_directory` — triggered when a tool touches paths outside the project working directory"; agent "Max steps" summary prompt; `subagent_depth` default 1; newest 8 releases (v1.18.24–31) provider/bugfix-only |
| S5 | kiro-cli | Primary docs (session management) | https://kiro.dev/docs/cli/chat/session-management.md | 2026-09-15 | "Sessions can only be active in one process at a time to prevent conversation corruption"; "When you exit a session, Kiro CLI prints the session ID along with the command to resume it (`kiro-cli chat --resume-id <ID>`). Use `/session-id` mid-session to print the same ID on demand." |
| S5b | kiro-cli | Primary docs (v3) | https://kiro.dev/docs/cli/v3/new-features.md | 2026-09-15 | "On first open, CLI prompts you to trust the workspace. In untrusted workspaces, the agent restricts capabilities (no shell, limited writes) until you explicitly trust it."; default deny of writes to `~/.kiro/settings/` and `.kiro/settings/` "(prevents the agent from modifying its own trust boundaries)" |
| S5c | kiro-cli | Primary docs (v3 permissions) *(subagent-read)* | https://kiro.dev/docs/cli/v3/permissions.md | 2026-09-15 | "Workspace-scoped rules (`~/.kiro/workspace-roots/<hash>/permissions.yaml`) are stored per-user outside the repository, so a cloned repo cannot inject permission rules" |
| S6 | PenguinHarness | Release notes *(subagent-read)* | https://github.com/Prism-Shadow/penguin-harness/releases/tag/v0.2.11 | 2026-09-15 | Compaction-relative context ring; schema-shaped correction guide on rejected tool arguments; idle-based model timeout; per-turn `Elapsed (API, tools)`; ETag/409 on concurrent file edits. No `docs/` directory in the repository (API 404) |
| S7 | Gemini CLI | Reference docs *(subagent-read)* | https://raw.githubusercontent.com/google-gemini/gemini-cli/main/docs/reference/commands.md , …/configuration.md | 2026-09-15 | "When a command is executed via `!` or in shell mode, the `GEMINI_CLI=1` environment variable is set in the subprocess's environment"; `tools.shell.backgroundCompletionBehavior` `silent|inject|notify`; `general.sessionRetention.maxAge`/`minRetention`; `/resume` content search |
| S8 | Aider | Reference docs *(subagent-read)* | https://aider.chat/docs/config/options.html , https://aider.chat/docs/usage/watch.html | 2026-09-15 | `--cache-keepalive-pings`; `--notifications-command`; `AI!`/`AI?` watch-file comments |

### Peer highlights and innovations

| Product | Sourced feature / innovation | Reference | Why it matters |
|---|---|---|---|
| Claude Code, Codex, kiro-cli | **Workspace trust gates repository-supplied executable configuration.** Claude Code holds project allow rules, settings-file hooks, `.mcp.json` server approvals and `headersHelper` commands until the user accepts a trust dialog that *lists what the folder would grant*; trust is keyed on the git root and stored outside the repo; `-p` never shows the dialog. Codex loads `.codex/config.toml` "only when you trust the project". kiro v3 prompts on first open and restricts an untrusted workspace. | S1, S1b, S1c, S2, S5b, S5c | A cloned repository is untrusted input. Anything it can make the agent *execute* at launch (hook commands, MCP server processes) or *pre-authorize* (allow rules) is a supply-chain hole unless the user consents first. |
| kiro-cli, Codex | **One live process per session.** kiro: "Sessions can only be active in one process at a time to prevent conversation corruption"; Codex 0.154.0 shows a read-only transcript when the conversation is open elsewhere, preserving the draft. | S5, S2b | Two writers on one snapshot/trajectory silently corrupt the conversation; the second opener should be told, not allowed to clobber. |
| Claude Code | **Subagents may omit project instructions** (`omitClaudeMd` frontmatter, 2.1.271; built-in Explore/Plan skip CLAUDE.md "to keep research fast and inexpensive"). | S1e, S1f | Every child pays the full instruction file per dispatch; a self-sufficient delegation prompt does not need it. |
| kiro-cli | **Resume hint on exit** — prints the session id and the exact resume command; `/session-id` on demand. | S5 | The id a user needs to come back is shown at the one moment they need it. |
| Claude Code, Gemini CLI | **Environment marker in spawned processes** — `CLAUDECODE=1` (Bash tool, hooks, stdio MCP servers), `GEMINI_CLI=1` for `!`/shell. | S1d, S7 | Scripts, git hooks and test runners can detect an agent host and drop pagers/interactive prompts; hook commands can tell who is calling them. |
| OpenCode | `doom_loop` permission: an identical tool call repeated 3× becomes an `ask`; `external_directory` is its own permission key; agent "Max steps" ends with a summary prompt. | S4 | Complements failure-based retry guards with a repetition guard; darwin already prompts on out-of-tree sensitive reads (SER-071) — noted as near-parity. |
| Codex | Managed worktrees per session (`--worktree`/`/worktree`, experimental); inline questions while the turn continues; `/ps` shows three recent lines per background terminal; Tab queues a slash command. | S2b, S2c | Worktrees are isolation (cf. gated SER-004 shape); `/ps` is darwin parity (SER-060); inline questions would break darwin's next-turn-only queue (SER-027) — recorded, not proposed. |
| DeepSeek harness | Headless `--session-id` + NDJSON; Node PTC in isolated processes with empty `process.env`; compaction bracketed by a durable lock; token meter states `usage` vs `estimated` baseline. | S3, S3b | Headless/NDJSON is darwin parity (SER-011); the lock-and-log compaction shape resembles SRF-027; the meter's explicit baseline kind is near darwin's `contextEstimate()` anchor. |
| PenguinHarness | Compaction-relative context ring; correction guide on rejected tool arguments; idle-based (not wall-clock) model timeout; per-turn `Elapsed (API, tools)` split. | S6 | Web/desktop-centric; the elapsed split is the one idea with a terminal analogue (busy suffix) — deferred, low importance. |
| Gemini CLI, Aider | `backgroundCompletionBehavior` `silent|inject|notify`; session retention `maxAge`/`minRetention`; Aider cache keepalive pings, `--notifications-command`. | S7, S8 | darwin's boolean `backgroundTaskWake` already covers inject/notify; retention and keepalive are scored below. |

### Current Darwin baseline

| Capability / architecture | Repository evidence | Current behavior |
|---|---|---|
| Repository-supplied hooks | `src/config.ts` `loadProjectPolicy` → `loadHookLayer` over `hookExtensionRoots(projectRoot)`; `src/paths.ts` `sensitiveDarwinPaths` lists `.darwin/hooks.json`, `.agents/hooks.json` and `sensitiveHookDirectories` | Project `.darwin/hooks.json`, `.darwin/hooks/*.json`, `.agents/hooks.json` (Codex dialect) and `.agents/hooks/*.json` are loaded at every startup and their `command` strings run via `/bin/sh -c` (`src/hooks/lifecycle-hooks.ts` `spawn`, `src/hooks/hook-process.ts`) on `PreToolUse`/`PostToolUse`/`TurnComplete`/`PermissionRequest`. No consent step: the files are protected from the *model* (sensitive-path classification, SER-071) but not from the *checkout*. |
| Repository-supplied MCP servers | `src/mcp/registry.ts` `loadMcpClients` (reads `<root>/.darwin/mcp.json` else `<root>/.mcp.json`), called from `AgentRuntime.create` (`src/agent/runtime.ts:696`) | Every declared stdio server is spawned at startup (`McpClient.loadServers`) before the first prompt; a cloned repository's `.mcp.json` therefore executes arbitrary commands on `darwin` launch. `/mcp` shows them afterwards (read-only). |
| Repository-supplied allow rules | `loadProjectPolicy`: primary rules from `permissionRulesPath()` = `~/.darwin/projects/<key>/permission-rules.json` (user-owned, outside the repo); **legacy fallback** reads `<root>/.darwin/config.json` `permissionRules` when the primary file is absent | The primary store already matches kiro's non-injectable layout (S5c). The legacy fallback is the hole: on a fresh clone no primary file exists, so a committed `.darwin/config.json` with `permissionRules.allow` grants rules silently (the `~/.darwin/config.json`/`.env*` exclusions still apply). |
| Workspace trust state | none — `rg -i trust src/` finds only data-trust comments | darwin has no notion of a trusted or untrusted project. Headless (`-p`) and TUI behave identically. |
| Session ownership | `src/agent/session.ts` `resolveSession`/`writePointer`; `src/trajectory/writer.ts` (append-only `fs.open`); SDK `SessionManager` writes `snapshot_latest.json` | No lease or lock. Two processes given the same `--resume <id>` (or two bare `--resume` in one project) both restore, both append to `trajectory.jsonl`, and both overwrite `snapshot_latest.json` — last writer wins, and each reads the other's turns only on the next restore. Nothing warns. |
| Exit surface | `src/tui/App.tsx:1716` `/exit`/`/quit` → `exit()`; `src/cli-main.ts` `runInteractive` `finally` (close, shutdown, `forceExitIfHung`) | Nothing is printed after Ink unmounts; the session id is visible only via `/status` during the session or `darwin sessions` afterwards. |
| Child system prompt | `src/agents/child-recipe.ts:69` `composeSystemPrompt(definition.systemPrompt, options.projectInstructions)`; `docs/architecture/sub-agents.md` ("the definition prompt plus the project's `AGENTS.md` instructions"); `src/agents/loader.ts` frontmatter accepts `name`, `description`, `tools` | Every `subagent`/`workflow` child, built-in `general` included, carries the full project instructions (up to `MAX_INSTRUCTIONS_BYTES` = 32 KiB) per dispatch; no definition can opt out. |
| Spawned-process environment | `src/tools/shell-env.ts` `scrubShellEnv` (withholds credential-shaped names, `ALWAYS_SURVIVE_NAMES`), applied by `src/agent/runtime.ts`/`src/tools/background-bash.ts`; `src/tui/shell-command.ts:223` passes `process.env` for `!`; hooks inherit via `spawn` | No variable identifies darwin to a child process; a script cannot distinguish an agent-driven run from an interactive one. |
| Repetition guard | `src/agent/retry-guard.ts` (load-bearing row "Repeated tool failures") | Guards same-signature *failures* (three allowed); identical *successful* calls repeat freely. |

### Comparison and gaps

| Peer evidence | Darwin evidence | Parity / gap / deliberate difference | Implication |
|---|---|---|---|
| Trust dialog before repository-supplied hooks, MCP approvals and allow rules take effect; trust keyed on the git root and stored outside the repo; headless never asks (S1, S1b, S1c, S2, S5b, S5c) | `loadProjectPolicy` runs project hook commands and `loadMcpClients` spawns project MCP servers at `AgentRuntime.create`, legacy project `permissionRules` granted on a fresh clone; no trust state anywhere | **Gap (safety).** Not a recorded decision: the load-bearing rows protect these files from the model, not from the checkout. | One bounded trust gate at startup: list exactly what the checkout would arm, hold it back until the user accepts, remember the answer per project in the user-owned `~/.darwin/projects/<key>/` store. |
| One process per session; the second opener gets a read-only view or a refusal (S5, S2b) | `resolveSession` has no lease; both processes write `snapshot_latest.json` and append `trajectory.jsonl` | **Gap (correctness).** | A per-session lease file with pid/host/start time; a live lease refuses `--resume <id>` with a bounded message and lets bare `--resume` fall back to a fresh session with a notice; a stale lease (dead pid, same host) is taken over. |
| Subagent frontmatter can omit the instruction file; built-in research agents skip it (S1e, S1f) | `child-recipe.ts` always composes project instructions; `loader.ts` has no such key | **Gap (cost).** `docs/architecture/sub-agents.md` records the current composition as behavior, not as an invariant. | One optional boolean frontmatter key on agent definitions; default unchanged; `/agents` listing states it. |
| Exit prints the session id and the resume command (S5) | Nothing printed after `waitUntilExit()` | **Gap (UX).** | One line to stdout after Ink unmounts, only for sessions that recorded a turn; nothing for headless. |
| `CLAUDECODE=1` / `GEMINI_CLI=1` in spawned processes (S1d, S7) | `scrubShellEnv` adds nothing; `!` passes raw `process.env`; hooks inherit | **Gap (interop).** | Set one documented marker (`DARWIN=1`) in every process darwin spawns — model shells, `!`, hooks, MCP stdio servers — via the existing env seams; never a value that leaks state. |
| `doom_loop`: identical call ×3 → `ask` (S4) | Retry guard counts failures only | Gap, low urgency; polling tools (`bash wait`/`status`, `strands_manage_background_task`) are legitimately identical | Deferred (see below), not queued this run. |
| `/ps` three recent lines per background terminal (S2c); `/session-id` (S5); NDJSON headless with session resume (S3); notification command (S8) | SER-060 `/tasks` tails; `/status` shows the session id; SER-011 structured headless; SER-038/SER-043/SER-078 | **Parity.** | None. |
| Inline questions mid-turn (S2b), queue steering | SER-027 next-turn-only queue (load-bearing) | **Deliberate difference.** | Recorded, not proposed. |
| Bash-edit diffs read from disk (S1e `bashEditDiffEnabled`) | Load-bearing "File-edit diffs: diff of the tool *input*, never read from disk" | **Deliberate difference.** | Gated below. |
| Model-graded approvals (Codex auto-review, DeepSeek auto-review preset) (S2c, S3b) | Static classifier + user gate (`src/agent/permission.ts`, load-bearing) | **Deliberate difference** (also gated 2026-09-08 as "auto mode"). | Gated below. |

### Ranked iteration directions

`Score = 2 × Importance + Architecture fit + Evidence confidence − Implementation difficulty − Implementation risk`; gate 6.

| Rank | Proposed ID | Direction | Importance | Architecture fit | Evidence confidence | Implementation difficulty | Implementation risk | Score | Evidence and rationale |
|---:|---|---|---:|---:|---:|---:|---:|---:|---|
| 1 | SER-090 | Workspace trust for repository-supplied executable configuration: on the first interactive launch in a project whose checkout declares hook commands (`.darwin/hooks*`, `.agents/hooks*`), MCP servers (`.darwin/mcp.json`/`.mcp.json`) or legacy `.darwin/config.json` `permissionRules`, show one bounded modal *before* any of it is armed, listing exactly what would run/grant (file, hook event counts, server names + commands, rule count); accept stores `{trusted, decidedAt}` in user-owned `~/.darwin/projects/<key>/trust.json`, decline runs the session without those layers and states the omission once; headless with no stored decision holds them back and writes one `trust:` stderr line (+ `run.started.trust` in structured output), never a dialog; user-global layers and skills/commands (prompt content) are unaffected; `/status` gains the trust state on its existing hooks/MCP rows | 5 | 4 | 5 | 3 | 3 | 13 | S1, S1b, S1c, S2, S5b, S5c; darwin `loadProjectPolicy`, `loadMcpClients` at `AgentRuntime.create`, `sensitiveDarwinPaths` already enumerates the exact files. Three peers converge; the `<key>` store is the one kiro (S5c) uses so a clone cannot self-approve. Difficulty is the ordering (decision before `create()` loads policy/MCP, `StartupScreen` already owns the terminal) and the headless contract. Risk: pty suites that start in a project with `.mcp.json` (this repo) need the stored decision or a flag. |
| 2 | SER-091 | Single live process per session: `resolveSession` acquires a per-session lease (`~/.darwin/sessions/<key>/session/<id>/lease.json`: pid, hostname, startedAt) with `wx`; a live lease (same host, pid alive) makes explicit `--resume <id>`/`--session <id>` a refusal in the `SessionNotFoundError` shape naming the pid and start time, and makes bare `--resume` start a fresh session with one notice; a stale lease (dead pid or foreign host older than a stated bound) is taken over and stated; released in `runtime.shutdown()` and by the unref'd exit fallback; `/clear` successor re-leases; `darwin sessions` marks a leased row `(open in pid N)` read-only | 4 | 4 | 4 | 2 | 3 | 11 | S5 ("only … one process at a time to prevent conversation corruption"), S2b (read-only view when open elsewhere); darwin `session.ts` has no lease, snapshot is last-writer-wins. Risk is the stale-lease path: it must never lock a user out (pid liveness, never a manual unlock file). |
| 3 | SER-092 | Resume hint on exit: after `waitUntilExit()` in `runInteractive`, when the session recorded at least one completed turn, write one line to stdout — `session <id> · resume: darwin --resume <id>` — after Ink has released the terminal; nothing for headless, nothing when no turn ran; `/status` already carries the id | 2 | 5 | 5 | 1 | 2 | 11 | S5 (kiro exit hint); darwin `cli-main.ts` prints nothing on exit, `runtime.info.sessionId` and the turn count are already available. Risk: pty suites that assert the final screen after `/exit`. |
| 4 | SER-093 | Agent definitions may omit project instructions: optional frontmatter `projectInstructions: false` (default `true`, unchanged) in `src/agents/loader.ts`; `buildRecipeChild` passes `undefined` to `composeSystemPrompt` for such a definition; `/agents` listing states `no project instructions` on that row; the `tools`-style validation error for a non-boolean value; built-in `general` unchanged | 3 | 4 | 4 | 2 | 2 | 10 | S1e (`omitClaudeMd`, 2.1.271), S1f (Explore/Plan skip CLAUDE.md); darwin `child-recipe.ts:69`, `sub-agents.md` documents the composition as behavior. Saves up to 32 KiB per dispatch for self-contained research children. |
| 5 | SER-094 | Environment marker in spawned processes: every process darwin spawns — model `bash` (foreground and background), `!` shell commands, native and Codex hook commands, stdio MCP servers — receives `DARWIN=1` (and nothing else new) via the existing seams (`scrubShellEnv` result, `shell-command.ts` env, `hook-process.ts`/`lifecycle-hooks.ts` spawn env, MCP server `env`); documented in README/`/help` in one sentence; never overrides a user-set `DARWIN`; `/export`/trajectory unchanged | 2 | 5 | 4 | 1 | 2 | 10 | S1d (`CLAUDECODE=1` in Bash, hooks, stdio MCP), S7 (`GEMINI_CLI=1`); darwin `shell-env.ts` adds nothing. Lets scripts/hooks detect an agent host. Risk: `verify-shell-env`/`verify-background-bash` pin env bytes. |

Deferred, above the gate but outside the five-direction cap (a later run may queue it with fresh evidence): OpenCode `doom_loop` — identical successful tool call ×3 prompts (S4); Importance 3, fit 4, evidence 2 (no darwin trajectory shows such a loop), difficulty 2, risk 3 → **7**. Polling tools would need an exclusion list.

### Gated-out directions

| Direction | Score | Gate | Why it fell below | Kept anyway? |
|---|---:|---:|---|---|
| Model-graded approvals (Codex auto-review with circuit breaker, DeepSeek `auto-review` preset) (S2c, S3b) | 1 (I 3, F 1, E 4, D 5, R 5) | 6 | Contradicts the load-bearing permission gate (static classifier, user decides, `deny()` never `confirm()`); a second model in the authorization path | no |
| Per-session managed git worktrees (`--worktree`/`/worktree`) (S2b) | 3 (I 3, F 2, E 4, D 5, R 4) | 6 | An isolation backend, the shape SER-004 already scoped; touches the "paths derived from the CLI cwd" invariant and every persistent-shell cwd rule | no |
| Bash-edit diffs read from disk (`bashEditDiffEnabled`) (S1e) | 3 (I 3, F 1, E 3, D 3, R 4) | 6 | Load-bearing "File-edit diffs … never read from disk"; changelog-only evidence (not in settings reference yet) | no |
| Prompt-cache keepalive pings (S8) | 3 (I 2, F 2, E 3, D 2, R 4) | 6 | Spends tokens while idle with no user action; darwin already states cache state and the likely miss cause (SER-074) | no |
| Session retention `maxAge`/`minRetention` auto-delete (S7) | 5 (I 2, F 3, E 3, D 2, R 3) | 6 | Deletes user data on a timer; `darwin sessions` is a read-only projection by decision (SER-025); an explicit prune subcommand may be re-rated later | no |
| `maxEffortLevel` lowest-wins effort cap across scopes (S1e) | 5 (I 1, F 3, E 4, D 2, R 2) | 6 | Org cost control for a single-user tool; `/effort` clamp-and-report already covers unsupported levels | no |
| Per-turn `Elapsed (API, tools)` split on the finished turn (S6) | 5 (I 2, F 3, E 3, D 3, R 2) | 6 | Needs a tool-interval union the turn state does not track; SER-007/SER-022 already report duration and spend | no |
| Inline questions while the turn continues (S2b) | — | — | Not scored: deliberate difference (SER-027 next-turn-only queue; mid-turn fold gated 2026-09-06) | — |

### Recommendation

- Batch to implement: `SER-090`, `SER-091`, `SER-092`, `SER-093`, `SER-094` (Priorities 122–126 on `directions-121-140.md`)
- Start with: `SER-090`
- Why this order: score order, and the two safety/correctness items (trust gate, session lease) come first because both touch startup (`resolveSession` runs inside `AgentRuntime.create`, the trust decision must precede it); SER-092 rests on the lease's exit path being settled (the hint prints after `shutdown()` releases the lease); SER-093 and SER-094 are independent, cheapest last. No score override.
- Independently observable acceptance:
  - SER-090 — a temp git repo with a committed `.mcp.json` (server = `sh -c 'touch <marker>'`) and `.darwin/hooks.json` (TurnComplete `touch <marker2>`): first pty launch shows the modal naming both files/servers and *neither marker exists*; decline → session runs, `/mcp` shows the server absent/held, notice states the omission; accept → `~/.darwin/projects/<key>/trust.json` exists, relaunch shows no modal, server spawned; `darwin -p` in an untrusted copy prints one `trust:` stderr line and no marker; `pnpm test`, `tui completion`, `tui mcp`.
  - SER-091 — two `darwin --resume <id>` processes on one session (second is a pty or `-p`): second refuses with pid/start time, first is unaffected, `lease.json` exists during the run and is gone after exit; kill -9 the holder, relaunch takes over and states it; bare `--resume` while leased starts a fresh session with a notice; `darwin sessions` marks the row; `pnpm test`, `tui resume`.
  - SER-092 — pty session, one prompt, `/exit`: last stdout line is `session <id> · resume: darwin --resume <id>`; `/exit` with no turn prints nothing; headless prints nothing; `pnpm test`.
  - SER-093 — a project agent with `projectInstructions: false`: dispatching it produces a child whose system prompt (verifiable through `spike/verify-subagents.ts`-style construction) lacks `<project-instructions>`; `/agents` row says so; invalid value → the loader's existing bounded skip reason; built-in `general` unchanged; `pnpm test`.
  - SER-094 — model `bash -c 'echo $DARWIN'` (foreground + background), `!echo $DARWIN`, a TurnComplete hook writing `$DARWIN` to a file, and a stdio MCP server that echoes its env all observe `1`; a user shell with `DARWIN=custom` exported sees `custom`; `pnpm test`.
- Backlog updates: `docs/research/backlog/directions-121-140.md` + SER-090…SER-094 appended `not-started` (Priorities 122–126); no rollover (page range ends at 140); no gate abandonments; SER-090 set `in-progress` only immediately before the developer handoff.

### Batch iteration outcome

| Direction | Child session and managed tasks | Host acceptance (exact independent checks and outcomes) | Commit | Final status | Iteration-log entry |
|---|---|---|---|---|---|
| SER-090 | Fresh child `session-20260915-131504369`; task `bg-3bafb509-1882-4196-bb40-c7c109ffe361`, exit 0, drained | Host read `workspace-trust.ts`, runtime/cli-main diff, decisions heading, marker assertions; `pnpm typecheck && pnpm test && verify-tui trust/mcp/completion && pnpm build && git diff --check` → task `bg-4983452e-e0b0-4275-b5ca-252bcc178860` exit 0, 8,997 PASS, no FAIL | `effb8ba` | `done` | Batch 133 |
| SER-091 | Fresh child `session-20260915-143744191`; task `bg-ba57106f-6af3-4333-be89-e006d1cfe306`, exit 0, drained | Host read session/runtime/cli-main/cli-sessions diff, ran `darwin sessions` read-only; `pnpm typecheck && pnpm test && verify-tui resume && pnpm build && git diff --check` → task `bg-a0503fc1-a84f-4116-9362-aa348d08b718` exit 0, 8,981 PASS, 0 FAIL | `55469cb` | `done` | Batch 134 |
| SER-092 | pending | pending | — | `in-progress` | — |

- Halt condition that ended the loop: pending
- Aggregate token spend across every child delegation: SER-090 `input=446 output=118624 cacheRead=49880766 cacheWrite=372633` ($23.0638); SER-091 `input=276 output=80702 cacheRead=21539666 cacheWrite=221565` ($12.1923); running aggregate input **722**, output **199,326**, cacheRead **71,420,432**, cacheWrite **594,198**, **$35.2561**
- Remaining backlog after this run: SER-092 `in-progress`; SER-093, SER-094 `not-started`
