# Darwin self-evolution research — 2026-09-08

## Run — 2026-09-08T03:27:54Z

### Backlog-first decision

- Backlog router inspected first: `docs/research/backlog_index.md`
- Metadata searched across routed priority pages: `docs/research/backlog/directions-001-020.md`, `directions-021-040.md`, `directions-041-060.md`, `directions-061-080.md`, `directions-081-100.md`, `directions-101-120.md` (`grep` for `## SER-|## SRF-`, `Status:`, `Priority:`, `Origin report:` only)
- Unfinished directions found: none — every record from SER-001 to SER-075 and SRF-001 to SRF-028 is `done` or `abandoned` (the 2026-09-08 SRF-026–028 batch closed in `229adaf`)
- Decision: fresh research (the user directed the `peer` path: "peer-product research")

### Research path

Rolled once, before any source was read, with `--path peer` because the user directed the path in the invoking prompt. `path-source` is printed as an override — this run was **not** a chance draw.

```text
research-path: peer
focus: comparable coding-agent product analysis
share: 50% (weight 5 of 10)
draw: none — path was not rolled
path-source: override (user-directed)
rolled-at: 2026-09-08T03:27:54.372Z
weights: tui=2 observability=0.5 sdk=1 open=1.5 peer=5
```

### Source availability and method

Tools available: `web_fetch` (readable projection) and `curl` for raw markdown, on 2026-09-08 (UTC). Every scope in §3.3 was opened at a primary source: Claude Code (`CHANGELOG.md` at `main`, `permissions.md`, `settings-reference.md`, `terminal-config.md`, `slash-commands.md` from code.claude.com), Codex (GitHub releases page; `developers.openai.com/codex/exec-policy.md`), DeepSeek harness (`docs/subsystems/approval.md` at `master`), OpenCode (`opencode.ai/docs/tui/`, `opencode.ai/docs/`), kiro-cli (`kiro.dev/docs/cli/chat.md`, `kiro.dev/docs/cli/chat/context.md`), PenguinHarness (`README.md` at `main`), plus WezTerm's escape-sequence reference as the neutral source for the terminal notification sequences two peers rely on. Claude Code's changelog top is still 2.1.263 and Codex's newest stable is 0.153.4 (hotfixes only) — both unchanged in substance since yesterday's peer run, so this run mined their reference docs (permission rules, notification channel, context breakdown) rather than their release notes. PenguinHarness's README documents no CLI permission-rule, notification, context-breakdown or approval-audit feature; no claim is made about it beyond what it prints. GitHub's HTML pages rendered partially ("Uh oh! There was an error while loading") but the directory listings and README bodies came through; where the projection was doubtful the raw markdown was fetched instead.

### Reference sources

| Ref | Product | Source type | URL | Accessed (UTC) | Claims supported |
|---|---|---|---|---|---|
| S1 | Claude Code | Primary docs (`permissions.md`, markdown edition) | <https://code.claude.com/docs/en/permissions.md> | 2026-09-08 | "Rules are evaluated in order: deny, then ask, then allow. The first match in that order determines the outcome"; "A broad deny rule like `Bash(aws *)` blocks every matching call, including calls that also match a narrower allow rule"; a bare tool name in `deny` "removes the tool from Claude's context entirely", a scoped rule "blocks matching calls when Claude attempts them"; "Restrict Bash network tools: use deny rules to block `curl`, `wget`"; deny rules cover Bash redirection targets and file readers such as `cat`/`head`/`tail`/`sed`; on symlinks "allow rules fall back to prompting you, while deny rules block outright" |
| S1b | Claude Code | Primary docs (`settings-reference.md`, `terminal-config.md`) | <https://code.claude.com/docs/en/settings-reference.md>, <https://code.claude.com/docs/en/terminal-config.md> | 2026-09-08 | `preferredNotifChannel`: `"auto"` "sends a desktop notification in iTerm2, Ghostty, and Kitty, rings the bell in Terminal.app only when its audible bell is off, and does nothing elsewhere"; other values `terminal_bell`, `iterm2`, `iterm2_with_bell`, `kitty`, `ghostty`, `notifications_disabled`; fires "when Claude finishes a task or pauses for a permission prompt"; "The desktop notification reaches your local machine over SSH"; iTerm2 needs "Send escape sequence-generated alerts" enabled; tmux needs passthrough |
| S1c | Claude Code | Primary docs (`slash-commands.md`) and release notes (`CHANGELOG.md` 2.1.261) | <https://code.claude.com/docs/en/slash-commands.md>, <https://raw.githubusercontent.com/anthropics/claude-code/main/CHANGELOG.md> | 2026-09-08 | `/context` has per-component rows: "The Skills row in `/context` reports the size of the listing after the budget is applied, so it matches what the model receives"; 2.1.261 `/skill-doctor` "show which loaded skills go unused and what they cost in context"; 2.1.261 `bashOutputMaxChars`/`taskOutputMaxChars` (inline output cap before file spill, up to 128K) |
| S2 | Codex | Primary docs (execution policy, markdown edition) | <https://developers.openai.com/codex/exec-policy.md> | 2026-09-08 | `prefix_rule()` fields: `pattern`, `decision` (`allow` / `prompt` / `forbidden`, "Codex applies the most restrictive decision when more than one rule matches (`forbidden` > `prompt` > `allow`)"), `justification` ("Codex may surface it in approval prompts or rejection messages"), `match`/`not_match` examples validated at load; "Admins can also enforce restrictive `prefix_rule` entries" |
| S2b | Codex | Primary release notes (GitHub releases, 0.153.1–0.153.4, 0.154.0-alpha.6) | <https://github.com/openai/codex/releases> | 2026-09-08 | 0.153.1–0.153.4 are model-picker hotfixes (GPT-6-Astra on Bedrock Mantle/Runtime routes, "asynchronous questions only when the tool is available"); no TUI/permission feature since 0.153.0 (covered 2026-09-07) |
| S3 | DeepSeek harness | Source repository docs (`docs/subsystems/approval.md` at `master`) | <https://github.com/deepseek-ai/deepseek-harness/blob/master/docs/subsystems/approval.md> | 2026-09-08 | Every approval request "receives a fresh `ApprovalRequestId`" pairing "the `approval/asked` and `approval/decided` audit events"; closed outcome `'allowed-once' \| 'rejected' \| 'cancelled' \| 'unavailable'`, "Callers fail closed on `unavailable`"; per-session `ApprovalPolicy = 'ask' \| 'never'` whose "effective value is the last `approval/policy` event in the session log"; "The audit events are log-only and do not enter the model transcript"; `ApprovalRequest` "deliberately omits tool arguments: an answerer attaches the prompt to the already-streamed tool call through `callId`" |
| S4 | PenguinHarness | Source repository README at `main` | <https://github.com/Prism-Shadow/penguin-harness/blob/main/README.md> | 2026-09-08 | `penguin chat` REPL "(`/compact`, `/clear`, `/exit`, Ctrl-C to interrupt)", `penguin run -m`, `penguin server`; SDK `approve: async () => "allow"` "per-tool-call approval"; roadmap "OpenShell integration (permission-governed shell)". No deny-rule, notification, context-breakdown or approval-audit documentation — no claim |
| S5 | OpenCode | Primary docs (TUI) | <https://opencode.ai/docs/tui/> | 2026-09-08 | `attention` block in `tui.json`: "The TUI can request attention for questions, permissions, session errors, and completed sessions … non-subagent events request desktop notifications only when the terminal is blurred"; `attention.notifications` "Allow terminal-mediated desktop notifications"; `/undo`/`/redo` "Internally, this uses Git to manage the file changes"; `/details` toggles tool execution details; `/thinking` toggles reasoning visibility; `!` output "is added to the conversation as a tool result" |
| S6 | kiro-cli | Primary docs (`chat.md`, `chat/context.md`, markdown editions) | <https://kiro.dev/docs/cli/chat.md>, <https://kiro.dev/docs/cli/chat/context.md> | 2026-09-08 | `/context show` prints "Current context window (5.9% used)" with a bar and per-component rows "Context files 0.9% / Tools 0.5% / Kiro responses 0.7% / Your prompts 3.8%", plus per-file "(0.1% of context window)"; `/context add\|remove\|clear` session-scoped; `!` commands: "Long output collapses to a head + tail view — press Ctrl+O to expand", TTY commands get full terminal access; `/chat save`/`/chat load` JSON; `--resume-picker`; "Context % updates after your next prompt … the backend determines actual token counts during request processing" |
| S7 | WezTerm (terminal reference) | Primary docs (escape-sequences page) | <https://wezterm.org/escape-sequences.html> | 2026-09-08 | OSC 9: "iTerm2 Show System Notification — Show a 'toast' notification `printf "\e]9;%s\e\\" "hello there"`"; OSC 777: "rxvt extension — Only the notify extension is supported; it shows a 'toast' notification `printf "\e]777;notify;%s;%s\e\\" "title" "body"`" |

### Peer highlights and innovations

| Product | Sourced feature / innovation | Reference | Why it matters |
|---|---|---|---|
| Claude Code, Codex, OpenCode | **User-written deny rules** that block a tool call without prompting and win over any allow rule: Claude Code `permissions.deny` evaluated first ("deny, then ask, then allow"); Codex `decision = "forbidden"` is the most restrictive decision and wins when several rules match, with an optional `justification` surfaced in the rejection; OpenCode rules resolve to `allow`/`ask`/`deny` (2026-08-18 S8) | S1, S2, (2026-08-18 S8) | A prohibition the user writes down once (`git push --force`, `curl … \| sh`, `rm -rf`) holds in every mode, for every child, and with no human watching — the one thing an allow-list plus prompts cannot express |
| Claude Code, OpenCode, Codex | **Terminal-mediated desktop notification** on task completion and permission prompts: Claude Code's default channel sends one in iTerm2/Ghostty/Kitty and "reaches your local machine over SSH"; OpenCode `attention.notifications` requests "terminal-mediated desktop notifications only when the terminal is blurred"; Codex `tui.notifications` (2026-09-07 S2) | S1b, S5, S7 | The escape-sequence path (OSC 9 / OSC 777 / kitty OSC 99) needs no platform API or dependency and works through SSH — the property that sank the 2026-08-24 *native* notification proposal does not apply to it |
| kiro-cli, Claude Code | **Context-window breakdown by component**: kiro `/context show` prints share per category (context files, tools, responses, prompts) and per file; Claude Code `/context` has per-row sizes (the Skills row "matches what the model receives") and `/skill-doctor` names unused skills and their cost | S6, S1c | When a session nears its window the user's only lever is `/compact`; a breakdown says *what* is large (a 30-tool MCP catalogue, a long AGENTS.md, tool results) and therefore what to change |
| DeepSeek harness | **Approval audit pair** `approval/asked` → `approval/decided` on the session log for every permission question, log-only, keyed to the streamed tool call by `callId`, never in the model transcript; the per-session policy override is itself a logged event so replay reconstructs it | S3 | The record of a session answers "which calls were prompted, which were auto-approved, which were denied and why" — evidence for graders and for the user, without duplicating tool arguments |
| kiro-cli | `!` output collapses to head+tail with Ctrl+O to expand; TTY commands get the real terminal; `/chat save`/`load` JSON; `--resume-picker` | S6 | Alternative shapes for `!` and session handling — noted, mostly parity or previously gated (PTY sessions 2026-09-07) |
| OpenCode | `/undo`/`/redo` revert *file changes* through Git alongside the message; `/details`, `/thinking` visibility toggles | S5 | File-level undo is a deliberate darwin difference (`/rewind` is conversation-only by load-bearing decision); the toggles are low-value against darwin's always-visible tool rows |
| Claude Code | `bashOutputMaxChars` / `taskOutputMaxChars` raise inline output before file spill | S1c | Parity: darwin's `maxResultTokens` (`src/config.ts`) is the same knob at token scale |
| Codex | Release stream since 0.153.0 is hotfixes only (model picker) | S2b | Nothing new to compare — recorded so the next run does not re-open it |

### Current Darwin baseline

| Capability / architecture | Repository evidence | Current behavior |
|---|---|---|
| Permission rules | `src/agent/permission-rules.ts` (header: "Wildcard allow-rules: 'stop asking about calls like this one'"; `matchesAnyRule`, `isRuleExempt`, `suggestRules`), `src/config.ts:315` `permissionRules?: { readonly allow: readonly string[] }`, `allowRulesField` (`src/config.ts:1301`), `PermissionGate.beforeToolCall` stage order (`src/agent/permission.ts:470–545`: write-scope guard → plan guard → `yolo` proceeds → `safe` proceeds → allow-rule → classifier → prompt), load-bearing § Wildcard allow-rules and `/permissions` | Allow-only. The project-scoped `permission-rules.json` has one key, `allow`; nothing a user writes can make a call *fail* — `yolo` approves everything, `safe` classification runs without asking, and `/permissions` "only narrows" the allow-list. The hard-coded exemptions (`~/.darwin/config.json`, `.env*`, sensitive reads) are the only prohibitions and are not user-extensible. The 2026-08-18 run recorded the asymmetry as "deliberate … with a safe default (unknown ⇒ prompt)" — a research note, not a load-bearing decision |
| Attention signals | `src/tui/terminal-bell.ts` (`ringTerminalBell`, one raw BEL to stdout, config `terminalBell` default `false`), `src/tui/terminal-title.ts` (OSC 2 title, `terminalTitle` default `true`), wired at `src/tui/App.tsx:1013` (turn complete) and the permission publication moment; `docs/research/research_2026-08-24.md` gated "native cross-platform desktop notifications" at Score 4 for platform APIs, focus detection and dependencies | A bell and a title. No terminal-mediated notification sequence; a user in another window or over SSH with the bell muted gets nothing when a prompt waits |
| Context reporting | `AgentRuntime.contextEstimate()` (`src/agent/runtime.ts:1358–1388`): anchor-measured total plus counted tail, else `countConversationTokens` (`src/agent/compact.ts:283`, one `model.countTokens(messages, { systemPrompt, toolSpecs })` call); `formatContextReport`/`formatContextValue` (`src/tui/context-format.ts`); `/context` refuses arguments (`src/tui/App.tsx:1293–1298`); `/status` shares `formatContextValue` | One line: `context — ~N tokens (measured M + ~T new) · P% of W window · K message(s)`. No per-component figure: system prompt (base, `<project-instructions>`, skills catalogue, working context), tool specs (built-ins vs each MCP server), and conversation are one number |
| System prompt composition | `composeSystemPrompt` (`src/agent/instructions.ts:100`) joins base + instructions fragment; skills catalogue and working context appended later in fixed order (load-bearing § System prompt composition); `RuntimeInfo` carries `skillNames`, `commandNames`, `agentNames` | The parts exist as strings at composition time; no accessor exposes their sizes |
| Tool catalogue | `mcpServerStatuses` (`src/mcp/registry.ts:68–85`, `toolNames` from `_registeredToolNames`), `/mcp` and `/status` projections | Names and counts per server are already a read-only projection; schema size per server is not |
| Trajectory record | `RECORDED_EVENT_TYPES` = `contentBlockEvent`, `beforeToolCallEvent`, `afterToolCallEvent`, `agentResultEvent` (`src/trajectory/record.ts:56–61`); observer records `shellCommand`, `taskNotification`, `contextCompacted` (`record.ts:69–78`); `TrajectoryRecordType` has no permission record; `PermissionGateOptions` (`permission.ts:239`) has no decision observer | A denied call is visible only as the `DENIED: …` error text inside `afterToolCallEvent.result`; an approved call is indistinguishable whether the user was asked, a rule matched, the classifier cleared it, or `yolo` was on. The headless drivers write `permission denied — <summary>` to stderr / `permission.denied` (`src/headless.ts:147–152`, `src/headless-protocol.ts:215`) but nothing reaches the record |
| Ask-the-user tool | 2026-08-23 and 2026-09-02 runs gated `ask_user` at Score 3 (string-only `send`, headless, queue ownership); Codex 0.153.4 mentions "asynchronous questions" (S2b) | Still a deliberate difference; darwin asks between turns |

### Comparison and gaps

| Peer evidence | Darwin evidence | Parity / gap / deliberate difference | Implication |
|---|---|---|---|
| Deny rules that beat allow rules and never prompt (S1, S2) | Allow-only `permissionRules.allow`; exemptions hard-coded | **Gap (safety).** The user can widen (allow-rules) and narrow back to the default (`/permissions` revoke) but cannot prohibit. A `bash:git push --force*` rule has no home | SER-076: `permissionRules.deny` with the same rule grammar, judged before `yolo`/`safe`/allow in every mode and for every child; any chained segment matching denies; metacharacters never exempt a deny; `/permissions` lists, never revokes |
| Codex `justification` on a rule (S2) | Rule strings only | Gap, minor | Folded into SER-076 as the denial text naming the rule; a free-text justification would need an object rule form — not this iteration |
| Terminal-mediated desktop notification, default-on in three terminals, over SSH (S1b, S5, S7) | BEL (opt-in) and OSC 2 title | **Gap.** The 2026-08-24 gate rejected *native* notifications (platform APIs, dependencies); the escape-sequence path is a different mechanism with none of those costs and the same stdout seam the bell uses | SER-078: opt-in `terminalNotify` writing one OSC notification sequence at the bell's two moments |
| `/context show` per-component shares (S6), Claude Code `/context` rows (S1c) | One-line total | **Gap.** The estimate machinery and `model.countTokens` exist; only the projection is missing | SER-077: `/context` prints bounded per-component rows (system prompt by section, tools by origin, conversation) under the existing total line; `/status` unchanged |
| `approval/asked` + `approval/decided` audit pair on the session log, keyed by `callId`, log-only (S3) | No permission record in the trajectory | **Gap (observability).** Self-reflection grades from the trajectory and cannot see how many prompts a turn cost or why a call ran silently | SER-079: one bounded `permissionDecision` observer record per gate outcome, keyed by `toolUseId`, never model-visible |
| `bashOutputMaxChars` (S1c) | `maxResultTokens` (`src/config.ts:219`) | Parity | None |
| `/undo`/`/redo` revert files via Git (S5) | `/rewind` is conversation-only, source-preserving (load-bearing § `/rewind`) | Deliberate difference | None |
| `!` head+tail collapse with Ctrl+O expand; TTY passthrough (S6) | `!` bounded projection (SER-009 vocabulary), non-interactive `bash -c` | Parity for the bound; PTY passthrough gated 2026-09-07 | None |
| `/details`, `/thinking` toggles (S5) | Tool rows always shown; thinking not rendered as a toggle | Low value; darwin's bounded rows already state omissions | Not proposed |
| Asynchronous user questions (S2b) | `ask_user` gated twice | Deliberate difference (unchanged semantics) | Not re-proposed |

### Ranked iteration directions

`Score = 2 × Importance + Architecture fit + Evidence confidence − Implementation difficulty − Implementation risk`

| Rank | Proposed ID | Direction | Importance | Architecture fit | Evidence confidence | Implementation difficulty | Implementation risk | Score | Evidence and rationale |
|---:|---|---|---:|---:|---:|---:|---:|---:|---|
| 1 | SER-076 | User-written deny rules: `permissionRules.deny` in the same project-scoped `permission-rules.json` and rule grammar (`bash:git push --force*`, `fileEditor:dist/**`, bare tool name), validated at load like `allow`; judged in `PermissionGate.beforeToolCall` right after the write-scope guard and before the plan guard, `yolo`, `safe`, allow-rules and the classifier, so it holds in every mode and for every child sharing the gate; a bash deny matches when **any** chained segment matches and shell metacharacters never exempt it (the conservative direction inverted for prohibition); the model receives one bounded `DENIED` error naming the rule; `/permissions` lists deny rules as `deny (configured)` and refuses to revoke them (revocation would widen); no prompt ever offers a deny rule; `/status` counts them separately | 4 | 4 | 5 | 3 | 3 | **11** | S1, S2, 2026-08-18 S8; `src/agent/permission-rules.ts`, `src/config.ts:1301`, `permission.ts:470–545`. The one permission expression darwin cannot make today. Same file, same grammar, one new stage; risk is a rule blocking a legitimate command, which fails closed and is the user's own rule |
| 2 | SER-077 | `/context` breakdown: `ContextEstimate` gains an optional `breakdown` computed on demand (only when `/context` runs, never per turn) by `model.countTokens` per component — system prompt by section (base, `<project-instructions>`, skills catalogue, working context), tool specs grouped by origin (darwin built-ins, each MCP server by name), conversation messages — printed as bounded rows (`~N tokens · P%` when the window is known) under the existing total line; components whose count fails read `not reported`; the total line and `/status` byte-identical | 4 | 4 | 4 | 3 | 2 | **11** | S6, S1c; `runtime.ts:1358–1388`, `compact.ts:283`, `context-format.ts`, `instructions.ts:100`, `mcp/registry.ts:68–85`. Pure projection over existing accessors plus one per-component count; Bedrock/Anthropic `countTokens` may call the provider's counting API — the same call the total already makes |
| 3 | SER-078 | Terminal-mediated attention notification: config `terminalNotify` (default `false`) writes one documented OSC notification sequence (OSC 777 `notify` and/or OSC 9, exact set decided from terminal documentation and recorded) with a bounded, sanitized title/body (`darwin · <project>` / `waiting for approval` or `turn complete`) at exactly the bell's two driver moments through the same real-stdout seam; TTY only, never headless, never children, never per frame; README states the tmux passthrough and iTerm2 alert-setting requirements | 3 | 4 | 4 | 2 | 2 | **10** | S1b, S5, S7; `src/tui/terminal-bell.ts`, `App.tsx:1013`. Materially different from the 2026-08-24 gated *native* proposal (no platform API, no dependency, works over SSH); zero behaviour when off |
| 4 | SER-079 | Permission decision audit in the trajectory: `PermissionGateOptions.onDecision?` publishes one frozen decision (`toolUseId`, `toolName`, `kind`, `risk`, effective `mode`, `source` dispatch id, `outcome` ∈ `write-scope-denied` \| `plan-denied` \| `deny-rule` \| `yolo` \| `safe` \| `allow-rule` \| `classifier` \| `user-approved` \| `user-denied` \| `restart-limit-denied`, matched/granted rule when any, `promptedUser` boolean) — never tool input; the runtime records it as one bounded `permissionDecision` observer record; `trajectory replay` (and therefore `/export`) prints one line only for prompted or denied decisions; `trajectory list` unchanged; never model-visible; headless drivers unchanged | 3 | 4 | 4 | 3 | 2 | **9** | S3, Codex Guardian history (2026-09-07 S2c); `record.ts:56–78`, `permission.ts:239`, `headless.ts:147`. Observer pattern already used by `shellCommand`/`taskNotification`/`contextCompacted`; depends on nothing but should follow SER-076 so `deny-rule` is a real outcome |

### Gated-out directions

| Direction | Score | Gate | Why it fell below | Kept anyway? |
|---|---:|---:|---|---|
| `!` TTY passthrough for interactive commands (S6) | 3 (I 3, F 2, E 3, D 5, R 4) | 6 | Same shape as the PTY-session proposal gated 2026-09-07: a new process model beside the one-shot `bash -c` group | no |
| `/undo`-style file reversion through Git (S5) | 2 (I 2, F 1, E 4, D 4, R 5) | 6 | Contradicts the load-bearing `/rewind` decision (source-preserving conversation branch); a second mutation channel into the worktree | no |
| `/details` / `/thinking` visibility toggles (S5) | 4 (I 2, F 3, E 4, D 2, R 3) | 6 | Darwin's tool rows already state omissions and finished rows are written once into `<Static>`; a toggle would need a second render of committed history | no |
| Rule `justification` text surfaced in the denial (S2) | 5 (I 2, F 3, E 4, D 2, R 2) | 6 | Needs an object rule form beside the string grammar; low value until deny rules exist — revisit after SER-076 | no |
| `/chat save`/`/chat load` JSON conversation files (S6) | 3 (I 2, F 2, E 4, D 3, R 4) | 6 | `/export` and `darwin sessions`/`--resume <id>` cover the use; loading foreign JSON into a session bypasses the snapshot store's read-only listing contract | no |

### Recommendation

- Batch to implement: `SER-076`, `SER-077`, `SER-078`, `SER-079`
- Start with: `SER-076`
- Why this order: SER-076 is the safety item and the only one whose absence lets an agent do something the user has no way to forbid; SER-077 and SER-078 are independent projections ordered by score; SER-079 last so that `deny-rule` is a real outcome when the record's vocabulary is fixed.
- Independently observable acceptance:
  - SER-076 — `pnpm typecheck`, `pnpm test`; a new free suite (`spike/verify-deny-rules.ts` or an extension of `verify-permissions-command.ts`) proving: a `bash:git push --force*` deny blocks `git status && git push --force` in `default`, `auto`, `plan` and `yolo`; a metacharacter command matching a deny segment is denied; a deny wins over a matching allow; the model-facing result names the rule; `/permissions` lists it and refuses to revoke it; an invalid deny string is a `ConfigError`; `verify-config.ts` still green; `tui completion` if the `/permissions` help text changes.
  - SER-077 — `verify-context-format.ts` extended with breakdown rows (bounded, `not reported` on failure); a runtime-level check that the total line is byte-identical with and without the breakdown; `tui completion` unchanged.
  - SER-078 — `verify-terminal-bell.ts` (or a sibling) proving off is byte-zero, on writes exactly one sequence per moment, body sanitization strips ESC/BEL/`;`, never in headless; `verify-config.ts` for the new key; grep proof that the module is the sole writer of the sequence.
  - SER-079 — `verify-trajectory.ts` extended: one record per gate outcome, `toolUseId` matches the recorded `beforeToolCallEvent`, no input field, replay prints prompted/denied only; `verify-permissions-command.ts`/gate suites unchanged; `pnpm test`.
- Backlog updates: `docs/research/backlog/directions-101-120.md` gains SER-076 (Priority 104), SER-077 (105), SER-078 (106), SER-079 (107) as `not-started`; none set `abandoned` by the gate at selection (all four ≥ 6); SER-076 is changed to `in-progress` only immediately before the developer handoff; no rollover (page range 101–120 has room).

### Batch iteration outcome

| Direction | Child session and managed tasks | Host acceptance (exact independent checks and outcomes) | Commit | Final status | Iteration-log entry |
|---|---|---|---|---|---|
| `SER-076` | child `session-20260908-034418300`, task `bg-8d99c660-2d0a-422f-8cd0-5a0232f3650c` (exit 0, 141 model calls); Host gate task `bg-a5799eb0-41c1-434b-b37a-2393eb5f5f8a` | Diff read (`permission-rules.ts`, `permission.ts`, `tool-hooks.ts`, `config.ts`, `App.tsx`); `pnpm typecheck` 0; `pnpm test` 0 (5784 PASS, 0 FAIL); `verify-deny-rules.ts` 95/95; `verify-tui.ts completion` 69/69; `AGENTS.md` 32,691 B; `pnpm build` 0 | `6bd5cdb` | `done` | Batch 108 |
| `SER-077` | child `session-20260908-042333049`, task `bg-bfa624c2-3c26-4abd-ba69-b9706ae14f13` (exit 0, 115 model calls); Host gate task `bg-63b167ed-f9c5-4030-80fa-77d1d9d961cd` | Diff read (`context-breakdown.ts`, `runtime.ts` accessor, `App.tsx` handler); `pnpm typecheck` 0; `pnpm test` 0 (5816 PASS, 0 FAIL); `verify-context-format.ts` 49/49; `verify-context-anchor.ts` 40/40; `verify-tui.ts completion` 69/69; `AGENTS.md` 32,753 B; `pnpm build` 0 | `19ef9ba` | `done` | Batch 109 |
| `SER-078` | child `session-20260908-045358172`, task `bg-a9e692e6-8318-4e8b-8896-a6c639908059` (exit 0, 96 model calls); Host gate task `bg-d1980bb7-8f79-484b-aab9-e8d3ae9913ef` | Diff read (`terminal-notify.ts`, `cli-main.ts`, `App.tsx`); `pnpm typecheck` 0; `pnpm test` 0 (5893 PASS, 0 FAIL); `verify-terminal-notify.ts` 65/65; `verify-config.ts` 378/378; grep: `]9;` only in `terminal-notify.ts`, imported only by `App.tsx`/`cli-main.ts`; `AGENTS.md` 32,753 B; `pnpm build` 0 | `878ca45` | `done` | Batch 110 |
| `SER-079` | child `session-20260908-052106559`, task `bg-ff3f2989-b59d-40b4-bb74-264baa4c8e7b` (exit 0, 150 model calls); Host gate task `bg-20ea02bc-9d87-41d9-8019-3ba4abf1b5f5` | Diff read (`permission.ts` `publish`/`guardBeforeHooks`, `runtime.ts` adapter, trajectory imports unchanged); `pnpm typecheck` 0; `pnpm test` 0 (5997 PASS, 0 FAIL); `verify-permission-audit.ts` 76/76; `verify-trajectory.ts` 368/368; `AGENTS.md` 32,753 B; `pnpm build` 0 | `c8b89c3` | `done` | Batch 111 |

- Halt condition that ended the loop: batch exhausted — SER-076, SER-077, SER-078 and SER-079 all `done`; no `not-started`/`in-progress` record remains in any routed page.
- Aggregate token spend across every child delegation (four child tasks, no retries, no corrections): `input=1,012 output=281,969 cacheRead=72,557,679 cacheWrite=1,107,810`; cost `45.8956` USD (`14.3458 + 9.6451 + 7.7791 + 14.3256`, all `global.anthropic.claude-fable-5-1`, LiteLLM base rates; every task reported both lines, no `-` bucket).
- Remaining backlog after this run: nothing unfinished — every record from SER-001 to SER-079 and SRF-001 to SRF-028 is `done` or `abandoned`; a fresh research run is the recommended next step. Gated this run and not queued: `!` TTY passthrough (3), Git-backed `/undo` (2), `/details`/`/thinking` toggles (4), rule `justification` text (5 — revisit now that SER-076 exists), `/chat save`/`load` JSON (3).

## Run — 2026-09-08T07:52:34Z

### Backlog-first decision

- Backlog router inspected first: `docs/research/backlog_index.md`
- Metadata searched across routed priority pages: `docs/research/backlog/directions-001-020.md` … `directions-101-120.md` (six pages; `grep` for `^- Status:` lines and `## ` headings only)
- Unfinished directions found: none — every record SER-001…SER-079 and SRF-001…SRF-028 is `done` or `abandoned` (the morning run's batch SRF-026–028 and SER-076–079 closed before this invocation)
- Decision: fresh research

### Research path

```text
research-path: peer
focus: comparable coding-agent product analysis
share: 50% (weight 5 of 10)
draw: 13 of 20 half-units
path-source: roll
rolled-at: 2026-09-08T07:52:34.071Z
weights: tui=2 observability=0.5 sdk=1 open=1.5 peer=5
```

Rolled once, before any source (repository or product) was opened. The morning run (`03:27:54Z`) was also `peer`, by user override; this run is the chance draw and deliberately mined different reference areas (project-instruction files and the shell environment) rather than re-reading the permission, notification and context-breakdown pages it already covered.

### Source availability and method

Tools available: `web_fetch` (readable projection), `curl` for raw markdown and the GitHub contents API, on 2026-09-08 (UTC). Every scope in §3.3 was opened at a primary source: Claude Code (`CHANGELOG.md` at `main` — still 2.1.263 at the top, unchanged since the morning run, so its release notes were not re-mined; `memory.md` from code.claude.com), Codex (`config-reference.md`, `config-advanced.md`, `agents-md.md` and the slash-command reference — `developers.openai.com/codex/*` now 308-redirects to `learn.chatgpt.com/docs/*`, followed explicitly), DeepSeek harness (`docs/subsystems/` listing, `credentials.md`, `shell.md`, `subprocess.md` and the source file `packages/subprocess/subprocess/src/index.ts` at `master`), OpenCode (`opencode.ai/docs/rules/`, HTML projected to text), kiro-cli (`kiro.dev/docs/cli/chat.md`, `llms.txt`, `cli/v3/tangent.md`; `cli/reference.md` has moved and the old `chat/tangent` and `chat/commands` slugs 404 — the current pages were located through `llms.txt`), PenguinHarness (`README.md` at `main`), plus Gemini CLI as the additional product (`docs/cli/gemini-md.md` and the `docs/cli/` listing at `main`; `docs/cli/commands.md` 404s). PenguinHarness's README documents no instruction-file, environment or `/init` behaviour (only `penguin chat` REPL commands, `approve` callback and the OpenShell roadmap item) — no claim is made about it. No claim is made about Claude Code's or Gemini CLI's subprocess environment handling: neither page opened states it, and it was not inferred.

Every darwin claim below was checked in source; the one behavioural claim (`echo $ANTHROPIC_API_KEY` classifies `safe`) was reproduced by calling the real `assessRisk` from `src/agent/permission.ts` with `pnpm tsx` (output quoted in the baseline table).

### Reference sources

| Ref | Product | Source type | URL | Accessed (UTC) | Claims supported |
|---|---|---|---|---|---|
| S1 | Claude Code | Primary docs (`memory.md`, markdown edition) | <https://code.claude.com/docs/en/memory.md> | 2026-09-08 | "Run `/init` to generate a starting CLAUDE.md automatically. Claude analyzes your codebase and creates a file with build commands, test instructions, and project conventions it discovers. If a CLAUDE.md already exists, `/init` suggests improvements rather than overwriting it"; `/init` "reads Cursor rules … and Copilot rules … and incorporates the relevant parts"; "Claude Code reads `CLAUDE.md`, not `AGENTS.md`. If your repository already uses `AGENTS.md` … create a `CLAUDE.md` that imports it"; loads `CLAUDE.md` "from your current working directory and every directory above it", subdirectory files "included when Claude reads files in those subdirectories"; `@path/to/import` imports with "a maximum depth of four hops" and an approval dialog for external imports; "target under 200 lines per CLAUDE.md file" |
| S1b | Claude Code | Primary release notes (`CHANGELOG.md` at `main`) | <https://raw.githubusercontent.com/anthropics/claude-code/main/CHANGELOG.md> | 2026-09-08 | Top entry still 2.1.263 ("Bug fixes and reliability improvements"); nothing new since the morning run — recorded so the next run does not re-open it |
| S2 | Codex | Primary docs (`config-advanced.md`, "Shell environment policy") | <https://learn.chatgpt.com/docs/config-file/config-advanced.md> (redirect target of `developers.openai.com/codex/config-advanced`) | 2026-09-08 | "`shell_environment_policy` controls which environment variables Codex passes to spawned commands. Start with an empty environment using `inherit = "none"`, or inherit a trimmed set using `inherit = "core"`"; "`ignore_default_excludes` defaults to `true`, so Codex doesn't automatically remove variable names containing `KEY`, `SECRET`, or `TOKEN`. Set it to `false` to apply those automatic exclusions"; filters are "case-insensitive and support `*` and `?`"; "Because `set` runs after exclusions, it can restore an excluded variable" |
| S2b | Codex | Primary docs (`config-reference.md`) | <https://learn.chatgpt.com/docs/config-file/config-reference.md> | 2026-09-08 | Keys `shell_environment_policy.inherit` (`all \| core \| none`), `.ignore_default_excludes`, `.filters` (`map<string, include \| exclude>`), `.set`; `project_doc_max_bytes` — "Maximum bytes read from `AGENTS.md` when building project instructions"; `project_doc_fallback_filenames` — "Additional filenames to try when `AGENTS.md` is missing" |
| S2c | Codex | Primary docs (`agents-md.md`) | <https://learn.chatgpt.com/docs/agent-configuration/agents-md.md> (redirect target of `developers.openai.com/codex/guides/agents-md`) | 2026-09-08 | "Starting at the project root (typically the Git root), Codex walks down to your current working directory … In each directory along the path, it checks for `AGENTS.override.md`, then `AGENTS.md`, then any fallback names"; "Codex concatenates files from the root down"; "stops adding files once the combined size reaches the limit defined by `project_doc_max_bytes` (32 KiB by default)" |
| S2d | Codex | Primary docs (slash-command reference) | <https://learn.chatgpt.com/docs/developer-commands.md?surface=cli> (redirect target of `developers.openai.com/codex/cli/slash-commands`) | 2026-09-08 | "`/init` — Generate an `AGENTS.md` scaffold in the current directory"; "Expected: Codex creates an `AGENTS.md` scaffold you can refine and commit for future sessions" |
| S3 | DeepSeek harness | Source repository (`packages/subprocess/subprocess/src/index.ts` at `master`) | <https://github.com/deepseek-ai/deepseek-harness/blob/master/packages/subprocess/subprocess/src/index.ts> | 2026-09-08 | "Credential-shaped environment names are NOT forwarded to children (the harness's own `DEEPSEEK_API_KEY`/secrets must not leak into a spawned process implicitly). One heuristic for every in-repo spawner; a deliberately supplied entry survives because explicit env layers merge after the scrub." `export const SENSITIVE_ENV_PATTERN = /KEY\|PASSWORD\|SECRET\|TOKEN/i`; `scrubbedParentEnv()` is "the canonical base every harness child starts from. `PATH`, `HOME`, locale, and proxy variables survive" |
| S3b | DeepSeek harness | Source repository docs (`docs/subsystems/shell.md`, `subprocess.md`, `credentials.md` at `master`) | <https://github.com/deepseek-ai/deepseek-harness/blob/master/docs/subsystems/shell.md> | 2026-09-08 | "The local executor scrubs ambient credentials before merging explicit caller-supplied env"; the subprocess seam "owns … the shared credential scrub (`scrubbedParentEnv`)"; credentials are referenced by environment-variable *name* and resolved per operation, "never exposing a value" to configuration surfaces |
| S4 | PenguinHarness | Source repository README at `main` | <https://github.com/Prism-Shadow/penguin-harness/blob/main/README.md> | 2026-09-08 | `penguin chat` REPL, `approve: async () => "allow"` per-tool-call approval, roadmap "OpenShell integration (permission-governed shell)". No instruction-file, `/init` or environment documentation — no claim |
| S5 | OpenCode | Primary docs (Rules) | <https://opencode.ai/docs/rules/> | 2026-09-08 | "To create a new AGENTS.md file, you can run the /init command"; "`/init` scans the important files in your repo, may ask a couple of targeted questions … and then creates or updates AGENTS.md with concise project-specific guidance … build, lint, and test commands … references to existing instruction sources like Cursor or Copilot rules"; "If you already have an AGENTS.md, /init will improve it in place"; Claude Code compatibility: "Project rules: CLAUDE.md in your project directory (used if no AGENTS.md exists)"; "if you have both AGENTS.md and CLAUDE.md, only AGENTS.md is used" |
| S6 | kiro-cli | Primary docs (`cli/v3/tangent.md`, `cli/chat.md`, `llms.txt`) | <https://kiro.dev/docs/cli/v3/tangent.md> | 2026-09-08 | "`/tangent` — At the root, branches into a side-conversation (auto-named `tangent-N`) that inherits your conversation so far. Inside a tangent, goes back one level toward the parent"; "Each tangent is a full session: it starts with your conversation's history up to the branch point, but everything after stays isolated"; "While in a tangent, a `↯ name` chip in the footer shows where you are"; use cases "Quick lookups without derailing", "Contain noisy debugging"; `llms.txt` also lists "Queue steering: Send messages to the agent while it's working to redirect its approach without cancelling" and "Goal: Start an autonomous agent loop" |
| S7 | Gemini CLI (additional product) | Source repository docs (`docs/cli/gemini-md.md` at `main`) | <https://github.com/google-gemini/gemini-cli/blob/main/docs/cli/gemini-md.md> | 2026-09-08 | Context files are loaded from `~/.gemini/GEMINI.md`, "workspace directories and their parent directories", plus "Just-in-time (JIT) context files … when a tool accesses a file or directory"; "The CLI footer displays the number of loaded context files"; `context.fileName` may be a list, e.g. `["AGENTS.md", "CONTEXT.md", "GEMINI.md"]`; `@file.md` imports |

### Peer highlights and innovations

| Product | Sourced feature / innovation | Reference | Why it matters |
|---|---|---|---|
| DeepSeek harness | Every in-repo spawner starts from `scrubbedParentEnv()`: names matching `/KEY\|PASSWORD\|SECRET\|TOKEN/i` are dropped by default; a deliberately supplied entry survives because explicit layers merge after the scrub | S3, S3b | The harness's own API key never reaches a model-spawned process implicitly — the model cannot `echo` it, and a test script cannot read it |
| Codex | `shell_environment_policy`: `inherit = all\|core\|none`, case-insensitive `filters` (`include`/`exclude`), `set`, and an automatic `KEY`/`SECRET`/`TOKEN` exclusion that is opt-in (`ignore_default_excludes = false`) | S2, S2b | Same concern, fuller policy surface; Codex chose opt-in, presumably (inference) because project tooling often needs credential-shaped variables |
| Claude Code / Codex / OpenCode | `/init` writes (or improves in place) the project instruction file after inspecting the repository — build/test commands, conventions, existing Cursor/Copilot rules | S1, S2d, S5 | Three peers converge on the same onboarding verb; darwin has the file (`AGENTS.md`) but no way to produce it from inside a session |
| OpenCode / Codex / Gemini CLI | Instruction-file fallbacks: OpenCode reads `CLAUDE.md` when no `AGENTS.md` exists; Codex `project_doc_fallback_filenames`; Gemini `context.fileName` list | S5, S2b, S7 | Repositories that already carry `CLAUDE.md` work unchanged; darwin silently preloads nothing for them |
| Codex / Claude Code / Gemini CLI | Hierarchical discovery: walk root→cwd and concatenate (Codex), load ancestors at launch and subdirectory files on demand (Claude Code), JIT files when a tool touches a directory (Gemini) | S2c, S1, S7 | Monorepo scoping — but darwin's `instructions.ts` header records the single-file choice deliberately (see baseline) |
| Claude Code / Gemini CLI | `@path` imports inside the instruction file (depth-capped; Claude Code prompts for imports resolving outside the working directory) | S1, S7 | Composition without duplication; the external-import prompt shows the sensitive-path exposure that comes with it |
| kiro-cli 3.0 | `/tangent`: branch a side-conversation that inherits history to the branch point, work in isolation, `/tangent` again returns to the parent; nested, named, picker via `/tangent ls`; footer chip shows location | S6 | Contains noisy debugging and quick lookups without polluting the main thread's context — darwin's `/rewind` restores a completed-prompt snapshot but has no "mark here, come back" gesture |
| kiro-cli | Queue steering (redirect a running turn without cancelling), `/goal` autonomous loop | S6 | Both are deliberate darwin differences (next-turn-only queue, SER-027; no autonomous loop) — recorded, not proposed |
| Claude Code | Changelog unchanged (2.1.263) since the morning run | S1b | Nothing new to compare |

### Current Darwin baseline

| Capability / architecture | Repository evidence | Current behavior |
|---|---|---|
| Model-facing persistent shell environment | pinned SDK `vended-tools/bash/bash.js:156` — `spawn('bash', [], { cwd, env: { ...process.env, PS1: '', PS2: '' }, detached: true })`; `CreateBashOptions` in the pinned patch carries only `cwd`/`projectRoot` (`src/tools/background-bash.ts:725` — `createBash({ cwd: projectRoot, projectRoot })`) | The shell inherits darwin's whole process environment, including `ANTHROPIC_API_KEY` (required by the anthropic provider — `src/config.ts:1803` reads `process.env[ANTHROPIC_API_KEY_ENV]`) and any `AWS_*`/`OPENAI_*` credentials the user exported |
| Background bash jobs | `src/tools/background-bash.ts:350–352` — `spawn('/bin/bash', ['-lc', command], { …, env: process.env })` | Same full inheritance |
| Static safety classifier on `echo`/`cat` | `src/agent/permission.ts:1117–1128` (`SAFE_BASH_COMMANDS` includes `echo`, `cat`), `src/agent/permission-rules.ts:42` (`SHELL_METACHARACTERS = /[><\`]\|\$\(/` — a bare `$VAR` is not a metacharacter); reproduced with `pnpm tsx` calling `assessRisk`: `"echo $ANTHROPIC_API_KEY" -> {"risk":"safe","riskReason":"read-only command"}`, `"cat /proc/self/environ" -> safe`, while `env` and `printenv ANTHROPIC_API_KEY` are `dangerous` ("not on the safe-command list") | In `default` mode the model can print darwin's own API key without a prompt; the value then lands in the model context, the trajectory tool result and `/export`. `yolo` and allow-rules (`bash:pnpm test*`) hand the same environment to arbitrary approved commands |
| Sensitive-path reads (SER-071) | `src/agent/permission-rules.ts:69–97` (`~/.ssh`, `~/.aws`, `~/.gnupg`, `~/.netrc`, `.env*`, darwin policy files) | File-based credentials are prompted for; the process environment — the credential store darwin itself uses — is not in the set |
| User `!` shell, hooks, MCP servers | `src/tui/shell-command.ts:223`, `src/hooks/hook-process.ts:89–92`, `src/hooks/lifecycle-hooks.ts:84`, `src/hooks/tool-hooks.ts:79` (`env: process.env`); `src/mcp/registry.ts:12` interpolates `${env:VAR}` into server env | User-authorized processes and user-configured servers inherit deliberately — a server's own API key arrives this way. Not a gap: the subject of the scrub is model-spawned commands |
| Project instructions | `src/agent/instructions.ts:1–12` ("Only the run directory's own `AGENTS.md` is read: no walking up to parent directories and no merging of several files"), `AGENTS_FILENAME = 'AGENTS.md'`, `MAX_INSTRUCTIONS_BYTES = 32 * 1024`; `loadProjectInstructions` returns `{ instructions: undefined, problem: undefined }` on `ENOENT` (`:66`); header row in `src/tui/App.tsx:2739–2749` and `darwin doctor` (`src/cli-doctor.ts:246–260`) report `AGENTS.md: loaded (N KB)` / `absent or empty` | Exactly one filename; a repository carrying only `CLAUDE.md` (Claude Code's convention, S1) preloads nothing and the header says so only by omission. The 32 KiB cap matches Codex's default `project_doc_max_bytes` (S2c) — parity |
| Built-in slash commands | `src/commands/custom-commands.ts:11` (`BUILTIN_COMMAND_NAMES`, 20 names, no `init`), `MAX_COMPLETIONS = 21` (`src/tui/InputBox.tsx:24`), `MAX_HELP_COMMANDS = 24` (`src/tui/help-format.ts:8`) | No way to scaffold or improve `AGENTS.md` from inside a session; a new built-in fits the completion cap with one row to spare and the help cap with four |
| Prompt-style built-in pattern | `src/commands/workflow-command.ts` (`parseWorkflowCommand`, fixed template, bare form is a local usage notice, "never a second execution channel"), wired in `src/tui/App.tsx:1709–1737`; `spike/verify-workflow-command.ts` | The established shape for a built-in that steers the model with one ordinary prompt and no new tool or channel |
| Conversation branching | `src/agent/rewind.ts` + `AgentRuntime.startRewind` (load-bearing § `/rewind`): ≤100 immutable snapshots captured only for completed bounded prompts; a fresh successor restores the selected snapshot, the selected prompt returns unsent; source/pointer/workspace untouched; omission notice names files, shell, hooks, MCP writes, subagents, jobs, memory; `src/tui/rewind-search.ts` picker; SER-059 Esc Esc opens it | The user can already return to any completed-prompt boundary, but has to find it in the picker after the detour; nothing marks "the point I will come back to" or shows that the current work is a detour |

### Comparison and gaps

| Peer evidence | Darwin evidence | Parity / gap / deliberate difference | Implication |
|---|---|---|---|
| Credential-shaped names scrubbed from every model-spawned child by default (S3); Codex automatic `KEY`/`SECRET`/`TOKEN` exclusion, opt-in (S2) | `bash.js:156`, `background-bash.ts:352` inherit `process.env`; `echo $ANTHROPIC_API_KEY` is `safe` | **Gap (safety).** The sensitive-read set (SER-071) protects credential *files*; the process environment — where darwin's own key lives — is handed to the model's shell whole | SER-082: withhold credential-shaped variables from the persistent shell and background jobs by default, with an explicit config passthrough list and a visible `/status` count; user `!`, hooks and MCP untouched |
| Full `shell_environment_policy` (inherit `none`/`core`, include allowlists, `set`) (S2, S2b) | none | Gap, but the bounded scrub above covers the safety case; the rest is policy surface darwin has no user asking for | Gated (see below) |
| `/init` scaffolds or improves the instruction file (S1, S2d, S5) | `BUILTIN_COMMAND_NAMES` has no `init`; `workflow-command.ts` is the prompt-trigger shape | **Gap.** Three peers, one verb; darwin's own pitch is "an `AGENTS.md` preloaded into the system prompt" (`AGENTS.md`, README:9) yet a new project starts with none | SER-081: `/init` as a prompt-style built-in — fixed template, ordinary turn, `fileEditor` write through the gate; bare form is the trigger, `/init <focus>` appends focus |
| `CLAUDE.md` used when no `AGENTS.md` exists (S5); `project_doc_fallback_filenames` (S2b); `context.fileName` list (S7) | `AGENTS_FILENAME` only; `ENOENT` → silent nothing | **Gap.** The commonest other convention preloads nothing, silently | SER-080: read `CLAUDE.md` when `AGENTS.md` is absent, label the fragment/header/doctor with the file actually used; `AGENTS.md` always wins when both exist (OpenCode's rule) |
| Root→cwd walk and concatenation (S2c), ancestor + on-demand subdirectory files (S1), JIT (S7) | `instructions.ts:4–6` records "no walking up … no merging" as the design | Deliberate difference | Not proposed (gated: contradicts a recorded decision) |
| `@path` imports with external-import approval (S1, S7) | none | Gap, but imports from a committed file can reach `~/.aws/credentials` — Claude Code needed a dialog for exactly that | Gated |
| `/tangent` branch-and-return with a footer chip (S6) | `/rewind` restores any completed-prompt snapshot; picker only | **Gap (workflow).** The mechanism exists; the missing part is a bookmark and a one-keyword return | SER-083: `/tangent` marks the return point over the existing snapshot machinery and `/tangent` again rewinds to it; header/busy row states `tangent` |
| Queue steering mid-turn, `/goal` loop (S6) | Prompt queue is next-turn-only (SER-027, load-bearing row); no autonomous loop | Deliberate differences | None |
| `project_doc_max_bytes` 32 KiB default (S2c) | `MAX_INSTRUCTIONS_BYTES = 32 * 1024` | Parity | None |

### Ranked iteration directions

`Score = 2 × Importance + Architecture fit + Evidence confidence − Implementation difficulty − Implementation risk`

| Rank | Proposed ID | Direction | Importance | Architecture fit | Evidence confidence | Implementation difficulty | Implementation risk | Score | Evidence and rationale |
|---:|---|---|---:|---:|---:|---:|---:|---:|---|
| 1 | SER-081 | `/init` — a prompt-style built-in (the `/workflow` shape) that expands to one fixed ordinary prompt: inspect the repository (build/test/typecheck commands, layout, conventions, existing `CLAUDE.md`/`.cursorrules`/`.github/copilot-instructions.md`) and create `AGENTS.md`, or when it exists improve it in place, keeping it under `MAX_INSTRUCTIONS_BYTES` and stating that darwin preloads it into every request; `/init <focus>` appends the focus under a `Focus:` marker; the write is an ordinary `fileEditor` call through the permission gate (prompted in `default`, denied in `plan`); reserved in `BUILTIN_COMMAND_NAMES`, described, listed by `/help`, refused while busy like other turn-producing commands are queued; never a tool, never a second channel | 3 | 5 | 5 | 2 | 1 | **13** | S1, S2d, S5; `src/commands/workflow-command.ts`, `custom-commands.ts:11`, `InputBox.tsx:24` (21 ≥ 21 built-ins), `help-format.ts:8`. Depends on SER-080 so the template can say which files darwin reads. Risk is near zero: nothing runs that a typed prompt could not |
| 2 | SER-080 | Read `CLAUDE.md` when `AGENTS.md` is absent: `loadProjectInstructions` tries `AGENTS.md`, then `CLAUDE.md`, uses the first that exists (an existing-but-unreadable `AGENTS.md` still reports its problem and never falls through); `ProjectInstructions` gains `filename`, the fragment's `source="…"` attribute, the header row, `/status`-adjacent header text and `darwin doctor` name the file actually loaded; both present → `AGENTS.md` only, never merged; the `instructions.ts` header comment extends its recorded decision (still one file, still no walking) | 3 | 4 | 5 | 1 | 2 | **12** | S5 ("used if no AGENTS.md exists", "only AGENTS.md is used"), S2b, S7; `instructions.ts:4–6, 12, 66`, `App.tsx:2739–2749`, `cli-doctor.ts:246–260`. Risk: a `CLAUDE.md` may reference Claude-Code-only verbs/imports (`@path` stays literal — say so in the fragment header) |
| 3 | SER-082 | Withhold credential-shaped environment variables from model-spawned shells: a pure `scrubShellEnv(env, passthrough)` (`src/tools/shell-env.ts`) drops names matching one fixed case-insensitive pattern (`KEY`, `SECRET`, `TOKEN`, `PASSWORD`, `CREDENTIAL`) from the persistent SDK shell (new `env?` in the pinned patch's `CreateBashOptions`, threaded into `BashSession.start`) and from `background-bash.ts` spawns; `PATH`/`HOME`/locale/proxy names always survive; config `shellEnv.passthrough: string[]` (exact names or `PREFIX_*`, validated like other config keys) restores named variables; `/status` gains one `shell env` row (`N credential-shaped variables withheld` + bounded names of passthroughs); one startup notice line when N > 0; children share the same builder; `!` user commands, hooks and MCP servers unchanged; `echo $ANTHROPIC_API_KEY` in the model's shell prints empty | 4 | 4 | 5 | 3 | 3 | **11** | S3 (`SENSITIVE_ENV_PATTERN`, "one heuristic for every in-repo spawner"), S2; `bash.js:156`, `background-bash.ts:352, 725`, `permission.ts:1117–1128`, reproduced `assessRisk` output. Risk is real (a project's test needing `STRIPE_API_KEY` breaks until the user adds a passthrough) — mitigated by the visible count/notice and the exact-name passthrough; default-on because darwin's own key is otherwise one `echo` away in `default` mode |
| 4 | SER-083 | `/tangent` over the rewind machinery: bare `/tangent` when idle and not in a tangent records the *next* completed prompt's rewind snapshot as the return point and shows `tangent` in the header state and busy rows; `/tangent` again (or `/tangent end`) runs the existing `startRewind` to that snapshot — same successor runtime, same omission notice (files, shell, hooks, MCP writes, subagents, jobs, memory are not undone) plus one line naming how many prompts were discarded; the restored prompt is *not* returned to the draft (the user asked to return, not to resend); `/clear`, `/rewind` and a failed snapshot capture end the tangent with a notice; `/status` states `tangent: since prompt N`; nested tangents refused with a notice | 3 | 4 | 4 | 3 | 3 | **8** | S6; `src/agent/rewind.ts`, `AgentRuntime.startRewind`, load-bearing § `/rewind` (cap 100, completed prompts only, capture skipped at cap — the tangent must handle "no snapshot" honestly), `rewind-search.ts`. One level only; naming/picker deliberately omitted — `/rewind`'s picker already lists every boundary |

### Gated-out directions

| Direction | Score | Gate | Why it fell below | Kept anyway? |
|---|---:|---:|---|---|
| Hierarchical instruction files: root→cwd walk and concatenation (S2c), ancestor + subdirectory on-demand load (S1), JIT (S7) | 3 (I 2, F 1, E 5, D 3, R 4) | 6 | Contradicts the recorded decision in `instructions.ts:4–6` ("no walking up … no merging"); on-demand loading would need a tool-result hook injecting instructions mid-turn — a second path for prompt text into context | no |
| `@path` imports inside `AGENTS.md` (S1, S7) | 5 (I 2, F 3, E 5, D 3, R 4) | 6 | A committed file could import `~/.aws/credentials` into the system prompt; Claude Code added an approval dialog for exactly this. Sensitive-read gating of prompt composition is new machinery for a convenience | no |
| Full Codex `shell_environment_policy` (inherit `none`/`core`, include allowlists, `set`) (S2, S2b) | 5 (I 2, F 3, E 5, D 4, R 3) | 6 | SER-082's bounded scrub plus passthrough covers the safety case; allowlist semantics break every project tool until tuned | no |
| Claude Code `/import` — copy another agent's instruction files, MCP servers, commands and skills into darwin's layout (S1) | 3 (I 2, F 2, E 4, D 4, R 3) | 6 | Darwin already reads `.mcp.json` and `.agents/` directly; copying config creates two sources of truth | no |
| kiro `/goal` autonomous objective loop (S6) | 0 (I 2, F 2, E 4, D 5, R 5) | 6 | An unattended loop with self-verification is a new driver, not an extension point; the developer skill already supervises bounded autonomous work | no |
| kiro queue steering mid-turn (S6) | — | — | Not scored: a deliberate difference (SER-027, next-turn-only queue; gated as "mid-turn fold" 2026-09-06) | — |

### Recommendation

- Batch to implement: `SER-080`, `SER-081`, `SER-082`, `SER-083`
- Start with: `SER-080`
- Why this order: SER-081's fixed prompt has to state which instruction files darwin reads, so the fallback (SER-080) lands first; SER-081 and SER-082 are independent and follow by score; SER-083 is last as the lowest-scored and the only one touching the rewind successor path.
- Independently observable acceptance:
  - SER-080 — in a temp repo with only `CLAUDE.md`, `darwin doctor` and the TUI header name `CLAUDE.md: loaded (N B)`; the system prompt fragment carries `source="CLAUDE.md"`; with both files present only `AGENTS.md` is loaded and `CLAUDE.md` is never read (strace/`readFile` spy in the spike); an unreadable `AGENTS.md` (directory in its place) still reports `AGENTS.md: skipped — …` and does not fall through; `pnpm test` green; new `spike/verify-instructions-fallback.ts`† added to `pnpm test`.
  - SER-081 — `/init` completes in `tui completion` with every built-in still visible (`MAX_COMPLETIONS` check), `/help` lists it, `pnpm tsx spike/verify-init-command.ts` (free) proves the expansion is one fixed prompt with the focus embedded verbatim and that bare `/init` in a repo *with* `AGENTS.md` expands to the improve-in-place variant; `.darwin/commands/init.md` is refused as reserved; a live run in a scratch repo produces an `AGENTS.md` under 32 KiB through a prompted `fileEditor create` (default mode) and is denied in plan mode.
  - SER-082 — `spike/verify-shell-env.ts`† proves `scrubShellEnv` drops `ANTHROPIC_API_KEY`, `AWS_SECRET_ACCESS_KEY`, `NPM_TOKEN`, `DB_PASSWORD`, keeps `PATH`, `HOME`, `LANG`, `HTTPS_PROXY`, and honours `passthrough: ['STRIPE_*', 'NPM_TOKEN']`; a real runtime bash call `echo "[$ANTHROPIC_API_KEY]"` returns `[]` while `!echo "[$ANTHROPIC_API_KEY]"` still shows the value; background `bash start` job env is scrubbed the same way; `/status` shows the `shell env` row; `verify-config.ts` refuses an invalid `shellEnv` key; existing `verify-background-bash.ts`† and `tui bashExit` unchanged.
  - SER-083 — `spike/verify-tangent.ts`† (free, over the rewind spike's fixtures): enter, two prompts, return → conversation equals the pre-tangent snapshot, no draft restored, notice names 2 discarded prompts and the omission list; nested `/tangent` refused; `/clear` during a tangent ends it with a notice; `tui tangent` scenario shows the header state. `verify-rewind.ts`† unchanged.
- Backlog updates: `docs/research/backlog/directions-101-120.md` — SER-080 (P108), SER-081 (P109), SER-082 (P110), SER-083 (P111) appended as `not-started` (the range has room to P120, no rollover); nothing set `abandoned` by the gate at selection; SER-080 changed to `in-progress` only immediately before the developer handoff.

### Batch iteration outcome

| Direction | Child session and managed tasks | Host acceptance (exact independent checks and outcomes) | Commit | Final status | Iteration-log entry |
|---|---|---|---|---|---|
| `SER-080` | child `session-20260908-081857202`, task `bg-5b299a00-c519-4f4c-b1a8-c96a8b12ce2e` (exit 0, no correction) | Diff read (loader, fragment, header helper, doctor, docs); `verify-agents-md.ts` 59/59, `verify-doctor-command.ts` 78/78, `verify-system-prompt.ts` 50/50, `verify-context-format.ts` 49/49, `verify-visual-language.tsx` 74/74; real `doctor` in CLAUDE.md-only / both (unreadable CLAUDE.md) / directory-AGENTS.md temp projects behaved as specified; `pnpm typecheck` 0; `pnpm test` 0 (6036 PASS, 0 FAIL); `pnpm build` 0; `AGENTS.md` 32,753 B | `cfaff3e` | `done` | Batch 112 |

- Halt condition that ended the loop: _(pending)_
- Aggregate token spend across every child delegation: _(pending)_
- Remaining backlog after this run: _(pending)_
