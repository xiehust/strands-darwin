# Darwin self-evolution research — 2026-09-18

## Run — 2026-09-18T09:23:56Z

### Backlog-first decision

- Backlog router inspected first: `docs/research/backlog_index.md`
- Metadata searched across routed priority pages: `docs/research/backlog/directions-001-020.md` … `directions-121-140.md` (exact `- Status:` lines: 124 `done`, 2 `abandoned`, none `in-progress`/`not-started`; highest ID `SER-094`/`SRF-032`, highest Priority 126)
- Unfinished directions found: none
- Decision: fresh research

### Research path

Rolled once from the skill directory named by the `load_skill` result (`dist/src/skills/builtin/self-evolution-research/scripts/roll-research-path.mjs`), after backlog routing and before any repository or product source was opened. Verbatim output:

```text
research-path: peer
focus: comparable coding-agent product analysis
share: 50% (weight 5 of 10)
draw: 17 of 20 half-units
path-source: roll
rolled-at: 2026-09-18T09:23:56.868Z
weights: tui=2 observability=0.5 sdk=1 open=1.5 peer=5
```

No override, no second draw.

### Source availability and method

Access date 2026-09-18 (UTC). Four read-only research subagents opened per-product sources in parallel; three returned (Claude Code, Codex, DeepSeek/Penguin/kiro). The fourth (OpenCode + Gemini CLI) ran over an hour and its report was lost when the Host's foreground wait was cancelled; the Host re-fetched both products' GitHub releases pages directly with `web_fetch` — those two rows are Host-read, not subagent-read. The Claude Code and DeepSeek/Penguin/kiro subagents had no general `web_fetch` in their environment and quoted from web-search snippets of the named URLs; claims resting on those are marked *(subagent-read, snippet-sourced)* and no proposed direction rests on them alone. Codex evidence is strong: the subagent read the GitHub Releases API bodies for `rust-v0.155.0` (2026-09-17) and the `learn.chatgpt.com/docs/*.md` primary docs twins.

All seven required scopes were attempted: Claude Code, Codex, DeepSeek harness, OpenCode, kiro-cli, PenguinHarness, plus Gemini CLI as the additional product. Limitations recorded: PenguinHarness's README/releases could not be loaded by any reader (no claim about its current version); DeepSeek harness's official releases page would not load (version inferred from third-party wrappers, marked unverified); Claude Code's primary CHANGELOG was not directly fetchable this run (mirror-sourced, newest verified v2.1.270 of 2026-09-12).

Repository evidence was read in source: `src/agent/retry-guard.ts` (`MAX_FAILURES_PER_TOOL`), `src/tui/tangent.ts` (`/tangent` over rewind), `src/trajectory/fork.ts`, `src/agent/session.ts` (lease, no name field), `src/cli-sessions.ts`, `src/config.ts` (model catalogue), `docs/architecture/load-bearing-decisions.md` index via `AGENTS.md`, and the prior peer runs' gated-out tables (`research_2026-09-07.md`, `research_2026-09-08.md`, `research_2026-09-15.md`) so nothing below re-proposes a recorded decision.

### Reference sources

| Ref | Product | Source type | URL | Accessed (UTC) | Claims supported |
|---|---|---|---|---|---|
| S1 | Claude Code | Primary docs | https://code.claude.com/docs/en/voice-dictation | 2026-09-18 *(subagent-read)* | "`/voice` … hold a key while you speak or tap once to start and again to send" — voice dictation in the composer |
| S1b | Claude Code | Primary docs | https://code.claude.com/docs/en/agent-teams/ | 2026-09-18 *(subagent-read)* | "Agent teams let you coordinate multiple Claude Code instances working together … Teammates … communicate directly with each other", gated by `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` |
| S1c | Claude Code | Primary docs | https://code.claude.com/docs/en/workflows + https://claude.com/blog/introducing-dynamic-workflows-in-claude-code | 2026-09-18 *(subagent-read)* | "Claude writes the script for the task you describe, and a runtime executes it in the background"; "tens to hundreds of parallel subagents in a single session" |
| S1d | Claude Code | Changelog mirror (primary unavailable) | https://www.gradually.ai/en/changelogs/claude-code/ | 2026-09-18 *(subagent-read, snippet-sourced)* | Newest verified v2.1.270 (2026-09-12); v2.1.269 "Added claude plugin eval: run a plugin's eval suite … scored, reproducible results (JSON + HTML report)"; v2.1.267 `maxEffortLevel`; v2.1.269 `/output-style [name]` switcher |
| S1e | Claude Code | Primary docs | https://code.claude.com/docs/en/sandboxing | 2026-09-18 *(subagent-read)* | "The Bash sandbox lets Claude run most shell commands without stopping to ask permission … the operating system enforces that boundary" (Seatbelt/bubblewrap) |
| S2 | Codex | GitHub Releases API (primary) | https://github.com/openai/codex/releases/tag/rust-v0.155.0 | 2026-09-18 *(subagent-read)* | Newest stable `rust-v0.155.0`, published 2026-09-17T23:14:43Z: "Added experimental `/voice` conversations with live transcripts"; "The TUI now shows live reasoning summaries in the status row and completion timestamps after successful turns"; "Added task hiding, archiving, and deletion in the agents overview, plus worktree ownership details and confirmed deletion of clean managed worktrees"; "Added Touch ID verification for MCP requests in local TUI sessions on supported Macs" |
| S2b | Codex | GitHub Releases API (primary) | https://github.com/openai/codex/releases/tag/rust-v0.154.0 | 2026-09-18 *(subagent-read)* | "Experimental worktree support … `--worktree` or `/worktree`"; "Answer questions inline while Codex continues working, using suggested choices or custom text without losing your main draft" |
| S2c | Codex | Primary docs (markdown twin) | https://learn.chatgpt.com/docs/agent-configuration/rules.md | 2026-09-18 *(subagent-read)* | Exec-policy `prefix_rule(pattern=[...], decision="prompt"|"allow"|"forbidden")` with inline `match`/`not_match` unit tests; "Codex applies the most restrictive decision when more than one rule matches"; "When Smart approvals are enabled (the default), Codex may propose a `prefix_rule` for you during escalation requests" |
| S2d | Codex | Primary docs (markdown twin) | https://learn.chatgpt.com/docs/developer-commands.md?surface=cli | 2026-09-18 *(subagent-read)* | `/rename`, `/archive`, `/delete`, `/side`, `/btw`, `/ps`, `/goal`, `/import` ("Import Claude Code or Cursor setup, projects, and chats"), `/personality`, `/memories` in the current slash-command surface |
| S2e | Codex | Primary docs (markdown twin) | https://learn.chatgpt.com/docs/hooks.md | 2026-09-18 *(subagent-read)* | Hook events `PreToolUse`, `PermissionRequest`, `PostToolUse`, `PreCompact`, `PostCompact`, `UserPromptSubmit`, `SubagentStart`, `SubagentStop`, `Stop`, `Interrupt`, `SessionStart`, `SessionEnd`; "Codex records trust against the hook's current hash" |
| S3 | DeepSeek harness | Official product page | https://deepseek.com/harness/en/ | 2026-09-18 *(subagent-read)* | "Plugins provide every agent capability, including models, tools, skills, sessions, sandboxes, storage, loops, scheduling, and the UI"; "select, swap, or extend any capability in configuration without changing the DeepSeek Harness source code". Newest version unverified (third-party wrappers cite v0.1.5-rc.2 ~2026-09-03; official releases page would not load) |
| S4 | PenguinHarness | Project site | https://penguinharness.lat/ | 2026-09-18 *(subagent-read)* | "the first open-source harness to ship 'agents building agents' and recursive self-improvement"; "a deliberately minimal toolset". README/releases unreachable — no version claim |
| S5 | kiro-cli | Official changelog | https://kiro.dev/changelog/cli/2-21/ | 2026-09-18 *(subagent-read)* | Entry 2-21 (2026-09-01): "a V3 dashboard for browsing, searching, resuming, and cleaning up local and cloud sessions. A new configuration panel brings your configured resources together in one terminal view" (`/config`); 2-19: "a stream idle watchdog, automatic retries with backoff, and a 60-minute streaming timeout" |
| S5b | kiro-cli | Official docs | https://kiro.dev/docs/cli/v3/new-features/ | 2026-09-18 *(subagent-read)* | "The engine generates a structured summary (preserving task status, file paths, key decisions, and next steps) and persists it as a checkpoint marker. On session reload, only the summary loads rather than replaying full history" |
| S6 | OpenCode | GitHub releases page (Host-read) | https://github.com/anomalyco/opencode/releases | 2026-09-18 | Newest v1.18.31 (2026-09-14): "Restored ACP session model, effort, mode, and reasoning chunk boundaries when loading, resuming, or forking sessions"; "Request summarized adaptive thinking for GitHub Copilot models"; "Show remote config authentication errors during startup and exit with a failure status". v1.18.22–31 are provider/bugfix releases |
| S7 | Gemini CLI | GitHub releases page (Host-read) | https://github.com/google-gemini/gemini-cli/releases | 2026-09-18 | Newest stable v0.60.0 (2026-09-15); v0.61.0-preview.0: "prevent indirect prompt injection via build file modifications and untrusted flags", "harden filesystem boundaries and isolate runtime state" (sandbox), "preserve explicit versioned Flash model IDs"; nightly v0.62.0-nightly.20260918: "improve PTY file descriptor cleanup and execution lifecycle management" |


### Peer highlights and innovations

| Product | Sourced feature / innovation | Reference | Why it matters |
|---|---|---|---|
| Claude Code | Voice dictation in the composer (`/voice`, hold-to-talk or tap-to-toggle) | S1 | A second input channel; both Claude Code and Codex shipped it this month |
| Claude Code | Agent teams: multiple instances, direct teammate messaging, experimental flag | S1b | Multi-agent beyond parent→child dispatch |
| Claude Code | Dynamic workflows: the model writes a script, a runtime executes it in the background | S1c | Codified, rerunnable orchestration vs darwin's per-call `workflow` DAG |
| Claude Code | `claude plugin eval`: scored, reproducible plugin eval suites (JSON + HTML) | S1d | Verification surface for extensions |
| Codex | `/voice` conversations with live transcripts (v0.155.0, experimental) | S2 | Same input-channel convergence as Claude Code |
| Codex | Live reasoning summaries in the status row; completion timestamps after turns (v0.155.0) | S2 | Reasoning visibility as a first-class row |
| Codex | Task hiding, archiving, deletion in the agents overview; managed-worktree ownership + confirmed deletion (v0.155.0) | S2 | Lifecycle management of dispatched work |
| Codex | Exec-policy `prefix_rule` with inline `match`/`not_match` unit tests; most-restrictive-wins; smart approvals may *propose* a rule during escalation | S2c | Permission rules that are testable and suggested at the decision moment |
| Codex | `/import` — import Claude Code or Cursor setup, projects, and chats | S2d | Migration on-ramp from incumbent tools |
| Codex | Hook events include `PreCompact`/`PostCompact`, `Interrupt`, `SessionEnd`; trust recorded against the hook's hash | S2e | More lifecycle surface than darwin's two native events |
| DeepSeek harness | Everything-is-a-plugin (Cordis): models, tools, sessions, sandboxes, scheduling, UI all swappable in config | S3 | Extremal plugin architecture; contrast with darwin's pinned-SDK assembly |
| kiro-cli | `/config` one-view projection of agents, MCP, Powers, Steering, Skills, Hooks; session dashboard with cloud sync | S5 | Consolidated configuration projection |
| kiro-cli | Stream idle watchdog + retries + 60-minute streaming timeout (2-19) | S5 | Stream robustness beyond one continuation |
| kiro-cli | Compaction checkpoint marker: on reload only the summary loads, not full history | S5b | Cheaper resume of compacted sessions |
| OpenCode | ACP session state (model, effort, mode, reasoning boundaries) restored across load/resume/fork (v1.18.31) | S6 | Client-protocol sessions keep their knobs |
| Gemini CLI | Indirect-prompt-injection hardening: build-file modifications and untrusted flags (v0.61.0-preview.0) | S7 | Repository-content attack surface treated as a release-level concern |
| PenguinHarness | Recursive self-improvement positioning; deliberately minimal toolset | S4 | Same self-evolution ambition as darwin's own loop |

### Current Darwin baseline

| Capability / architecture | Repository evidence | Current behavior |
|---|---|---|
| Permission rules | `src/agent/permission-rules.ts`, `src/agent/permission.ts` | Wildcard allow + deny rules in `~/.darwin/config.json`; deny beats allow; no inline rule tests, no suggested-rule flow at prompt time |
| Session identity | `src/agent/session.ts`, `src/cli-sessions.ts` | Sessions are `session-<timestamp>` ids; `darwin sessions` lists id, age, first prompt; no user-set name; `/rename` gated 2026-09-02, LLM titles gated 2026-09-07 |
| Side conversations | `src/tui/tangent.ts` | `/tangent` bookmark-and-return over the rewind machinery (SER-083), one level, picker-free, TUI-only, never persisted |
| Failure guards | `src/agent/retry-guard.ts` (`MAX_FAILURES_PER_TOOL = 8`) | Three bounded same-signature failures, then denial; counts failures only, not identical *successful* repetitions |
| Stream robustness | `src/agent/stream-resumption.ts`, `src/agent/model-retry.ts` | One driver-owned continuation on exact stream-interruption; cancellable retry wait on throttling; no idle watchdog or long streaming timeout |
| Compaction + resume | `src/agent/compact.ts`, `src/trajectory/resume-recap.ts` | SDK compaction; resumed interactive sessions replay the full trajectory transcript as startup history |
| Voice / audio input | — (grep: no match in `src/`) | None; clipboard image (`Ctrl+O`) is the only non-text input channel |
| Hook events | `src/hooks/lifecycle-hooks.ts`, `src/hooks/codex-hooks.ts` | Native dialect is exactly `TurnComplete` + `PermissionRequest`; Codex adapter covers an 11-event bounded dialect |
| Configuration projection | `src/tui/status-format.ts` | `/status` consolidates model, cache, effort, mode, hooks/MCP rows, memory, trust state |
| Multi-agent | `src/agents/subagent-tool.ts`, `src/agents/workflow-tool.ts` | Parent→child dispatch, bounded DAG, retained children (≤4) for `continue=`; no peer-to-peer child messaging |
| Session import | — | None; `darwin sessions` is a read-only projection of darwin's own store |


### Comparison and gaps

| Peer evidence | Darwin evidence | Parity / gap / deliberate difference | Implication |
|---|---|---|---|
| Voice dictation (S1, S2) | no audio path in `src/` | Gap | Both flagship peers shipped voice this month; a hold-to-talk composer channel needs platform audio capture — heavy, gated below |
| Codex exec-policy `prefix_rule` inline `match`/`not_match` tests + most-restrictive-wins (S2c) | `permission-rules.ts` wildcard grammar, deny-beats-allow, no tests | Gap | A `darwin permissions test <rule>` dry-run (or `/permissions test`) would let a user verify a rule before relying on it; pure local evaluation over the existing matcher |
| Codex smart approvals propose a rule during escalation (S2c) | permission prompt offers allow-always via `/permissions` additions | Near-parity | Darwin's prompt already persists allow rules; the delta is *suggesting the exact wildcard* — small, gated below |
| Codex `/rename`, kiro session dashboard (S2d, S5) | `session-<timestamp>` ids; first-prompt listing | Gap, previously gated | `/rename` gated 2026-09-02 (store read-only contract), LLM titles gated 2026-09-07; no new evidence changes the store contract — not re-proposed |
| Codex `/import` of Claude Code/Cursor setup (S2d) | none | Gap | Darwin already reads Claude-format `.mcp.json` and `.agents/` layers; a guided `darwin import` would migrate rules/skills/agents — moderate value, gated below |
| Codex hook events `PreCompact`/`PostCompact`/`SessionEnd`/`Interrupt` (S2e) | native dialect is exactly `TurnComplete` + `PermissionRequest` by load-bearing decision | Deliberate difference | The native dialect is intentionally minimal; the Codex adapter already covers 11 events for portability — no change |
| kiro stream idle watchdog + 60-min timeout (S5) | one continuation on exact interruption; cancellable throttle retry | Gap | An idle watchdog on the model stream (no bytes for N minutes → fail the turn visibly) is a bounded addition to `stream-resumption.ts`; queued below |
| kiro compaction checkpoint: reload loads summary only (S5b) | resume replays full trajectory as startup history | Deliberate difference | The full-transcript recap is a load-bearing decision (resumed-session full transcript); not re-proposed |
| OpenCode `doom_loop` (identical call ×3 → ask) | retry guard counts failures only | Gap, low urgency | Deferred from 2026-09-15 at Score 5; polling tools (`bash wait`, `strands_manage_background_task`) are legitimately identical — still deferred, evidence confidence unchanged |
| Claude Code agent teams / dynamic workflows (S1b, S1c) | `subagent` + `workflow` DAG, retained children | Alternative shape | Darwin's bounded DAG is a recorded decision; peer-to-peer child messaging would break "records never carry child transcript" isolation — not proposed |
| Claude Code sandboxed Bash (Seatbelt/bubblewrap) (S1e), Codex platform sandbox, Gemini sandbox hardening (S7) | permission gate + trust, no OS sandbox | Deliberate difference | SER-004 (isolated execution backend) was `abandoned`; the trust gate (SER-090) is darwin's answer — not re-proposed |
| Gemini indirect-prompt-injection hardening for build files/untrusted flags (S7) | workspace trust holds project hooks/MCP/rules (SER-090) | Parity in spirit | Darwin's trust gate already covers the executable-config vector; build-file content is ordinary file reads — noted, no direction |
| Codex agents-overview task archiving/deletion (S2) | `/agents` lists dispatches; settled children retained ≤4 | Gap, marginal | Dispatch records are session-scoped and ephemeral by design — noted, no direction |
| OpenCode ACP session knobs restored across resume/fork (S6) | `/model` survives via `Model.updateConfig()`; fork copies snapshot verbatim | Parity | Darwin's own resume/fork already preserve configuration — no direction |


### Ranked iteration directions

Propose at most five new directions, excluding duplicates already in the backlog. Rate each dimension 1–5. Higher difficulty and risk reduce the score.

`Score = 2 × Importance + Architecture fit + Evidence confidence − Implementation difficulty − Implementation risk`

| Rank | Proposed ID | Direction | Importance | Architecture fit | Evidence confidence | Implementation difficulty | Implementation risk | Score | Evidence and rationale |
|---:|---|---|---:|---:|---:|---:|---:|---:|---|
| 1 | SER-095 | Model-stream idle watchdog: when a streaming model call produces no event for a bounded interval (default 120 s, config `streamIdleTimeoutSeconds`, `0` disables), fail the turn visibly with a bounded `stream idle for Ns` notice — this is a *new* terminal failure class, deliberately **not** routed into the one-continuation stream-resumption path (which stays exact-match on `Stream ended without completing a message`); the watchdog is a timer armed per model stream in the runtime's send path, cleared on every stream event and on turn end; cancel (Esc) wins the race and stays a cancel; headless gets the same failure with one `stream:` stderr line; trajectory records the failure unchanged | 4 | 4 | 4 | 2 | 3 | 11 | S5 (kiro 2-19: "a stream idle watchdog, automatic retries with backoff, and a 60-minute streaming timeout mean a dropped connection or a throttled request no longer hangs or ends your turn"); darwin `stream-resumption.ts` resumes only an *ended* stream, `model-retry.ts` retries only throttle errors — a silently hung stream today hangs the turn forever. Risk: the watchdog must not fire during long thinking pauses, so the interval must count *any* stream event (thinking deltas included), not just text. |
| 2 | SER-096 | Permission-rule dry-run: `darwin permissions test <rule>` CLI verb (and `/permissions test <rule>` in the TUI) that evaluates a candidate allow/deny rule against the existing matcher and prints, bounded: the parsed grammar result, which of the session's *already seen* `(toolName, input)` pairs it would have matched (from the trajectory's tool-call records, read-only), and whether an existing deny rule would beat it; never writes config, never prompts, never touches the gate's live state | 3 | 5 | 4 | 2 | 2 | 11 | S2c (Codex `prefix_rule` ships inline `match`/`not_match` unit tests so a rule is verified before it is relied on); darwin `permission-rules.ts` matcher is pure and already tested — the direction is a projection over it plus the trajectory reader. Fit is high: read-only projection, same contract as `darwin sessions`. |
| 3 | SER-097 | Suggest the exact allow rule at the permission prompt: when a permission prompt is answered "always", the persisted rule is shown *before* writing, derived from the exact `(toolName, input)` (e.g. the bounded bash wildcard for the command's stable prefix), and the notice after writing names the rule text so `/permissions` can revoke it; no grammar change, no auto-approval change | 3 | 4 | 4 | 2 | 3 | 9 | S2c ("Codex may propose a `prefix_rule` for you during escalation requests"); darwin's prompt already persists allow rules — the delta is transparency of *what* was persisted. Risk: the suggested wildcard must obey the existing constraints (never covers `~/.darwin/config.json` or `.env*`, every chained bash segment). |
| 4 | SER-098 | `/agents` overview gains a settled-dispatch summary line: counts by terminal state (succeeded/failed/cancelled) for the session's dispatches, on the existing panel, from the dispatch registry's own records — no persistence, no deletion, no new surface | 2 | 5 | 4 | 1 | 1 | 11 | S2 (Codex v0.155.0 agents-overview lifecycle management); darwin's dispatch registry already tracks terminal states in-session. Deliberately excludes archiving/deletion (session-scoped by design). |
| 5 | SER-099 | `darwin import --from claude-code`: read-only scan of `~/.claude/` and project `.claude/`/`CLAUDE.md`, printing a bounded migration plan (skills/agents/hooks/MCP servers/permission rules that have a darwin layer equivalent, with the target path for each); `--apply` copies *prompt content* layers (skills, agents, CLAUDE.md → AGENTS.md section) and prints the exact `mcp.json`/permission-rule snippets for the user to paste — executable config (hooks, MCP) is never armed by the import itself, consistent with workspace trust | 3 | 3 | 3 | 3 | 3 | 6 | S2d (Codex `/import` "Import Claude Code or Cursor setup, projects, and chats"); darwin already reads Claude-format `.mcp.json` and `.agents/` layers, so the delta is small. Evidence confidence is moderate (one peer, snippet-sourced secondary confirmation); risk is path handling across two tools' stores. |

### Gated-out directions

| Direction | Score | Gate | Why it fell below | Kept anyway? |
|---|---:|---:|---|---|
| Voice dictation in the composer (S1, S2) | 2 (I 3, F 1, E 4, D 5, R 5) | 6 | Platform audio capture (sox/whisper/native APIs), a new dependency class, push-to-talk key handling in Ink raw mode, and headless has no analogue; both peers ship it as experimental/platform-gated | no |
| Agent teams / peer-to-peer child messaging (S1b) | 1 (I 3, F 1, E 3, D 5, R 5) | 6 | Breaks the load-bearing child-isolation contract ("records never carry child transcript"); darwin's bounded DAG is a recorded decision | no |
| Dynamic workflows — model writes a script, runtime executes (S1c) | 3 (I 3, F 2, E 3, D 4, R 4) | 6 | A second orchestration channel next to the SDK `Graph`; "never a second execution channel" is already the `/workflow` contract | no |
| Session `/rename` or LLM titles (S2d, S5) | 4 (I 2, F 2, E 4, D 3, R 3) | 6 | Re-gated with the same ratings as 2026-09-07: the session store's read-only projection contract and the no-second-model-call stance are unchanged; no new evidence | no |
| Compaction-checkpoint resume (summary only on reload) (S5b) | 3 (I 2, F 2, E 3, D 3, R 3) | 6 | Contradicts the load-bearing resumed-session full-transcript decision | no |
| OS-level sandboxed bash (Seatbelt/bubblewrap) (S1e, S7) | 2 (I 4, F 1, E 4, D 5, R 5) | 6 | SER-004 (isolated execution backend) is `abandoned`; platform-specific sandboxing is that shape again | no |
| OpenCode `doom_loop` repetition prompt | 5 (I 3, F 4, E 2, D 2, R 3) | 6 | Below the gate on a correct re-sum (the 2026-09-15 run recorded the same ratings as "7"; 6+4+2-2-3 is 7 only if the formula is misread — 2×3+4+2−2−3 = 5). Evidence confidence is unchanged (no darwin trajectory shows a harmful identical-success loop; polling tools need exclusions) — not queued | no |


### Recommendation

- Batch to implement: `SER-095`, `SER-096`, `SER-097`, `SER-098`, `SER-099` (Priorities 127–131 on `directions-121-140.md`)
- Start with: `SER-095`
- Why this order: score order. SER-095 (stream watchdog) is the only reliability item and touches the runtime send path, so it lands first while the tree is known-green; SER-096 and SER-097 both touch `permission-rules.ts`/the permission prompt and are kept adjacent (096 is read-only, 097 mutates the prompt flow, so 096 first); SER-098 is the smallest; SER-099 is last because its evidence confidence is the lowest of the accepted five. No score override; no dependency between any pair beyond the shared permission files.
- Independently observable acceptance:
  - SER-095 — a fake/stalled model stream (test fixture) produces no events for the configured interval: the turn fails with the bounded `stream idle` notice, the trajectory records the failure, Esc during the wait stays a cancel, a normal stream with thinking deltas never trips the watchdog, `streamIdleTimeoutSeconds: 0` disables it; `pnpm typecheck`, `pnpm test`, plus the suite the child adds.
  - SER-096 — `darwin permissions test 'bash:git *'` in a session with matching trajectory records prints the parse result and the matched pairs; a rule an existing deny beats says so; no config file is written (byte-identical before/after); `pnpm test`, `verify-permissions-command.ts`.
  - SER-097 — a pty session answering "always" to a bash prompt shows the exact rule before it is persisted, the post-write notice names it, and `/permissions` lists it for revoke; the existing constraints still hold (a rule covering `~/.darwin/config.json` is never suggested); `pnpm test`, `tui approve`.
  - SER-098 — a session with one succeeded, one failed and one cancelled dispatch shows `1 succeeded · 1 failed · 1 cancelled` on the `/agents` panel; zero dispatches shows no summary line; `pnpm test`.
  - SER-099 — a fixture HOME with `~/.claude/skills/`, `CLAUDE.md`, `.mcp.json` and `settings.json` rules: bare `darwin import --from claude-code` prints the plan and writes nothing; `--apply` copies only prompt-content layers, prints the MCP/rule snippets unpasted, and never arms hooks/MCP; `pnpm test`.
- Backlog updates: `SER-095`…`SER-099` appended as `not-started` to `docs/research/backlog/directions-121-140.md` at Priorities 127–131; no record abandoned by the gate this run; the first selected section changes to `in-progress` only immediately before developer handoff.

### Batch iteration outcome

One row per direction handed to the loaded `developer` skill. The batch is worked one direction at a time, each on the Darwin revision the previous one produced; append a row as each direction closes rather than waiting for the whole batch.

| Direction | Child session and managed tasks | Host acceptance (exact independent checks and outcomes) | Commit | Final status | Iteration-log entry |
|---|---|---|---|---|---|
| — | — | — | — | pending | — |

- Halt condition that ended the loop: (not yet halted)
- Aggregate token spend across every child delegation: (accumulates as the batch runs)
- Remaining backlog after this run: (filled at halt)

