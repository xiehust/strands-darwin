# Darwin self-evolution research — 2026-08-24

## Run — 2026-08-24T01:30:51Z

### Backlog-first decision

- Backlog inspected first: `docs/research/backlog_index.md`
- Unfinished directions found: none
- Decision: fresh research

### Research path

```text
research-path: peer
focus: comparable coding-agent product analysis
share: 50% (weight 5 of 10)
draw: 19 of 20 half-units
path-source: roll
rolled-at: 2026-08-24T01:29:05.829Z
weights: tui=2 observability=0.5 sdk=1 open=1.5 peer=5
```

### Source availability and method

Web search, primary documentation pages, public GitHub source/raw files, and the local Darwin repository were available on 2026-08-24 UTC. Claude Code, Codex, DeepSeek Harness, PenguinHarness, and OpenCode were verified from primary sources. Claims below are limited to what those sources state; PenguinHarness's benchmark and cost claims remain publisher claims because its README still says public release of the suite is pending. Repository source, specifications, tests, and recent history were inspected before characterizing Darwin. This run focused on deterministic lifecycle automation because multiple peers expose session/turn completion or attention-needed events while Darwin's otherwise mature layered hook system exposes only model tool-call boundaries.

### Reference sources

| Ref | Product | Source type | URL | Accessed (UTC) | Claims supported |
|---|---|---|---|---|---|
| S1 | Claude Code | Primary docs | <https://code.claude.com/docs/en/hooks> | 2026-08-24 | Lifecycle hooks include `Notification`, `Stop`, and `SubagentStop`; notification types distinguish permission/input/completion cases, and hook output can request allowlisted terminal notification sequences. |
| S2 | Codex | Primary docs | <https://developers.openai.com/codex/config-reference/> | 2026-08-24 | User-level `notify` is an external command array invoked with a JSON notification payload; project config cannot override notification keys. |
| S3 | DeepSeek Harness | Primary architecture documentation | <https://github.com/deepseek-ai/deepseek-harness/blob/master/docs/architecture.md> | 2026-08-24 | Durable `turn/start`/`turn/end` events and live agent events are explicit plugin extension points; event domain is chosen by whether facts must survive reload or observe work in flight. |
| S4 | PenguinHarness | Primary repository README | <https://github.com/Prism-Shadow/penguin-harness/blob/main/README.md> | 2026-08-24 | PenguinHarness presents benchmark-driven self-evolution with per-round snapshots and Trace observability; its public benchmark suite remains on the roadmap. |
| S5 | OpenCode | Primary docs source | <https://github.com/anomalyco/opencode/blob/dev/packages/web/src/content/docs/plugins.mdx> | 2026-08-24 | Plugins subscribe to lifecycle events including `session.idle`; the official example sends a desktop notification on session completion, and the desktop app notifies on ready/error states. |

### Peer highlights and innovations

| Product | Sourced feature / innovation | Reference | Why it matters |
|---|---|---|---|
| Claude Code | Separate lifecycle events identify ordinary completion, subagent completion, and moments needing attention; terminal notification output is mediated rather than written directly by a hook. | S1 | Automation can distinguish “done” from “come back and decide,” while terminal ownership stays with the host. |
| Codex | A user-level external notification command receives structured JSON and cannot be replaced by repository-local config. | S2 | Completion notification is useful, but allowing an untrusted checkout to install an arbitrary turn-end command would be the wrong trust boundary. |
| DeepSeek Harness | Turn boundaries are first-class events, separate from tool interception and durable session facts. | S3 | Lifecycle automation should attach at the driver/runtime boundary, not masquerade as a synthetic tool call or modify the SDK loop. |
| PenguinHarness | Publisher-visible Trace and evaluation rounds emphasize auditability, but the benchmark remains unpublished. | S4 | Darwin's trajectory and supervision paper trail already cover the auditable loop; no benchmark direction is justified by unreproducible numbers. |
| OpenCode | `session.idle` is a plugin event used by the official docs for completion notification. | S5 | A completion hook is established coding-agent UX, especially when a long turn finishes outside the focused terminal. |

### Current Darwin baseline

| Capability / architecture | Repository evidence | Current behavior |
|---|---|---|
| Layered executable hooks | `src/config.ts:912-965,1030-1139` (`hooksField`, `loadProjectPolicy`); `src/paths.ts`; `docs/user-guide/extensions.md:122-124` | Global/project `.agents` and `.darwin` hook files merge deterministically and fail closed, but the accepted event vocabulary is only `PreToolUse` and `PostToolUse`. |
| Hook execution and process cleanup | `src/hooks/tool-hooks.ts:48-140` (`runToolHookCommand`); `.trellis/spec/backend/error-handling.md:58-65` | Hook commands receive bounded-structure JSON on stdin, run in the project cwd with output off the TUI, and cancellation reaps the process group. This runner is coupled to tool name/input and has no lifecycle payload seam. |
| Turn completion seam | `src/tui/App.tsx:560-620` (`runTurn`); `src/headless-runner.ts`; `src/agent/runtime.ts:707-760` | Interactive turns have one explicit success/failure/finally boundary after ordinary `runtime.send`; headless has a separate driver. No configured command runs when a turn completes or fails. |
| Attention-needed seam | `src/tui/permission-queue.ts:29-130`; `src/tui/App.tsx:320-326,1211-1246` | Permission requests are explicit live session state and can wait indefinitely for the user, but no external signal tells a user who has switched terminals that Darwin needs a decision. |
| Hook trust boundary | `src/config.ts:1042-1058`; `.trellis/spec/backend/error-handling.md:58-65`; `AGENTS.md` extension-layer invariant | Project hook files are deliberately executable policy and already protected from allow-rules. Unlike Codex notification config, Darwin knowingly permits repository-scoped hooks; any new event must retain fail-closed loading and never give a hook a second route to model context or tool authorization. |
| Observability and durable record | `src/trajectory/writer.ts`; `.trellis/spec/backend/session-trajectory.md`; `src/agent/diagnostics.ts` | Turn outcomes are already append-only trajectory facts and diagnostics are optional. Lifecycle commands should consume a projection only; they must not add synthetic conversation or trajectory records. |

### Comparison and gaps

| Peer evidence | Darwin evidence | Parity / gap / deliberate difference | Implication |
|---|---|---|---|
| Claude, Codex, and OpenCode expose completion/attention notification automation (S1, S2, S5) | Darwin hooks stop at `PreToolUse`/`PostToolUse` (`hooksField`, `ToolHookGate`) | **Gap** | Add narrow lifecycle command hooks for turn completion and permission-needed, reusing layering and process ownership rather than adding built-in OS-specific notification code. |
| DeepSeek models turn boundaries as extension events outside tools (S3) | `runTurn` already owns the interactive terminal outcome; runtime remains the sole SDK assembly | **Architecture fit** | Dispatch from the ordinary drivers after/beside their existing state transitions; never forge a tool event, intercept `Agent.stream`, or fork the SDK loop. |
| Codex keeps notification command config user-owned (S2) | Darwin's extension contract intentionally supports executable project hook policy | **Deliberate trust-model difference** | Do not silently introduce a separate global-only config system. Preserve visible source paths, fail-closed active files, and the existing rule that hook paths are un-ruleable; document that project lifecycle hooks execute repository code. |
| Claude mediates terminal escape sequences (S1) | Ink owns Darwin's terminal and current hooks keep stdout/stderr off it | **Parity to preserve** | Hooks must not write directly into the TUI or model context. External notification programs may use OS facilities; Darwin reports only bounded hook failures on existing notice/stderr surfaces. |
| PenguinHarness advertises benchmark-led evolution but still withholds the suite (S4) | SER-005 was explicitly abandoned and Darwin already records trajectories plus independent acceptance | **No corrected premise** | Do not reopen the benchmark direction or restate its score. |

### Ranked iteration directions

`Score = 2 × Importance + Architecture fit + Evidence confidence − Implementation difficulty − Implementation risk`

Only directions scoring at or above `MINIMUM_IMPLEMENTATION_SCORE = 6` enter the backlog. Dependencies come first.

| Rank | Proposed ID | Direction | Importance | Architecture fit | Evidence confidence | Implementation difficulty | Implementation risk | Score | Evidence and rationale |
|---:|---|---|---:|---:|---:|---:|---:|---:|---|
| 1 | SER-038 | Extend Darwin's layered command hooks with bounded lifecycle events for `TurnComplete` and `PermissionRequest`: structured outcome/source payloads, deterministic global/project ordering, non-blocking observation-only execution, and process-group cleanup; no terminal writes, model context, permission decisions, trajectory records, or SDK-loop interception | 4 | 5 | 5 | 4 | 3 | **11** | Three peer products expose completion or attention-needed automation (S1, S2, S5), and DeepSeek validates turn events as a separate extension domain (S3). Darwin already has layered fail-closed hook discovery and a reaping shell runner; the work is a narrow event vocabulary plus driver/permission publication seams. Difficulty is 4 because TUI and headless outcomes, permission withdrawal/cancel, payload bounds, process lifetime, `/clear`, and shutdown need one honest contract. Risk is 3 because hooks execute user-configured commands, but they are observation-only and can be kept outside the model/gate/trajectory channels. |

### Gated-out directions

| Direction | Score | Gate | Why it fell below | Kept anyway? |
|---|---:|---:|---|---|
| Build native cross-platform desktop notifications into Darwin | 4 (I 3, F 2, E 5, D 4, R 5) | 6 | Platform APIs, SSH/tmux behavior, focus detection, dependencies, and user preference policy add product surface that configurable lifecycle commands already cover. Claude's mediated terminal sequence is useful evidence for hook safety, not a reason for Darwin to own every notifier. | no |
| Add generic arbitrary lifecycle plugins across every runtime event | 2 (I 3, F 2, E 4, D 5, R 5) | 6 | Darwin's load-bearing architecture keeps the SDK loop and state channels narrow. A general plugin API would expose mutable runtime internals, require versioning many payloads, and duplicate SDK extension points. The accepted need is only two observable lifecycle moments. | no |
| Re-open a local benchmark/evaluation corpus from PenguinHarness claims | 2 (unchanged from the 2026-08-23 run: I 3, F 2, E 2, D 5, R 3) | 6 | The primary README still says public release of the benchmark suite is pending, so no evidence correction changes SER-005's abandoned premise or prior ratings. | no |
| Detach subagent calls into background jobs and notify on completion | 3 (unchanged from the 2026-08-23 run: I 3, F 3, E 4, D 5, R 5) | 6 | Completion notification does not resolve detached-job ownership, result reinjection, cancellation, `/clear`, or shared-worktree mutation. The new evidence supports lifecycle observation, not asynchronous subagent semantics. | no |

### Recommendation

- Batch to implement: `SER-038`
- Start with: `SER-038`
- Why this order: it is the only accepted direction and has no dependency. It extends the existing layered hook policy at two driver-owned lifecycle seams, preserving runtime assembly, permission ownership, trajectory observation, and Ink terminal ownership.
- Independently observable acceptance: add focused offline tests that load all four extension layers and prove exact `TurnComplete`/`PermissionRequest` Pre-like order, strict schema rejection and source reporting, one bounded JSON object per command, success/failure/cancelled outcomes, parent/subagent source labels where available, exactly-once permission notification despite queued/withdrawn prompts, observation-only nonzero/launch failures, bounded non-blocking turn completion, TERM→KILL reaping on cancel/shutdown, no hook stdout/stderr in model/TUI/trajectory, no permission resolution or tool execution from a lifecycle hook, and absent config spawning nothing. Exercise interactive and headless drivers without provider calls, `/clear` successor behavior, then run `verify-tool-hooks.ts`, permission/subagent/trajectory/headless focused suites, `pnpm typecheck`, `pnpm test`, and `pnpm build`.
- Backlog updates: add `SER-038` as `not-started`; change it to `in-progress` only after the clean starting-point checks pass and immediately before developer handoff.

### Batch iteration outcome

| Direction | Child session and managed tasks | Host acceptance (exact independent checks and outcomes) | Commit | Final status | Iteration-log entry |
|---|---|---|---|---|---|
| SER-038 | `session-20260824-013615973`; implementation `bg-07db22aa-f364-453d-8ddc-cdba3c44bd92` and same-session focused correction `bg-65b30294-3a97-42c0-9bfa-19375f167f2f` (both exit 0) | Host inspected the implementation, found the focused lifecycle test incorrectly assumed detached child completion order (17/19), and accepted only after a per-command-sink correction. Independently passed lifecycle (20), config (231), state layers (37), tool hooks (44), permission mode (100), subagents (69), headless (80), structured headless (11), clear-session (44), trajectory (267), `pnpm typecheck`, full `pnpm test`, `pnpm build`, Trellis archive validation, commit/diff/clean-tree and AGENTS-size checks. | `8ae7855` + correction `b673cd5` (+ archive `1ac2894`) | done | [`Batch 52`](../iteration-log.md) |


- Halt condition that ended the loop: batch exhausted — every accepted direction is `done`
- Aggregate token spend across every child delegation: `input=456 output=66,856 cacheRead=25,133,403 cacheWrite=226,436`
- Remaining backlog after this run: none unfinished; fresh research is appropriate on the next invocation
