# Darwin self-evolution research — 2026-08-26

## Run — 2026-08-26T11:43:24Z

### Backlog-first decision

- Backlog inspected first: `docs/research/backlog_index.md`
- Unfinished directions found: none
- Decision: fresh research

### Research path

```text
research-path: peer
focus: comparable coding-agent product analysis
share: 50% (weight 5 of 10)
draw: 12 of 20 half-units
path-source: roll
rolled-at: 2026-08-26T11:43:24.929Z
weights: tui=2 observability=0.5 sdk=1 open=1.5 peer=5
```

### Source availability and method

Primary documentation and public source repositories were available over HTTPS on 2026-08-26 UTC. Claude Code, Codex, DeepSeek Harness, PenguinHarness, and OpenCode were all opened and verified. Product claims below are limited to the cited pages or checked-out source; PenguinHarness benchmark and cost numbers are explicitly publisher claims, not independent measurements. Darwin comparisons come from current repository source, tests, specs, README, and history at `a2efd5b`.

### Reference sources

| Ref | Product | Source type | URL | Accessed (UTC) | Claims supported |
|---|---|---|---|---|---|
| S1 | Claude Code | Primary docs | https://code.claude.com/docs/en/interactive-mode | 2026-08-26 | `Ctrl+R` opens interactive reverse prompt-history search; fullscreen mode filters as typed, navigates matches, supports session/project/all-project scope, accepts with Enter/Tab, cancels with Escape, and collapses duplicates newest-first. |
| S2 | Claude Code | Primary docs | https://code.claude.com/docs/en/checkpointing | 2026-08-26 | Claude Code automatically captures code state before each prompt and can rewind conversation, code, or both; it keeps the 100 most recent session checkpoints. |
| S3 | Codex | Primary docs | https://developers.openai.com/codex/cli/features | 2026-08-26 | Codex documents `Ctrl+R` prompt-history search and interactive TUI operation. |
| S4 | Codex | Primary docs | https://developers.openai.com/codex/app/review | 2026-08-26 | `/review` starts a dedicated reviewer over a selected diff and reports prioritized findings without changing the working tree. |
| S5 | DeepSeek Harness | Primary source repository | https://github.com/deepseek-ai/deepseek-harness/blob/b150a551b8d465e31e418e1b2eaf5e79bbb7d28e/docs/architecture.md | 2026-08-26 | The append-only session event log drives model context, replay, UI, fork/resume and telemetry; `ctx.sessions.fork(source, boundary?, childSessionId?)` can fork at a boundary. |
| S6 | PenguinHarness | Primary source repository | https://github.com/Prism-Shadow/penguin-harness/blob/b90f5aa76534ee714f0336e6b98827f58240d298/README.md | 2026-08-26 | Publisher describes benchmark/evaluation/optimization skills with a snapshot before every evolution round and trace visibility; public benchmark suite remains roadmap work. |
| S7 | PenguinHarness | Primary source repository | https://github.com/Prism-Shadow/penguin-harness/blob/b90f5aa76534ee714f0336e6b98827f58240d298/packages/core/README.md | 2026-08-26 | Core exposes append-only traces, complete trace-based session restore, per-call approvals, concurrent tools, interrupt carry-over and compaction. |
| S8 | OpenCode | Primary source repository | https://github.com/sst/opencode/blob/1216c550944de69f73732a907deabcfd5f477cdb/packages/tui/src/feature-plugins/home/tips-view.tsx | 2026-08-26 | `/undo` reverts the last message and file changes, `/redo` restores them, and `/share` publishes a session link. |
| S9 | OpenCode | Primary source repository | https://github.com/sst/opencode/blob/1216c550944de69f73732a907deabcfd5f477cdb/packages/opencode/src/session/revert.ts | 2026-08-26 | Revert stores/restores a filesystem snapshot, removes later messages/parts, and provides an unrevert path. |

### Peer highlights and innovations

| Product | Sourced feature / innovation | Reference | Why it matters |
|---|---|---|---|
| Claude Code | Interactive, scoped reverse prompt search rather than only sequential `Up` recall. | S1 | Long-lived users can retrieve a known old prompt without walking every newer entry. |
| Claude Code | Prompt-bound code/conversation checkpoints with selective rewind. | S2 | Recovery couples conversational state and mutations instead of leaving users to reconcile them manually. |
| Codex | A dedicated, read-only review workflow returns prioritized findings. | S4 | Review has a clear no-mutation boundary and independent role rather than depending on an ordinary prompt. |
| DeepSeek Harness | One durable event stream is both context authority and the boundary-aware fork substrate. | S5 | A fork can branch a historical conversation boundary without rewriting its source record. |
| PenguinHarness | Evaluation/optimization rounds are snapshot- and trace-visible; benchmark superiority remains a publisher claim while the suite is not public. | S6, S7 | It reinforces measurable iteration, but does not supply independently reproducible evidence for Darwin to copy. |
| OpenCode | `/undo` and `/redo` jointly restore session messages and filesystem snapshots. | S8, S9 | This is powerful recovery, but its mutation and history-rewrite semantics conflict with Darwin's observer-only trajectory unless separately designed. |

### Current Darwin baseline

| Capability / architecture | Repository evidence | Current behavior |
|---|---|---|
| Prompt recall | `src/tui/App.tsx` prompt-recall state/read path; `src/tui/prompt-recall.ts`; `src/trajectory/prompt-history.ts`; `spike/verify-prompt-recall.ts` | `Up`/`Down` sequentially walk project trajectory prompts only after menu, queued-message take-back and cursor-row ownership have passed. There is no `Ctrl+R` search mode (`rg 'Ctrl.R|history search' src` has no implementation hit). |
| Offline trajectory fork | `src/cli-trajectory.ts::runTrajectoryCommand`; `src/trajectory/fork.ts`; `.trellis/spec/backend/session-trajectory.md` | `darwin trajectory fork` is offline and non-mutating to the source, but the interactive TUI has no rewind/undo surface and trajectory remains an observer rather than the SDK snapshot authority. |
| Session restore | `src/agent/session.ts`; `src/trajectory/resume-recap.ts`; `spike/verify-resume-recap.ts` | SDK snapshots restore model history; trajectory supplies a bounded human recap, not filesystem rollback. |
| Review workflow | `.darwin`/`.agents` custom commands and skills; `src/skills/plugin.ts`; `spike/verify-custom-commands.ts` | A project can define a review skill/command, but Darwin has no canonical built-in dedicated reviewer and no generic child transcript in parent trajectory. |
| Self-evolution evaluation | `docs/research/backlog_index.md`; `docs/iteration-log.md`; developer skill contract in `.trellis/spec/backend/strands-sdk-contracts.md` | Every accepted direction gets focused and full independent checks, but SER-005's stable scorecard was explicitly abandoned; no new evidence here resolves that product decision. |
| Safety/state invariants | `AGENTS.md` load-bearing decisions; `.trellis/spec/backend/strands-sdk-contracts.md`; `.trellis/spec/backend/session-trajectory.md` | The SDK loop stays intact; trajectory is observer-only; persistent shell and file mutations are real host/worktree state; `/clear` creates a successor runtime rather than rewriting one. |

### Comparison and gaps

| Peer evidence | Darwin evidence | Parity / gap / deliberate difference | Implication |
|---|---|---|---|
| Claude Code and Codex provide `Ctrl+R` history search (S1, S3). | Darwin has bounded trajectory-backed sequential recall and mature editor/menu key precedence, but no search mode. | Narrow usability gap over an existing read-only data source. | Add a bounded local search overlay that reuses prompt history and preserves all current key owners. |
| Claude Code checkpoints and OpenCode undo/redo couple history with file snapshots (S2, S8, S9). | Darwin trajectory is explicitly observer-only, snapshots belong to SDK conversation restore, and shell/file changes are not transactionally captured. | Deliberate architectural difference; parity would require a new mutation authority and rollback safety policy. | Do not queue without a user decision on rollback scope, dirty-tree interaction, shell side effects and snapshot authority. |
| DeepSeek can fork a session at a durable event boundary (S5). | Darwin already has offline fork primitives, but current SDK snapshots—not trajectory—own resumable model history. | Partial parity; arbitrary-boundary forking would need a trustworthy snapshot/conversation reconstruction contract. | Research may revisit after measuring SDK snapshot boundaries; current evidence does not justify an implementation iteration. |
| Codex has a dedicated read-only reviewer (S4). | Darwin can delegate a reviewer through existing skills/subagents, and project workflows already run independent acceptance. | Product packaging gap, not a missing primitive. | A built-in reviewer risks duplicating skills and needs product decisions about scope and findings UX; do not queue from this run. |
| PenguinHarness markets benchmark-driven self-optimization (S6). | Darwin's SER-005 scorecard was explicitly abandoned, while each developer iteration already has independently observed checks. | Existing product decision controls. | Do not silently reopen SER-005; publisher-only benchmark evidence is insufficient to correct its rating or decision. |

### Ranked iteration directions

`Score = 2 × Importance + Architecture fit + Evidence confidence − Implementation difficulty − Implementation risk`

| Rank | Proposed ID | Direction | Importance | Architecture fit | Evidence confidence | Implementation difficulty | Implementation risk | Score | Evidence and rationale |
|---:|---|---|---:|---:|---:|---:|---:|---:|---|
| 1 | SER-039 | Add bounded `Ctrl+R` reverse prompt-history search over Darwin's existing project trajectory: filter newest-first with duplicate collapse, navigate matches, accept into the editor, cancel without changing the draft, and preserve completion/queue/permission/compaction key ownership and frame bounds. | 3 | 5 | 5 | 3 | 2 | 11 | S1 and S3 establish peer behavior. Darwin already has the mutation-free reader (`src/trajectory/prompt-history.ts`), recall state (`src/tui/prompt-recall.ts`), editor mirrors and one-row bounded recall projection (`src/tui/App.tsx`, `src/tui/InputBox.tsx`). Scope stays project-only to match Darwin's existing state boundary; cross-project scope is deliberately excluded. |

### Gated-out directions

| Direction | Score | Gate | Why it fell below | Kept anyway? |
|---|---:|---:|---|---|
| Add prompt-bound code/conversation rewind with `/undo` and `/redo`. | 4 | 6 | Importance 4 and evidence 5 are outweighed by architecture fit 1, difficulty 5 and risk 5: Darwin has no transactional record of arbitrary shell side effects, trajectory must remain observer-only, and dirty-worktree rollback policy is unresolved. | no |
| Add a canonical built-in read-only `/review` reviewer. | 5 | 6 | Importance 2, fit 3, evidence 5, difficulty 3, risk 4: existing skills/subagents can express review, while canonical scope, finding persistence and reviewer independence need product decisions. | no |
| Add arbitrary historical-boundary session fork in the TUI. | 5 | 6 | Importance 2, fit 3, evidence 4, difficulty 3, risk 3: Darwin's trajectory and SDK snapshot authorities differ, so a safe reconstruction premise is unproven. | no |
| Reopen a stable benchmark scorecard for self-evolution. | 4 | 6 | Importance 4, fit 3, evidence 2, difficulty 5, risk 4: PenguinHarness's suite is still not public and SER-005 was explicitly abandoned; this run has no corrected rating or new user decision. | no |

### Recommendation

- Batch to implement: `SER-039`
- Start with: `SER-039`
- Why this order: it is the sole accepted direction and has no dependency. It reuses a proven local reader and editor seam without changing model, permission, session or trajectory authority.
- Independently observable acceptance: focused pure search-state tests; trajectory prompt-history mutation/absence/damage/overlong behavior retained; free pty proof for `Ctrl+R` open/filter/navigate/accept/cancel, empty history, draft preservation, `Escape`, completion/queue precedence, permission denial and compaction ownership; `pnpm typecheck`, `pnpm test`, `pnpm build`, Trellis validation, frame-budget checks, clean diff/commit.
- Backlog updates: add `SER-039` as `not-started`; change it to `in-progress` only immediately before developer handoff.

### Batch iteration outcome

| Direction | Child session and managed tasks | Host acceptance (exact independent checks and outcomes) | Commit | Final status | Iteration-log entry |
|---|---|---|---|---|---|
| SER-039 | Child `session-20260826-115313304`; managed task `bg-7136ab3f-6aeb-4861-a88c-59870aafa8e0` succeeded, exit 0; no correction turn. | Host inspected the 24-file implementation and independently passed pure search 19, frame budget 80, help 26, recall 61, free pty `historySearch` 11, `compacting` 5, `cursor` 5, `completion` 66, `pathCompletion` 27, `recall` 22, `recallEmpty` 4, `permissionEscape` 3, `pnpm typecheck`, full `pnpm test`, `pnpm build`, Trellis archive validation, structural no-I/O/model/network grep, commit/diff/clean-tree and AGENTS-size checks. The unchanged `queue` fixture also failed at pre-change `315cb71`, so it is recorded as pre-existing PTY nondeterminism rather than a SER-039 acceptance failure. | `9cdbffc` | done | `docs/iteration-log.md` — Batch 60 |

- Halt condition that ended the loop: batch exhausted — the sole accepted direction is `done`.
- Aggregate token spend across every child delegation: input=310 output=39,330 cacheRead=16,770,648 cacheWrite=163,026.
- Remaining backlog after this run: no `in-progress` or `not-started` direction remains; fresh research may run on the next invocation.

## Run — 2026-08-26T12:29:54Z

### Backlog-first decision

- Backlog inspected first: `docs/research/backlog_index.md`
- Unfinished directions found: none
- Decision: fresh research, directed by the user toward how Darwin should implement `/rewind`

### Research path

```text
research-path: peer
focus: comparable coding-agent product analysis
share: 50% (weight 5 of 10)
draw: 13 of 20 half-units
path-source: roll
rolled-at: 2026-08-26T12:27:31.386Z
weights: tui=2 observability=0.5 sdk=1 open=1.5 peer=5
```

### Source availability and method

Primary product documentation, public source repositories, the installed Strands TypeScript SDK API, and Darwin repository source were available on 2026-08-26 UTC. Claude Code, Codex, DeepSeek Harness, PenguinHarness, and OpenCode were all opened. Product claims are limited to the cited pages or pinned revisions; PenguinHarness's “snapshot before every evolution round” remains a publisher statement and is not treated as evidence of an interactive rewind feature. Darwin was inspected at `0e65eb1` with `@strands-agents/sdk@1.12.0` installed. The decisive change since this day's earlier run is verified SDK capability: immutable conversation snapshots, listing, and targeted restore are now public APIs, so conversation rewind no longer requires making Darwin's trajectory authoritative.

### Reference sources

| Ref | Product | Source type | URL | Accessed (UTC) | Claims supported |
|---|---|---|---|---|---|
| S1 | Claude Code | Primary docs | https://code.claude.com/docs/en/checkpointing | 2026-08-26 | `/rewind` or empty-composer double-Escape opens prompt checkpoints; checkpoints are taken before each user prompt, retain the latest 100, and offer conversation-only, code-only, combined restore, and two summarize modes. Bash, most subagent, external, symlink, and hard-link changes are explicitly not restored. |
| S2 | Codex | Primary source repository | https://github.com/openai/codex/blob/f5420174dafba153913a3e697f89002c338dfd7e/codex-rs/tui/src/app_backtrack.rs | 2026-08-26 | Double-Escape backtracking selects a completed user prompt, forks the source thread before that turn, and restores the selected prompt into the new composer for editing; stale, in-progress, and mid-turn steer targets are rejected. |
| S3 | DeepSeek Harness | Primary source repository | https://github.com/deepseek-ai/deepseek-harness/blob/b150a551b8d465e31e418e1b2eaf5e79bbb7d28e/docs/architecture.md | 2026-08-26 | Its append-only session log is model-context authority and `ctx.sessions.fork(source, boundary?, childSessionId?)` forks a live session at an optional boundary. |
| S4 | PenguinHarness | Primary source repository | https://github.com/Prism-Shadow/penguin-harness/blob/b90f5aa76534ee714f0336e6b98827f58240d298/packages/core/README.md | 2026-08-26 | Sessions restore fully from append-only Trace; the root README separately claims a snapshot before each self-evolution round, but documents no interactive prompt rewind contract. |
| S5 | OpenCode | Primary docs and source | https://opencode.ai/docs/tui/ ; https://github.com/sst/opencode/blob/1216c550944de69f73732a907deabcfd5f477cdb/packages/opencode/src/session/revert.ts | 2026-08-26 | `/undo` and `/redo` restore messages and file changes in a Git repository; source records a pre-revert snapshot, applies patches, and keeps an unrevert path. |
| S6 | Strands Agents TypeScript SDK | Primary API docs | https://strandsagents.com/docs/api/typescript/SessionManager/ ; https://strandsagents.com/docs/api/typescript/SaveLatestStrategy/ | 2026-08-26 | `SessionManager` exposes immutable checkpoint creation through `snapshotTrigger`, chronological `listSnapshotIds()`, and targeted `restoreSnapshot({snapshotId})`; immutable snapshots are append-only and distinct from mutable `snapshot_latest`. |

### Peer highlights and innovations

| Product | Sourced feature / innovation | Reference | Why it matters |
|---|---|---|---|
| Claude Code | One prompt-bound chooser separates conversation rewind from filesystem restore and documents exactly what filesystem restore misses. | S1 | `/rewind` is not one atomic promise: safe products make the state domains and omissions explicit. |
| Codex | Backtracking is a source-preserving conversation branch, not destructive transcript truncation. | S2 | A bad branch can be abandoned without destroying the source session, and the old prompt is immediately editable. |
| DeepSeek Harness | Boundary forks are straightforward because the durable event stream is also model-context authority. | S3 | Darwin cannot copy this mechanism directly: its trajectory is intentionally only an observer. |
| PenguinHarness | Trace can restore the session, but no verified interactive rewind UX is documented. | S4 | Trace durability alone is not evidence that a product safely exposes historical branching. |
| OpenCode | Undo/redo couples a session revert record with filesystem snapshots and an explicit inverse operation. | S5 | Filesystem rollback needs its own mutation authority and recovery path; it is not a small addition to conversation restore. |
| Strands SDK | Public immutable snapshots and targeted restore now provide the missing conversation authority. | S6 | Darwin can implement conversation rewind through the SDK extension point rather than reconstructing messages from trajectory or forking the agent loop. |

### Current Darwin baseline

| Capability / architecture | Repository evidence | Current behavior |
|---|---|---|
| Conversation snapshots | `src/agent/session.ts::createSessionManager`; `src/agent/runtime.ts::create`; installed SDK `SessionManager` API | Darwin configures `saveLatestOn: 'invocation'`, so only mutable `snapshot_latest` is used. The installed SDK also supports immutable snapshots and targeted restore, but Darwin supplies no `snapshotTrigger` and exposes no list/restore operation. |
| Source-preserving whole-session fork | `src/trajectory/fork.ts::forkSession`; `src/cli-trajectory.ts::runTrajectoryCommand` | `darwin trajectory fork <id>` byte-copies the source's latest SDK snapshot and full trajectory into a new session. It cannot choose a historical snapshot or reopen the selected prompt in the live TUI. |
| Authority boundary | `.trellis/spec/backend/session-trajectory.md:40-43`; `.trellis/spec/backend/strands-sdk-contracts.md:1547-1565` | SDK snapshots alone restore model state; trajectory is append-only observation and must never be replayed into model context. Restoring a snapshot also restores system prompt and skill app state, after which Darwin refreshes only current working context, learned-memory projection, and cache placement. |
| Runtime succession | `src/agent/runtime.ts::startNewSession`; `src/tui/App.tsx::App` | `/clear` proves a user-only command can assemble a successor runtime, retire the predecessor, inherit process-owned MCP/background-bash state, and avoid moving the resume pointer until the successor finishes a turn. |
| Filesystem mutation coverage | `src/agent/runtime.ts` tool catalogue; `src/hooks/tool-hooks.ts::ToolHookGate`; `src/tui/shell-command.ts`; subagent tools | Writes can come from `fileEditor`, persistent `bash`, user `!`, MCP tools, project hooks, or children. Darwin has no complete transactional attribution or backup layer, and background processes can mutate after a checkpoint. |
| Historical UI | `src/tui/prompt-history-search.ts`; `src/trajectory/prompt-history.ts`; `src/tui/App.tsx`; `spike/verify-prompt-history-search.ts` | The TUI now has bounded local history selection and mature key ownership, but it selects prompt text only and never changes model state. |

### Comparison and gaps

| Peer evidence | Darwin evidence | Parity / gap / deliberate difference | Implication |
|---|---|---|---|
| Claude and Codex expose prompt-bound conversation backtracking (S1, S2). | Darwin has prompt history and whole-session latest-state fork, but no historical SDK checkpoint catalogue. | Clear conversation-recovery gap. | Create immutable SDK checkpoints before invocations, map them to bounded prompt metadata, and branch from a selected checkpoint. |
| Codex preserves the source thread (S2). | Darwin's `forkSession` already makes source bytes immutable and `/clear` already switches runtimes safely. | Strong architecture precedent, but only at latest state. | `/rewind` should create a successor session from a selected source checkpoint; never truncate the active source trajectory or overwrite its latest snapshot. |
| DeepSeek derives historical forks from its authoritative event log (S3). | Darwin's trajectory is explicitly non-authoritative. | Deliberate difference. | Use trajectory only for display/provenance when available; model restoration must use the SDK snapshot even when `trajectory: false`. |
| Claude and OpenCode restore files, but with stated omissions or Git coupling (S1, S5). | Darwin permits more mutation routes than either a file-tool-only backup or a Git patch can safely attribute. | Still unsafe for a general code rewind. | Ship conversation-only rewind first and state “workspace unchanged”; do not imply that code, shell side effects, hooks, children, or background jobs were rolled back. |
| Strands now exposes immutable snapshots and targeted restore (S6). | Darwin currently opts into only mutable latest snapshots. | Newly verified extension point; this corrects the earlier premise that arbitrary-boundary conversation restoration required trajectory reconstruction. | The implementation can remain a thin SDK session policy plus TUI/runtime orchestration, preserving the “SDK loop is never forked” decision. |

### Ranked iteration directions

`Score = 2 × Importance + Architecture fit + Evidence confidence − Implementation difficulty − Implementation risk`

| Rank | Proposed ID | Direction | Importance | Architecture fit | Evidence confidence | Implementation difficulty | Implementation risk | Score | Evidence and rationale |
|---:|---|---|---:|---:|---:|---:|---:|---:|---|
| 1 | SER-040 | Add conversation-only `/rewind` as a source-preserving branch at a completed prompt boundary: create and catalogue bounded SDK immutable snapshots before invocations, choose a prior prompt in-session, restore that SDK checkpoint into a fresh successor session, and put the selected prompt back in the editor. The command must never mutate the source session or workspace and must explicitly report that code, shell side effects, background jobs, hooks, subagents, and learned-memory files were not rewound. | 4 | 4 | 5 | 4 | 3 | 10 | S1/S2 establish the UX and source-preserving branch shape; S6 supplies the previously missing authoritative restore primitive. `createSessionManager`, `AgentRuntime.create`/`startNewSession`, `forkSession`, the prompt-history overlay, and `/clear` are existing seams. The risk is bounded by conversation-only scope, strict completed-boundary validation, a fresh session id, no source overwrite, and no trajectory-to-model reconstruction. |

### Gated-out directions

| Direction | Score | Gate | Why it fell below | Kept anyway? |
|---|---:|---:|---|---|
| Add general code + conversation `/rewind` parity. | 5 | 6 | Importance 4 and evidence 5 do not overcome architecture fit 2, difficulty 5, and risk 5. S1 now confirms the exact omissions Darwin had already identified: Bash, most child, external, and linked-file changes escape file-tool checkpoints; Darwin additionally has `!`, MCP, hooks, and live background jobs. | no |
| Reconstruct historical model context from trajectory and truncate the current session in place. | 0 | 6 | Importance 2, fit 1, evidence 4, difficulty 4, risk 5: this would invert the observer-only trajectory contract, destroy source-preserving recovery, and drift from SDK snapshots after compaction. | no |
| Add a fileEditor-only “code rewind” while leaving every other mutation route untouched. | 4 | 6 | Importance 3, fit 3, evidence 5, difficulty 5, risk 5: although implementable, the name would over-promise safety in a product where Bash, hooks, MCP, children, `!`, and concurrent user edits remain live. | no |

### Recommendation

- Batch to implement: `SER-040`
- Start with: `SER-040`
- Why this order: it is the sole accepted direction. Conversation branching is independently useful and now has a public SDK authority; filesystem restore remains below the gate and is not a hidden dependency.
- Independently observable acceptance: focused SDK checkpoint catalogue tests (initial/pre-invocation boundary, bounded retention/labels, failed-turn and resumed-session behavior, `trajectory: false` independence); source and workspace byte hashes unchanged by selection/branch; selected checkpoint restored into a new session while source latest/immutable snapshots/trajectory remain byte-identical; selected prompt restored to the editor; explicit “workspace unchanged” notice; refusal while busy, for stale/unmapped/in-progress targets, and when no checkpoint exists; resume pointer unchanged until a successor turn completes; inherited permission mode/MCP/background task ownership; prompt/cache/skill/working-context restoration contracts; free pty `/rewind` and `completion`; `pnpm typecheck`, `pnpm test`, `pnpm build`, Trellis validation, clean diff/commit and AGENTS.md size.
- Backlog updates: add `SER-040` as `not-started`; change it to `in-progress` only immediately before developer handoff.

### Batch iteration outcome

| Direction | Child session and managed tasks | Host acceptance (exact independent checks and outcomes) | Commit | Final status | Iteration-log entry |
|---|---|---|---|---|---|
| SER-040 | Child `session-20260826-123525049`; managed tasks `bg-f64d9202-996c-4274-bac3-980df3cc0f9d`, `bg-5d688c44-6ec3-4061-ac2f-9c0ccb51c85c`, and `bg-4b27036a-3d7d-4770-84be-9f8099a557e9`, all exit 0. | Host inspected the implementation, rejected `db57a87` because rewind-owned immutable snapshot creation was unbounded, and accepted only after `9ef6cc0` imposed a serialized hard 100-snapshot cap. Host independently passed runtime rewind 20/20, chooser 7/7, free pty `rewind` 7/7, completion 67/67, frame budget 80/80, help 26/26, prompt queue 28/28, `pnpm typecheck`, full `pnpm test`, `pnpm build`, Trellis archive validation, commit/diff/clean-tree and AGENTS-size checks. | `db57a87` + `9ef6cc0` | done | `docs/iteration-log.md` — Batch 61 |

- Halt condition that ended the loop: batch exhausted — the sole accepted direction is `done`.
- Aggregate token spend across every child delegation: input=814 output=144,918 cacheRead=55,156,644 cacheWrite=584,417.
- Remaining backlog after this run: no `in-progress` or `not-started` direction remains; fresh research may run on the next invocation.
