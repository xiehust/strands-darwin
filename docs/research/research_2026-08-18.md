# Darwin self-evolution research — 2026-08-18

## Run — 2026-08-18T01:25:31Z

### Backlog-first decision

- Backlog inspected first: [`backlog_index.md`](./backlog_index.md)
- Unfinished directions found at invocation start: **`SER-012` (`in-progress`)** — and it turned out to be a
  *stale row*, not unfinished work. The direction was implemented and Host-accepted on 2026-08-17 in
  `53105b0` + `6fc919a` + `906c400` + `06ef164` + `16cee9a` + `d5b0d68` (+ archive `02dbcb2`), with a
  full supervision batch recorded in [`../iteration-log.md`](../iteration-log.md); only the status column
  was never updated. This run closed it *on evidence, not on the child's report*: all seven commits
  confirmed ancestors of `b7e227f` with `git merge-base --is-ancestor`, the official core present in
  `src/skills/{loader,plugin,prompt,resource-safety}.ts` reaching `AgentSkills` through
  `@strands-agents/sdk/vended-plugins/skills`, clean working tree, `pnpm typecheck` exit 0, and
  `pnpm test` reporting 33 suite summaries all `0 failed` with no `FAIL`.
- Decision: **fresh research**, taken only after `SER-012` was closed as `done`, at which point no
  `in-progress` or `not-started` row remained. `SER-004`/`SER-005` stay explicitly `abandoned` by user
  decision and were not reopened.

### Research path

Rolled exactly once, before any repository or product source was opened. Verbatim script output:

```text
research-path: peer
focus: comparable coding-agent product analysis
share: 50% (weight 5 of 10)
draw: 17 of 20 half-units
path-source: roll
rolled-at: 2026-08-18T01:20:14.030Z
weights: tui=2 observability=0.5 sdk=1 open=1.5 peer=5
```

This was a chance roll, not a user override, and it was not re-rolled.

### Source availability and method

Web search and direct HTTPS access to primary product documentation and repositories were available on
2026-08-18 UTC; every page below was fetched with `curl` and read locally (Claude Code, Codex and
OpenCode publish markdown renderings of their docs, which is what was read). All five required scopes
were verified from primary sources: Claude Code, Codex, DeepSeek Harness, PenguinHarness, and the
additional product **OpenCode**. PenguinHarness accuracy/cost figures remain publisher claims and are
labelled as such; no benchmark claim is used to justify a direction.

Repository evidence comes from the current tree at `b7e227f` — source, `spike/` suites, `.trellis/spec/`
and `AGENTS.md`. As section 3.2 requires, previously shipped, gated-out or user-abandoned work is treated
as such: structured headless output (`SER-011`), trajectory list/search/replay/fork (`SER-003`,
`SER-006`, `SER-007`), plan mode itself (`SER-001`), parallel labelled subagents (`SER-002`) and the
no-queue busy-submit contract (`SER-010`) are **not** re-proposed, and the app-server, filesystem-rewind
and evaluation-corpus scopes stay closed with their reasons restated below.

### Reference sources

| Ref | Product | Source type | URL | Accessed (UTC) | Claims supported |
|---|---|---|---|---|---|
| S1 | Claude Code | Primary product docs | <https://code.claude.com/docs/en/permission-modes> | 2026-08-18 | Permission modes are switched **during a session** (`Shift+Tab` cycles `default` → `acceptEdits` → `plan`, with `bypassPermissions`/`auto` slotting in when available); the active mode is shown as a status indicator; "Asking Claude in chat to change the permission mode doesn't work"; a resumed session keeps the mode it was in unless `--permission-mode` is passed; and if the mode changes **while a classifier check is pending**, the verdict the new mode would not have requested is discarded rather than applied. |
| S2 | Claude Code | Primary product docs | <https://code.claude.com/docs/en/interactive-mode> | 2026-08-18 | `Shift+Tab` cycles permission modes; `@` triggers file-path autocomplete; `!` starts shell mode; prompt history is kept **per working directory** and Up-arrow recall reaches prompts from past sessions of the same project, with consecutive duplicates collapsed to one entry; `Ctrl+R` is interactive reverse history search with highlighted matches and `Enter`/`Tab`/`Esc` semantics. Also: on the newest model families Claude Code **no longer provides the todo tools** and the task list stays empty because the model tracks multi-step work without a written checklist. |
| S3 | Codex | Primary product docs | <https://learn.chatgpt.com/docs/developer-commands?surface=cli> | 2026-08-18 | Interactive shortcuts: `@` searches for a workspace file and adds its **path** to the prompt; `Up`/`Down` restore draft history; `Ctrl+R` searches prompt history; `!` prefix runs a local shell command under the current approval and sandbox settings; `Tab` queues a follow-up while working. Slash commands include `/permissions` (switch approval preset mid-session, e.g. Auto ↔ Read Only), `/status` (active model, approval policy, writable roots, token usage) and `/statusline` (choose footer items). `codex resume` reopens a session by id or `--last`; `codex fork` opens a session picker. |
| S4 | Codex | Primary product docs | <https://developers.openai.com/codex/cli/features/> | 2026-08-18 | One terminal loop covers interactive work, automation, review and delegation; `/permissions` is presented as "set the boundaries for each run — inspect the active sandbox and writable roots before you continue"; `codex review` reports findings without modifying the working tree. |
| S5 | DeepSeek Harness | Primary product page | <https://deepseek.com/harness/en/> | 2026-08-18 | Developer preview; every capability (models, tools, skills, sessions, sandboxes, storage, loops, scheduling, UI) is a swappable Cordis plugin; everything the model sees is recorded in an append-only session log (system prompts, reasoning, tool calls/results, subagent scheduling, context injections) and the Trajectory view inspects those records **by source**, with resume, fork, search and replay over the same event stream; four runtime modes (Standard, Code, Minimal, Creator). |
| S6 | PenguinHarness | Publisher repository README | <https://github.com/Prism-Shadow/penguin-harness> (raw `README.md` on `main`) | 2026-08-18 | Desktop/CLI/SDK entry points over one `~/.penguin/data` root; four built-in skill groups including an Agent-Tuning group (`agent-creation`, `benchmark-design`, `agent-evaluation`, `agent-optimization`); self-evolution loop takes "a snapshot before every round" with every request observable in a Trace view. Accuracy and cost figures ("1/70 of Claude Code's cost") are **publisher claims**, not independent evidence, and support no direction here. |
| S7 | OpenCode | Primary product docs | <https://opencode.ai/docs/tui/> | 2026-08-18 | `@` does a fuzzy file search in the working directory and the **content of the file is added to the conversation automatically**; configured reference aliases also appear in `@` autocomplete; `!` runs a shell command whose output is added as a tool result; `/sessions` (aliases `/resume`, `/continue`) lists and switches sessions; `/undo`/`/redo` remove the last message *and revert file changes*, implemented with git and requiring a git repository; `/editor` composes the message in `$EDITOR`; `/export` writes the conversation to Markdown. |
| S8 | OpenCode | Primary product docs | <https://opencode.ai/docs/permissions/> | 2026-08-18 | Each rule resolves to `allow` / `ask` / `deny`; `--auto` approves everything not explicitly denied and **explicit deny rules are still enforced**; in the TUI the command palette offers "Enable/Disable auto-approve permissions" to change modes mid-session, and "when auto mode is active, the prompt displays a muted auto indicator next to the current agent"; granular per-tool object rules with wildcards, last match winning; `external_directory` governs paths outside the working directory. |

### Peer highlights and innovations

| Product | Sourced feature / innovation | Reference | Why it matters |
|---|---|---|---|
| Claude Code | Permission mode is a **live, one-keystroke session control** with a persistent indicator, and the docs specify the awkward case: a mode switch while a classifier verdict is pending discards the verdict instead of applying it. | S1, S2 | The plan → execute workflow is a mid-session transition, not a relaunch. The pending-verdict rule is the non-obvious correctness detail any live switch has to answer. |
| Codex | `/permissions` switches the approval preset mid-session; `/status` prints model, approval policy, writable roots and token usage in one place. | S3, S4 | Second independent peer treating approval policy as runtime state the user inspects and changes, not a launch argument. |
| OpenCode | Auto-approve is toggled from the command palette and advertised by a muted indicator beside the agent; rules can be `deny`, and deny survives `--auto`. | S8 | Third independent peer. Also shows the *shape*: the toggle is user-only, and the current state is always visible where the user types. |
| Claude Code, Codex, OpenCode | `@` in the composer completes workspace file paths. The three disagree on what happens next: Codex inserts **the path only**, OpenCode **inlines file content**, Claude Code triggers path autocomplete. | S2, S3, S7 | Convergent input affordance with an explicit fork in semantics — the inlining choice is a context/permission decision, not a UI detail. |
| Claude Code, Codex | Prompt history is first-class: Claude Code stores it **per working directory** and Up-arrow recall spans past sessions of the same project, with `Ctrl+R` reverse search; Codex restores draft history with `Up`/`Down` and searches with `Ctrl+R`. | S2, S3 | Two peers, one of them specifying the storage scope and the duplicate-collapsing rule that make recall usable in long-lived projects. |
| Claude Code | On the newest model families the todo tools are **not provided at all** and the task list stays empty by design. | S2 | Direct counter-evidence to adding a TodoWrite-style checklist tool to Darwin, which targets Claude 4.6+; the peer trend is away from it, so no such direction is proposed. |
| DeepSeek Harness | One append-only log of everything the model saw, inspectable **by source**, with resume/fork/search/replay over the same stream. | S5 | Darwin reached parity on the operations in `SER-003`/`SER-006`/`SER-007`. The remaining difference (per-source records, incl. child scheduling) is a *deliberate* Darwin decision, not a gap. |
| PenguinHarness | Agent-tuning skill group plus a snapshot before every self-evolution round; one data root shared by desktop, CLI and SDK. | S6 | Re-confirms the two scopes already closed here — the evaluation corpus (`SER-005`, user-abandoned) and filesystem checkpoints (gated 2026-08-17) — with no new evidence resolving why they were closed. |
| Codex, OpenCode | `!` prefix runs a user-typed shell command inside the session and feeds its output back to the model (Codex: under the current approval and sandbox settings; OpenCode: as a tool result). | S3, S7 | A real convergence, but it needs a Darwin product decision about the permission gate and about how that output enters context — see the note under Comparison. |

### Current Darwin baseline

| Capability / architecture | Repository evidence | Current behavior |
|---|---|---|
| Approval modes | `ApprovalMode = 'default' \| 'auto' \| 'plan' \| 'yolo'` and `APPROVAL_MODES` (`src/agent/permission.ts:45`), `--permission-mode` / `--yolo` (`src/cli-args.ts:128,163`), `permissionMode` in config | The mode is chosen **once, at launch**, and every gate decision reads `this.options.mode` (`permission.ts:224,248,263`). There is no setter: `rg 'setMode\|changeMode'` finds only `AgentRuntime.changeModel` (`runtime.ts:829`). Leaving plan mode means killing the process — the plan denial itself says "ask the user to run outside plan mode before changing or executing anything" (`permission.ts:228`). |
| Mode visibility | `Header` (`src/tui/App.tsx:938–967`) | The mode already has a row: yellow `mode: yolo — every tool call runs without confirmation`, `mode: plan — read-only; …`, else `mode: <mode>`. So *display* exists; only *change* does not. |
| Mode is not model-writable | `src/agent/permission.ts:578` ("The agent must not silently rewrite its own permission mode"), `src/agent/permission-rules.ts` exemptions | Darwin already holds the rule Claude Code states in prose (S1): the agent cannot widen its own policy, and no allow-rule may cover `~/.darwin/config.json` or `.env*`. |
| Live reconfiguration precedent | `/effort` → `Model.updateConfig()` (`AGENTS.md` "Thinking effort"), `/model` → `AgentRuntime.changeModel` (`runtime.ts:829`), `/clear` → successor runtime (`startNewSession`) | Darwin already changes session-scoped policy mid-run through three different, tested seams, with the config write *reported, not awaited*. A live mode switch has precedent to copy rather than invent. |
| Prompt completion | `computeCompletions(input, commandNames)` (`src/tui/App.tsx:1429`), `MAX_COMPLETIONS` (`src/tui/InputBox.tsx`), `BUILTIN_COMMAND_NAMES` (`src/commands/custom-commands.ts:11`) | Completion is a pure function that fires **only** for a bare `/prefix` and returns nothing once the input contains a space. There is no `@`, no path source, and no fuzzy match. Custom project commands already extend the same list. |
| Prompt editing and history | `src/tui/prompt-editor.ts` exports (layout, insert, backspace, delete, `moveHorizontal`, `moveVertical`, `moveToRowEdge`, `killToRowEdge`, `deleteWordBefore`), key handling in `App.tsx:807–840` | A capable multi-line editor with word/line kill — and **no history at all**: `rg 'history\|Ctrl\+R'` over `prompt-editor.ts`/`InputBox.tsx` returns nothing. `Up`/`Down` select completion rows when a menu is open and otherwise move the cursor between visual rows, so any recall binding has to coexist with both. |
| A per-project record of past prompts already exists | `src/trajectory/{reader,record,search}.ts`, `~/.darwin/sessions/<project-key>/<session-id>/trajectory.jsonl` (`.trellis/spec/backend/session-trajectory.md`) | User prompts of every past turn are already durably recorded per project, readable **with no model call and no network**, and already searched by `darwin trajectory search`. A history feature needs a reader, not a new store. |
| Bounded frame | `src/tui/frame-budget.ts`, `.trellis/spec/frontend/live-frame.md` | Every redrawn participant states its rows in a fixed priority order; the header must not gain a row (`verify-tui.ts approve` on 50 rows enforces it). Any new menu/indicator is a frame-budget participant, and heights are counted, never estimated. |
| Shipped or deliberately closed peer scopes | `SER-011` (structured headless), `SER-003`/`006`/`007` (trajectory), `SER-010` (busy Enter retains rather than queues), `SER-002` (parallel labelled children), gated rows in [`research_2026-08-17.md`](./research_2026-08-17.md) | Structured headless output, trajectory operations, no-queue submit and parallel child dispatch are settled. App server, filesystem rewind and the evaluation corpus are closed with recorded reasons. |

### Comparison and gaps

| Peer evidence | Darwin evidence | Parity / gap / deliberate difference | Implication |
|---|---|---|---|
| Three independent peers change approval policy mid-session and keep the active policy visible (S1 Shift+Tab + indicator, S3 `/permissions` + `/status`, S8 palette toggle + muted indicator) | Mode is fixed at construction (`permission.ts:45`, `cli-args.ts:128`) with no setter; the header already renders the mode row (`App.tsx:955–967`) | **High-confidence gap** at a seam Darwin already owns three times over (`/effort`, `/model`, `/clear`) | Add a user-only, session-scoped mode switch. S1 supplies the two hard parts: the model must not be able to do it (Darwin already agrees, `permission.ts:578`), and a switch during a pending `auto` classifier check or a queued prompt must not apply a verdict the new mode would not have asked for. |
| `@` completes workspace paths in all three peers (S2, S3, S7), with Codex inserting only the path and OpenCode inlining content | `computeCompletions` handles a bare `/prefix` only (`App.tsx:1429–1436`); no path source anywhere in the TUI | **Gap** in the input affordance; the inlining half is a deliberate choice Darwin should decline | Take the Codex shape: complete and insert the **path**, and let the existing `fileEditor` read carry the content under the existing gate and trajectory. Inlining content would create a second, ungated route for file bytes into context — exactly what `contextOffload`/permission policy exists to keep singular. |
| Prompt history is per-directory, spans past sessions, collapses consecutive duplicates, and has reverse search (S2); Codex restores drafts with `Up`/`Down` and searches with `Ctrl+R` (S3) | No history of any kind in `prompt-editor.ts`/`InputBox.tsx`; but every past user prompt is already recorded per project in `trajectory.jsonl` and readable offline (`src/trajectory/reader.ts`) | **Gap**, with the storage problem already solved | Recall reads the existing per-project trajectory records; no new store, no new file format, no model call. The binding must not break completion-row `Up`/`Down` or multi-row cursor movement (`App.tsx:807–840`). |
| `!` runs a user-typed shell command in-session and returns its output to the model (S3, S7) | No shell prefix; `expandInput` handles `/skill-name` only; bash always goes through the gate and the tool panel | Real convergence, **not proposed this run** | It needs two product decisions Darwin's requirement and repository evidence do not settle: whether a user-typed command bypasses the permission gate (and what that means in `plan` mode), and how its output enters context and the trajectory. Rated Importance 3 / fit 3 / evidence 5 / difficulty 3 / risk 3 = **8**, i.e. above the gate — it is being deprioritized deliberately, not gated out, and a later run can queue it once those two decisions exist. |
| Session pickers: `codex resume`/`codex fork` (S3), OpenCode `/sessions` (S7), Claude Code recall spanning past sessions (S2) | `--resume` / `--session <id>` (`cli-args.ts:113,160`), `darwin trajectory list` for discovery, and `/clear`'s successor-runtime machinery (`startNewSession`) | Partial parity through two separate surfaces (a launch flag and an offline command) | A mid-session `/resume <id>` is now architecturally cheap after `/clear`, but discovery already exists offline and the value is modest; not proposed this run. |
| OpenCode `deny` rules survive `--auto` (S8); Claude Code has deny rules (S1 protected paths) | Darwin has allow-rules only (`src/agent/permission-rules.ts`), with hard-coded exemptions instead of user deny rules | Deliberate asymmetry with a safe default (unknown ⇒ prompt), plus one real gap: nothing in-session lists or revokes the rules already granted to `permission-rules.json` | Worth a future direction (inspect/revoke granted rules); folding it into the mode switch would widen that direction's blast radius, so it is recorded here rather than queued. |
| OpenCode `/undo`/`/redo` revert file changes through git (S7); PenguinHarness snapshots every round (S6); Claude Code/Cursor checkpoints (2026-08-17 run) | No checkpoint manager anywhere; bash can mutate outside `fileEditor`; the user shares the tree | **Still gated.** OpenCode's git implementation does not answer the reason it was gated — attribution of arbitrary bash mutations and concurrent user edits | Do not re-propose. A rewind that restores over a user's concurrent work is a correctness hazard sold as a safety feature. |
| DeepSeek records subagent scheduling and every context injection, inspectable by source (S5) | Trajectory deliberately records **no** subagent event (`AGENTS.md`, `session-trajectory.md`): "observability must never become a second path for child transcript into parent context" | **Deliberate difference**, recorded | Not a gap. Re-proposing it would contradict a written contract. |
| PenguinHarness agent-tuning/benchmark skills and cost claims (S6) | `SER-005` abandoned by explicit user decision | Known, closed by decision | Recorded, not reopened; the benchmark numbers are publisher claims either way. |
| Claude Code withdraws todo tools on new model families (S2) | Darwin has no todo/plan tool (`rg -i 'todo\|update_plan'` over `src/` finds nothing) | **Not a gap** — peer evidence points the other way for Claude 4.6+ | No direction proposed. |

### Ranked iteration directions

`Score = 2 × Importance + Architecture fit + Evidence confidence − Implementation difficulty − Implementation risk`.

| Rank | Proposed ID | Direction | Importance | Architecture fit | Evidence confidence | Implementation difficulty | Implementation risk | Score | Evidence and rationale |
|---:|---|---|---:|---:|---:|---:|---:|---:|---|
| 1 | SER-013 | Switch the permission mode inside a running session, user-only and session-scoped, with the effective mode always visible and no in-flight decision resolved under the wrong mode | 4 | 5 | 5 | 2 | 3 | **13** | Three independent peers (S1, S3, S8) treat approval policy as live session state; Darwin fixes it at construction (`permission.ts:45`, `cli-args.ts:128`) and its own plan denial tells the user to "run outside plan mode" (`permission.ts:228`), which today means restarting. Fit is high: the gate reads `this.options.mode` on every call, the header row exists (`App.tsx:955–967`), and `/effort`, `/model` and `/clear` are three tested precedents for live session-scoped reconfiguration. Risk is the reason it is not a 15: this widens *enforcement*, so it must stay user-only (`permission.ts:578`), must keep the plan guard ahead of hooks/rules/classifier/bridge (`strands-sdk-contracts.md` § plan mode), must be session-scoped rather than written to config (a persisted widening is what the rule-exemption policy exists to prevent), and must handle S1's pending-verdict case — a queued prompt or an in-flight `auto` classifier check must never be resolved under a mode that would not have asked. Importance is 4 rather than 5 because a restart plus `--resume` is a real, if lossy, workaround. |
| 2 | SER-014 | Complete workspace file paths from `@` in the prompt, inserting the path only — never inlining file content | 3 | 4 | 5 | 3 | 2 | **10** | Three independent peers offer `@` (S2, S3, S7). `computeCompletions` (`App.tsx:1429`) is a pure, tested extension point that today fires only for a bare `/prefix`, and `MAX_COMPLETIONS`/frame-budget rules already govern the menu. Deliberately the Codex shape (S3), not OpenCode's (S7): inserting the path keeps file bytes flowing through `fileEditor` under the existing gate and trajectory, while inlining would add a second ungated route into context. Difficulty is the file scan, not the UI — it must be bounded (entry cap, no symlink escape, ignore `.git`/`node_modules`), counted rather than estimated for the frame, and must not stall the editor on a large tree. |
| 3 | SER-015 | Recall previous prompts in the editor, read from the project's existing trajectory records, without disturbing completion or cursor keys | 3 | 4 | 4 | 3 | 2 | **9** | Two peers document it in detail, one of them specifying scope and de-duplication (S2: per working directory, spans past sessions, consecutive duplicates collapsed; S3: `Up`/`Down` drafts plus `Ctrl+R`). Darwin has no history at all (`prompt-editor.ts`, `InputBox.tsx`) yet already records every past user prompt per project and reads it with no model call and no network (`src/trajectory/reader.ts`, `session-trajectory.md`), so this is a reader over existing bytes rather than a new store — which is also why evidence confidence is 4 rather than 5: no peer tells us what a *trajectory-sourced* history should do about a session recorded with `trajectory: false`. The binding is the risk: `Up`/`Down` already mean completion-row selection and multi-row cursor movement (`App.tsx:807–840`), and recall must not eat either. |

### Gated-out directions

| Direction | Score | Gate | Why it fell below | Kept anyway? |
|---|---:|---:|---|---|
| Add a TodoWrite/`update_plan`-style checklist tool with a TUI task view (Importance 2, fit 3, evidence 2, difficulty 3, risk 2) | **4** | 6 | Evidence points the other way: Claude Code no longer provides the todo tools on its newest model families and lets the list stay empty because the model tracks multi-step work itself (S2), and Darwin targets Claude 4.6+. Importance and evidence are both low for that reason, and Darwin already has `/tasks` and `/agents` for the *runtime* things a user cannot infer. | `no` |
| Automatic filesystem checkpoints with `/undo`/`/redo` (Importance 4, fit 2, evidence 5, difficulty 5, risk 5) | **5** | 6 | Re-rated with new peer evidence (S6, S7) and still below: OpenCode's git-backed implementation does not solve why this was gated on 2026-08-17 — Darwin cannot attribute arbitrary bash mutations or a user's concurrent edits, so a revert can destroy work that was never the agent's. Difficulty and risk stay at 5. | `no` |
| In-session `/resume <id>` session switching with a picker (Importance 2, fit 4, evidence 4, difficulty 3, risk 3) | **6** — at the gate, but **not queued** | 6 | Peers offer it (S3, S7) and `/clear` made the machinery cheap, but discovery already exists offline (`darwin trajectory list`) and `--session <id>` already reopens any session, so the marginal value is small. It sits exactly at the gate with nothing arguing for it over the three ranked directions; recorded so a later run can re-rate it rather than rediscover it. | `no` |
| Long-lived local app server / SDK control plane (S3 `codex app-server`, S6 headless service) | **4** (unchanged from 2026-08-17) | 6 | Authentication, concurrent clients, remote permission transport, process ownership and a stable API multiply the scope; nothing in this run's evidence removes those product decisions. | `no` |

No rating was restated to move a direction across the gate, and no below-gate exception is used. Two
above-gate items were deliberately **not** queued and are recorded with their ratings instead of being
misfiled as gated: the `!` shell prefix (score 8 — blocked on two product decisions, see Comparison) and
in-session listing/revocation of granted allow-rules (a real gap named in Comparison; folding it into
`SER-013` would widen that direction's blast radius, so it waits for its own run). `SER-004`/`SER-005`
were not reopened, and no direction re-proposes a contract `.trellis/spec/` records as deliberate.

### Recommendation

- Batch to implement: `SER-013`, `SER-014`, `SER-015`
- Start with: `SER-013`
- Why this order: score order, and it is also the safety order. `SER-013` is the only one of the three
  that touches enforcement, so it should land while the tree is otherwise unchanged and be accepted on
  its own evidence. `SER-014` and `SER-015` then both edit the prompt/completion surface, and doing the
  new completion *source* before the new `Up`/`Down` *binding* means the second iteration can see the
  final menu behaviour instead of guessing at it. There are no hard dependencies, so no score override
  is claimed.
- Independently observable acceptance:
  - **SER-013** — the effective mode is switchable mid-session from the user's own input and from
    nowhere else (a model-driven attempt changes nothing and is refused); every mode transition is
    reflected in the existing header row without adding a frame row (`verify-tui.ts approve`, 50 rows);
    with `plan` selected mid-session, a subsequent write/execute is denied before hooks, allow-rules,
    the `auto` classifier and the bridge, for parent *and* child agents, exactly as
    `verify-permission-modes.ts` already asserts for startup plan; leaving `plan` mid-session lets a
    write proceed through the ordinary gate; a prompt queued or a classifier check in flight when the
    mode changes is never resolved under a mode that would not have asked for it; the change is
    session-scoped and writes nothing to `~/.darwin/config.json`; `pnpm typecheck`, `pnpm test`, the
    focused permission suites, a real-pty scenario, `git diff --check` and Trellis validation pass.
  - **SER-014** — typing `@` offers bounded path completions, accepting one inserts the path text and
    nothing else, no file content is read or injected by the completion itself, the menu obeys
    `MAX_COMPLETIONS`/frame-budget rules with counted heights, `.git`/`node_modules`-class directories
    and symlink escapes are excluded, a large tree neither stalls the editor nor overflows the frame,
    and the existing `/` completion behaviour is unchanged (`verify-tui.ts completion` still passes with
    every built-in visible).
  - **SER-015** — recall returns previous prompts of *this project* newest-first with consecutive
    duplicates collapsed; it never fires while a completion menu is open and never replaces multi-row
    cursor movement; a session recorded with `trajectory: false` degrades to "no history" rather than an
    error; recall performs no model call and no network access; and existing prompt-editor, cursor and
    multiline suites stay green.
- Backlog updates: add `SER-013`, `SER-014`, `SER-015` as `not-started` at priorities 13, 14, 15; no
  existing row changes status by the gate. `SER-012` was closed to `done` *before* this research, on
  independent evidence recorded in its row. Change exactly one direction to `in-progress` immediately
  before each developer handoff.

### Batch iteration outcome

| Direction | Child session and managed tasks | Host acceptance (exact independent checks and outcomes) | Commit | Final status | Iteration-log entry |
|---|---|---|---|---|---|
| `SER-013` | child session `session-20260818-012951871`; single managed task `bg-0f0062ef-94aa-4a92-9508-c7153cb9bf4d`, exit 0, no correction turn | Host read the full 23-file diff and re-ran `pnpm typecheck` (0), `pnpm test` (37 summaries, all `0 failed`), new `verify-permission-mode-switch` (100), `verify-permission-modes` (101), `verify-tool-hooks` (44), `verify-subagents` (68), `verify-config` (205), `verify-headless` (80), `verify-prompt-editor` (28), `verify-clear-session` (37), free pty `mode` (25) / `completion` (29) / `clear` (19) / `plan` (4), `git diff --check`, `git show --check` per commit, Trellis validation, clean tree. Live `verify-tui.ts approve` 23/23 on **6 of 7** runs; the one failure asserted all 23 then timed out on exit after the model volunteered an extra `bash ls` — reproduced **identically** at the pre-change commit `31a5880` (1 of 5 runs in a separate worktree), so pre-existing nondeterminism. Host also ran its own throwaway-HOME end-to-end: every transition reported, invalid argument inert, `config.json` byte-identical by sha256, fresh process back on configured policy. | `ff0a9f5` + `eeb32a2` | `done` | [`Batch 16`](../iteration-log.md) |
| `SER-014` | child session `session-20260818-022510423`; single managed task `bg-4c6db950-a46e-4cd4-9085-386dfa17931b`, exit 0, no correction turn; launched from source at `9e00301` | Host read the full 16-file diff and re-ran `pnpm typecheck` (0), `pnpm test` (38 summaries, all `0 failed`), new `verify-path-completion` (59), `verify-frame-budget` (54), `verify-prompt-editor` (28), free pty `pathCompletion` (18) / `completion` (29) / `cursor` (5) / `multiline` (9) / `mode` (25) / `clear` (19) / `tallDraft` (8), `git diff --check`, `git show --check` per commit, Trellis validation, clean tree; no live call needed. Host's own 15-assertion probe: a canary inside a completed file appears nowhere in the reading or the accepted draft, `node_modules`/`.git` neither offered nor walked, a symlink out of the root not offered while one inside is, no absolute or `..` candidate, `@` inside a word not a trigger, a 9,000-entry directory bounded and stated — measured 31 ms for that scan and 0.63 ms worst per-keystroke match. | `70d3655` + `279121b` | `done` | [`Batch 17`](../iteration-log.md) |
| `SER-015` | child session `session-20260818-025938746`; single managed task `bg-a81befde-394b-4214-9b14-2b2ef2378581`, exit 0, no correction turn; launched from source at `9d3b4cc` | Host read the full 20-file diff and re-ran `pnpm typecheck` (0), `pnpm test` (39 summaries, all `0 failed`), new `verify-prompt-recall` (61), `verify-frame-budget` (61), `verify-trajectory` (257), `verify-path-completion` (59), `verify-prompt-editor` (28), free pty `recall` (20) / `recallEmpty` (4) / `completion` (29) / `pathCompletion` (18) / `cursor` (5) / `multiline` (9) / `mode` (25) / `clear` (19) / `tallDraft` (8), `git diff --check`, `git show --check` per commit, Trellis validation, clean tree; no live call needed. Host's own 12-assertion probe: newest-first, duplicates collapsed, a prompt recalled from an earlier session of the same project, every record byte-identical by sha256, no resume pointer created, still working with `AWS_REGION`/endpoint/profile sabotaged, a record-less project reading as no history, a corrupt plus half-written line tolerated, an over-long prompt skipped and stated — measured 1.9 ms. | `13e8968` + `7abc916` | `done` | [`Batch 18`](../iteration-log.md) |

- Halt condition that ended the loop: **batch exhausted** — all three accepted directions are `done`
  after independent acceptance, each implemented on the revision the previous one produced
  (`7dd38e1` → `9e00301` → `cbae037` → `9d3b4cc`), and no direction needed a correction turn.
- Aggregate token spend across every child delegation: `input=934 output=307,859
  cacheRead=74,741,143 cacheWrite=762,238`. Per task: `SER-013` `input=324 output=100,605
  cacheRead=24,886,871 cacheWrite=251,526`; `SER-014` `input=216 output=81,016 cacheRead=13,906,719
  cacheWrite=208,374`; `SER-015` `input=394 output=126,238 cacheRead=35,947,553 cacheWrite=302,338`.
  Every task reported a `usage:` line; none reported `-` for any field.
- Remaining backlog after this run: nothing unfinished. `SER-001`–`SER-003` and `SER-006`–`SER-015`
  are `done`; `SER-004`/`SER-005` remain explicitly `abandoned` by user decision. Fresh research is
  appropriate on the next invocation. Two items this run deliberately did **not** queue are recorded
  above for whoever researches next: the `!` shell prefix (score 8, blocked on two product decisions)
  and in-session listing/revocation of granted allow-rules.


## Run — 2026-08-18T06:09:36Z

### Backlog-first decision

- Backlog inspected first: [`backlog_index.md`](./backlog_index.md)
- Unfinished directions found: **none**. `SER-001`–`SER-003` and `SER-006`–`SER-015` were `done`; `SER-004`/`SER-005` remained explicitly `abandoned`.
- Decision: **fresh research**, on the user-directed TUI path.

### Research path

The user explicitly directed the TUI path. The bundled script was run once, before opening any repository source, with `--path tui`; its output is copied verbatim:

```text
research-path: tui
focus: TUI interaction and visual polish
share: 20% (weight 2 of 10)
draw: none — path was not rolled
path-source: override (user-directed)
rolled-at: 2026-08-18T06:07:34.882Z
weights: tui=2 observability=0.5 sdk=1 open=1.5 peer=5
```

### Source availability and method

This is a repository self-review on 2026-08-18 UTC. No peer product was consulted: the path's evidence is Darwin's current source, real-pty checks, frontend contracts, README, and recent TUI history. Repository evidence was inspected at `eec27a7`; `pnpm typecheck` and `pnpm test` both passed before proposing work. A real local pty startup was also captured at 80 columns to observe the rendered hierarchy rather than inferring it from JSX.

### Reference sources

No peer-product source applies on this self-review path. The repository evidence used instead is:

| Ref | Repository evidence | Claim supported |
|---|---|---|
| R1 | [`src/tui/App.tsx`](../../src/tui/App.tsx), `Header`, `hintForStatus`, and the live-frame assembly | Startup identity, model/session, mode, loaded resources and help are separate free-form rows; the full skill-name list and long help sentence wrap at terminal width. The same component owns the streaming/thinking status placement. |
| R2 | [`src/tui/MessageList.tsx`](../../src/tui/MessageList.tsx), `HistoryEntry`; [`src/tui/ToolCallPanel.tsx`](../../src/tui/ToolCallPanel.tsx) | Conversation hierarchy is labels plus blank space; assistant and successful-tool state both use green, while most tool detail is uniformly dim. |
| R3 | [`src/tui/InputBox.tsx`](../../src/tui/InputBox.tsx), `InputBox`; [`src/tui/PermissionPrompt.tsx`](../../src/tui/PermissionPrompt.tsx), `PermissionPrompt` | The editor, menu and modal are behaviorally strong and bounded, but styling is embedded directly in each component. Cyan independently means the user prompt, selected completion, permission source and rule shortcut. |
| R4 | [`src/tui/frame-budget.ts`](../../src/tui/frame-budget.ts); [`.trellis/spec/frontend/live-frame.md`](../../.trellis/spec/frontend/live-frame.md) | Every redrawn row is load-bearing: a visual redesign must use the existing row budget, count wrapped rows, and keep the modal decision reachable. |
| R5 | [`.trellis/spec/frontend/tui-testing.md`](../../.trellis/spec/frontend/tui-testing.md); [`spike/verify-tui.ts`](../../spike/verify-tui.ts); [`spike/verify-frame-budget.ts`](../../spike/verify-frame-budget.ts) | Real pty output, cursor ownership, latest-frame assertions, short terminals and render-to-string height checks are the established acceptance seams. |
| R6 | [`README.md`](../../README.md), opening TUI transcript | The documented screenshot still presents the earlier sparse UI and an obsolete permission-row shape, so the product's visual contract is not represented by a maintained fixture. |

### Peer highlights and innovations

Not applicable: no peer product was opened on this TUI self-review path.

### Current Darwin baseline

| Capability / architecture | Repository evidence | Current behavior |
|---|---|---|
| Visual vocabulary | R1–R3; direct style audit found 28 `color` attributes and 32 `dimColor` uses across five TUI components | Color and emphasis are local literals rather than named semantic roles. The individual choices are readable, but there is no one place defining identity, active state, success, warning, danger, muted metadata, or selection. |
| Startup hierarchy | R1; 80-column pty capture | The header is truthful but visually flat. In this repository, the full 13-skill list wrapped over several rows and the help sentence wrapped again, competing with the conversation before the first prompt. |
| Conversation hierarchy | R2 | User and assistant turns are distinguished by small labels and whitespace. Tool result, notice and answer blocks do not share a strong visual grammar, so scanning a long transcript relies heavily on reading every label. |
| Composer and completion | R3 | The editor has robust multiline/cursor/menu behavior, but the idle prompt has no enclosing surface and selection is primarily a cyan glyph/name change. Status help is one long dim sentence. |
| Safety modal | R3, R4 | Permission detail is correctly bounded and the decision row remains reachable. Its yellow round border is the clearest visual component and must not lose information or rows in a redesign. |
| Rendering safety | R4, R5 | Frame budgeting, streamed-to-`Static` history and real pty checks already provide the right extension boundary. Styling can be broad without changing the SDK loop or state model. |

### Comparison and gaps

| Repository evidence | Assessment | User-visible implication |
|---|---|---|
| R1 shows a header assembled as unrelated text rows, including a complete skill list and a long command hint. | **High-confidence hierarchy gap.** Truthful state is present, but primary identity, live policy and secondary metadata receive nearly the same treatment. | The first screen feels like diagnostic output rather than a designed workspace, and narrow terminals spend scarce rows listing names available in completion anyway. |
| R2–R3 and the style audit show the same few literal colours reused for unrelated meanings. | **System gap, not a colour-preference complaint.** There is no semantic palette/component vocabulary to keep a broad restyle coherent. | Selection, identity, provenance and safety shortcuts can all read as the same state; later additions must rediscover styling conventions. |
| R2 shows labels and blank rows as the main transcript structure. | **Scanning gap.** The data is all there, but role, tool lifecycle and notice severity do not form one repeated visual grammar. | A long coding turn is harder to skim for “what I asked / what Darwin said / what ran / what failed.” |
| R3–R5 show strong behavioral tests but no explicit end-to-end visual composition fixture; README is stale (R6). | **Verification/documentation gap.** Geometry is tested deeply, while the intended composed appearance is not pinned. | A future local colour/marker edit can degrade hierarchy without failing, and users see documentation that no longer matches the product. |

### Ranked iteration directions

`Score = 2 × Importance + Architecture fit + Evidence confidence − Implementation difficulty − Implementation risk`.

| Rank | Proposed ID | Direction | Importance | Architecture fit | Evidence confidence | Implementation difficulty | Implementation risk | Score | Evidence and rationale |
|---:|---|---|---:|---:|---:|---:|---:|---:|---|
| 1 | SER-016 | Establish and apply a cohesive Darwin visual language across the TUI: compact status-first header, stronger turn/tool/notice hierarchy, a clearly active composer and completion selection, and a polished but information-equivalent permission modal | 5 | 5 | 5 | 4 | 3 | **13** | R1–R6 show one cross-surface problem and the safe seams for fixing it. This is deliberately one direction: a palette-only commit has no product value, while restyling one component would preserve the inconsistency. Central semantic style definitions should drive all five visual components without a dependency or a configurable theme. The baseline header should use the same or fewer rows (summarize capabilities rather than enumerate them); required state remains textually observable. Transcript markers and emphasis must work with colour disabled. The modal keeps exact risk, source, detail, queue, rule and decision information. Existing frame, cursor, `Static`, permission and key-ownership contracts remain unchanged. Add deterministic render/pty coverage for the composed hierarchy and refresh the README transcript. |

### Gated-out directions

| Direction | Score | Gate | Why it fell below | Kept anyway? |
|---|---:|---:|---|---|
| Add user-configurable themes and custom colour values (Importance 2, fit 2, evidence 2, difficulty 4, risk 4) | **0** | 6 | The repository demonstrates inconsistency in the built-in visual language, not user demand for theme configuration. Configuration multiplies terminal-capability, validation and support surfaces before Darwin has one coherent default. | `no` |
| Add mouse-driven selection and clickable controls (Importance 2, fit 1, evidence 2, difficulty 4, risk 5) | **-2** | 6 | `.trellis/spec/frontend/tui-testing.md` explicitly preserves native scrollback and drag selection by refusing terminal mouse tracking. This would contradict a deliberate interaction contract to solve no demonstrated gap. | `no` |

No dimension was restated to cross the gate, and no below-gate exception is used.

### Recommendation

- Batch to implement: `SER-016`
- Start with: `SER-016`
- Why this order: it is one atomic cross-surface visual-system direction. Splitting “tokens” from “application” would create an intermediate iteration with no observable user value and invite the components to drift again.
- Independently observable acceptance: the real TUI has a cohesive, terminal-safe visual hierarchy across startup, turns, active/completed tools, severity notices, composer/completion and permission; the baseline header consumes no more rows than before and compactly summarizes loaded capabilities; all critical states remain distinguishable in ANSI-stripped output; no new dependency/configuration is added; `spike/verify-frame-budget.ts`, focused rendering checks, and free real-pty scenarios (`completion`, `pathCompletion`, `recall`, `cursor`, `multiline`, `mode`, `plan`, `clear`, `tallDraft`) pass; the 50-row real `approve` scenario keeps the complete modal; `pnpm typecheck`, `pnpm test`, Trellis validation, diff checks and clean-tree verification pass; README matches the accepted design.
- Backlog updates: add `SER-016` as `not-started` at priority 16, then change exactly it to `in-progress` immediately before developer handoff.

### Batch iteration outcome

| Direction | Child session and managed tasks | Host acceptance (exact independent checks and outcomes) | Commit | Final status | Iteration-log entry |
|---|---|---|---|---|---|
| `SER-016` | child session `session-20260818-061057110`; managed task `bg-f42e105b-399c-44e0-9107-651af1226311`, exit 0, no correction turn; launched from source at `eec27a7` | Host read the full 22-file commit and independently re-ran `pnpm typecheck`; `pnpm test` (40 suite summaries, all `0 failed`); focused `verify-visual-language` / frame-budget / streamed-static suites (22/61/58); free pty `completion` / `pathCompletion` / `recall` / `cursor` / `multiline` / `mode` / `plan` / `clear` / `tallDraft` (29/18/20/5/9/25/4/19/8); real Bedrock 120x50 `approve` (23/23); Trellis validation, `git diff --check`, `git show --check`, and clean-tree verification. | `ab71a8c` | `done` | [`Batch 19`](../iteration-log.md) |

- Halt condition that ended the loop: **batch exhausted** — the only accepted direction is independently accepted and `done`.
- Aggregate token spend across every child delegation: `input=300 output=34,754 cacheRead=12,965,635 cacheWrite=144,231`; the single managed task reported every field.
- Remaining backlog after this run: nothing unfinished. `SER-001`–`SER-003` and `SER-006`–`SER-016` are `done`; `SER-004`/`SER-005` remain explicitly `abandoned`. Fresh research is appropriate on a later invocation.

## Run — 2026-08-18T09:15:03Z

### Backlog-first decision

- Backlog inspected first: [`backlog_index.md`](./backlog_index.md)
- Unfinished directions found at invocation start: **none**. `SER-001`–`SER-003` and `SER-006`–`SER-016`
  are `done` with recorded acceptance evidence; `SER-004`/`SER-005` are `abandoned` by explicit user
  decision. The most recent batch (`SER-016`, origin run `06:09:36Z` in this file) is exhausted.
- Decision: **fresh research**.

### Research path

Rolled exactly once, before any repository or product source was opened. Verbatim script output:

```text
research-path: peer
focus: comparable coding-agent product analysis
share: 50% (weight 5 of 10)
draw: 13 of 20 half-units
path-source: roll
rolled-at: 2026-08-18T09:15:03.995Z
weights: tui=2 observability=0.5 sdk=1 open=1.5 peer=5
```

This was a chance roll, not a user override, and it was not re-rolled.

### Source availability and method

Web access via `curl` was available on 2026-08-18 UTC; every page below was fetched and read locally
(Claude Code publishes markdown renderings of its docs; the Codex, DeepSeek and OpenCode pages were
fetched as HTML and read after tag-stripping; the PenguinHarness README was fetched raw from GitHub).
All five required scopes were verified from primary sources: Claude Code, Codex, DeepSeek Harness,
PenguinHarness, and the additional product **OpenCode**. PenguinHarness accuracy/cost figures remain
publisher claims and support no direction here.

Repository evidence comes from the current tree at `c9bb869` (clean, `pnpm typecheck` exit 0). The
same-day `01:25:31Z` peer run in this file is treated as prior art: everything it shipped
(`SER-013`–`SER-015`), gated, or deliberately deferred (the `!` shell prefix, the mid-session session
switcher, permission-rule inspection flagged as "worth a future direction") is carried forward as
such, not re-derived.

### Reference sources

| Ref | Product | Source type | URL | Accessed (UTC) | Claims supported |
|---|---|---|---|---|---|
| S1 | Claude Code | Primary product docs (commands reference) | <https://code.claude.com/docs/en/commands> | 2026-08-18 | `/permissions` opens an interactive dialog to "view rules by scope, add or remove rules, manage working directories, and review recent auto mode denials", opens immediately mid-turn and applies changes to the next tool call; `/mcp` manages MCP server connections and OAuth (interactive list, `reconnect <server>`, `enable`/`disable`, and a plain-text status summary in `-p` mode); `/export [filename]` exports the conversation as plain text to a file or clipboard; `/diff` is an interactive per-turn diff viewer; `/copy [N]` copies the Nth-latest response. |
| S2 | Codex | Primary product docs (CLI commands) | <https://learn.chatgpt.com/docs/developer-commands?surface=cli> | 2026-08-18 | `/mcp` "List configured Model Context Protocol (MCP) tools … add `verbose` for server details"; expected output is "the configured MCP tools Codex can call in this session"; `/permissions` relaxes or tightens approvals mid-session; `/status` shows model, approval policy, writable roots, token usage; `/copy` (also `Ctrl+O`) copies the latest completed output; `/diff` shows the git diff; `/resume` reopens a saved chat; `/statusline`/`/theme`/`/pets` personalize the footer. |
| S3 | Codex | Primary product docs (CLI features) | <https://developers.openai.com/codex/cli/features/> | 2026-08-18 | `codex mcp`: "Add local or remote MCP servers, authenticate when needed, and inspect the tools available to the current session before Codex uses them"; `/permissions`: "inspect the active sandbox and writable roots before you continue". |
| S4 | DeepSeek Harness | Primary product page | <https://deepseek.com/harness/en/> | 2026-08-18 | Developer preview; every capability is a Cordis plugin; everything the model sees is in an append-only session log inspectable by source in the Trajectory view, with resume/fork/search/replay over the same stream; four runtime modes. No new claim beyond what the `01:25:31Z` run recorded. |
| S5 | PenguinHarness | Publisher repository README | <https://github.com/Prism-Shadow/penguin-harness> (raw `README.md` on `main`) | 2026-08-18 | `penguin web` serves multi-session chat, agent/skill/model management, usage stats, Trace observability, and an evaluation center; self-evolution takes "a snapshot before every round"; roadmap lists "OpenShell integration (permission-governed shell)" as unshipped. Accuracy/cost figures are publisher claims and support nothing here. |
| S6 | OpenCode | Primary product docs (TUI) | <https://opencode.ai/docs/tui/> | 2026-08-18 | `/export` "Export current conversation to Markdown and open in your default editor" (uses `$EDITOR`); `/sessions` lists and switches sessions; `/undo`/`/redo` revert file changes through git and require a git repository; `/models`, `/themes`, `/thinking` toggles. |
| S7 | OpenCode | Primary product docs (MCP servers) | <https://opencode.ai/docs/mcp-servers/> | 2026-08-18 | Local and remote MCP servers configured in `opencode.jsonc`; a server can be disabled with `enabled: false` without removing it; the docs warn that MCP tools "add to the context. This can quickly add up if you have a lot of tools", recommending care in which servers are enabled. |

### Peer highlights and innovations

| Product | Sourced feature / innovation | Reference | Why it matters |
|---|---|---|---|
| Claude Code | Permission **rules** are session-inspectable and editable: `/permissions` shows every allow/ask/deny rule by scope and lets the user add or remove rules, mid-turn, effective on the next tool call. | S1 | A rule is a standing widening of what runs silently. Claude Code treats the rule *list* — not just the mode — as live session state the user can audit and retract. |
| Codex | `/permissions` includes *inspection* ("inspect the active sandbox and writable roots before you continue"), not only switching. | S2, S3 | Second peer framing permissions as something the user reviews in-session, not only sets. |
| Claude Code, Codex | In-session MCP visibility: Claude Code `/mcp` lists servers with connection state, supports `reconnect`/`enable`/`disable` and a `-p` text summary; Codex `/mcp` lists the tools callable this session with `verbose` server diagnostics. | S1, S2, S3 | Both peers answer "which external tools can the model actually call right now, and is the server up" without leaving the session. |
| OpenCode | MCP config supports `enabled: false` per server, and the docs explicitly warn about the context cost of MCP tool listings. | S7 | Third peer confirming MCP inspection/management is a normal product surface; the context-cost warning matches Darwin's own header-count motivation. |
| Claude Code, OpenCode, Codex | Conversation export: Claude Code `/export [filename]` (plain text, file or clipboard); OpenCode `/export` (Markdown into `$EDITOR`); Codex `/copy` (latest output to clipboard). | S1, S2, S6 | Three peers make the transcript hand-off (to a PR description, an issue, a colleague) a one-step in-session action. |
| Claude Code, Codex | Interactive diff viewers (`/diff`) over uncommitted changes and per-turn edits. | S1, S2 | A full-screen interactive viewer conflicts with Darwin's bounded-live-frame contract; recorded and gated below rather than ignored. |
| DeepSeek Harness | Append-only by-source session log with resume/fork/search/replay. | S4 | Parity already reached (`SER-003`/`006`/`007`); the by-source child records remain a recorded deliberate Darwin difference. No new direction. |
| PenguinHarness | Web UI with usage stats and Trace observability; permission-governed shell still on the roadmap. | S5 | Reconfirms closed scopes (`SER-005` evaluation corpus, app-server gating). No new direction. |

### Current Darwin baseline

| Capability / architecture | Repository evidence | Current behavior |
|---|---|---|
| Allow-rules accumulate and persist, but are invisible and irrevocable in-session | `PermissionGate.addAllowRule`/`get allowRules` (`src/agent/permission.ts:324,329`); persistence via `writePermissionRules` → `~/.darwin/projects/<project-key>/permission-rules.json` (`src/config.ts:963–1033`); grant notice at `src/tui/App.tsx:769` | A decision may carry a rule; the gate honours it from that moment and the UI persists it. There is **no removal path anywhere**: `rg 'removeAllowRule|revoke'` over `src/` finds nothing, and no command lists the live rules. A mistaken "always allow" is permanent until the user hand-edits JSON outside the session. The `01:25:31Z` run flagged exactly this as "worth a future direction (inspect/revoke granted rules)". |
| MCP servers are counted, never inspectable | `McpLoadResult` (`src/mcp/registry.ts:38`), header capability count (`src/tui/App.tsx:1345,1353`), `runtime.info.mcpServerCount` (`src/agent/runtime.ts:615`) | The header shows `N MCP server(s)` and startup warnings name config-level problems, but nothing in-session lists servers, their tools, or their state. `loadMcpClients` sets `continueOnError`, so a server that fails to spawn is logged by the SDK and "its `listTools()` yields an empty list" (`registry.ts:66–68`) — the user experiences *silently missing tools*. The SDK exposes everything an inspection needs: `McpClient.clientName`, `connectionState`, `serverVersion`, `listTools()`, and `connect(reconnect: true)` for an explicit retry (`node_modules/@strands-agents/sdk/dist/src/mcp/client.d.ts:121–193`). |
| The transcript is exportable only after the fact, via a separate process | `darwin trajectory replay` (`src/cli-trajectory.ts`), `replayRead`/`formatReplay` (`src/trajectory/replay.ts:166,233`), per-session `trajectory.jsonl` (`.trellis/spec/backend/session-trajectory.md`) | Replay reuses `turnReducer`, needs no model call and no network — but it is a `darwin trajectory` subcommand addressed by session id, not an in-session action. There is no `/export`; sharing the current conversation means finding the session id and running a second process. `formatReplay` already respects `AnswerPart` flags so an export cannot drift from the live transcript. |
| Local-report command pattern | `/usage`, `/tasks`, `/agents`, `/context`, `/trajectory` handlers (`src/tui/App.tsx:481–598`) | Pure formatter + `notice` dispatch, answered mid-turn, never a model call. Any new inspection command has a settled shape to copy, including the `takes no arguments` guard and error-to-notice degradation. |
| Command menu discoverability | `BUILTIN_COMMAND_NAMES` + one-phrase descriptions (`src/commands/custom-commands.ts:11–19`), `verify-tui.ts completion` keeps every built-in visible | Typing `/` already lists every command with a description, so a `/help` command would duplicate an existing surface; only key-binding discoverability is genuinely missing. |
| Frame and severity contracts any new command must obey | `src/tui/frame-budget.ts`, `.trellis/spec/frontend/live-frame.md`, `src/tui/visual-language.ts` | Notices go through `<Static>` history, not the live frame; the header must not gain a row; heights are counted, never estimated. |

### Comparison and gaps

| Peer evidence | Darwin evidence | Parity / gap / deliberate difference | Implication |
|---|---|---|---|
| Claude Code `/permissions` views and removes rules by scope, mid-turn (S1); Codex `/permissions` inspects the active boundaries (S2, S3) | Rules accumulate via `addAllowRule` and persist to `permission-rules.json` with no list and no removal path (`permission.ts:324`, `config.ts:1033`) | **Gap, safety-adjacent** — Darwin's own architecture says a rule "turns a prompt into silence", yet the user cannot see or retract that silence without hand-editing JSON | Add in-session inspection and revocation. Revocation only *narrows* permissions, so the risk profile is the inverse of `SER-013`'s; the exemption policy (`isRuleExempt`) and user-only boundary carry over unchanged. |
| Claude Code `/mcp` (state, reconnect, enable/disable, `-p` summary) (S1); Codex `/mcp` + `verbose` diagnostics (S2, S3); OpenCode per-server `enabled` toggle and context-cost warning (S7) | Header count only; failed server ⇒ silently empty tool list (`registry.ts:66–68`, `App.tsx:1353`) | **Gap** — three peers answer "which tools can the model call, and is the server up"; Darwin cannot, even though `McpClient` exposes name, state, tools and an explicit reconnect | Add an `/mcp` local report: servers with connection state, tool names/counts, config source, overridden/ignored files. An explicit `reconnect` maps to the SDK's `connect(true)` but is a lifecycle mutation — scope it deliberately. |
| Claude Code `/export [filename]` (S1); OpenCode `/export` Markdown (S6); Codex `/copy` (S2) | `darwin trajectory replay` exists but is a separate process addressed by session id (`src/cli-trajectory.ts`); no in-session export | **Gap, moderate** — the machinery (reader, `formatReplay`, `AnswerPart` fidelity) already exists; only the in-session seam is missing | Add `/export <path>` writing the current session's transcript from its own trajectory record. `trajectory: false` must degrade to "nothing to export", never an error — same absence-is-an-answer rule as prompt recall. Clipboard integration is deliberately excluded (SSH-hostile, environment-dependent). |
| Claude Code and Codex `/diff` interactive viewers (S1, S2) | No diff surface; the user shares the tree and `git diff` is one terminal away | Real peer convergence, but a full-screen interactive viewer contradicts the bounded-live-frame contract (`live-frame.md`) | Gated below rather than proposed. |
| Codex `/resume` picker, OpenCode `/sessions` (S2, S6) | `--resume`/`--session` at launch; `/clear` successor machinery | Known, deliberately deferred in the `01:25:31Z` run; nothing new today changes the value/risk balance — exit + `--resume` remains a complete workaround while the session-identity contract (`markResumable`) makes a live switch the riskiest kind of change | Gated below with honest ratings. |
| Codex/OpenCode `!` shell prefix | No shell prefix; bash always goes through the gate | **Still deferred, not gated**: scored 8 in the `01:25:31Z` run and left pending two product decisions (gate treatment; context/trajectory entry) that repository evidence still does not settle | Restated for the user to decide; this run does not guess. |
| Every peer ships `/help` | The `/` menu already lists every built-in with a description (`custom-commands.ts:14–19`; pinned by `verify-tui.ts completion`) | Mostly parity through a different surface; the residual gap is key-binding discoverability only | Not proposed: the formula would score it well only because it is trivially cheap (Importance 2, difficulty 1), and spending a supervision iteration on a surface that substantially exists fails the qualitative test the ranking contract requires. Recorded for a future onboarding/docs direction. |
| Codex `/statusline`, `/theme`, `/pets` (S2) | `visual-language.ts` single coherent default (SER-016) | Deliberate difference — theme configuration was gated at score 0 in the `06:09:36Z` run | Not re-proposed; no new evidence. |

### Ranked iteration directions

`Score = 2 × Importance + Architecture fit + Evidence confidence − Implementation difficulty − Implementation risk`.

| Rank | Proposed ID | Direction | Importance | Architecture fit | Evidence confidence | Implementation difficulty | Implementation risk | Score | Evidence and rationale |
|---:|---|---|---:|---:|---:|---:|---:|---:|---|
| 1 | SER-017 | List and revoke permission allow-rules in-session (`/permissions`): show every live rule with its origin (configured vs granted this session), revoke one or all, user-only, with revocation persisted and incapable of widening anything | 4 | 5 | 4 | 2 | 2 | **13** | Claude Code's `/permissions` manages the rule list itself, mid-turn (S1), and Codex frames `/permissions` as inspection of active boundaries (S2, S3); OpenCode's model is rule-based throughout (S7 and the `01:25:31Z` run's S8). Darwin's gate already exposes `get allowRules` and `addAllowRule` (`permission.ts:324,329`) and the persistence seam exists (`config.ts:1033`); the local-report command pattern (`App.tsx:481–598`) and the `SER-013` user-only/withdrawal precedents cover the hard parts. Importance is 4 because this is the audit-and-retract half of a safety surface Darwin already half-owns: today a mistaken "always allow" silences prompts forever unless the user hand-edits JSON. Risk 2 because revocation only narrows: the failure mode of a bug is an extra prompt, not a silent widening. Evidence 4, not 5: Claude Code alone demonstrates in-session rule *removal*; Codex's surface manages presets/boundaries. |
| 2 | SER-018 | Inspect MCP servers in-session (`/mcp`): per-server connection state, tool names/counts, config source and overridden/ignored files, with a failed server stated as failed instead of silently contributing zero tools | 3 | 5 | 5 | 2 | 2 | **12** | Three peers, primary docs: Claude Code `/mcp` with state and reconnect (S1), Codex `/mcp` + `verbose` diagnostics (S2, S3), OpenCode per-server enablement and context-cost guidance (S7). Darwin counts servers in the header but cannot answer "is the server up, and what can the model call" — and `continueOnError` makes the failure mode *silently missing tools* (`registry.ts:66–68`). The SDK already exposes `clientName`, `connectionState`, `serverVersion`, `listTools()` and `connect(reconnect: true)` (`client.d.ts:121–193`), so this is a projection, not new plumbing. Scope decision recorded here: inspection is required; an explicit `/mcp reconnect <server>` is optional and only if it stays a bounded, reported action — connection lifecycle is otherwise owned by startup/shutdown paths that `AGENTS.md` fences behind exit-behavior suites. |
| 3 | SER-019 | Export the current conversation in-session (`/export <path>`): write the transcript from this session's own trajectory record to a user-named file, reusing the replay projection; absence of a record degrades to "nothing to export" | 2 | 4 | 5 | 2 | 2 | **9** | Three peers, primary docs: Claude Code `/export [filename]` (S1), OpenCode `/export` to Markdown (S6), Codex `/copy` (S2). Darwin's projection already exists and is drift-proof by construction — `replayRead`/`formatReplay` reuse `turnReducer` and respect `AnswerPart` (`replay.ts:166,233`) — so the work is one seam: locate the *current* session's record, project it, write one file, report the write as a notice. Importance 2 because `darwin trajectory replay` in a second terminal is a real workaround; still proposed because the batch's other rows leave room and the peer convergence is unanimous. Constraints that keep it honest: the trajectory stays observer-only (export *reads* the record, never blocks a turn), `trajectory: false` reads as "nothing to export" exactly like prompt recall's absence rule, and clipboard is out of scope (SSH-hostile). |

Ordering: the three directions are independent; priority follows score. No direction depends on another,
so a halt inside the batch strands nothing.

### Gated-out directions

| Direction | Score | Gate | Why it fell below | Kept anyway? |
|---|---:|---:|---|---|
| Interactive `/diff` viewer over uncommitted/per-turn changes (Importance 2, fit 2, evidence 5, difficulty 3, risk 3) | **5** | 6 | Fit is the problem: a full-screen interactive viewer contradicts the bounded-live-frame contract (`.trellis/spec/frontend/live-frame.md`), and a non-interactive diffstat notice adds little over `git diff` in the next terminal, which the shared-working-tree model already assumes. | `no` |
| Mid-session session switcher (`/sessions` / `/resume <id>`) (Importance 2, fit 3, evidence 5, difficulty 4, risk 4) | **4** | 6 | Exit + `--resume` is a complete workaround, and a live switch lands in the most contract-dense area Darwin has (session identity fixed at construction, `markResumable`'s invariant, retire-vs-shutdown). Deferred by the `01:25:31Z` run for the same reasons; no new evidence today. | `no` |
| `/copy` last answer to clipboard (Importance 1, fit 2, evidence 5, difficulty 3, risk 3) | **3** | 6 | Clipboard access is environment-dependent (OSC 52 support, SSH, tmux) and the failure modes are silent; `/export` to a file covers the need portably. | `no` |

No dimension was restated to cross the gate, and no below-gate exception is used. The `!` shell prefix
(scored 8 on `01:25:31Z`) remains **deferred pending user product decisions**, not gated: this run
neither re-scores nor proposes it.

### Recommendation

- Batch to implement: `SER-017`, `SER-018`, `SER-019` (priority = score order; no inter-dependencies)
- Start with: `SER-017`
- Why this order: SER-017 is safety-adjacent (audit and retraction of standing permission widenings) and
  scores highest; SER-018 and SER-019 are independent inspection/projection features that can land on
  whatever revision precedes them.
- Independently observable acceptance:
  - `SER-017` — a granted rule appears in the report with its origin; revoking it makes the next matching
    call prompt again (provable in a pty scenario); revocation is persisted so a fresh process does not
    resurrect the rule; a configured-file rule and a session-granted rule are distinguished; no path may
    widen: the command must reject additions or rewrites that are not revocations unless separately
    prompted through the existing grant flow; `pnpm typecheck`, `pnpm test`, the free pty scenarios that
    pin the completion menu and permission box, and Trellis validation all pass.
  - `SER-018` — with one healthy and one broken server configured, `/mcp` names both, states the broken
    one as failed rather than omitting it, lists tool names/counts for the healthy one, and names the
    config file(s) in effect including an ignored root `.mcp.json`; no model call; mid-turn availability;
    frame contracts hold (report is a notice through `<Static>`).
  - `SER-019` — `/export out.md` writes a file whose content matches `darwin trajectory replay` for the
    same session byte-for-byte modulo the header the command states; `trajectory: false` yields a
    "nothing to export" notice and no file; the write never blocks or reorders a turn; an unwritable
    path degrades to one error notice.
- Backlog updates: add `SER-017`, `SER-018`, `SER-019` as `not-started` (priorities 17–19); no row
  abandoned by the gate (nothing existing fell below it); `SER-017` moves to `in-progress` immediately
  before developer handoff.

### Batch iteration outcome

| Direction | Child session and managed tasks | Host acceptance (exact independent checks and outcomes) | Commit | Final status | Iteration-log entry |
|---|---|---|---|---|---|
| `SER-017` | child session `session-20260818-093028503`; managed task `bg-02b125c3-b033-47e2-b6ec-438b8074465a`, exit 0, no correction turn; launched from source at `ada6581`. (A first launch, `bg-2d2a231c`, was stopped ~1.5 s in — before any model call — because the Host's pipeline swallowed the output stream; it produced nothing and is recorded here rather than hidden.) | Host read the full 17-file diff and re-ran `pnpm typecheck` (exit 0); `pnpm test` (exit 0, 41 suite summaries, all `0 failed`); `spike/verify-permissions-command.ts` (42); free pty `completion` (30, `/permissions` row asserted), `pathCompletion` (18), `recall` (20), `mode` (25); Trellis validation; `git diff --check`/`git show --check`; plus a 14-assertion Host-written probe in a throwaway HOME over the real gate, config seam and handler (origins, immediate revocation, no resurrection, widening canary, filter-only proof, usage degradation). No live model call needed — the permission modal is unchanged. | `8d120dd` | `done` | [`Batch 20`](../iteration-log.md) |
| `SER-018` | child session `session-20260818-100731833`; managed task `bg-f67fc62b-4ce7-4cf0-8ebb-c7707f72d2cd`, exit 0, no correction turn; launched from source at `55f71f5` | Host read the full 18-file diff and re-ran `pnpm typecheck` (exit 0); `pnpm test` (exit 0, 42 suite summaries, all `0 failed`); `spike/verify-mcp-command.ts` (33); the new free pty `mcp` (9); `completion` (31); `pathCompletion` (18); Trellis validation; `git diff --check`/`git show --check`; plus an 11-assertion Host-written probe against real MCP servers in a throwaway HOME (healthy + broken server both named, failure stated not omitted, mutation-freedom before/after the read, ignored root `.mcp.json` stated, absence degrading to the candidate-file notice). `reconnect` deliberately not shipped: `connect(true)` does not re-register tools, so it would advertise tools the model cannot call. | `a443981` | `done` | [`Batch 21`](../iteration-log.md) |
| `SER-019` | child session `session-20260818-104704270`; managed task `bg-d5b3a70c-ed8a-427e-84be-5f12bd119c95`, exit 0, no correction turn; launched from source at `c6f5628` | Host read the full 13-file diff and re-ran `pnpm typecheck` (exit 0); `pnpm test` (exit 0, 43 suite summaries, all `0 failed`); `spike/verify-export-command.ts` (32); free pty `completion` (35), `mcp` (9), `recall` (20); Trellis validation; `git diff --check`; plus a 12-assertion Host-written probe against a real recorded session (record + resume pointer byte-identical by sha256 after export, body byte-identical to `formatReplay`, atomic overwrite refusal, sessions-dir guard, absence readings writing no file, usage, unwritable-path degradation). | `dd08f8f` | `done` | [`Batch 22`](../iteration-log.md) |

- Halt condition that ended the loop: **batch exhausted** — all three directions are independently accepted and `done`.
- Aggregate token spend across every child delegation: `input=526 output=140,626 cacheRead=26,229,812 cacheWrite=454,660` (SER-017 `160/49,146/8,014,158/151,595` + SER-018 `200/52,109/10,652,629/165,195` + SER-019 `166/39,371/7,563,025/137,870`; every managed task reported all four fields). The stopped first SER-017 launch (`bg-2d2a231c`) emitted no `usage:` line — killed before any model call — and is recorded as absent, not zero.
- Remaining backlog after this run: nothing unfinished. `SER-001`–`SER-003` and `SER-006`–`SER-019` are `done`; `SER-004`/`SER-005` remain explicitly `abandoned`. Fresh research is appropriate on a later invocation.

## Run — 2026-08-18T12:30:29Z

### Backlog-first decision

- Backlog inspected first: `docs/research/backlog_index.md`
- Unfinished directions found: none — `SER-001`–`SER-003`, `SER-006`–`SER-019` are `done`; `SER-004`/`SER-005` are `abandoned`
- Decision: fresh research

### Research path

```text
research-path: tui
focus: TUI interaction and visual polish
share: 20% (weight 2 of 10)
draw: none — path was not rolled
path-source: override (user-directed)
rolled-at: 2026-08-18T12:27:03.508Z
weights: tui=2 observability=0.5 sdk=1 open=1.5 peer=5
```

The user directed this run at the TUI path ("继续反思TUI 有什么大的改进和美观度重构，希望有更酷炫的效果" — big improvements, aesthetic refactoring, cooler effects). This is a directed run, not chance, and is recorded as such.

### Source availability and method

This is a `tui` self-review run: the primary evidence is this repository — source under `src/tui/`, the specs `.trellis/spec/frontend/tui-testing.md` and `.trellis/spec/frontend/live-frame.md` (both read first), and the SER-016 visual-language acceptance record. No peer-product survey was performed and the five-scope peer table is deliberately not filled. Two peer claims are cited opportunistically because two proposed directions lean on them; both were verified via web search on 2026-08-18 against primary/product-tracker sources, and no other peer product was consulted or characterized.

### Reference sources

| Ref | Product | Source type | URL | Accessed (UTC) | Claims supported |
|---|---|---|---|---|---|
| S1 | Codex CLI | Primary product docs | `https://developers.openai.com/codex/cli/features` | 2026-08-18 | "Read syntax-highlighted markdown code blocks and diffs in the TUI, then use /theme to preview and save a preferred theme" — Codex renders markdown and diffs in its terminal UI |
| S2 | Claude Code | Product issue tracker (behavioral record) | `https://github.com/anthropics/claude-code/issues/48198` | 2026-08-18 | The edit-approval flow showed a red/green line diff by default (users filed a regression when an update hid it), i.e. a coloured diff at the approval prompt is Claude Code's established shape |
| — | DeepSeek harness / PenguinHarness / others | not consulted | — | — | no claim made — self-review path, no peer requirement |

### Current Darwin baseline

| Capability / architecture | Repository evidence | Current behavior |
|---|---|---|
| Assistant answer rendering | `src/tui/MessageList.tsx:72,87` (`<Text>{item.text}</Text>`), live rows at `MessageList.tsx:56-60` | Answer text is drawn as plain uninterpreted text. Models emit markdown, so users see raw `**bold**`, `# heading`, backtick runs and bare ``` fences — the single most visible polish gap left after SER-016 |
| Answer streaming pipeline | `src/tui/turn-state.ts` `commitFinishedLines`/`closeAnswer`; `AnswerPart` whole/first/middle/last; divergence reconciliation at `turn-state.ts:402-437` | Finished lines are committed to `<Static>` mid-turn and can never be redrawn; the committed *text* is also the reconciliation and replay source of truth, so any styling must be presentation-time over unchanged text |
| File-edit approval detail | `src/agent/permission.ts:96` ("Raw tool input, for a UI that wants to show or diff it itself"); `src/tui/PermissionPrompt.tsx` detail blocks; `permissionDetailRows` in `frame-budget.ts` | The gate already exposes raw input precisely so a UI could diff it — no UI ever has. A `fileEditor str_replace` approval shows `Path:`, `Old:`-style raw blocks with no old/new alignment and no colour; the user visually diffs two wrapped strings by eye before approving a write |
| Finished tool result rendering | `src/tui/ToolCallPanel.tsx` `ToolCallResult` — all preview lines uniformly `dimColor` | An applied edit's result is a dim text block; no +/- structure, nothing distinguishes added from removed content |
| Busy-state feedback | `src/tui/App.tsx:1255` (static `working…` hint), `App.tsx:1217` (`thinking…` dim row), spinner in `ToolCallPanel.tsx` (tools only) | While the model streams or thinks, the user sees a static hint. No elapsed time, no live token spend — despite `runtime.usage` existing since SER-007 and an existing per-frame tick when tools are active. `/usage` answers only when asked |
| Visual language | `src/tui/visual-language.ts`; SER-016 acceptance; `tui-testing.md` § "visual hierarchy is asserted structurally" | Colour is enhancement; stable ANSI-independent markers are the contract. Any new styling must keep a monochrome/stripped rendering legible and assertable |
| Frame discipline | `.trellis/spec/frontend/live-frame.md`; `frame-budget.ts` | Heights are counted, never estimated; the header gains no baseline rows; whatever redraws must fit — a constraint every direction below inherits |

### Comparison and gaps

| Peer evidence | Darwin evidence | Parity / gap / deliberate difference | Implication |
|---|---|---|---|
| Codex renders markdown (incl. syntax-highlighted code blocks) in the TUI (S1) | `MessageList.tsx:72,87` plain `<Text>` | Gap | Styled answers are the largest legibility and "product polish" win available; must be render-time-only over the committed text |
| Claude Code shows a red/green diff at edit approval (S2); Codex shows diffs in the TUI (S1) | `permission.ts:96` exposes raw input for exactly this; no consumer | Gap — and a planned seam left unused | A bounded coloured diff in the permission modal and tool result raises both beauty and review safety; information-equivalence (SER-016) must hold |
| Peers show liveliness (spinners with elapsed/tokens) — not independently sourced this run | `App.tsx:1255` static `working…` | Gap (repo-evidenced only) | A ticking elapsed + live spend suffix makes the TUI feel alive at near-zero information cost; rated on repository evidence alone |
| Codex `/theme` (S1) | `visual-language.ts` fixed palette | Deliberate difference for now | SER-016 chose one dependency-free semantic palette; a theming layer is rejected below the gate this run |

### Ranked iteration directions

| Rank | Proposed ID | Direction | Importance | Architecture fit | Evidence confidence | Implementation difficulty | Implementation risk | Score | Evidence and rationale |
|---:|---|---|---:|---:|---:|---:|---:|---:|---|
| 1 | SER-020 | Render file edits as bounded coloured line diffs at the permission prompt and in the finished tool result — computed from the raw input the gate already exposes, information-equivalent, with `+`/`-` markers that survive ANSI stripping | 4 | 5 | 5 | 3 | 2 | 13 | S2 (Claude Code default red/green approval diff), S1 (Codex TUI diffs); `permission.ts:96` is an explicit unused seam; `str_replace` carries old and new strings in the input so no file read is needed; `permissionDetailRows`/frame budget already bound the rows; SER-016's information-equivalence and SER-009's truncation contracts carry over unchanged — approving must still write the exact untruncated value |
| 2 | SER-021 | Render assistant answers as styled markdown (headings, bold/italic, inline code, fenced code blocks) at presentation time, over the committed text unchanged — streaming, `<Static>`-committed pieces, replay/export byte-stability and divergence reconciliation all preserved | 5 | 4 | 5 | 4 | 3 | 12 | S1 (Codex renders markdown); `MessageList.tsx:72,87` plain text is the gap. The hard part is real and named: `turn-state.ts` commits finished lines mid-turn and `AnswerPart` splits an answer across `<Static>` entries, so fence/style state must be derived deterministically per piece; the reducer's committed text stays the reconciliation/replay source of truth (`turn-state.ts:402`, `replay.ts`), so styling is render-only; pty assertions strip ANSI and must keep a stable assertable substring |
| 3 | SER-022 | Make the busy state alive: elapsed time and live token spend as a suffix of the existing `working…`/`thinking…` rows, ticking with the frame — no new frame row, no new information channel | 3 | 5 | 3 | 2 | 2 | 10 | Repository evidence only (stated per the self-review rule): `App.tsx:1255` is static text; `runtime.usage` (SER-007) already meters the live turn in-process; the spinner tick precedent exists in `ToolCallPanel.tsx`. Evidence confidence 3 because no peer source was consulted for this item; header/live-frame contracts (`live-frame.md`) forbid a new baseline row, hence the suffix shape |

### Gated-out directions

| Direction | Score | Gate | Why it fell below | Kept anyway? |
|---|---:|---:|---|---|
| Startup banner / welcome box (one-time `<Static>` identity moment) | 5 | 6 | Importance 2 (one-time cosmetics), Architecture fit 3 — SER-016 deliberately pinned a *compact status-first header*, and a banner pushes against that recorded decision; Evidence 2 (no peer source consulted), Difficulty 2, Risk 2 | no |
| `/theme` palette switching (Codex-style, S1) | 5 | 6 | Importance 2, Architecture fit 3 (SER-016 chose one dependency-free semantic palette as the contract; a theme layer multiplies every structural fixture), Evidence 4, Difficulty 3, Risk 3 | no |

### Recommendation

- Batch to implement: SER-020 → SER-021 → SER-022
- Start with: SER-020
- Why this order: descending score; no hard dependencies. SER-020 is the highest-leverage and lowest-risk (pure string projection into an already-bounded surface); SER-021 is the biggest visible change but carries the streaming/`<Static>` state complexity; SER-022 is a small closer that touches rows both earlier directions leave alone.
- Independently observable acceptance:
  - SER-020: a real pty approval for a `str_replace` shows `+`/`-` diff rows inside the bounded box with the decision row intact at 120×50 (`verify-tui.ts approve`); approving still writes the exact untruncated value; ANSI-stripped output keeps the `+`/`-` markers; a focused suite pins diff projection bounds/Unicode; `pnpm typecheck`/`pnpm test` green.
  - SER-021: a recorded answer containing headings/bold/code renders styled live while `/export` and `darwin trajectory replay` of the same session stay byte-identical to before; the divergence warning path still fires on a forced mismatch; free pty scenarios (`completion`, `recall`, etc.) and the structural visual suite pass; a long streamed markdown answer commits to `<Static>` without screen-clear storms (bounded frame preserved).
  - SER-022: while a real turn streams, the working row shows ticking elapsed and live token spend as a suffix; no new frame row at 50 rows (`approve` still 23/23); idle shows none of it; `pnpm test` green.
- Backlog updates: SER-020/021/022 added as `not-started` (priorities 20/21/22); banner and `/theme` recorded above as rejected; SER-020 moves to `in-progress` only immediately before developer handoff.

### Batch iteration outcome

| Direction | Child session and managed tasks | Host acceptance (exact independent checks and outcomes) | Commit | Final status | Iteration-log entry |
|---|---|---|---|---|---|
| `SER-020` | child session `session-20260818-123849412`; task `bg-6a49b177` (exit 1 — transient stream timeout during research, nothing written), retry task `bg-c0253f1f` (exit 0, completed with no correction turn); launched from source at `4023e93` | Host read the full 20-file diff and re-ran `pnpm typecheck` (exit 0); `pnpm test` (exit 0, 44 suites all `0 failed`); `verify-edit-diff.ts` (62); `verify-visual-language.tsx` (36); live 120×50 `verify-tui.ts approve` (26/26); `git diff --check`/`git show --check`; Trellis validation; purity grep (no file-reading API in `edit-diff.ts`); 9-assertion Host probe (byte-exact recovery of both sides from markers, Unicode intact, delete vs empty replacement, create all-added, unknown shape `undefined`, 3,000-line pathological diff equivalent in 6ms). | `dfbab04` | `done` | [`Batch 23`](../iteration-log.md) |

- Halt condition that ended the loop: _(recorded as the batch closes)_
- Aggregate token spend across every child delegation: _(recorded as the batch closes)_
- Remaining backlog after this run: _(recorded as the batch closes)_
