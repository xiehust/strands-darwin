# Darwin self-evolution research — 2026-09-13

## Run — 2026-09-13T05:09:44Z

### Backlog-first decision

The required router `docs/research/backlog_index.md` and all six routed pages (001–020 through 101–120) existed. Metadata-only selection found 117 directions: 115 `done`, two `abandoned`, no unfinished record. All statuses were valid. Fresh research was therefore permitted. Starting revision: `c1f34b4`; working tree clean. Existing headings and priorities were searched for duplicates; next stable ID is SER-086 and next Priority is 118.

### Research path

```text
research-path: peer
focus: comparable coding-agent product analysis
share: 50% (weight 5 of 10)
draw: 12 of 20 half-units
path-source: roll
rolled-at: 2026-09-13T05:09:44.777Z
weights: tui=2 observability=0.5 sdk=1 open=1.5 peer=5
```

Rolled once, before repository/product research sources (after required backlog routing). No override or second draw.

### Source availability and method

Primary documentation and repositories were accessed with web search and bounded `web_fetch` on 2026-09-13 UTC. Search snippets located sources; findings below use opened primary pages. All seven required scopes were verified at the stated scope, not exhaustively evaluated or executed. HTML projections and some long pages were truncated; claims use only retrieved content. OpenAI's former features URL redirected to `learn.chatgpt.com`; the primary slash-command Markdown page there was opened directly. GitHub's repository pages included navigation noise; DeepSeek's product page provided the substantive architecture claims. PenguinHarness claims below are attributed to its README, not independently measured performance.

Repository inspection covered `AGENTS.md`, README, applicable load-bearing rationale, source, verification suites and recent history before comparison. Two guessed file reads failed; actual paths were located via `rg --files` before proceeding. No implementation was performed during research.

### Reference sources

| Ref | Product | Source type | URL | Accessed (UTC) | Claims supported |
|---|---|---|---|---|---|
| S1 | Claude Code | Primary interactive-mode docs | https://code.claude.com/docs/en/interactive-mode | 2026-09-13 | Ctrl+G / Ctrl+X Ctrl+E open an external editor; Ctrl+S stashes/restores prompt, cursor and pasted content. |
| S2 | Codex | Primary CLI command reference | https://learn.chatgpt.com/docs/cli/slash-commands.md | 2026-09-13 | `/review` asks for a working-tree review; `/diff` shows Git changes including untracked files; `/rename` names a chat; slash commands can queue for the next turn. |
| S3 | DeepSeek harness | Primary product architecture page and repository | https://www.deepseek.com/harness/en/ ; https://github.com/deepseek-ai/deepseek-harness | 2026-09-13 | Cordis plugins supply models/tools/skills/sessions/loops/UI; append-only logs support resume/fork/search/replay and include subagent scheduling/context injections; Standard/Code/Minimal/Creator runtime modes. |
| S4 | OpenCode | Primary commands docs | https://opencode.ai/v2/docs/commands | 2026-09-13 | Markdown review commands expand to user prompts; `$ARGUMENTS` substitution; templates without placeholders append arguments after a blank line; shell blocks execute outside agent permission flow; command metadata can select a background child. |
| S5 | kiro-cli | Primary session-management docs | https://kiro.dev/docs/cli/chat/session-management/ | 2026-09-13 | Per-directory auto-save, explicit resume IDs, V3 dashboard/content search and active-session exclusion; concurrent same-session resume is refused. |
| S6 | PenguinHarness | Primary repository README | https://github.com/Prism-Shadow/penguin-harness | 2026-09-13 | README describes skill-driven evaluation/optimization, a snapshot before every round and request Trace view; web UI exposes session/agent/skill/model management and evaluation center. No benchmark or speed claim adopted. |
| S7 | Aider | Primary command reference | https://aider.chat/docs/usage/commands.html | 2026-09-13 | `/editor` edits prompts externally; `/diff` shows changes since the last message; `/test` adds shell output on nonzero exit; `/run` optionally adds output. |

### Peer highlights and innovations

| Product | Sourced feature / innovation | Reference | Why it matters (inference) |
|---|---|---|---|
| Claude Code | External prompt editor; exact draft stash | S1 | Long prompts need recoverable composition, not only line editing. |
| Codex | First-class review and diff entry points | S2 | Review is discoverable after implementation without writing a new instruction each time. |
| DeepSeek harness | Plugin-composed runtime and unified trace stream | S3 | Capability substitution and source attribution are explicit architecture choices. |
| OpenCode | Prompt commands with argument fallback and child routing | S4 | A minimal Markdown command remains useful with a target even without remembering placeholder syntax. |
| kiro-cli | Per-directory session persistence plus searchable dashboard | S5 | Saved work can be rediscovered; active-session refusal protects writer ownership. |
| PenguinHarness | Skill-driven evolution with snapshots and Trace view | S6 | An iteration's result can be tied to its observed execution, rather than a self-reported success. |
| Aider | External editor, diff and test-result commands | S7 | Common development transitions are reachable directly from the composer. |

### Current Darwin baseline

| Capability / architecture | Repository evidence | Current behavior |
|---|---|---|
| SDK reuse and gate | `AGENTS.md`; `docs/architecture/load-bearing-decisions.md` §§ SDK reuse, Permissions, Extension layering; `src/agent/runtime.ts:expandSlashCommand` | SDK owns the loop; prompt expansion does not execute tools; ordinary tools retain the existing gate. |
| Prompt commands | `src/commands/custom-commands.ts:expandCustomCommand`, `BUILTIN_COMMAND_NAMES`; `spike/verify-custom-commands.ts:expansion`; `docs/user-guide/extensions.md` Custom commands | Only `$ARGUMENTS` is substituted. The existing test explicitly expects a placeholder-free command to discard supplied args. No built-in `review` exists. |
| Built-in prompt seams | `src/commands/workflow-command.ts:parseWorkflowCommand`; `src/commands/init-command.ts:parseInitCommand`; `src/agent/runtime.ts:expandSlashCommand`; `spike/verify-workflow-command.ts`, `spike/verify-init-command.ts` | Built-ins precede skills/custom commands and share expansion across drivers; pure prompt templates are already a supported extension shape. |
| TUI discovery/queue | `src/tui/prompt-completion.ts:computeCompletions`; `src/tui/App.tsx:submit`; `spike/verify-tui.ts` header and completion scenario | Canonical command names feed completion; busy prompt-producing submissions queue. Local help/status need no model. |
| Sessions/trace/privacy | `src/agent/session.ts`; `docs/architecture/load-bearing-decisions.md` §§ Session trajectory, Subagents; `docs/architecture/sub-agents.md`; README | Project-keyed SDK snapshots and bounded observer trajectory support sessions/replay/fork; child transcripts intentionally remain private. A shared mutable trace store is not the design. |
| Existing editing/evolution | `AGENTS.md` §§ Prompt recall/The prompt queue; `src/tui/App.tsx` editor/image refs; backlog SER-084/085 headings; README Evolution by iteration | Cut/yank and stash already shipped. Evolution already requires independent Host acceptance. Do not re-queue either capability. |
| Recent work | `git log -12 --oneline` at `c1f34b4`: `c1f34b4`, `ea3c9de`, `b912d6d`; README | Recent changes address background indicators, rewind transcript restoration and AgentCore upload acceptance. These are not new gaps. |

**Direct reproduction:** at `c1f34b4`, a `pnpm tsx -e` call to `expandCustomCommand` with template `Review code for bugs.` and input `/review src/auth.ts` returned message `Review code for bugs.`. The target disappeared. This is an observed contract change opportunity, not a newly discovered violation of the old test.

### Comparison and gaps

| Peer evidence | Darwin evidence | Parity / gap / deliberate difference | Implication |
|---|---|---|---|
| OpenCode no-placeholder fallback (S4) | `expandCustomCommand`; direct reproduction; old test assertion | Gap: explicit user args silently vanish. Old behavior is test-pinned, so change must be intentional and documented. | SER-086: retain template bytes and append `\n\n` plus existing trimmed args when no placeholder occurs; preserve existing replacement otherwise. |
| Codex review entry point; OpenCode review template (S2/S4) | `BUILTIN_COMMAND_NAMES`; `parseInitCommand` / `expandSlashCommand` | Discoverability gap, not missing review tools. | SER-087: one pure `/review [focus]` expansion, no new agent orchestration. |
| Claude/Aider external editor (S1/S7) | App composer ownership and frame budget; no editor command in canonical names | Gap, but terminal handoff/process cleanup carries more cost than demonstrated demand. | Reject this run below gate; keep current multiline input. |
| Codex/Aider diff (S2/S7) | file-edit diff and `!` shell contracts in `AGENTS.md` | Partial parity: input-derived edit diffs and explicit `!git diff` already exist; whole-tree diff surface would duplicate a shell workflow. | No new diff command in this batch. |
| DeepSeek trace/plugin modes; Penguin evolution (S3/S6) | SDK reuse, bounded trajectory, child privacy, developer skill | Deliberate difference: no replacement loop, arbitrary code scheduler, child trace hydration or autonomous benchmark authority. | Do not broaden these boundaries merely for peer parity. |
| kiro dashboard/active-session exclusion (S5) | `session.ts`, CLI sessions contract | Resume/list parity; richer dashboard and concurrency contract require a dedicated persistence investigation. No reproduced concurrent-writer claim in this run. | Do not queue an unproven session-store redesign. |

### Ranked iteration directions

`Score = 2 × Importance + Architecture fit + Evidence confidence − Difficulty − Risk`; minimum 6. Ratings below are initial ratings, not adjusted to cross the gate.

| Rank | Proposed ID | Direction | Importance | Architecture fit | Evidence confidence | Difficulty | Risk | Score | Evidence and rationale |
|---:|---|---|---:|---:|---:|---:|---:|---:|---|
| 1 | SER-086 | Preserve arguments for placeholder-free custom commands | 4 | 5 | 5 | 1 | 2 | 15 | S4 plus reproduced `expandCustomCommand` loss. High value because target/focus text changes the requested task. Explicitly supersedes old test-pinned semantics; no parser or executor added. |
| 2 | SER-087 | `/review [focus]` as a pure built-in prompt expansion | 3 | 5 | 5 | 2 | 2 | 12 | S2/S4 plus current built-in registry. Improves discoverability through existing seams; prompt asks for findings, not edits, but is explicitly not a sandbox. Implement after SER-086 to settle generic-command compatibility before reserving the new name. |

### Gated-out directions

| Direction | Score | Gate | Why it fell below | Kept anyway? |
|---|---:|---:|---|---|
| External prompt editor (S1/S7) | 5 | 6 | I=2, fit=4, evidence=4, difficulty=4, risk=3: real peer feature, but TTY ownership, child cleanup and temporary draft confidentiality exceed demonstrated user need. | no |
| Configurable plugin-owned loop / programmatic orchestration (S3) | 1 | 6 | I=3, fit=1, evidence=3, difficulty=5, risk=4: would displace the documented SDK loop and ordinary gate rather than extend them. | no |
| Broader per-request/child trace UI (S3/S6) | 3 | 6 | I=2, fit=2, evidence=4, difficulty=3, risk=4: privacy/boundedness tradeoffs and a new surface; no reproduced gap warrants replacing the observer contract. | no |

The existing yank/stash, replay, workflow, and independent evolution capabilities are duplicates/parity, not gated records. Session-store concurrency and automatic test loops were not specified as directions: this survey did not establish their implementation premise. No below-gate exception is retained.

### Recommendation

- Batch: **SER-086 (Priority 118), then SER-087 (Priority 119)**, both initially `not-started` on `backlog/directions-101-120.md`. No rollover and no index status summary.
- SER-086 acceptance: direct pure expansion with nonempty/no args, repeated placeholders, placeholder-looking and shell-looking argument text, multiline/Unicode preservation, case-insensitive lookup and unknown commands; real loader and ordinary runtime expansion; focused custom-command and headless checks; full typecheck/test. No shell execution, positional placeholders, recursive interpolation, or SDK changes. Preserve literal trajectory input and existing driver expansion.
- SER-087 acceptance: pure case-insensitive exact-name parser; bare command reviews staged/unstaged changes and relevant untracked files; optional trimmed focus preserved as text; fixed prompt requests evidence-backed prioritized findings with file/line references, test gaps and explicit no-findings/limits, and prohibits edits/commits unless separately requested. Ordinary shared runtime expansion; canonical reservation/help/completion; no alternate tool/executor/automatic subagent, no mode switch or claim of enforced read-only review; queued busy submission, attachment handling and literal recorded input retained. Verify focused parser/runtime/headless tests and real free `tui completion`; full typecheck/test/build.
- Reserving `/review` intentionally displaces an identically named custom command or skill invocation, through existing reservation diagnostics. Document this compatibility change; rename conflicting test fixture commands without weakening their loader assertions.
- For both: README, narrative guide and reference EN/zh-CN plus applicable architecture rationale in step. Keep AGENTS.md untouched (near byte cap). Each implementation uses a fresh headless source CLI worker under the loaded developer supervision protocol. No new dependencies, real config/cloud writes, history rewrite, or unauthorized data deletion. The Host owns backlog/report/log changes; workers own implementation and tests/docs.
- The initial clean `c1f34b4` baseline gate is running as `bg-b81e5a4e-bef2-4328-af63-2f8611cbcae6`. Only green HEADs are handed to workers; research-only commits do not require duplicate full gates when source is unchanged.

### Batch iteration outcome

| Direction | Child session and managed tasks | Host acceptance (exact independent checks and outcomes) | Commit | Final status | Iteration-log entry |
|---|---|---|---|---|---|
| SER-086 | `session-20260913-052155663`; `bg-b11d08e3-ee17-487e-b077-822defb8ff10` exit 0, fully drained | Host `pnpm typecheck && pnpm test && pnpm build && git diff --check && git status --short` (`bg-7150cd5b-da55-4513-9a94-501ee4bf3018`) exit 0; 8,120 PASS lines/custom-command130; diff and EN/zh-CN docs inspected, dist refreshed | `0c49aa2` | `done` | `docs/iteration-log.md` Batch 129 |
| SER-087 | `session-20260913-054619047`; `bg-bd580aba-9666-4bf8-b617-d0c42099ca3a` exit 0, fully drained | Host `pnpm typecheck && pnpm test && pnpm tsx spike/verify-tui.ts completion && pnpm build && git diff --check && git status --short` (`bg-8026888e-141d-4102-ad11-7f8a654773b9`) exit 0; full suite 8,285 PASS lines plus completion75; all changed source/tests/docs reviewed | `d29f091` | `done` | `docs/iteration-log.md` Batch 130 |

| Worker | input | output | cacheRead | cacheWrite | USD total |
|---|---:|---:|---:|---:|---|
| SER-086 initial | 52 | 13,542 | 1,809,749 | 93,286 | unknown |
| SER-087 initial | 98 | 24,813 | 5,690,294 | 167,384 | unknown |
| Aggregate | 150 | 38,355 | 7,500,043 | 260,670 | unknown |

Both workers reported `model=global.openai.gpt-6-astra pricing=none`, no correction/retry/descendant tasks. Buckets are independent and include no invented zero for pricing. No research subagent was used. Existing runtime cloud-memory settings were unchanged; worker footer reported project auto mode, not evidence of upload disablement. Verification fixtures themselves were isolated and offline.

- Halt condition: **batch exhausted**. Both origin records are `done`; no remaining assumption was falsified.
- No backlog direction was abandoned this run. External editor (5), plugin-loop replacement (1) and broader trace UI (3) were rejected at proposal time and remain report-only.
- Remaining backlog: no `not-started` or `in-progress` records. Next invocation may conduct fresh research after its own metadata check and roll.
- Acceptance does not establish model review quality or sandboxing. `/review` is prompt guidance under existing permissions; name collisions are documented. Full user-facing EN/zh-CN docs synchronized and `dist` refreshed.
- Delivery: after the closure commit, the Host performs one ordinary current-branch push and verifies `git log @{u}..` before the final user report; no force-push or history rewrite is permitted.
