# Darwin self-evolution research — 2026-09-20

## Run — 2026-09-20T11:20:00.840Z

### Backlog-first decision

- Router read first: [`backlog_index.md`](backlog_index.md).
- Metadata searched across all seven routed pages, priorities 001–140: 131 records, all `done` or `abandoned`; no unfinished direction or batch.
- Highest allocated priority: 131; highest SER ID: SER-099. Existing headings checked for duplication, including SER-014 (path completion), SER-029 (selection window), SER-088 (search preview safety) and SER-089 (paste ownership). Completed records' evidence/notes were not used.
- Decision: fresh research. The script ran once before repository/source review. The initial metadata parser assumed bold field labels and was corrected to the actual plain `- Status:` format; the successful line-oriented pass validated every status without loading completed notes.

### Research path

```text
research-path: tui
focus: TUI interaction and visual polish
share: 20% (weight 2 of 10)
draw: 3 of 20 half-units
path-source: roll
rolled-at: 2026-09-20T11:20:00.840Z
weights: tui=2 observability=0.5 sdk=1 open=1.5 peer=5
```

### Source availability and method

Accessed 2026-09-20 UTC from clean HEAD `fb83589`. Local filesystem, git, CodeGraph and real Ink `renderToString` were available. No external product sources were opened and no peer product was consulted. The architecture sections were read before reviewing their implementations. Claims below distinguish source facts, measured reproductions and inferred user impact. This is a bounded composer/completion audit, not a claim that all TUI surfaces were exhaustively tested.

### Reference sources

| Ref | Product | Source type | Repository evidence | Accessed (UTC) | Claims supported |
|---|---|---|---|---|---|
| R1 | Darwin | Architecture | [`AGENTS.md`](../../AGENTS.md); [`load-bearing-decisions.md`](../architecture/load-bearing-decisions.md), §§ TUI — the frame budget, `@` path completion, Prompt recall | 2026-09-20 | Counted rows must fit; path insertion is text-only; SER-088 already projects controls in search previews, not completion or composer. |
| R2 | Darwin | Source | [`path-completion.ts`](../../src/tui/path-completion.ts): `readLevel`, `scanWorkspacePaths`, `applyPathCompletion` | 2026-09-20 | Directory names enter candidates unchanged and accepting inserts the exact chosen path; no file-content read. |
| R3 | Darwin | Source and executed rendering | [`InputBox.tsx`](../../src/tui/InputBox.tsx): `InputBox`; [`frame-budget.ts`](../../src/tui/frame-budget.ts): `planPromptBox` | 2026-09-20 | Completion label renders raw `name`; `truncate-end` does not collapse LF; menu budget counts one row per item. |
| R4 | Darwin | Source and executed rendering | [`prompt-editor.ts`](../../src/tui/prompt-editor.ts): `segment`, `layoutEditor`, `cellWidth`; [`App.tsx`](../../src/tui/App.tsx): `normalizeDraftText`, `usePaste` | 2026-09-20 | Paste normalizes C0 input, but path acceptance bypasses it by design; layout returns control-containing raw text and counts ANSI payload characters that Ink treats as styling. |
| R5 | Darwin | Existing verification and helper | [`search-preview.ts`](../../src/tui/search-preview.ts): `searchPreview`; [`search-preview-checks.ts`](../../spike/search-preview-checks.ts); [`verify-path-completion.ts`](../../spike/verify-path-completion.ts); [`verify-prompt-editor.ts`](../../spike/verify-prompt-editor.ts); [`verify-tui.ts`](../../spike/verify-tui.ts) header | 2026-09-20 | Existing safe search projection and real Ink/pty verification seams; completion/word/undo scenarios are free. |
| R6 | Darwin | User documentation and git history | [`README.md`](../../README.md), Prompt editing paragraph; `git log -- src/tui/prompt-editor.ts src/tui/InputBox.tsx src/tui/path-completion.ts` (`3857ba3`, `a22d72d`, `c32e5f6`, `d29f091`) | 2026-09-20 | Existing word navigation, cut/yank, undo and completion features; proposing them anew would duplicate shipped work. |

### Peer highlights and innovations

Not applicable: this is a repository-only TUI self-review; no peer product was consulted.

### Current Darwin baseline

| Capability / architecture | Repository evidence | Current behavior |
|---|---|---|
| Bounded live frame | R1, R3 | Shared grants, selected completion window, explicit omission count. Fullscreen repaint on ordinary oversized content is a defect, not a feature request. |
| Path-only completion | R1, R2 | Bounded async directory scan; no file contents or new permission channel; raw path identity survives acceptance. |
| Grapheme-aware composer | R4, R6 | Cursor offsets refer to raw source; tabs use four display cells; LF makes draft rows; word movement and undo already exist. |
| Search preview safety | R1, R5 | Search labels visibly represent line breaks and controls without changing query or accepted prompt bytes. |

### Comparison and gaps

| Evidence | Darwin evidence | Gap / deliberate difference | Implication |
|---|---|---|---|
| Measured InputBox render at 24 columns, maxRows=6 | R2, R3 | `ordinary.txt` produces 4 rows; `('line\\n').repeat(12) + 'file.txt'` produces 16. One LF-containing candidate defeats the grant. | Repository names can grow the live menu beyond its counted height; architecture documents the resulting scrollback/repaint risk. |
| Measured ANSI candidate render | R3 | `ansi\\u001b[31mRED\\u001b[0m.txt` reaches output as active red styling (Ink rewrites reset to `ESC[39m`), not literal filename text. | Users do not see the actual name characters. No broader terminal-execution exploit claim is made. |
| Measured acceptance then composer render | R2, R4 | Exact raw path survives `applyPathCompletion`, as intended; `layoutEditor` also passes the escapes through and counts width 19 / cursor column 24 for an 80-column draft whose ANSI bytes Ink consumes. | A menu-only fix would leave the accepted draft visually unsafe and the cursor misaligned. One direction must close both presentation seams without sanitizing stored input. |

Reproductions ran under `node --import tsx --input-type=module -e` using actual React/Ink `renderToString`, `InputBox`, `layoutEditor`, and `applyPathCompletion`. The first `pnpm tsx -e` attempt failed because its CommonJS transform cannot load Yoga's top-level await; the ESM invocation above succeeded. No repository implementation was changed for research.

### Ranked iteration directions

`Score = 2 × Importance + Architecture fit + Evidence confidence − Difficulty − Risk`; gate 6.

| Rank | Proposed ID | Direction | Importance | Architecture fit | Evidence confidence | Difficulty | Risk | Score | Evidence and rationale |
|---:|---|---|---:|---:|---:|---:|---:|---:|---|
| 1 | SER-100 | Keep completion names and accepted drafts terminal-safe at presentation, preserving exact path identity and grapheme cursor mapping | 4 | 5 | 5 | 4 | 3 | 11 | R1–R5; verified row overflow and styling/cursor mismatch. Pure TUI projection fits existing seams; source-to-display mapping raises difficulty/risk. One atomic direction because a menu-only fix does not protect the accepted draft. Priority 132; no batch dependency. |

### Gated-out directions

| Direction | Score | Gate | Why it fell below | Kept anyway? |
|---|---:|---:|---|---|
| Fuzzy path matching | 3 (I=2, fit=2, confidence=2, difficulty=3, risk=2) | 6 | R2 explicitly documents stable prefix/basename ordering as intentional; no evidence of a user need outweighing that decision. | no |

Existing undo, word movement, search paste routing and search-preview safety (R1, R5, R6) were not proposed again. No below-gate backlog record was found or abandoned.

### Recommendation

- Batch and start: **SER-100** only. Priority 132 fits the current 121–140 page; no rollover or index mutation.
- Completion labels: visibly represent C0/DEL/C1 controls and Unicode line/paragraph separators on one counted row, including LF/CR/CRLF, tab, ANSI and OSC payloads. Reuse the established search-preview policy where suitable; do not rewrite candidate identity, ordering or accepted path bytes.
- Composer: display remaining terminal controls safely with counted display-cell/source-offset mapping. Preserve ordinary Unicode/grapheme behavior, LF multiline editing, tab layout, movement, deletion and undo semantics. Do not sanitize stored draft text to hide the defect. Scope is these live input surfaces, not a new global transcript sanitizer.
- Acceptance: real hostile filesystem names survive bounded scan and exact Tab/Enter insertion; real Ink menu render fits zero/partial/full grants at 24/100 columns with truthful selection/omissions; accepted draft has no executable control payload and correct cursor/deletion mapping; a real CLI pty covers selection then editing and exact unsent text without a model/file-content read. Run `pnpm typecheck`, `pnpm test`, focused prompt-editor/path-completion/frame-budget/search-preview suites, and free TUI `completion`, `pathCompletion`, `wordNav`, `undo`; rebuild after acceptance.
- Authorization: implementation through one fresh headless developer child in this repository; scoped code/tests/docs commits authorized, no new dependency, SDK-loop/policy change, destructive migration or force push. User docs and architecture must describe display-only escaping and unchanged inserted bytes.
- Backlog: append SER-100 as `not-started` to [`directions-121-140.md`](backlog/directions-121-140.md); select it only after a green clean starting point. No other direction in this batch.

### Batch iteration outcome

Pending supervision; no direction is accepted yet. The first preflight (`pnpm typecheck && pnpm test`, task `bg-0b8288ea-35e6-4cd5-b47d-82fd36700033`) passed typecheck but caught the Host's new origin-link fragment as a broken filesystem path in `verify-skills.ts` (161 passed, 1 failed). The origin link was corrected to the report file, with no source change; a complete clean-HEAD gate must pass before delegation.
