// English captions, one per slide, in slide order — the notes of the English deck
// (build-deck.cjs --lang=en). Keep in step with captions.cjs.
const CAPTIONS = [
  // 1 cover
  "A three-week experiment: let a coding agent take over its own development. The project is darwin, built on the Strands Agents TypeScript SDK. After Claude Code built the first version on August 13, every commit in the repository has been written by darwin itself. What follows is how it got here, step by step.",
  // 2 why
  "Two things to test. First, whether today's models and agent frameworks are good enough for a coding agent to run the whole chain itself — proposing requirements, implementing, testing, accepting, committing — with a human deciding only at the boundaries; the idea comes from Karpathy's autoresearch and Sakana's Darwin Gödel Machine. Second, a stress test of the Strands SDK: permission interception, context compaction, sub-agent orchestration, model switching — all claimed, never pushed to the limit at once by a project complex enough.",
  // 3 route
  "The big picture first. Three weeks of iteration answered two questions. Layer one: where do directions come from? From me proposing each one, to Claude Code proposing for me, to darwin assigning itself work, to darwin researching, prioritising and rolling a die to escape the local optimum — the source moved step by step out of human hands. Layer two: once a direction is set, how does each round go well? Reflecting on its own trajectories, fixing the process after each pitfall, and the mechanisms that kept the loop in hand for three weeks.",
  // 4 step 1
  "Step 1 was a baseline built with Claude Code. The prompt was one sentence: using the Strands SDK TS edition, build a single-agent TUI coding agent with skills and MCP. Two days, 19 commits, about 3,100 lines of TypeScript, and v0.0.1 was out. Everything the SDK offered was used as is; the only hand-built part was a skills loader the SDK didn't have yet. This version is the fixed baseline; every commit since has been written by the newest darwin.",
  // 5 divider layer 1
  "Layer one: where do directions come from? The hardest step in self-iteration is deciding what to change next. This layer is about handing that decision away from the human: the human proposes, another agent proposes, darwin assigns itself work, and finally darwin researches and prioritises on its own, with randomness to keep it from staring at one spot.",
  // 6 steps 2–3
  "Steps 2 and 3. At first I started darwin inside its own repository and proposed requirements one by one — configurable system prompt, prompt caching, /usage, wildcard permissions, /effort — which confirmed that darwin editing its own source does not break itself. But the human was the bottleneck. On August 14 I told Claude Code to play the developer: don't touch the code, only propose requirements, and have the newest darwin do each one. The source of direction moved to another agent, still outside darwin.",
  // 7 step 4
  "Step 4: darwin drives darwin. Claude Code broke the goal into three requirements — headless mode, background bash, task-completion notification — and assembled them into the built-in developer skill. The interactive darwin acts as Host and starts a headless child to implement; the Host reads the diff and runs the tests itself, commits, builds, and the next round starts a child from the new build. The first run produced six commits. From here the human's role became: give direction, make product decisions, authorise pushes.",
  // 8 step 5
  "Step 5: finding its own directions. The self-evolution-research skill checks the backlog first and only researches when nothing is left: study comparable products, compare with darwin's code and architecture, propose at most five directions. Each is scored 1 to 5 on importance, architecture fit, evidence, difficulty and risk; Score = 2 × importance + fit + evidence − difficulty − risk, and anything below 6 is recorded as considered and rejected. Average marks everywhere give exactly 6 — an unremarkable direction is not worth an iteration.",
  // 9 step 6
  "Step 6: roll a die. Left to itself the model always went to comparable products and proposed features others already had — a local optimum. Borrowing from stochastic gradient descent, a script now draws the research path by weight before anything is read: 50% peer products, 20% TUI self-inspection, 15% open-ended, 10% unused SDK capability, 5% observability. The roll is binding: once per run, copied verbatim into the report, no re-rolls, and a human override must be labelled as such. Rolling after reading is choosing.",
  // 10 dice results
  "With the die, different kinds of directions showed up. The TUI path found Esc to close popups, moving by word, undoing a deletion; observability added failed-turn records and per-turn token spend; the SDK path replaced the hand-written skills core with the official AgentSkills and built a workflow DAG on the SDK Graph; the open path found that /compact looped forever when nothing shrank, and that model retry waits were invisible and uncancellable. None of this comes from looking at peer products. The hand-off is complete: the human keeps only the weights, the gate and what counts as worth doing.",
  // 11 divider layer 2
  "Layer two: once a direction is set, how does each round go well? Fewer detours, fewer tokens, acceptance that can't be fooled. This layer covers how darwin finds improvements in its own run records, what went wrong over three weeks, and what kept the loop from running out of control.",
  // 12 step 7
  "Step 7: find problems in its own trajectory. Every session has an append-only trajectory.jsonl. The self-reflection skill starts a fresh headless darwin to read it and write against a template: grade completion (perfect, high, medium, low), find detours that changing darwin itself could avoid, and push worthwhile improvements through the same score gate into the backlog. The first reflection found two: a turn frozen after a stream interruption waiting for a human to type continue, and a wasted call from an extra field in bash status. Both were fixed by developer.",
  // 13 loop
  "Here the loop is complete: research or reflection produces directions, scores past the gate enter the backlog, /developer supervises a headless child, the Host accepts independently and commits, a new version is built, and it returns to the start to research the next direction. The human keeps direction, product trade-offs, safety boundaries and push authorisation. darwin runs the rest.",
  // 14 pitfall 1 tokens
  "The first problem was token consumption. One batch on August 17: 706 model calls, 296K output, 398M cache read. A single child session ran from planning through a fourth correction, with cached context per call growing from 230K to 790K; and each call averaged only 1.11 tool calls. Two fixes: one complete worker instead of a planning child and an implementation child each reviewed once, with the Host accepting only at the end; and a prompt rule to batch independent reads in one message.",
  // 15 pitfall 2
  "Four more lessons. Supervising the child cost more than the child's work: 322 of 390 tool calls in one batch were the Host polling, each a full-context model call; first a blocking wait, then the reverse — start returns at once, the Host may end its turn, and a notification enters the prompt queue when the job finishes. Reflection must dare to grade low: four of ten did, and one became a new direction. Same failure, no new hypothesis, no retry: six variants of one failing call until a human cancelled, hence the retry guard. And a process framework can become a drag: Trellis helped for three weeks and became a second rulebook after a stronger model arrived, so it was removed.",
  // 16 mechanisms
  "Four things kept it running for three weeks. Everything worth remembering goes into files: AGENTS.md is preloaded into the system prompt, the iteration log gets an entry per batch. Tests without mocks: about 130 scripts driving a real pty, real git repos, real models. Say up front what belongs to the human and when the batch stops. Most important, each round is done by the darwin just built: a change enters real work the moment it lands — a good one smooths the next round, a bad one gets run into by it.",
  // 17 three weeks later
  "The numbers after three weeks: 672 commits, 99 supervised iteration batches, 95 backlog directions with 93 done, about 37,000 lines of TypeScript, about 130 verification scripts. All implementation code after the baseline was written by darwin. It now has streaming Markdown and file diffs, four permission modes, resumable sessions and trajectory replay, subagent and workflow delegation, hooks and MCP, model switching, and three self-evolution skills.",
  // 18 Strands SDK
  "Verdict on the Strands SDK: the extension points are enough, and the agent loop was never forked. Models, tools, context management, permission interception and multi-agent orchestration all use native SDK capability, including ContextOffloader, Graph, backgroundTasks and AgentSkills. The gaps were in details of the built-in tools and plugins, filled by one pnpm patch: process handling in bash, exclusions and search in ContextOffloader, the miss hint and replace_all in fileEditor. Every patch was written by darwin after hitting the problem in its own iteration.",
  // 19 DeepSWE
  "Running the same problems as Claude Code: the first 20 DeepSWE tasks, same model Claude Opus 5, same tasks, only the agent changes, one run per task at effort high and again at medium. The conclusion: about equal in capability, darwin cheaper — scores match at both levels, 12 to 12 at high and 13 to 13 at medium, with darwin costing 7% and 11% less. A side finding: Opus 5 at medium is far more cost-effective than at high — both harnesses cut cost 40% and 37% with no loss of score. The limits: single sample, non-random tasks, and gaps of one or two tasks are within noise.",
  // 20 RSI / autoresearch
  "Two terms that get mixed up with this project. RSI is Good's 1965 idea — a system improves itself and the improved system improves better; today it means rewriting weights or improving the training pipeline. autoresearch is Karpathy's March 2026 setup: the agent edits one training script, five minutes a run, keep or roll back. The Darwin Gödel Machine lets a coding agent modify itself with an archive and selection. The coordinates for comparison come from Lilian Weng's July 2026 \"Harness Engineering for Self-Improvement\": three design patterns, an optimisation ladder, and self-improvement loops like Self-Harness and AHE.",
  // 21 where darwin sits
  "Self-improving loops fall into four families: reflective text evolution, editing only prompts and context (GEPA, ACE, MCE); program and workflow search, where code is the search space (ADAS, AFlow, AlphaEvolve); self-modifying harnesses, where the agent edits its own harness (DGM, Self-Harness, AHE); and meta-optimizers that improve the improver (STOP, Meta-Harness). darwin sits in the third: it edits its own harness code, all seven components, as a bounded, single-lineage, human-gated variant; its three self-evolution skills are themselves an optimizer being improved, so it touches the fourth. All four share one recipe. darwin has the first two steps — evidence from its own traces and bounded edits — and lacks the last two: a held-out utility gate, and an evaluator and permissions outside the loop.",
  // 22 next: Harbor
  "Next: put a number on good and bad. Today the standard is qualitative and internal — subjective ratings, acceptance that only checks for regressions, reflection that grades its own session. The plan is to run Terminal-Bench 2.0 and DeepSWE through Harbor: every candidate commit compared with the baseline on pass@1, cost and duration under the same model and parameters; a few smoke tasks for commits touching the agent core, the full set at each release. It answers the previous slide's three gaps: a held-out set, a verifier outside the loop, a scalar signal. So far it is a proposal and a submodule; the first step is one smoke task.",
  // 23 closing
  "Can it be called self-evolution? I lean towards caution. Within a clear boundary it can find directions, implement, accept and record, and pass what it learned to the next generation; the quality of directions and where the boundary sits are still human judgements. What the experiment shows is that in a 30k-line repository over three weeks, the work outside those judgements can be handed over. After three-plus weeks, roughly 10–20% of directions still needed my explicit guidance; left to explore, it might have got there, but at a much greater cost. To keep iterating, fork the repo and run /self-evolution-research; to just use it, npm install -g strands-darwin. Code and all records at github.com/xiehust/strands-darwin.",
];

module.exports = { CAPTIONS };
