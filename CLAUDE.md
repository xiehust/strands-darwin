# CLAUDE.md

Project instructions live in [`AGENTS.md`](AGENTS.md) — read it in full. darwin preloads that
file into its own system prompt, not this one, so this file is only a pointer: a second copy
here would drift.
