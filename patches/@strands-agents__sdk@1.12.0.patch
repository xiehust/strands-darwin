diff --git a/dist/src/conversation-manager/compression/context-compression.js b/dist/src/conversation-manager/compression/context-compression.js
index 84e3d1bb0bcd499d3dd2cd07c08baa79807ca79c..7e1c407bb04714309226c40b5daa00827799f477 100644
--- a/dist/src/conversation-manager/compression/context-compression.js
+++ b/dist/src/conversation-manager/compression/context-compression.js
@@ -126,9 +126,16 @@ export async function generateSummary(messagesToSummarize, model, systemPrompt)
     if (!result?.done || !result.value) {
         throw new Error('Failed to generate summary: no response from model');
     }
+    // Thinking models include reasoning blocks in the summary response; a
+    // user-role message must never carry reasoning content (providers reject
+    // the request), so keep only the non-reasoning blocks.
+    const summaryContent = result.value.message.content.filter((block) => block.type !== 'reasoningBlock');
+    if (summaryContent.length === 0) {
+        throw new Error('Failed to generate summary: response contained no non-reasoning content');
+    }
     return new Message({
         role: 'user',
-        content: result.value.message.content,
+        content: summaryContent,
     });
 }
 /**
diff --git a/dist/src/models/openai/errors.js b/dist/src/models/openai/errors.js
index 17a473fe3bcdcc02e17cd95283c832d9021634c1..6e5cda23f672684b3926450322459279e2355066 100644
--- a/dist/src/models/openai/errors.js
+++ b/dist/src/models/openai/errors.js
@@ -20,6 +20,7 @@ const CONTEXT_WINDOW_OVERFLOW_PATTERNS = [
     'input length and `max_tokens` exceed context limit',
     'too many total text bytes',
     'exceed customer model maximum',
+    'exceed model maximum',
 ];
 /**
  * Error patterns that indicate rate limiting.
diff --git a/dist/src/models/openai/responses-adapter.d.ts b/dist/src/models/openai/responses-adapter.d.ts
index e17f00ac284c1e3357d55205c57b431061141b55..a7b4edd12a5ac33277a5ad2c4bcea4c4d8c1ea7d 100644
--- a/dist/src/models/openai/responses-adapter.d.ts
+++ b/dist/src/models/openai/responses-adapter.d.ts
@@ -52,6 +52,7 @@ export interface ResponsesStreamState {
         outputTokens: number;
         totalTokens: number;
         cacheReadInputTokens?: number;
+        cacheWriteInputTokens?: number;
     } | null;
     stopReason: StopReason;
 }
diff --git a/dist/src/models/openai/responses-adapter.js b/dist/src/models/openai/responses-adapter.js
index 23b1dad5bf6ca0dc60ec22d70bf195c1556ba29a..455fc41794c2b6bbe0cc5c86d428cf8c7134810f 100644
--- a/dist/src/models/openai/responses-adapter.js
+++ b/dist/src/models/openai/responses-adapter.js
@@ -326,9 +326,13 @@ function mapResponsesUsage(usage) {
         totalTokens: usage.total_tokens,
     };
     const cached = usage.input_tokens_details?.cached_tokens;
-    if (typeof cached === 'number' && cached > 0) {
+    if (typeof cached === 'number' && cached >= 0) {
         mapped.cacheReadInputTokens = cached;
     }
+    const cacheWrite = usage.input_tokens_details?.cache_write_tokens;
+    if (typeof cacheWrite === 'number' && cacheWrite >= 0) {
+        mapped.cacheWriteInputTokens = cacheWrite;
+    }
     return mapped;
 }
 function createResponsesStreamError(message, code) {
diff --git a/dist/src/vended-plugins/context-offloader/plugin.d.ts b/dist/src/vended-plugins/context-offloader/plugin.d.ts
index b0223555c1f0410441546be860dbae824ac9857c..0cefd600f9b2324d23033f07c7b44f74fceb123f 100644
--- a/dist/src/vended-plugins/context-offloader/plugin.d.ts
+++ b/dist/src/vended-plugins/context-offloader/plugin.d.ts
@@ -70,6 +70,7 @@ export declare class ContextOffloader implements Plugin {
     private readonly _includeRetrievalTool;
     private readonly _evictAfterCycles;
     private readonly _storageByAgent;
+    private readonly _scannedAgents;
     private readonly _keyStoredAt;
     private _retrievalTool;
     constructor(config: ContextOffloaderConfig);
@@ -82,6 +83,9 @@ export declare class ContextOffloader implements Plugin {
     private _createRetrievalTool;
     private _storeBlock;
     private _buildPreviewText;
+    private _isStoredOffloadPlaceholder;
+    private _repairRestoredToolResults;
+    private _offloadToolResult;
     private _handleToolResult;
 }
 //# sourceMappingURL=plugin.d.ts.map
\ No newline at end of file
diff --git a/dist/src/vended-plugins/context-offloader/plugin.js b/dist/src/vended-plugins/context-offloader/plugin.js
index 2569b5ce9a20e4e0ccf5f1e00f7f6ea62e9d4094..53164390bf0897fef9e174ee2b883411ddb517d8 100644
--- a/dist/src/vended-plugins/context-offloader/plugin.js
+++ b/dist/src/vended-plugins/context-offloader/plugin.js
@@ -1,5 +1,5 @@
 import { AfterToolCallEvent, BeforeModelCallEvent } from '../../hooks/events.js';
-import { TextBlock, JsonBlock, ToolResultBlock, Message } from '../../types/messages.js';
+import { TextBlock, JsonBlock, ToolUseBlock, ToolResultBlock, Message } from '../../types/messages.js';
 import { ImageBlock, VideoBlock, DocumentBlock } from '../../types/media.js';
 import { tool } from '../../tools/tool-factory.js';
 import { z } from 'zod';
@@ -48,6 +48,21 @@ const CHARS_PER_TOKEN = 4;
 const DEFAULT_MAX_RESULT_TOKENS = 2_500;
 const DEFAULT_PREVIEW_TOKENS = 1_000;
 const RETRIEVAL_TOOL_NAME = 'retrieve_offloaded_content';
+function offloadGuidance(includeRetrievalTool) {
+    let guidance = 'Tool result was offloaded to external storage due to size.\n' +
+        'Use the preview below if it answers your question.\n';
+    if (includeRetrievalTool) {
+        guidance +=
+            'If you need more detail, use retrieve_offloaded_content with a reference and:\n' +
+                '  - pattern: regex or keyword to find matching lines with context\n' +
+                '  - line_range: { start, end } to read a specific span of lines\n' +
+                'Retrieve full content (omit pattern/line_range) as a last resort.';
+    }
+    else {
+        guidance += 'If you need more detail, use your available tools to access specific data.';
+    }
+    return guidance;
+}
 const retrievalInputSchema = z.object({
     reference: z.string().describe('The reference string from the offload placeholder (e.g. "mem_1_tool-123_0").'),
     pattern: z
@@ -164,6 +179,7 @@ export class ContextOffloader {
     _includeRetrievalTool;
     _evictAfterCycles;
     _storageByAgent = new WeakMap();
+    _scannedAgents = new WeakSet();
     _keyStoredAt = new Map();
     _retrievalTool;
     constructor(config) {
@@ -214,7 +230,8 @@ export class ContextOffloader {
         }
         this._storageForAgent(agent);
         agent.addHook(AfterToolCallEvent, (event) => this._handleToolResult(event));
-        agent.addHook(BeforeModelCallEvent, () => {
+        agent.addHook(BeforeModelCallEvent, async (event) => {
+            await this._repairRestoredToolResults(event.agent);
             const cycleCount = agent.metrics.cycleCount;
             if (this._storage instanceof LegacyInMemoryStorage) {
                 this._storage._evict(cycleCount);
@@ -345,40 +362,130 @@ export class ContextOffloader {
             .filter((r) => r.ref)
             .map((r) => `  ${r.ref} (${r.description})`)
             .join('\n');
-        let guidance = 'Tool result was offloaded to external storage due to size.\n' +
-            'Use the preview below if it answers your question.\n';
-        if (this._includeRetrievalTool) {
-            guidance +=
-                'If you need more detail, use retrieve_offloaded_content with a reference and:\n' +
-                    '  - pattern: regex or keyword to find matching lines with context\n' +
-                    '  - line_range: { start, end } to read a specific span of lines\n' +
-                    'Retrieve full content (omit pattern/line_range) as a last resort.';
-        }
-        else {
-            guidance += 'If you need more detail, use your available tools to access specific data.';
-        }
+        const guidance = offloadGuidance(this._includeRetrievalTool);
         return (`[Offloaded: ${content.length} blocks, ~${tokenCount.toLocaleString()} tokens]\n` +
             `${guidance}\n\n` +
             `${preview}\n\n` +
             `[Stored references:]\n${refLines}`);
     }
-    async _handleToolResult(event) {
-        if (event.result.status === 'error')
-            return;
-        // Skip delegation tool results — their content is the final delegated answer
-        // and must not be truncated/offloaded. The delegation plugin transforms this
-        // result into the AgentResult.lastMessage, so offloading would replace the
-        // final answer with an unrecoverable placeholder.
-        if (event.tool instanceof AgentAsTool && event.tool.delegate)
-            return;
-        // Skip results from the retrieval tool to prevent circular offloading
-        if (this._includeRetrievalTool && event.toolUse.name === RETRIEVAL_TOOL_NAME)
+    async _isStoredOffloadPlaceholder(agent, result) {
+        const first = result.content[0];
+        if (!(first instanceof TextBlock))
+            return false;
+        const marker = first.text.match(/^\[Offloaded: (\d+) blocks, ~[\d,]+ tokens\]\n/);
+        if (!marker)
+            return false;
+        const guidancePrefix = `${marker[0]}${offloadGuidance(this._includeRetrievalTool)}\n\n`;
+        const referencesMarker = '\n\n[Stored references:]\n';
+        const referencesAt = first.text.lastIndexOf(referencesMarker);
+        if (!first.text.startsWith(guidancePrefix) || referencesAt < guidancePrefix.length)
+            return false;
+        // A tool can return marker-like text. Even when it names a real stored key,
+        // never trust a purported preview larger than this plugin could have built: a
+        // forged oversized preview would otherwise bypass restored-history repair.
+        const preview = first.text.slice(guidancePrefix.length, referencesAt);
+        if (preview.length > this._previewTokens * CHARS_PER_TOKEN)
+            return false;
+        const referencesText = first.text.slice(referencesAt + referencesMarker.length);
+        if (referencesText === '' || referencesText.endsWith('\n'))
+            return false;
+        const referenceLines = referencesText.split('\n');
+        const parsedReferences = referenceLines.map((line) => line.match(/^  (\S+) \([^\r\n]+\)$/));
+        if (parsedReferences.some((match) => match === null))
+            return false;
+        const originalBlockCount = Number(marker[1]);
+        const references = parsedReferences.map((match) => match[1]);
+        const supplementalReferences = [];
+        for (const supplemental of result.content.slice(1)) {
+            if (!(supplemental instanceof TextBlock))
+                return false;
+            const media = supplemental.text.match(/^\[(?:image|video|document): .+, \d+ bytes(?: \| ref: (\S+))?\]$/);
+            if (!media)
+                return false;
+            if (media[1])
+                supplementalReferences.push(media[1]);
+        }
+        if (supplementalReferences.some((reference) => !references.includes(reference)))
+            return false;
+        const referenceIndexes = references.map((reference) => {
+            const directPrefix = `${result.toolUseId}_`;
+            const nestedPrefix = `_${directPrefix}`;
+            const suffix = reference.startsWith(directPrefix)
+                ? reference.slice(directPrefix.length)
+                : reference.includes(nestedPrefix)
+                    ? reference.slice(reference.lastIndexOf(nestedPrefix) + nestedPrefix.length)
+                    : '';
+            return /^\d+$/.test(suffix) ? Number(suffix) : -1;
+        });
+        const uniqueIndexes = new Set(referenceIndexes);
+        if (originalBlockCount < 1 || references.length > originalBlockCount ||
+            referenceIndexes.some((index) => index < 0 || index >= originalBlockCount) ||
+            uniqueIndexes.size !== referenceIndexes.length)
+            return false;
+        try {
+            const storage = this._storageForAgent(agent);
+            for (const reference of references) {
+                const stored = await retrieveContent(storage, reference);
+                if (stored.content.length === 0 || stored.contentType === '')
+                    return false;
+            }
+            return true;
+        }
+        catch {
+            return false;
+        }
+    }
+
+    async _repairRestoredToolResults(agent) {
+        if (this._scannedAgents.has(agent))
             return;
-        const content = event.result.content;
-        const toolUseId = event.result.toolUseId;
-        const tokenCount = await event.agent.model.countTokens([new Message({ role: 'user', content: [event.result] })]);
+        // Mark before awaiting count/storage so a failure cannot turn the scan into a
+        // per-cycle retry loop. Session restore has already run during initialize(),
+        // while request assembly clones messages only after this hook completes.
+        this._scannedAgents.add(agent);
+        const delegationToolUseIds = new Set();
+        const retrievalToolUseIds = new Set();
+        for (const message of agent.messages) {
+            for (const block of message.content) {
+                if (!(block instanceof ToolUseBlock))
+                    continue;
+                const tool = agent.toolRegistry.get(block.name);
+                if (tool instanceof AgentAsTool && tool.delegate) {
+                    delegationToolUseIds.add(block.toolUseId);
+                }
+                if (this._includeRetrievalTool && block.name === RETRIEVAL_TOOL_NAME) {
+                    retrievalToolUseIds.add(block.toolUseId);
+                }
+            }
+        }
+        for (const message of agent.messages) {
+            for (let index = 0; index < message.content.length; index += 1) {
+                const block = message.content[index];
+                if (!(block instanceof ToolResultBlock) || block.status !== 'success' ||
+                    delegationToolUseIds.has(block.toolUseId) || retrievalToolUseIds.has(block.toolUseId))
+                    continue;
+                try {
+                    if (await this._isStoredOffloadPlaceholder(agent, block))
+                        continue;
+                    const replacement = await this._offloadToolResult(agent, block);
+                    if (replacement) {
+                        // Replace only the content block. The Message object (and therefore
+                        // its trackingId/metadata/position) and the matching toolUseId stay intact.
+                        message.content[index] = replacement;
+                    }
+                }
+                catch (err) {
+                    logger.warn(`tool_use_id=<${block.toolUseId}> | failed to inspect restored tool result, keeping original`, err);
+                }
+            }
+        }
+    }
+    async _offloadToolResult(agent, result) {
+        const content = result.content;
+        const toolUseId = result.toolUseId;
+        const tokenCount = await agent.model.countTokens([new Message({ role: 'user', content: [result] })]);
         if (tokenCount <= this._maxResultTokens)
-            return;
+            return undefined;
         // Extract text preview from text/JSON blocks
         const textParts = [];
         for (const block of content) {
@@ -388,22 +495,28 @@ export class ContextOffloader {
                 textParts.push(JSON.stringify(block.json, null, 2));
         }
         const fullText = textParts.join('\n');
-        // Store each content block to the storage backend
+        // Store each content block to the storage backend. Only publish references
+        // after every write succeeds; a failure leaves the caller's result untouched.
         let references;
-        try {
-            const storage = this._storageForAgent(event.agent);
-            references = await Promise.all(content.map((block, i) => this._storeBlock(storage, block, `${toolUseId}_${i}`)));
+        const storage = this._storageForAgent(agent);
+        const settled = await Promise.allSettled(content.map((block, i) => this._storeBlock(storage, block, `${toolUseId}_${i}`)));
+        const failed = settled.find((entry) => entry.status === 'rejected');
+        if (failed) {
             if (!isOffloaderStorage(storage)) {
-                const storedAt = event.agent.metrics.cycleCount;
-                for (const entry of references) {
-                    if (entry.ref)
-                        this._keyStoredAt.set(entry.ref, storedAt);
-                }
+                await Promise.allSettled(settled.flatMap((entry) => entry.status === 'fulfilled' && entry.value.ref
+                    ? [storage.delete(entry.value.ref)]
+                    : []));
             }
+            logger.warn(`tool_use_id=<${toolUseId}> | failed to offload tool result, keeping original`, failed.reason);
+            return undefined;
         }
-        catch (err) {
-            logger.warn(`tool_use_id=<${toolUseId}> | failed to offload tool result, keeping original`, err);
-            return;
+        references = settled.map((entry) => entry.value);
+        if (!isOffloaderStorage(storage)) {
+            const storedAt = agent.metrics.cycleCount;
+            for (const entry of references) {
+                if (entry.ref)
+                    this._keyStoredAt.set(entry.ref, storedAt);
+            }
         }
         logger.debug(`tool_use_id=<${toolUseId}>, blocks=<${references.length}>, tokens=<${tokenCount}> | tool result offloaded`);
         // Build replacement content: preview text + media placeholders
@@ -428,11 +541,28 @@ export class ContextOffloader {
                 newContent.push(new TextBlock(`[${label}, ${size} bytes${ref ? ` | ref: ${ref}` : ''}]`));
             }
         }
-        event.result = new ToolResultBlock({
-            toolUseId: event.result.toolUseId,
-            status: event.result.status,
+        return new ToolResultBlock({
+            toolUseId: result.toolUseId,
+            status: result.status,
             content: newContent,
         });
     }
+    async _handleToolResult(event) {
+        if (event.result.status === 'error')
+            return;
+        // Skip delegation tool results — their content is the final delegated answer
+        // and must not be truncated/offloaded. The delegation plugin transforms this
+        // result into the AgentResult.lastMessage, so offloading would replace the
+        // final answer with an unrecoverable placeholder.
+        if (event.tool instanceof AgentAsTool && event.tool.delegate)
+            return;
+        // Skip results from the retrieval tool to prevent circular offloading
+        if (this._includeRetrievalTool && event.toolUse.name === RETRIEVAL_TOOL_NAME)
+            return;
+        const replacement = await this._offloadToolResult(event.agent, event.result);
+        if (replacement)
+            event.result = replacement;
+    }
+
 }
 //# sourceMappingURL=plugin.js.map
\ No newline at end of file
diff --git a/dist/src/vended-tools/bash/bash.d.ts b/dist/src/vended-tools/bash/bash.d.ts
index a0a5b0e40e22e6108df5d832e9b1ba051e2f99cd..8907d109ce2866e376df312ebc0bae1a68acec3d 100644
--- a/dist/src/vended-tools/bash/bash.d.ts
+++ b/dist/src/vended-tools/bash/bash.d.ts
@@ -1,12 +1,19 @@
-import type { BashOutput } from './types.js';
+import type { InvokableTool } from '../../index.js';
+import type { BashInput, BashOutput } from './types.js';
+export interface CreateBashOptions {
+    /** Initial cwd for every new persistent shell owned by this tool. */
+    cwd?: string;
+    /** Session project root used only for conservative wrong-root path preflight. */
+    projectRoot?: string;
+}
+/**
+ * Create a host-only bash tool with configured persistent-shell path context.
+ */
+export declare function createBash(options?: CreateBashOptions): InvokableTool<BashInput, BashOutput | string>;
 /**
  * Host-only bash tool with a persistent session across calls.
  * State (variables, working directory) persists within the session.
  * Node.js only.
  */
-export declare const bash: import("../../index.js").InvokableTool<{
-    mode: "execute" | "restart";
-    command?: string | undefined;
-    timeout?: number | undefined;
-}, BashOutput | "Bash session restarted">;
+export declare const bash: InvokableTool<BashInput, BashOutput | string>;
 //# sourceMappingURL=bash.d.ts.map
\ No newline at end of file
diff --git a/dist/src/vended-tools/bash/bash.js b/dist/src/vended-tools/bash/bash.js
index 44fb775853169dab0fde7aee5e7cc275a060de9c..06b2123ec4821b12e53f04da5a6f1cb336770d5c 100644
--- a/dist/src/vended-tools/bash/bash.js
+++ b/dist/src/vended-tools/bash/bash.js
@@ -1,8 +1,13 @@
 import { tool } from '../../tools/tool-factory.js';
 import { z } from 'zod';
 import { spawn } from 'child_process';
+import { existsSync, statSync } from 'fs';
+import path from 'path';
 import { Buffer } from 'buffer';
 import { BashTimeoutError, BashSessionError } from './types.js';
+const SHELL_RESTART_NOTICE = 'Notice: Persistent bash shell exited with code 0; it will restart before the next command.';
+const UNSAFE_PREFLIGHT_SYNTAX = /[\n\r'"\\;&|<>$`(){}\[\]*?!]/;
+const PLAIN_RELATIVE_PATH = /^(?:\.\/)?[A-Za-z0-9_.-]+(?:\/[A-Za-z0-9_.-]+)+\/?$/;
 const bashInputSchema = z.object({
     mode: z
         .enum(['execute', 'restart'])
@@ -10,6 +15,57 @@ const bashInputSchema = z.object({
     command: z.string().optional().describe('The bash command to execute (required when mode is "execute")'),
     timeout: z.number().positive().optional().describe('Timeout in seconds (default: 120, applies only to execute mode)'),
 });
+/**
+ * Recognize only the simple path shapes observed in SRF-014. General shell
+ * syntax deliberately fails open rather than being tokenized or rewritten.
+ */
+function preflightPath(command) {
+    const trimmed = command.trim();
+    if (!trimmed || UNSAFE_PREFLIGHT_SYNTAX.test(trimmed)) {
+        return null;
+    }
+    const words = trimmed.split(/[ \t]+/);
+    if (words[0] === 'cd') {
+        if (words.length !== 2 || words[1].startsWith('-') || path.isAbsolute(words[1])) {
+            return null;
+        }
+        return { value: words[1], requireDirectory: true };
+    }
+    const candidate = words[0];
+    if (!PLAIN_RELATIVE_PATH.test(candidate) || candidate.startsWith('-') || path.isAbsolute(candidate)) {
+        return null;
+    }
+    return { value: candidate, requireDirectory: false };
+}
+function pathExists(candidate, requireDirectory) {
+    try {
+        return existsSync(candidate) && (!requireDirectory || statSync(candidate).isDirectory());
+    }
+    catch {
+        return false;
+    }
+}
+function wrongRootDiagnostic(command, cwd, projectRoot) {
+    const candidate = preflightPath(command);
+    if (candidate === null) {
+        return null;
+    }
+    const cwdPath = path.resolve(cwd, candidate.value);
+    const projectPath = path.resolve(projectRoot, candidate.value);
+    if (cwdPath === projectPath || pathExists(cwdPath, candidate.requireDirectory) || !pathExists(projectPath, candidate.requireDirectory)) {
+        return null;
+    }
+    return {
+        output: '',
+        error: `Command not run: relative path "${candidate.value}" does not exist from the persistent shell cwd, but it does exist from the session project root.\n` +
+            `cwd: ${cwd}\n` +
+            `cwd path: ${cwdPath}\n` +
+            `project-root path: ${projectPath}\n` +
+            `Return to the project root with "cd ${projectRoot}" or use "${projectPath}".`,
+        cwd,
+    };
+}
+
 /**
  * Internal class for managing a bash session.
  */
@@ -18,9 +74,21 @@ class BashSession {
     _started = false;
     _timeout;
     _sentinel;
-    constructor(timeout = 120) {
+    _stderrSentinel;
+    _exitCodeSentinel;
+    _cwdSentinel;
+    _initialCwd;
+    _projectRoot;
+    _cwd;
+    constructor(timeout = 120, initialCwd = process.cwd(), projectRoot = null) {
         this._timeout = timeout;
         this._sentinel = `__BASH_DONE_${Date.now()}_${Math.random().toString(36).slice(2)}__`;
+        this._stderrSentinel = `${this._sentinel}_STDERR`;
+        this._exitCodeSentinel = `${this._sentinel}_EXIT`;
+        this._cwdSentinel = `${this._sentinel}_CWD`;
+        this._initialCwd = path.resolve(initialCwd);
+        this._projectRoot = projectRoot === null ? null : path.resolve(projectRoot);
+        this._cwd = this._initialCwd;
     }
     /**
      * Starts the bash process if not already started.
@@ -31,7 +99,7 @@ class BashSession {
         }
         try {
             this._process = spawn('bash', [], {
-                cwd: process.cwd(),
+                cwd: this._initialCwd,
                 env: { ...process.env, PS1: '', PS2: '' },
             });
             if (!this._process.stdin || !this._process.stdout || !this._process.stderr) {
@@ -61,9 +129,14 @@ class BashSession {
         activeSessions.delete(this);
     }
     /**
-     * Runs a command in the bash session.
+     * Runs a command in the bash session. The per-Agent foreground queue ensures
+     * this method has exclusive ownership of the persistent process and streams.
      */
     async run(command, timeout) {
+        const diagnostic = this._projectRoot === null ? null : wrongRootDiagnostic(command, this._cwd, this._projectRoot);
+        if (diagnostic !== null) {
+            return diagnostic;
+        }
         this.start();
         if (!this._process || !this._process.stdin || !this._process.stdout || !this._process.stderr) {
             throw new BashSessionError('Bash session not properly initialized');
@@ -73,32 +146,87 @@ class BashSession {
         let stderrData = '';
         let timeoutHandle = null;
         let isTimedOut = false;
+        let stdoutDone = false;
+        let stderrDone = false;
         return new Promise((resolve, reject) => {
             const stdout = this._process.stdout;
             const stderr = this._process.stderr;
             const stdin = this._process.stdin;
             // Handlers for stdout
+            const maybeResolve = () => {
+                if (!stdoutDone || !stderrDone) {
+                    return;
+                }
+                cleanup();
+                const cwdStart = stdoutData.lastIndexOf(this._cwdSentinel);
+                const cwdEnd = cwdStart < 0 ? -1 : stdoutData.indexOf('\0', cwdStart);
+                if (cwdStart < 0 || cwdEnd < 0) {
+                    reject(new BashSessionError('Bash session did not report its effective cwd'));
+                    return;
+                }
+                const reportedCwd = stdoutData.slice(cwdStart + this._cwdSentinel.length, cwdEnd).replace(/\n$/, '');
+                if (path.isAbsolute(reportedCwd)) {
+                    this._cwd = reportedCwd;
+                }
+                const afterCwd = stdoutData[cwdEnd + 1] === '\n' ? cwdEnd + 2 : cwdEnd + 1;
+                const withoutCwd = `${stdoutData.slice(0, cwdStart)}${stdoutData.slice(afterCwd)}`;
+                const exitStart = withoutCwd.lastIndexOf(this._exitCodeSentinel);
+                const exitEnd = exitStart < 0 ? -1 : withoutCwd.indexOf('\n', exitStart);
+                if (exitStart < 0 || exitEnd < 0) {
+                    reject(new BashSessionError('Bash session did not report the command exit code'));
+                    return;
+                }
+                const exitCode = Number(withoutCwd.slice(exitStart + this._exitCodeSentinel.length, exitEnd));
+                if (!Number.isInteger(exitCode)) {
+                    reject(new BashSessionError('Bash session reported an invalid command exit code'));
+                    return;
+                }
+                const output = `${withoutCwd.slice(0, exitStart)}${withoutCwd.slice(exitEnd + 1)}`.replace(this._sentinel, '').trim();
+                const error = stderrData.replace(this._stderrSentinel, '').trim();
+                resolve({ output, error, cwd: this._cwd, exitCode });
+            };
             const onStdoutData = (chunk) => {
                 const data = Buffer.from(chunk).toString('utf-8');
                 stdoutData += data;
                 // Check for sentinel
                 if (stdoutData.includes(this._sentinel)) {
-                    cleanup();
-                    // Remove sentinel from output
-                    const output = stdoutData.replace(this._sentinel, '').trim();
-                    const error = stderrData.trim();
-                    resolve({ output, error });
+                    stdoutDone = true;
+                    maybeResolve();
                 }
             };
             // Handlers for stderr
             const onStderrData = (chunk) => {
                 stderrData += Buffer.from(chunk).toString('utf-8');
+                if (stderrData.includes(this._stderrSentinel)) {
+                    stderrDone = true;
+                    maybeResolve();
+                }
             };
-            // Handler for process close
-            const onClose = (code) => {
+            // Handler for process close. Node emits close only after stdio has closed,
+            // so the buffers contain everything this invocation produced.
+            const onClose = (code, signal) => {
                 if (!isTimedOut) {
                     cleanup();
-                    reject(new BashSessionError(`Bash process exited unexpectedly with code ${code ?? 'unknown'}`));
+                    const output = stdoutData.trim();
+                    const error = stderrData.trim();
+                    if (code === 0 && signal === null) {
+                        resolve({
+                            output,
+                            error: error ? `${error}\n${SHELL_RESTART_NOTICE}` : SHELL_RESTART_NOTICE,
+                            cwd: this._cwd,
+                        });
+                        return;
+                    }
+                    const metadata = signal === null
+                        ? `code ${code ?? 'unknown'}`
+                        : `signal ${signal}${code === null ? '' : ` (code ${code})`}`;
+                    const failure = new BashSessionError(`Bash process exited unexpectedly with ${metadata}`);
+                    failure.exitCode = code;
+                    failure.signal = signal;
+                    failure.output = output;
+                    failure.error = error;
+                    failure.cwd = this._cwd;
+                    reject(failure);
                 }
             };
             // Handler for process errors
@@ -136,7 +264,7 @@ class BashSession {
             this._process.on('error', onError);
             // Send command with sentinel
             try {
-                stdin.write(`${command}\necho "${this._sentinel}"\n`);
+                stdin.write(`${command}\n__darwin_exit_code=$?\ncommand printf '${this._exitCodeSentinel}%s\\n' "$__darwin_exit_code"\ncommand printf '${this._cwdSentinel}%s\\0\\n' "$(command pwd -P)"\necho "${this._sentinel}"\necho "${this._stderrSentinel}" >&2\n`);
             }
             catch (err) {
                 cleanup();
@@ -151,6 +279,16 @@ class BashSession {
  */
 // eslint-disable-next-line @typescript-eslint/no-explicit-any
 const sessions = new WeakMap();
+/**
+ * Serialize all foreground operations for one Agent, including explicit restart.
+ */
+const foregroundTails = new WeakMap();
+function enqueueForeground(agent, operation) {
+    const previous = foregroundTails.get(agent) ?? Promise.resolve();
+    const result = previous.then(operation, operation);
+    foregroundTails.set(agent, result.then(() => undefined, () => undefined));
+    return result;
+}
 /**
  * Track all active sessions for cleanup on process exit.
  */
@@ -221,42 +359,50 @@ const DEFAULT_DESCRIPTION = 'Executes bash shell commands in a persistent sessio
  * State (variables, working directory) persists within the session.
  * Node.js only.
  */
-export const bash = tool({
-    name: 'bash',
-    description: DEFAULT_DESCRIPTION,
-    inputSchema: bashInputSchema,
-    callback: async (input, context) => {
-        if (!context) {
-            throw new Error('Tool context is required for bash operations');
-        }
-        const agent = context.agent;
-        // Validate execute mode has command
-        if (input.mode === 'execute' && !input.command) {
-            throw new Error('command is required when mode is "execute"');
-        }
-        // Handle restart mode
-        if (input.mode === 'restart') {
-            const existingSession = sessions.get(agent);
-            if (existingSession) {
-                existingSession.stop();
-                sessions.delete(agent);
+export function createBash(options = {}) {
+    const initialCwd = path.resolve(options.cwd ?? process.cwd());
+    const projectRoot = options.projectRoot === undefined ? null : path.resolve(options.projectRoot);
+    const reportRestartCwd = options.cwd !== undefined || options.projectRoot !== undefined;
+    return tool({
+        name: 'bash',
+        description: DEFAULT_DESCRIPTION,
+        inputSchema: bashInputSchema,
+        callback: async (input, context) => {
+            if (!context) {
+                throw new Error('Tool context is required for bash operations');
             }
-            // Create new session (will be added to activeSessions when started)
-            const newSession = new BashSession(120);
-            sessions.set(agent, newSession);
-            sessionFinalizer.register(agent, newSession);
-            return 'Bash session restarted';
-        }
-        // Handle execute mode
-        // Get or create session
-        let session = sessions.get(agent);
-        if (!session) {
-            session = new BashSession(input.timeout ?? 120);
-            sessions.set(agent, session);
-            sessionFinalizer.register(agent, session);
-        }
-        // Execute command
-        return session.run(input.command, input.timeout);
-    },
-});
+            const agent = context.agent;
+            // Validate execute mode has command
+            if (input.mode === 'execute' && !input.command) {
+                throw new Error('command is required when mode is "execute"');
+            }
+            return enqueueForeground(agent, async () => {
+                // Handle restart mode
+                if (input.mode === 'restart') {
+                    const existingSession = sessions.get(agent);
+                    if (existingSession) {
+                        existingSession.stop();
+                        sessions.delete(agent);
+                    }
+                    // Create new session (will be added to activeSessions when started)
+                    const newSession = new BashSession(120, initialCwd, projectRoot);
+                    sessions.set(agent, newSession);
+                    sessionFinalizer.register(agent, newSession);
+                    return reportRestartCwd ? `Bash session restarted\ncwd: ${initialCwd}` : 'Bash session restarted';
+                }
+                // Handle execute mode
+                // Get or create session
+                let session = sessions.get(agent);
+                if (!session) {
+                    session = new BashSession(input.timeout ?? 120, initialCwd, projectRoot);
+                    sessions.set(agent, session);
+                    sessionFinalizer.register(agent, session);
+                }
+                // Execute command
+                return session.run(input.command, input.timeout);
+            });
+        },
+    });
+}
+export const bash = createBash();
 //# sourceMappingURL=bash.js.map
\ No newline at end of file
diff --git a/dist/src/vended-tools/bash/index.d.ts b/dist/src/vended-tools/bash/index.d.ts
index 74f8db2f387fa4374920935939b9229f94187aba..9ce7b8ab45e820e7b0561b76d30f88f4a1c8c0b4 100644
--- a/dist/src/vended-tools/bash/index.d.ts
+++ b/dist/src/vended-tools/bash/index.d.ts
@@ -5,7 +5,7 @@
  * stateless tool that routes commands through a sandbox, which runs whichever
  * shell that sandbox provides.
  */
-export { bash } from './bash.js';
+export { bash, createBash } from './bash.js';
 export { makeShell, makeBash } from './make-shell.js';
 export type { MakeShellOptions, MakeBashOptions } from './make-shell.js';
 export { SANDBOX_SHELL_DESCRIPTION, SANDBOX_BASH_DESCRIPTION, BashTimeoutError, BashSessionError, ShellTimeoutError, ShellExecutionError, } from './types.js';
diff --git a/dist/src/vended-tools/bash/index.js b/dist/src/vended-tools/bash/index.js
index 19409ad6160e57325f377097c83795579cc31cf0..606a301fb63090684f4d928011552140fcca4087 100644
--- a/dist/src/vended-tools/bash/index.js
+++ b/dist/src/vended-tools/bash/index.js
@@ -5,7 +5,7 @@
  * stateless tool that routes commands through a sandbox, which runs whichever
  * shell that sandbox provides.
  */
-export { bash } from './bash.js';
+export { bash, createBash } from './bash.js';
 export { makeShell, makeBash } from './make-shell.js';
 export { SANDBOX_SHELL_DESCRIPTION, SANDBOX_BASH_DESCRIPTION, BashTimeoutError, BashSessionError, ShellTimeoutError, ShellExecutionError, } from './types.js';
 //# sourceMappingURL=index.js.map
\ No newline at end of file
diff --git a/dist/src/vended-tools/bash/types.d.ts b/dist/src/vended-tools/bash/types.d.ts
index 6b64c4e390b20e3a440d31e7e4b2b7fabab73b09..0e8a97cca8e72363de51ec9ad86a4e4824460da3 100644
--- a/dist/src/vended-tools/bash/types.d.ts
+++ b/dist/src/vended-tools/bash/types.d.ts
@@ -50,10 +50,16 @@ export interface BashOutput {
      * Empty string if no errors occurred.
      */
     error: string;
+    /**
+     * Effective persistent-shell cwd after the command.
+     */
+    cwd: string;
+    /** Exit status of the foreground command, before Darwin's shell sentinels. */
+    exitCode: number;
     /**
      * Allow indexing with string keys for JSONValue compatibility.
      */
-    [key: string]: string;
+    [key: string]: string | number;
 }
 /**
  * Error thrown when a bash command exceeds its timeout.
@@ -65,6 +71,12 @@ export declare class BashTimeoutError extends Error {
  * Error thrown when a bash session encounters an error.
  */
 export declare class BashSessionError extends Error {
+    /** Exit metadata is present when a running persistent shell closes. */
+    exitCode?: number | null;
+    signal?: NodeJS.Signals | null;
+    output?: string;
+    error?: string;
+    cwd?: string;
     constructor(message: string);
 }
 /**
diff --git a/dist/src/vended-tools/file-editor/file-editor.js b/dist/src/vended-tools/file-editor/file-editor.js
index 044cf09a239dad3a846c156d0811bbead244535e..178240ef48642d8be9f469ca3d762d3cbe69ec21 100644
--- a/dist/src/vended-tools/file-editor/file-editor.js
+++ b/dist/src/vended-tools/file-editor/file-editor.js
@@ -7,6 +7,11 @@ import * as path from 'path';
 const SNIPPET_LINES = 4;
 const DEFAULT_MAX_FILE_SIZE = 1048576; // 1MB
 const MAX_DIRECTORY_DEPTH = 2;
+const MISS_CONTEXT_LINES = 2;
+const MISS_CONTEXT_LINE_CODE_POINTS = 240;
+const MISS_QUERY_CODE_POINTS = 8192;
+const MISS_MAX_SEEDS = 64;
+const MISS_MAX_SEED_OCCURRENCES = 16;
 /**
  * Zod schema for file editor input validation.
  */
@@ -110,15 +115,139 @@ function applyViewRange(fileContent, viewRange) {
     if (start < 1 || start > nLines) {
         throw new Error(`Invalid \`view_range\`: [${start}, ${end}]. Its first element \`${start}\` should be within the range of lines of the file: [1, ${nLines}]`);
     }
-    if (end !== -1 && end > nLines) {
+    const shouldClampEnd = fileContent.length > 0 && end > nLines;
+    const effectiveEnd = shouldClampEnd ? nLines : end;
+    if (end !== -1 && end > nLines && !shouldClampEnd) {
         throw new Error(`Invalid \`view_range\`: [${start}, ${end}]. Its second element \`${end}\` should be smaller than the number of lines in the file: \`${nLines}\``);
     }
-    if (end !== -1 && end < start) {
+    if (end !== -1 && effectiveEnd < start) {
         throw new Error(`Invalid \`view_range\`: [${start}, ${end}]. Its second element \`${end}\` should be larger or equal than its first \`${start}\``);
     }
-    const content = end === -1 ? lines.slice(start - 1).join('\n') : lines.slice(start - 1, end).join('\n');
+    const content = end === -1 ? lines.slice(start - 1).join('\n') : lines.slice(start - 1, effectiveEnd).join('\n');
     return { content, initLine: start };
 }
+function formatOldStrForError(oldStr) {
+    const limit = 240;
+    if (oldStr.length <= limit * 2) {
+        const codePoints = Array.from(oldStr);
+        if (codePoints.length <= limit)
+            return oldStr;
+        return `${codePoints.slice(0, limit).join('')}… [old_str truncated]`;
+    }
+    return `${Array.from(oldStr.slice(0, limit * 2)).slice(0, limit).join('')}… [old_str truncated]`;
+}
+
+function findMissContextOffset(fileContent, oldStr) {
+    if (oldStr.length === 0) {
+        return { reason: 'old_str is empty, so there is no safe useful close match to locate.' };
+    }
+    if (oldStr.length > MISS_QUERY_CODE_POINTS * 2) {
+        return { reason: `old_str exceeds the advisory search cap of ${MISS_QUERY_CODE_POINTS} Unicode code points.` };
+    }
+    const query = Array.from(oldStr);
+    if (query.length > MISS_QUERY_CODE_POINTS) {
+        return { reason: `old_str exceeds the advisory search cap of ${MISS_QUERY_CODE_POINTS} Unicode code points.` };
+    }
+    const currentOffsets = new Int32Array(Array.from(fileContent).length + 1);
+    let codePointIndex = 0;
+    let utf16Index = 0;
+    for (const codePoint of fileContent) {
+        currentOffsets[codePointIndex] = utf16Index;
+        codePointIndex += 1;
+        utf16Index += codePoint.length;
+    }
+    currentOffsets[codePointIndex] = utf16Index;
+    const seedWidth = Math.min(64, Math.max(8, Math.floor(query.length / 4)));
+    if (query.length < seedWidth) {
+        return { reason: 'No safe useful close textual match was found in the current file.' };
+    }
+    const seedCount = Math.min(MISS_MAX_SEEDS, Math.floor(query.length / seedWidth));
+    const candidates = new Map();
+    for (let seedIndex = 0; seedIndex < seedCount; seedIndex += 1) {
+        const queryOffset = seedCount === 1
+            ? 0
+            : Math.floor(seedIndex * (query.length - seedWidth) / (seedCount - 1));
+        const seed = query.slice(queryOffset, queryOffset + seedWidth).join('');
+        if (seed.trim().length === 0)
+            continue;
+        let from = 0;
+        for (let occurrence = 0; occurrence < MISS_MAX_SEED_OCCURRENCES; occurrence += 1) {
+            const utf16Offset = fileContent.indexOf(seed, from);
+            if (utf16Offset === -1)
+                break;
+            let low = 0;
+            let high = codePointIndex;
+            while (low < high) {
+                const middle = Math.floor((low + high) / 2);
+                if (currentOffsets[middle] < utf16Offset)
+                    low = middle + 1;
+                else
+                    high = middle;
+            }
+            const currentOffset = low;
+            const projectedStart = currentOffset - queryOffset;
+            if (projectedStart >= 0 && projectedStart < codePointIndex) {
+                const candidate = candidates.get(projectedStart) ?? { ranges: [], firstOffset: currentOffset };
+                candidate.ranges.push([queryOffset, queryOffset + seedWidth]);
+                candidate.firstOffset = Math.min(candidate.firstOffset, currentOffset);
+                candidates.set(projectedStart, candidate);
+            }
+            from = utf16Offset + seed.length;
+        }
+    }
+    let best;
+    for (const [projectedStart, candidate] of candidates) {
+        const ranges = candidate.ranges.sort((left, right) => left[0] - right[0]);
+        let covered = 0;
+        let rangeEnd = -1;
+        for (const [start, end] of ranges) {
+            if (end <= rangeEnd)
+                continue;
+            covered += end - Math.max(start, rangeEnd);
+            rangeEnd = end;
+        }
+        const threshold = Math.max(12, Math.min(128, Math.ceil(query.length / 3)));
+        if (covered < threshold)
+            continue;
+        const ranked = { projectedStart, offset: candidate.firstOffset, covered, seeds: ranges.length };
+        if (!best
+            || ranked.covered > best.covered
+            || (ranked.covered === best.covered && ranked.seeds > best.seeds)
+            || (ranked.covered === best.covered && ranked.seeds === best.seeds && ranked.offset < best.offset)) {
+            best = ranked;
+        }
+    }
+    return best ? { offset: best.offset } : { reason: 'No safe useful close textual match was found in the current file.' };
+}
+function formatMissContext(fileContent, oldStr) {
+    const match = findMissContextOffset(fileContent, oldStr);
+    const heading = 'Advisory context only; no fuzzy replacement was attempted.';
+    if (match.offset === undefined) {
+        return `${heading}\n${match.reason}`;
+    }
+    const beforeMatch = Array.from(fileContent).slice(0, match.offset).join('');
+    const matchLine = beforeMatch.split('\n').length - 1;
+    const lines = fileContent.split('\n');
+    const startLine = Math.max(0, matchLine - MISS_CONTEXT_LINES);
+    const endLine = Math.min(lines.length, matchLine + MISS_CONTEXT_LINES + 1);
+    const numbered = lines.slice(startLine, endLine).map((line, index) => {
+        const codePoints = Array.from(line);
+        const shown = codePoints.length > MISS_CONTEXT_LINE_CODE_POINTS
+            ? `${codePoints.slice(0, MISS_CONTEXT_LINE_CODE_POINTS).join('')}… [line truncated]`
+            : line;
+        return `${String(startLine + index + 1).padStart(6)}  ${shown}`;
+    });
+    const omissions = [];
+    if (startLine > 0)
+        omissions.push(`${startLine} earlier line${startLine === 1 ? '' : 's'} omitted`);
+    if (endLine < lines.length) {
+        const later = lines.length - endLine;
+        omissions.push(`${later} later line${later === 1 ? '' : 's'} omitted`);
+    }
+    const omissionNotice = omissions.length > 0 ? `\n[Excerpt truncated: ${omissions.join('; ')}.]` : '';
+    return `${heading}\nClosest current context (lines ${startLine + 1}-${endLine}):\n${numbered.join('\n')}${omissionNotice}`;
+}
+
 /**
  * Performs a unique str_replace transformation on file content. Validates that
  * `oldStr` appears exactly once. Returns the new content plus a snippet around
@@ -128,9 +257,18 @@ function buildStrReplaceResult(originalContent, oldStr, newStr, filePath) {
     const fileContent = originalContent.replace(/\t/g, '        ');
     const expandedOldStr = oldStr.replace(/\t/g, '        ');
     const expandedNewStr = newStr ? newStr.replace(/\t/g, '        ') : '';
-    const occurrences = (fileContent.match(new RegExp(escapeRegExp(expandedOldStr), 'g')) || []).length;
+    let occurrences = 0;
+    let occurrenceOffset = fileContent.indexOf(expandedOldStr);
+    while (occurrenceOffset !== -1) {
+        occurrences += 1;
+        if (occurrences > 1)
+            break;
+        occurrenceOffset = fileContent.indexOf(expandedOldStr, occurrenceOffset + Math.max(1, expandedOldStr.length));
+    }
     if (occurrences === 0) {
-        throw new Error(`No replacement was performed, old_str \`${oldStr}\` did not appear verbatim in ${filePath}.`);
+        const advisory = formatMissContext(fileContent, expandedOldStr);
+        const displayedOldStr = formatOldStrForError(oldStr);
+        throw new Error(`No replacement was performed, old_str \`${displayedOldStr}\` did not appear verbatim in ${filePath}.\n\n${advisory}`);
     }
     if (occurrences > 1) {
         const lines = fileContent.split('\n');
