# Implementation plan

1. Capture the installed SDK foreground call path and a minimal pre-fix parallel-listener reproduction in task evidence.
2. Extend the existing pinned SDK patch with per-session foreground serialization and structured shell-close outcomes.
3. Add focused real-process regression cases for exit 0 recovery, output ownership, next-shell health, nonzero/signal metadata, ordinary persistence, and restart compatibility.
4. Run the focused background-bash suite during edits; after source settles run typecheck, the full offline suite, and all architecture-named free process/TUI checks once.
5. Update the SDK contract and Process exit architecture text, validate/finish/archive Trellis artifacts, and commit without changing Host-owned backlog/reflection/iteration evidence.
