# Implementation plan

1. Pin the SDK-private zero-occurrence branch, provider schema, sandbox write seam, existing patch,
   and focused test behavior in task artifacts.
2. Add deterministic capped advisory matching and bounded line-numbered formatting only to the
   installed SDK miss branch, then regenerate the tracked pnpm patch.
3. Extend the provider-facing real-file suite for near match, no match, ambiguity, adversarial caps,
   Unicode, zero-write/metadata invariants, exact success, and unchanged unrelated errors.
4. Update the SDK contract and AGENTS load-bearing index with strategy, caps, invariants, and upgrade
   checks.
5. Run focused checks while editing. Once settled, run typecheck, the full offline suite once, build,
   patch reapplication/syntax, Trellis validation, diff checks, and commit without staging the
   Host-owned backlog file.
