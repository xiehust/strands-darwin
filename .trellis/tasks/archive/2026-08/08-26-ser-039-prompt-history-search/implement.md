# Implementation plan — SER-039

1. Add the pure search state machine and focused tests over existing reader fixtures.
2. Integrate immediate state/read-generation mirrors and key precedence in `App.tsx`.
3. Add bounded counted search rows to `InputBox` and frame planning.
4. Add a free PTY scenario and preserve focused neighboring scenarios.
5. Update help, README, load-bearing specs, task context, and journal.
6. Run focused verification → typecheck → complete test gate → build → repository checks; commit and archive task.
