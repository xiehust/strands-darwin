# Choosing a commit type

- `feat` — adds user-visible capability
- `fix` — corrects broken behaviour
- `docs` — documentation only
- `refactor` — restructuring with no behaviour change
- `test` — tests only
- `chore` — build, deps, tooling
